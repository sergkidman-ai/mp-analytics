#!/usr/bin/env python3
# поток: infra
"""J0: человеческая сводка baseline из kb_baseline_*.json. В stdout — не более 20 строк.

Ручные наблюдения (валидация regex, контролируемые прогоны) внесены как аннотации:
они получены вне парсера и помечены источником.
"""
import json, statistics as st

J = '/opt/mp-analytics/reports/data/kb_baseline_2026-08-19.json'
MD = '/opt/mp-analytics/docs/reports/kb_baseline_2026-08-19.md'
d = json.load(open(J, encoding='utf-8'))
L, A = [], None
L = []
def A(x=''): L.append(x)

f = d['files']
A('# J0 BASELINE — Context & Memory (замер до внедрения)\n')
A('Дата: 2026-08-19. Источник: `/root/.claude/projects/**/*.jsonl` — только чтение.')
A('Ничего не менялось: ни память, ни `CLAUDE.md`, ни settings, ни хуки, ни worktree, ни сервисы.\n')
A('## 0. Методика (по решению Сергея 19.08)\n')
A('Раздельно фиксируются: **raw usage-поля** транскрипта · **пол пустого проекта** ·')
A('**provenance** (версия, модель, effort, entrypoint, permissionMode, cwd, ветка, поток).')
A('Вычитание пола применяется **только внутри одинаковой конфигурации**. Полевые сессии')
A('интерактивные, полы сняты в `--print` → **разность между режимами не считается и метрика**')
A('**«PROJECT CONTEXT OVERHEAD» для полевых сессий не вводится**. Сравнение — парное:')
A('поток сам с собой, конфигурация с той же конфигурацией.\n')
A('## 1. Покрытие данных\n')
A('| Показатель | Значение |')
A('|---|---|')
A(f"| файлов-транскриптов всего | **{f['total']}** ({f['sessions']} сессий + {f['subagents']} субагентов) |")
A(f"| субагентских с usage | {f['subagents_with_usage']} — в baseline turn-1 **не входят** (это не старт сессии) |")
A(f"| сессий пригодных (raw>1000) | {d['sessions_usable']}, из них mp-проект: **{d['sessions_mp']}** |")
A(f"| сессий без usage вовсе | {d['sessions_unusable']} |")
A('\nДоля записей с нужными полями (из числа сессий):\n')
A('| Поле | Есть |')
A('|---|---|')
for k, v in d['field_presence'].items():
    A(f'| `{k}` | {v} |')
A('\nФактическая схема usage: `input_tokens`, `cache_creation_input_tokens`,')
A('`cache_read_input_tokens`, `output_tokens`, `service_tier`, `cache_creation`,')
A('`server_tool_use`, `iterations`, `speed` — присутствуют во всех 52 160 assistant-записях.')
A('`effort` есть на уровне записи (48 625 строк), `permissionMode` — 10 227, `isCompactSummary` — 318.\n')
A('## 2. Токены turn-1 (raw, mp-сессии)\n')
A('| Поле | n | медиана | p90 | min | max |')
A('|---|---|---|---|---|---|')
for k, label in (('raw_turn1_total', 'сумма трёх полей'), ('input_tokens', 'input_tokens'),
                 ('cache_creation_input_tokens', 'cache_creation'),
                 ('cache_read_input_tokens', 'cache_read'), ('output_tokens', 'output')):
    s = d[k]
    A(f"| {label} | {s['n']} | **{s['median']}** | {s['p90']} | {s['min']} | {s['max']} |")
A('\nЧитается так: реальный вход первого ответа — это почти целиком кэш')
A('(`cache_read` медиана 40 427 + `cache_creation` 29 290), а `input_tokens` = 2 токена.')
A('**Любая метрика, смотрящая только на `input_tokens`, покажет ноль и обманет.**\n')
A('## 3. Контролируемый замер (одна конфигурация: haiku, `--print`, 2.1.235)\n')
A('| Конфигурация | turn-1 | дельта |')
A('|---|---|---|')
A('| пустой проект (пол харнесса) | 17 755 | — |')
A('| + проектный `CLAUDE.md` (18 616 симв) | 25 963 | +8 208 |')
A('| + `MEMORY.md` (18 295 симв) | 34 373 | +8 410 |')
A('| + 130 неотслеживаемых файлов (git status) | 34 379 | +6 |')
A('| реальный `/opt/mp-analytics` | **35 452** | +17 697 к полу |')
A('| пустой проект, opus-5 | 23 969 | пол другой модели |')
A('\nЭто **единственное место**, где вычитание пола корректно: одна версия, одна модель,')
A('один режим, один набор инструментов. Здесь же видно: знание проекта = 16 618 токенов')
A('(CLAUDE.md 8 208 + индекс памяти 8 410), это 94 % надстройки над полом.')
A('Реальная цена символа на боевом тексте: 2.27 (CLAUDE.md) и 2.18 (MEMORY.md) симв/ток.\n')
A('## 4. По потокам (полевые сессии, только достоверный cwd)\n')
A('| Поток | Сессий | raw turn-1 медиана | Карточек/сессия | Коррекций-кандидатов |')
A('|---|---|---|---|---|')
for k, v in d['by_stream'].items():
    A(f"| {k} | {v['n']} | {v['raw']['median']} | {int(v['mem_reads_median'])} | {v['corr_total']} |")
A('\n## 5. Конфигурации (provenance, топ-8)\n')
A('| версия \\| модель \\| effort \\| entrypoint | сессий | raw медиана |')
A('|---|---|---|')
for k, v in list(d['by_config'].items())[:8]:
    A(f"| {k} | {v['n']} | {v['raw_median']} |")
A('\nВерсий Claude Code в выборке — 23, моделей — 5 (opus-5, opus-4-8, sonnet-5, haiku, synthetic).')
A('Поэтому единый абсолютный порог по raw невозможен: сравнивать только внутри конфигурации.\n')
A('## 6. Дрейф памяти (B8)\n')
A('| Скоуп | Карточек | Записано за 7 дней | За 30 дней | Последняя запись |')
A('|---|---|---|---|---|')
for k, v in d['memory_drift'].items():
    A(f"| `{k}` | {v['cards']} | **{v['last_7d']}** | {v['last_30d']} | {(v['newest'] or '')[:16]} |")
A('\nДрейф активен: за 7 дней в `/root` записано вдвое больше карточек, чем в скоуп репозитория.')
A('Плюс по cwd: сессий, запущенных вне git-дерева проекта, — см. строку `root-scope` в §4.\n')
A('## 7. Коррекции (B5/B6) — с измеренной точностью\n')
A(f"Широкий regex дал **{d['corrections_total']}** кандидатов в {d['corrections_sessions']} сессиях.")
A('**Ручная проверка всех 25** (Claude, 19.08): истинные коррекции — **8**, пограничные')
A('(претензия/оспаривание цифр без прямой поправки знания) — **5**, ложные (задания, решения,')
A('вопросы, текст договора) — **12**. Точность строгая **32 %**, мягкая 52 %.')
A('Полнота неизвестна и заведомо ниже 100 %: вежливые поправки формулировками не ловятся.')
A('→ **B5/B6 признаны ненадёжными как порог.** Годятся только как тренд с ручной сверкой.\n')
A('Узкий детектор рецидива («тысячу раз», «несколько раз говорил/спрашивал», «я же говорил»)')
A('дал 5 попаданий, из них **3 — артефакты компакт-сводок** (сводка цитирует реплику),')
A('2 — настоящие (07.08 про источник маржи, 17.08 про «реклама+органика вместе»).')
A('После фильтра «текст начинается с This session is being continued» точность узкого детектора **2/2**.')
A('→ рекомендуется как основной детектор рецидива, но абсолютное значение (2 за 40 дней) слишком')
A('мало для статистики: порог ставится на «не выросло», а не на процент.\n')
A('## 8. Использование памяти (B4) и «переоткрытие известного» (B7)\n')
s = d['mem_reads']
A(f"Чтений тел карточек за сессию: медиана **{s['median']}**, p90 {s['p90']}, max {s['max']}.")
A('Больше половины сессий не открывают ни одной карточки, платя за индекс 8 410 токенов.\n')
A('B7 (прокси «переоткрытие известного»): 16 525 чтений путей репозитория, из них 7 826 (47 %)')
A('попали в пути, упомянутые карточками. **Выборочная проверка показала, что прокси непригоден')
A('как бизнес-метрика**: в топе `web/app.py`, `docs/HANDOFF.md`, `docs/BRIEF_FIN.md` — их чтение')
A('является нормальной работой, а не повторным открытием уже записанного знания.')
A('→ B7 остаётся описательным, в пороги MVP-1 **не входит**.\n')
A('## 9. Ограничения baseline\n')
A('**Измерено точно:** raw usage-поля turn-1 и их состав; контролируемые прогоны в одной')
A('конфигурации; количество и даты карточек памяти по скоупам; версии/модели/effort/cwd;')
A('число сессий и субагентских транскриптов.\n')
A('**Прокси (не порог):** B5/B6 коррекции (точность 32 %); B7 переоткрытие (топ-совпадения')
A('ложные); поток по имени рабочего дерева там, где нет `.workstream`.\n')
A('**Не восстановимо из старых логов:** системный промпт и его состав (в транскрипт не пишется,')
A('поэтому декомпозиция turn-1 сделана контрольными прогонами, а не разбором истории);')
A('пол харнесса для старых версий (23 версии, откатывать установку ради замера нельзя);')
A('interactive-надбавка режима (полы сняты в `--print`); минуты ручного обслуживания знания;')
A('исходный текст `CLAUDE.md`/`MEMORY.md` на момент каждой прошлой сессии (сравниваем с сегодняшним).\n')
A('---\n')
A('Машинные данные: `reports/data/kb_baseline_2026-08-19.json`;')
A('кандидаты коррекций: `kb_correction_candidates.json`; кандидаты gold-set: `kb_goldset_candidates.json`;')
A('полы: `kb_harness_floors.json`.')

open(MD, 'w', encoding='utf-8').write('\n'.join(L) + '\n')
print('MD:', MD, '| строк:', len(L))
print('mp-сессий:', d['sessions_mp'], '| raw медиана:', d['raw_turn1_total']['median'])
print('дрейф 7д: repo', d['memory_drift']['repo']['last_7d'], 'root', d['memory_drift']['root']['last_7d'])
