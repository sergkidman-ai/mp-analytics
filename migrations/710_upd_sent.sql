-- 710 — отметка «УПД отправлен Наталии в телеграм».
--
-- Зачем отдельно от upd_status: upd_status='made' означает «загружен в Диадок и подписан»,
-- это делает человек. sent_at — что файл ушёл ответственному, и нужен он ровно для того,
-- чтобы еженедельная рассылка не отправляла один и тот же УПД повторно.
ALTER TABLE wb_redeem_notice ADD COLUMN IF NOT EXISTS sent_at timestamptz;
