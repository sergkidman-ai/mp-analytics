-- поток: card — значение атрибута «Нужен код маркировки» рядом с замечанием
--
-- 26.08.2026: ТК заполнила атрибут по 333 карточкам, и замечание
-- warning_attribute_values_empty «Нужен код маркировки» ушло у 329 в тот же час. Разобрать
-- «пусто → это к ТК» и «заполнено, а замечание висит → это к дожимателю» по одному срезу
-- было нечем: в card_status лежал только код замечания, а значение атрибута приходилось
-- каждый раз доставать вручную из справочника категории (id 23536, категория 17028935/95980).
--
-- Разница принципиальная и стоит отдельной колонки:
--   пусто     — повтор бесполезен, значения нет вовсе (опыт 23.08: 0/5), задание для ТК;
--   заполнено — значение ЕСТЬ, площадка держит устаревший вердикт, это случай класса W.
alter table card_status add column if not exists mark_attr text;

comment on column card_status.mark_attr is
    'значение атрибута 23536 «Нужен код маркировки» на момент съёма: NULL — не спрашивали '
    '(замечания о маркировке у карточки нет), «» — атрибут пуст (работа ТК), иначе значение';
