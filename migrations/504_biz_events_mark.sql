-- поток: ev — значок события в ленте (удар по складу и т.п.).
-- Ставит сборщик, не человек: значок обязан пересчитываться вместе с правилами.
ALTER TABLE biz_events ADD COLUMN IF NOT EXISTS mark text;
