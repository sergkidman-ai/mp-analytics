-- поток: prc — пометка «письмо было, но расписанию не учит».
--
-- Приход остаётся в истории (иначе сторож решит, что поставщик молчит, и поднимет тревогу
-- в день, когда прайс пришёл), но в расчёт обычного времени, дней недели и разрыва не идёт.
-- Первый случай — Одиссей 28.08.2026: письмо в 17:49 вместо обычных 11:19, разовая задержка,
-- и если учить её как норму, срок ожидания уезжает на полтора часа для всех следующих дней.
ALTER TABLE prc_mail_arrival
  ADD COLUMN IF NOT EXISTS ignored boolean NOT NULL DEFAULT false;
