-- 035: источник строки услуг (file — ручная выгрузка ЛК, api — единый отчёт Партнёр-API).
-- Идемпотентность теперь помесячная: тот, кто пишет месяц последним, им и владеет,
-- поэтому API (свежие ~70 дней) и файл (январь–апрель) не затирают друг друга.
ALTER TABLE raw_yandex_services ADD COLUMN IF NOT EXISTS source text NOT NULL DEFAULT 'file';
