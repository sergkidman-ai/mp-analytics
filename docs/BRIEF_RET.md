# BRIEF_RET — возвраты товара (что физически забрать с ПВЗ)

> `ret` ≠ `rev`. Здесь **физика возврата** (коробка лежит в ПВЗ, её надо забрать).
> Отзывы и вопросы покупателей — поток `rev`. Деньги возвратов и сторно COGS — поток `fin`.

**Ветка / worktree:** `ret/returns-bot` в `.claude/worktrees/ret`; **влито в `main`** 02.08.2026
(`5a5e2e2` — код), 06.08.2026 (`f6a5a2c` — фаза 5, дальше фаза 6 fast-forward) и 07.08.2026
(`d338f79` — фазы 7–8), по «ок» Сергея.
Прод работает из `/opt/mp-analytics`, worktree держим для следующих правок потока.
**Модель сессии:** Sonnet (механика: API площадок, сбор, рассылка).

**Статус на 07.08.2026: закрыто и работает в проде.** До 04.08 бот жил на трёх площадках.
06.08 Сергей указал на две дыры по Ozon — Real-FBS (Почтой России) и вывоз со склада FBO;
обе закрыты (фаза 5). Следом формат сводки (фаза 6), кнопка «Обновить» (фаза 7) и правки
по живой эксплуатации: ложные «забрать» у Яндекса и нечитаемый штрихкод (фаза 8).
Забрать на 07.08: **17** — Ozon 1, WB 13, Яндекс 3.
**10.08.2026 — фаза 9: приёмка** (второй адресат, своя дата получения, недельный отчёт
по понедельникам). В worktree, ждёт «ок» на мерж.

## Задача

Отдельный Telegram-бот раз в день присылает полный список висящих FBS-возвратов по
Ozon / Яндекс / WB: сгруппировано по ПВЗ, с адресом, составом возврата, сроком забрать
и штрихкодом получения (картинкой — там, где площадка его отдаёт).

## Сделано

**Фазы 1–3 — разведка API, таблицы `mp_returns`/`mp_return_items` (миграция 300, серия 3xx
= поток `ret`), бот @ds_vozvrat_bot и рассылка.** Подробности —
`docs/handoff/archive/BRIEF_RET_phases_1-6.md`.

## Текущая картина (прогон 07.08.2026, пять источников)

Живых строк в базе 4036. В сводке — **17 «забрать»**: WB 13, Яндекс 3, Ozon 1. Плюс «в пути»
(92) и «разобраться» (15) — лежат в базе, но не показываются (`SHOW_STAGES`).
Сообщений два: Цифровой (WB+Яндекс) и Дисквэр (Ozon+WB, картинка штрихкода Ozon).

Сводка шлётся **отдельным сообщением на юрлицо** — карта аккаунтов в `returns_bot/orgs.py`.
Все три кампании Яндекса отнесены к Цифровому (ключ и businessId один) — **подтверждено Сергеем
02.08.2026**. Появится Яндекс у Дисквэра — заводить `ya_acc2` в источнике, а не разносить кампании.

Адрес печатается коротко (улица + дом), см. `render.short_address`: сначала выбираем сегмент со
словом-типом улицы, затем срезаем приклеенный город («МСК Улица Годовикова 11к5» → «Улица
Годовикова 11к5»), но НЕ трогаем постфиксные типы («Ленинградское шоссе» не превращается в
«шоссе»). Имена товаров: Ozon свои; Яндекс — `raw_yandex_offer` → `ms_product.external_code`;
WB — `wb_cards.nm_id`.

## Правила классификации — только в `returns_bot/pending.py`

Стадии `pickup` (забрать) / `transit` (едет) / `attention` (потерян/утилизация) / `closed`.
WB: статус приходит русской строкой, гейт — `isStatusActive` (0 = история при любом статусе);
`Готов к выдаче` → pickup, `В пути в пвз` → transit, живой неизвестный статус → `attention`
(в сводке не виден, `SHOW_STAGES` — только pickup; заметить можно прогоном `collect`).
Ozon `Utilized`/`WriteOff` и Яндекс `EXPIRED`/`RECEIVED_FOR_EXPROPRIATION` — **closed**:
товара уже нет, действия невозможны, в ежедневный список они шум. Правка правил — только здесь.

**Что бот показывает — `pending.SHOW_STAGES`, сейчас только `pickup`** (решение Сергея 02.08.2026).
Убраны из показа: «в пути» (Едет к вам / Едет на склад Ozon / Ожидает отправки / Привезёт курьер)
и «разобраться» (Ищем товар / потерян / готовится к утилизации) — действия по ним всё равно нет.
**Собираем и храним по-прежнему всё**, вернуть показ = добавить стадию в `SHOW_STAGES`.
«Привезёт курьер» (`ReturningToSellerByCourier`) Сергей отложил «возможно, понадобится позже» —
возвращать отдельной стадией, а не всем блоком `transit`.

**Фазы 4–6 — WB, две дыры по Ozon (Real-FBS и вывоз со склада FBO), формат сводки.**
Как делали — `docs/handoff/archive/BRIEF_RET_phases_1-6.md`. Живые правила классификации
всех пяти источников — в `returns_bot/pending.py`, там же основания.

**Фаза 7 — кнопка «Обновить» (07.08.2026).** Под каждой сводкой inline-кнопка: тот же
`/obnovit`, но не набирая команду. Обработчик живёт в `returns-bot.service`, поэтому кнопка
под ночной рассылкой тоже рабочая.

- **сбор быстрый** (`gather(quick=True)`): у WB одно окно вместо трёх, у вывоза FBO одно
  вместо двух — 8 с против 40 с. Старую закрытую историю (3900 из 4000 строк) кнопка
  не перечитывает, её освежает ночной полный прогон;
- **`store(mark_gone=False)` при быстром сборе обязателен**: при узком окне «нет в выдаче»
  ≠ «пропал», иначе быстрый сбор похоронит всё, что не попало в окно;
- в итоге вместо бессмысленного «обновлено 4036, позиций 4100» — **сколько получено нами
  с прошлого обновления** (`collect.received_since`, считается по `status_raw`, см. ниже);
- параллельный сбор заблокирован флагом `_busy`: вторая нажатая кнопка получает отказ,
  а не встаёт в очередь и не идёт в API площадок второй раз.

**Фаза 8 — правки по живой эксплуатации (07.08.2026).**

- **Яндекс `RECEIVED` — не «забрать», а «в пути».** Означает «принят в пункте выдачи»
  логистикой, а не «лежит и ждёт нас». Сергей проверил заказы 58716444738 и 59558307072:
  в ЛК у них «Готов к отправке», к выдаче их нет. Признак реальной готовности —
  `pickupTillDate`: по базе заполнен ровно у `READY_FOR_PICKUP` и пуст у всех `RECEIVED`.
  Забрать: 20 → 17;
- **штрихкод Ozon шлём файлом** (`sendDocument`), не фото: `sendPhoto` перегоняет PNG
  784×144 в JPEG, тонкие штрихи плывут и сканер в пункте выдачи код не берёт;
- **срок жизни штрихкода в подписи.** В API его нет нигде: `/giveout/barcode` отдаёт одно
  значение, `/giveout/info` и `/giveout/list` по обоим аккаунтам пусты. Дата напечатана
  только на бланке PDF (`/giveout/get-pdf`) — разбираем оттуда через `pdftotext` (шрифты
  подмножеством, свой парсер врёт). Код живёт сутки, повторными запросами **не продлевается**,
  у аккаунтов сроки разные. Печатаем по Москве (сервер в UTC), истёк — «⛔ обновить в кабинете»:
  сами обновить не можем, `/barcode-reset` пишет в кабинет;
- **сам печатный бланк не отправляем** (решение Сергея: лишнее), но запрашиваем — ради срока.

**Фаза 9 — приёмка: недельный отчёт «что уже получено» (10.08.2026).**
Ежедневная сводка отвечает «что забрать», этот отчёт — «что приехало и что с этим делать».

- **Второй адресат.** Алексей Шаров (`780828338`) добавлен в `TG_RETURNS_ALLOWED_IDS`
  и `TG_RETURNS_NOTIFY_ID` — получает и ежедневную сводку, и недельную приёмку.
  Наталье нужен тот же отчёт, но её `chat_id` в боте нет: добавим, когда напишет `/start`.
- **Чужой чат теперь виден в журнале** (`bot._who`, коммит `30f944b`). До этого сообщение
  неразрешённого пользователя молча отбрасывалось, а Telegram удалял апдейт вместе с `chat_id` —
  человека нельзя было добавить, пока он не напишет ещё раз.
- **Своя дата получения `received_at`** (миграция 302). У площадок такой даты нет: у Ozon
  `arrived_moment` — приезд коробки в ПВЗ, у ВБ `readyToReturnDt` — готовность к выдаче,
  у Яндекса и Real-FBS нет ничего. `collect.store()` штампует её один раз, когда впервые видит
  статус «получено» (`RECEIVED_STATUSES`); перезаписывать нельзя — дата поедет при каждом сборе.
  История до 10.08.2026 залита разово из `arrived_at`/`created_at` (1479 строк из 4064).
- **Был ли контакт с покупателем** — строго из `return_type` площадки (`weekly_received.CONTACT`),
  без догадок: Ozon `ClientReturn`/`PartialReturn`/`FullReturn` и Яндекс `RETURN` — был;
  Ozon `Cancellation` и Яндекс `UNREDEEMED` — не было; **у ВБ признака нет вовсе** — отдельная
  корзина «не определить», такие проверяем как вскрытые.
- **Вывоз со стока FBO Ozon — все коробки на проверку** (Сергей 10.08.2026). Работаем только по
  ФБС, на склад Ozon товар попадает после отмены, невыкупа или возврата после получения, то есть
  вскрытым может быть любой. Прежняя формулировка «упаковка заводская, проверять только Брак»
  **неверна**; «Брак» помечается 🔴 и идёт первым. Дата в отчёте — `delivery_date` (приезд
  коробки), не `created_at`: заявка создаётся сильно раньше приезда.
- **Расписание** — `returns-weekly.timer`, понедельник 06:20 UTC = 09:20 МСК, за прошлую
  неделю пн–вс по Москве. На 15 минут позже ежедневной: обе шлёт один бот.

## Нужно от Сергея

**«Ок» на мерж фазы 9 в `main`** (`dc2917f` + `cfc4237` в worktree `ret`) и на установку
`returns-weekly.timer`. Миграция 302 в прод-БД уже применена, отчёт проверен прогоном.

Отклонено Сергеем 02.08.2026: подсказка «код выдачи — QR в приложении WB» и строка «лежит N дней»
в сводке. В сводке остаётся только то, что есть сейчас: где лежит и что внутри.

## Запреты

- Только **чтение** API площадок. Ничего не согласовываем, не оспариваем, не заявляем на вывоз,
  в кабинеты не пишем.
- Витрины fin (`margin_by_sku`, `ms_return_cogs`) не трогаем: там деньги, здесь физика.
- **Свой QR из номера возврата не рисуем.** Только коды, которые площадка отдала сама
  (Ozon PNG/`logistic.barcode`). Если ПВЗ сканирует другой код — картинка вводит в заблуждение.
- Мерж в `main` — только по явному «ок» Сергея.

## Побочная правка вне потока (чтобы `inv` знал)

02.08.2026 по просьбе Сергея в **потоке `inv`** сделана правка отчёта бота счетов: жирным
группа поставщика, покупатель и сумма. Ветка `inv/tg-bold-fields` (worktree `.claude/worktrees/
inv-tg-bold`), коммит `a7499ad`, **влито в main**, `invoice-bot.service` и `invoice-mail.service`
перезапущены. Суть: сообщения слались без `parse_mode` → добавлен `format_report_html()` в
`invoice_to_po.py` (экранирует текст целиком, потом жирнит три подстроки) и проброс `parse_mode`
в `tg_bot.send/broadcast_others` и `mail_poller.tg_send`. У УПД (`upd_to_supply`) своей
html-версии нет — его отчёт просто экранируется, без жирного.

## Следующий шаг

Ждём «ок» на мерж фазы 9 (см. «Нужно от Сергея»); после мержа — `systemctl enable --now
returns-weekly.timer` и разовый прогон для проверки. Наталью добавить в `.env`, когда напишет боту.

07.08.2026 фазы 7 и 8 влиты в main (`d338f79`) по «ок» Сергея,
`returns-bot.service` перезапущен, прод проверен: забрать 17 (Ozon 1, WB 13, Яндекс 3),
живых строк 4036, штрихкод собирается со сроком. Сергей проверяет считывание кода на пункте —
если снова не возьмут, следующий шаг: вернуть печатный бланк PDF (он в `ozon.giveout()` уже есть).
Дальше — по запросу (кандидаты: вернуть «привезёт курьер» отдельной стадией, `ya_acc2`
при появлении Яндекса у Дисквэра).

## Команды

```bash
./venv/bin/python -m returns_bot.collect --dry-run          # посчитать, в БД не писать
./venv/bin/python -m returns_bot.collect --only ozon_rfbs ozon_removal   # только новые источники
./venv/bin/python -m returns_bot.collect                    # собрать и записать
./venv/bin/python -m returns_bot.daily_push --dry-run --no-collect   # текст сводки в консоль
./venv/bin/python -m returns_bot.daily_push --to <chat_id>  # разовая отправка одному
./venv/bin/python -m returns_bot.weekly_received --dry-run  # недельная приёмка в консоль
./venv/bin/python -m returns_bot.weekly_received --week 2026-08-03   # другая неделя
python3 tools/territory_guard.py --status                   # домен сессии = ret
```
