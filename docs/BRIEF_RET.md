# BRIEF_RET — возвраты товара (что физически забрать с ПВЗ)

> `ret` ≠ `rev`. Здесь **физика возврата** (коробка лежит в ПВЗ, её надо забрать).
> Отзывы и вопросы покупателей — поток `rev`. Деньги возвратов и сторно COGS — поток `fin`.

**Ветка / worktree:** `ret/returns-bot` в `.claude/worktrees/ret`; **влито в `main`** 02.08.2026
(`5a5e2e2`, по «ок» Сергея), прод работает из `/opt/mp-analytics`.
**Модель сессии:** Sonnet (механика: API площадок, сбор, рассылка).

## Задача

Отдельный Telegram-бот раз в день присылает полный список висящих FBS-возвратов по
Ozon / Яндекс / WB: сгруппировано по ПВЗ, с адресом, составом возврата, сроком забрать
и штрихкодом получения (картинкой — там, где площадка его отдаёт).

## Сделано

**Фаза 1 — разведка (02.08.2026).** Живые probe-запросы, отчёт `docs/reports/returns_api_recon.md`.
- Ozon `POST /v1/returns/list` — 200 по обоим аккаунтам, пагинация `last_id`/`has_next`, limit 500.
  Машинный статус — `visual.status.sys_name` (русский `display_name` только для показа).
  Деньги приходят объектом `{"currency_code":"RUB","price":N}`. `place` = где возврат сейчас,
  `target_place` = куда приедет (туда и ехать). `logistic.final_moment` непустой = процесс закрыт.
- Ozon `POST /v1/return/giveout/barcode` + `/get-png` — 200, отдаёт **готовый PNG** штрихкода
  получения возвратов (один на аккаунт). `giveout/list` требует `limit` в (0,1000].
- Яндекс `GET /campaigns/{cid}/returns` — 200 по всем 3 кампаниям, пагинация `page_token`.
  `pickupTillDate` заполняется ровно при `shipmentStatus == READY_FOR_PICKUP`.
  Адрес ПВЗ — объект, `city` приходит как «Москва,Москва». **Штрихкода в API нет.**
- WB — 401 «token scope not allowed». **Оказалось ложным следом**, см. «WB: API нет» ниже.

**Фаза 2 — данные.** `migrations/300_mp_returns.sql` применена (серия **3xx** = поток `ret`):
`raw_mp_returns` (сырьё jsonb), `mp_returns` (шапка), `mp_return_items` (состав).
`returns_bot/{net,pending,collect}.py` + `returns_bot/sources/{ozon,yandex}.py`.
Идемпотентность проверена: два прогона подряд → те же числа, `first_seen` не съехал.
Пропал из выдачи API → `gone_at`, из сводки уходит.

**Фаза 3 — бот.** Токен получен 02.08, бот **@ds_vozvrat_bot**; адресат — chat_id из
`TG_RETURNS_NOTIFY_ID`. Разовая отправка сводки прошла (текст + PNG штрихкода Ozon).
 `returns_bot/{tg,codes,render,bot,daily_push}.py` + юниты
`returns-bot.service`, `returns-daily.{service,timer}`. Сводка режется под лимит Telegram автоматически.
**Юниты поставлены и включены 02.08.2026:** `returns-bot.service` (long-polling) активен,
`returns-daily.timer` — следующий запуск 06:05 UTC = 09:05 МСК.

## Текущая картина (прогон 02.08.2026, все три площадки)

Всего в базе 3707 возвратов. В сводке — **21 «забрать»**: WB 12 (acc1 9, acc2 3), Яндекс 6,
Ozon 3. Плюс 74 «в пути» и 7 «разобраться» — лежат в базе, но не показываются (`SHOW_STAGES`).
Сообщений два: Цифровой (1517 знаков, WB+Яндекс) и Дисквэр (Ozon+WB, картинка штрихкода Ozon).

Сводка шлётся **отдельным сообщением на юрлицо** — карта аккаунтов в `returns_bot/orgs.py`.
Все три кампании Яндекса отнесены к Цифровому (ключ и businessId один) — **подтверждено Сергеем
02.08.2026**. Появится Яндекс у Дисквэра — заводить `ya_acc2` в источнике, а не разносить кампании.

Адрес печатается коротко (улица + дом), см. `render.short_address`: сначала выбираем сегмент со
словом-типом улицы, затем срезаем приклеенный город («МСК Улица Годовикова 11к5» → «Улица
Годовикова 11к5»), но НЕ трогаем постфиксные типы («Ленинградское шоссе» не превращается в
«шоссе»). Имена товаров: Ozon свои; Яндекс — `raw_yandex_offer` → `ms_product.external_code`;
WB — `wb_cards.nm_id`.

## Правила классификации — только в `returns_bot/pending.py`

Стадии `pickup` (забрать) / `transit` (едет) / `attention` (потерян/утилизация) / `closed`.
WB: статус приходит русской строкой, гейт — `isStatusActive` (0 = история при любом статусе);
`Готов к выдаче` → pickup, `В пути в пвз` → transit, живой неизвестный статус → `attention`
(в сводке не виден, `SHOW_STAGES` — только pickup; заметить можно прогоном `collect`).
Ozon `Utilized`/`WriteOff` и Яндекс `EXPIRED`/`RECEIVED_FOR_EXPROPRIATION` — **closed**:
товара уже нет, действия невозможны, в ежедневный список они шум. Правка правил — только здесь.

**Что бот показывает — `pending.SHOW_STAGES`, сейчас только `pickup`** (решение Сергея 02.08.2026).
Убраны из показа: «в пути» (Едет к вам / Едет на склад Ozon / Ожидает отправки / Привезёт курьер)
и «разобраться» (Ищем товар / потерян / готовится к утилизации) — действия по ним всё равно нет.
**Собираем и храним по-прежнему всё**, вернуть показ = добавить стадию в `SHOW_STAGES`.
«Привезёт курьер» (`ReturningToSellerByCourier`) Сергей отложил «возможно, понадобится позже» —
возвращать отдельной стадией, а не всем блоком `transit`.

**Фаза 4 — WB (02.08.2026), подключён.** Возвраты WB живут **в Аналитике**, а не в Marketplace API:
`GET seller-analytics-api /api/v1/analytics/goods-return`, **базовый** токен (скоуп «Аналитика»).
`returns_bot/sources/wb.py`. Окно ≤31 дня по `orderDt` → тянем тремя окнами назад (93 дня),
дедуп по `shkId`. Ключ возврата = `shkId` (физическое место), номер в сводке = `stickerId`.

Тупики, чтобы не искать заново: `/api/v3/supplies/returns` **не существует** — слово `returns`
подставляется как `{supplyId}`, отсюда 400 `IncorrectParameter` при любых параметрах (контроль:
`/api/v3/supplies/zzz-not-a-supply` даёт ту же ошибку). Прежняя 401 «token scope not allowed»
была ложным следом: авторизация WB отвечает ДО маршрутизации. `returns-api /api/v1/claims` —
заявки покупателей на брак, без адреса/срока/стикера, для забора непригодно (ближе к `rev`).
Выданные `WB_TOKEN_RETURNS_ACC1/2` (маска `2064` = «Маркетплейс»+«Возвраты», до 31.01.2027)
в итоге **не нужны** — лежат в `.env` про запас. Базовый токен не трогали.
Открытые копии токенов из `dropbox/*.txt` удалены 02.08.2026 (`shred`), строки вычищены из
`dropbox/index.log`; в `dropbox/` JWT больше нет.

Чего WB не даёт: **срока «забрать до»** (`expiredDt` у живых строк пуст) и **картинки штрихкода**
(аналога `giveout/get-png` нет). Стикер печатаем текстом.

**Код выдачи в ПВЗ у WB — из ПОКУПАТЕЛЬСКОГО приложения, не из API** (проверено 02.08.2026 по
официальной инструкции WB Partners «Возврат товара со склада Wildberries продавцу»): «Когда у
возврата будет статус „Готов к выдаче“, его можно будет забрать по QR-коду из приложения», профиль
на wildberries.ru должен принадлежать владельцу кабинета WB Partners, возврат виден в его разделе
«Заказы». То есть продавец получает свой товар как обычный покупатель. Публичного покупательского
API у WB нет; `shkId`/`stickerId`/`srid` из `goods-return` — идентификаторы коробки и наклейки, а
НЕ код выдачи, свой QR из них не рисуем (см. запреты). Вывод: бот отвечает на «что и где лежит»,
код открывает человек в приложении WB — потолок автоматизации по WB здесь.

## Нужно от Сергея

Открытых вопросов нет.

Отклонено Сергеем 02.08.2026: подсказка «код выдачи — QR в приложении WB» и строка «лежит N дней»
в сводке. В сводке остаётся только то, что есть сейчас: где лежит и что внутри.

## Запреты

- Только **чтение** API площадок. Ничего не согласовываем, не оспариваем, не заявляем на вывоз,
  в кабинеты не пишем.
- Витрины fin (`margin_by_sku`, `ms_return_cogs`) не трогаем: там деньги, здесь физика.
- **Свой QR из номера возврата не рисуем.** Только коды, которые площадка отдала сама
  (Ozon PNG/`logistic.barcode`). Если ПВЗ сканирует другой код — картинка вводит в заблуждение.
- Мерж в `main` — только по явному «ок» Сергея.

## Следующий шаг

Посмотреть первую автоматическую сводку 03.08 в 09:05 МСК (`journalctl -u returns-daily -n 30`)
и по её виду решить пункты из «Нужно от Сергея».

## Команды

```bash
./venv/bin/python -m returns_bot.collect --dry-run          # посчитать, в БД не писать
./venv/bin/python -m returns_bot.collect                    # собрать и записать
./venv/bin/python -m returns_bot.daily_push --dry-run --no-collect   # текст сводки в консоль
./venv/bin/python -m returns_bot.daily_push --to <chat_id>  # разовая отправка одному
python3 tools/territory_guard.py --status                   # домен сессии = ret
```
