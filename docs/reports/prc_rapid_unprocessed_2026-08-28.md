# Рапид (SuperFine) — разбор файла несопоставленного, 28.08.2026

Поток `prc`. Вопрос: подключать ли `rapid.txt` к механизму «Новинки», как подключён Булат.
**Ответ: подключать нечего — новинок в файле нет вовсе.**

## Что в файле

Формат тот же `;`-CSV, что у Булата, но заполнен богаче: `name` 41/41, `Barcode` 30/41,
`msId` пуст у всех (строки по определению несопоставленные). Резолвер имени с сайта
поставщика, как `bulat_site`, здесь НЕ нужен — наименование приходит в письме.

Письмо приходит 5 раз в сутки (02:24, 07:24, 09:51, 16:24, 21:24 МСК), за август 116 писем.

## Главный факт

За август в письмах **44 разных артикулов, и у всех 44 есть живая карточка
МойСклада с ТОЧНЫМ совпадением артикула** (`ms_product.article`), с кодом поставщика
(суффиксы `sf` 33 и `oem` 8 — обе организации Рапида) и заполненным внешним кодом.
Дублей карточек по артикулу — 0. Архивных — 0.

То есть внешний загрузчик отдаёт как «несопоставленное» то, что сам же months держит
на складе: все 41 «мелькнувших» позиции стояли в `supplier_stock` и 26.08, и 27.08, и 28.08.

## Как распределены строки

| держится | артикулов | что это |
|---|---|---|
| в 1 письме | 41 | всплеск 28.08 07:24 — 41 строка, к 09:51 письмо снова пустое |
| в 2–5 письмах | 1 | SF-W1360X(ЧБС), 26–27.08 |
| в 6+ письмах | 2 | SF-CRG071(СЧ) и OEM W1360X(ЧБС), 21–25.08 |

Обычное состояние файла — 0–2 строки; за неделю 22–28.08 непустых писем меньше половины.

## Три «стойкие» позиции — тоже с карточками

| артикул письма | писем | карточка МС | код | вн. код |
|---|---|---|---|---|
| `SF-CRG071(СЧ)` | 22 | Картридж Canon 071 LBP120ser/MF270ser 1.2K С | 6559sf | 6559 |
| `OEM W1360X(ЧБС)` | 22 | Картридж HP W1360X С ЧИПОМ БЕЗ СЧЕТЧИКА LJ-M | 6865oem | 6865 |
| `SF-W1360X(ЧБС)` | 4 | Картридж HP W1360X С ЧИПОМ БЕЗ СЧЕТЧИКА LJ-M | 6865sf | 6865 |

Штрихкод из письма (`4627199115351` у SF-CRG071) в `ms_barcode` не найден ни один из 30 —
возможная зацепка к тому, по какому полю матчит загрузчик, но доказательством не является.

## Вывод и что делать

1. **Подключён к общему обработчику** по решению Сергея 28.08 (`SUPPLIERS["rapid"]`,
   `UNPROCESSED["rapid"]`, таймер `prc-price-watch@rapid.timer`). Первый `--apply` на файле
   28.08 07:24 (41 строка): 39 закрылись сами по артикулу (`exists`), 2 ушли человеку
   (`SF-CF218A` — сверху встали ОРИГИНАЛЬНЫЕ карточки без кода, `SF-E16`). Новинок 0 —
   ровно как и ожидалось по разбору.
2. **Это не наша задача разбора, а дефект внешнего загрузчика:** он не находит карточку,
   которая есть, заведена в июне–июле и всё это время стоит у него же на складе. Вопрос
   владельцу загрузчика — по какому полю идёт сопоставление (тот же открытый вопрос, что
   и по Булату). Всплеск 28.08 07:24 на 41 строку и возврат к нулю через 2.5 часа —
   похоже на разовый сбой, а не на новый ассортимент.
3. **Если хочется сторожа** — полезнее не разбор файла, а порог: строк в `rapid.txt`
   больше N подряд в нескольких письмах — сказать в бот. Иначе разовые всплески шумят.

Сырьё разбора: `/tmp/claude-0/-opt-mp-analytics/161d05c3-f2f4-4a8e-9a5e-ce8b3c0aef3e/scratchpad/rapid_month.json` (116 писем, 44 артикулов).
