# 14. Массовый сбор габаритов с nix.ru — сборщик готов к запуску (03.08.2026)

Путь: браузер + карточки `/autocatalog/`. Внутренний JSON `/scripts/` не используется.
К nix.ru с сервера не обращались ни одним запросом; проверки делались офлайн, на уже
сохранённом материале прошлого прогона.

## 1. Ключи поиска — колонка `article` добавлена

`tools/rebuild/step8_nix_input.py`: артикул берётся из **уже выгруженной** таблицы
`ms_product` (поле «Артикул» МойСклада), к API МС не обращались. Карточка модели —
та, чьё название стоит в этой же строке (та же выборка, что и для OEM).

| | строк |
|---|---|
| всего моделей в `nix_input.csv` | 2540 |
| получили `article` | **2459** |
| без `article` (в выгрузке пусто) | 81 |
| есть хотя бы один ключ (`article` или `oem_article`) | 2475 |
| нет ни одного ключа → пропуск с причиной в журнал | 65 |
| **пойдут в сбор** (есть ключ и читается наш тип товара) | **2160** |

Из очереди: только `article` — 1482, оба ключа — 662, только OEM — 16.
Остальные 328 из 2475 — бумага, чернила, ленточные кассеты, мастер-плёнка: наш тип
не читается однозначно, сверять тип не с чем → пропуск («лучше пусто, чем чужой размер»).
Обе строки посылаются в поиск **как есть**, посимвольно: без нормализации, без снятия
префиксов/суффиксов, без приведения регистра.

## 2. Что изменено в механике (`tools/nix_collect.py`, новый файл)

- **источник ссылок — ответ выдачи, а не DOM** (правка 03.08, см. §7): ждём ответ
  `FastSearch/goods`, который делает сама страница, и читаем `goods.html`;
- имя сохраняемого файла — **из canonical-ссылки внутри HTML**, не из заголовка;
- карточки складываются в `nix_raw/<external_code>/`;
- ключ `article` → при пустом результате отбора `oem_article`; оба пусты — пропуск с причиной;
- запуск, паузы и окно браузера — как в рабочем пробнике: `headless=False`, `locale=ru-RU`,
  viewport 1360×900, десктопный User-Agent, один поток, пауза 5–8 с случайная (и между
  моделями, и между карточками), resume по журналу, лимит моделей аргументом.

## 3. Как устроен отбор (до открытия карточек)

Из выдачи забираются **все** ссылки `/autocatalog/` с заголовками, затем отсев по заголовку:
код обязан стоять в заголовке; тип обязан совпасть с нашим (чип, девелопер, термоплёнка,
барабан = фотобарабан = драм-юнит, тонер-туба, картридж = тонер-картридж, тонер); тип не
читается — карточка пропускается; главная и «Архив каталога» — не карточки. Из прошедших
открываются **до пяти**. Каждая ссылка с вердиктом пишется в `nix_links.csv` — фильтр
проверяем задним числом.

Проверка отбора офлайн на выдаче прошлого прогона (10 моделей, заголовки как есть):
Q5949A 71→26 прошло, CE505X 67→23, Q7516A 41→16, 006R01179 10→5, C13S050630 3→1,
C13S050629 2→1; CC388A 5→0 (в выдаче только чипы), NPG-28 7→0 (драм-юниты),
C13S051099 2→0 (барабан без нашего кода), LS050099 1→0 (карточек нет). Это те же
6 из 10, что дали размеры вручную, — фильтр воспроизводит прежний результат и теперь
берёт до 5 источников вместо одного.

## 4. Разбор карточек

Парсер прежний: «Размеры упаковки (измерено в НИКСе)», «Вес брутто (измерено в НИКСе)»;
см→мм только точным ×10, исходная строка сохраняется рядом. По каждой величине в
`nix_dims.csv` пишутся файл, канонический адрес, заголовок карточки, метка и точная строка.
Проверено на 17 сохранённых карточках: 16 с размерами, 1 без — совпадает с прошлым разбором,
имена файлов из canonical уникальны (17/17), усечений и `.html.html` больше нет.

## 5. Флаги коробов — `tools/rebuild/step11_nix_flags.py`

Флаг — пометка, а не повод менять число. Правило 1: один и тот же короб у одного бренда
на ≥5 разных моделях → «универсальная коробка». Правило 2: внутри модели один источник
по объёму ≥2× следующего → «выброс вверх», совпадающие остаются. Выход —
`nix_dims_flagged.csv` с колонками `флаг`, `короб_сорт_мм`, `объём_л`, `бренд`.

Два подозрительных из прошлого прогона — **только по найденным источникам, без эталонов**:
- **Q7516A**: Cactus 199.2×321.4×487.1 мм = 31.19 л и NV-Print 185.9×246.9×471.1 мм = 21.62 л.
  Два независимых источника дают одинаково крупный короб (отношение 1.44×, до порога выброса
  не дотягивает). Вывод по имеющемуся: это не опечатка одного прайса, но и не подтверждение
  розничной упаковки — нужны остальные источники нового прогона.
- **006R01179**: Hi-Black 451.5×76.5×75.3 мм = 2.60 л и Sakura 315×150×93 мм = 4.39 л.
  Формы разные, объёмы одного порядка, выброса нет.

## 6. Готовность

Скрипт готов к запуску на 100 моделях: `python nix_collect.py 100` (рядом положить
`nix_input.csv`). Оценка времени: ~1 мин на модель (выдача + до 5 карточек с паузами) →
около 1.5–2 часов на 100 моделей. Вернуть: `nix_collect_log.csv`, `nix_links.csv`,
`nix_dims.csv`, `nix_collect.log` и папку `nix_raw` архивом.

В лог по каждой модели: пришёл ли ответ выдачи и сколько в нём позиций (`total`), ключ и
сработал ли он, ссылок в выдаче, прошло отбор, карточек открыто, размеров прочитано.

## 7. Правка после сорванного прогона: «ссылок 101» (03.08)

**Что было.** Прогон встал на 10-й модели: у всех артикулов в логе одинаковые «ссылок 101»,
отбор 0. 101 — это разметка страницы ДО отрисовки результатов. Переход на `state='attached'`
убрал единственное, что работало: прежний таймаут 30 с давал странице время выполнить
`FastSearch/goods` и обновить DOM.

**Что сделано.** Момент готовности вёрстки больше не угадывается:

1. страница открывается как раньше (`goto` → `reload`, тот же UA, то же окно);
2. ждём **ответ** `FastSearch/goods` — запрос делает сама страница, мы его не формируем
   и в `/scripts/` не ходим;
3. **ссылки и заголовки берутся из поля `goods.html` этого ответа**, DOM не читается вовсе;
4. ответа нет за 30 с → в журнал «выдача не получена», модель идёт дальше.
   Содержимое страницы вместо выдачи **не подставляется**;
5. тело первого ответа умирает при `reload` (`Protocol error`) — такой ответ пропускается,
   берётся следующий;
6. в журнале по каждому ключу: `article_ответ` / `oem_ответ` (да/нет), `article_total` /
   `oem_total`, ссылок, прошло отбор — подмена страницы видна с первой строки;
7. resume: пройденной считается модель, по которой выдача **получена** либо стоит явный
   «пропуск». Строка «выдача не получена» переигрывается. Журнал старого формата
   (шапка без новых колонок) автоматически переименовывается в `nix_collect_log.old.csv`,
   так что 10 моделей сорванного прогона будут пройдены заново.

**Проверка на сохранённых ответах вчерашнего прогона** (к nix.ru с сервера не обращались):

| код | `total` | ссылок из `goods.html` | прошло отбор |
|---|---|---|---|
| **Q5949A** | 75 | **71** | 26 |
| CE505X | 72 | 67 | 23 |
| Q7516A | 43 | 40 | 16 |
| NPG-28 | 6 | 6 | 0 (драм-юниты) |
| CC388A | 4 | 4 | 0 (чипы) |

Число 71 по Q5949A воспроизведено, заголовки читаются у всех ссылок. Отдельно проверены:
проход одной модели целиком (мёртвый первый ответ пропущен, взят следующий), поведение при
неприходе выдачи (0 ссылок, ничего не подставлено), ротация журнала старого формата и то,
что строка «выдача не получена» не считается пройденной. Живая проверка на 3-4 артикулах —
за Сергеем: с сервера к nix.ru не обращаемся.

## 8. Поиск по артикулу и предел времени (правка после прогона 03.08)

### 8.1. Параметр режима «артикул» по записям НЕ определяется

Проверены все 6 записанных тел `FastSearch/goods` (`nix_requests.jsonl`). В каждом 26 полей,
между запросами различаются **только два**: `keywords` и `sign`. Все шесть сделаны в режиме
по умолчанию («товар»), примера с включённым переключателем в материале нет вовсе.
Поля, похожие на режим, во всех запросах одинаковы и пусты: `fts=''`, `autoFTS='0'`,
`fts_sort='0'`, `fts_stats='true'`, `filter_type=''`, `tag=''`, `sch_id=''`, `sch_good_id=''`.
Какое из них включает поиск по артикулу — по этим данным сказать нельзя, а угадывать запрещено.

**Что сделано вместо угадывания.** Параметр не подделывается: скрипт включает сам
переключатель на странице, как человек. Флажок ищется по подписи («артикул») среди всех
`input[type=checkbox|radio]` — по `label[for]`, по родительской метке, по тексту рядом.
В журнал (`переключатель_артикула`) пишется, что найдено и включилось: подпись, `name`, `id`,
`value` — либо «переключатель „артикул“ не найден». Если после включения страница не переискала
сама, ключ вводится в поле поиска и жмётся Enter.

**Параметр снимется сам на первом же прогоне.** Тела запросов выдачи пишутся в
`nix_search_probe.jsonl` (модель, ключ, режим, тело). Сравнение строки режима «артикул»
со строкой режима «товар» даст имя поля буквально — и фактический `store` заодно.
Если переключатель не найдётся, в журнале это будет видно первой же строкой, и снимать
параметр придётся пробником вручную.

### 8.2. Отбор раздвоен по способу поиска

| найдено по | чем подтверждается | где |
|---|---|---|
| `article` | артикул встречается **в самой карточке** | текст/разметка открытой карточки |
| `oem_article` | код стоит **в заголовке** | заголовок из выдачи |

Оба сравнения буквальные, посимвольные, без нормализации. Проверка типа товара действует
в обоих случаях. В режиме `article` строка выдачи, рядом с которой артикул уже виден, идёт
как «прошла»; если таких нет — открываются «кандидаты» и проверяются по карточке. Карточка,
где артикул не подтвердился, в `nix_dims.csv` не попадает (вердикт остаётся в `nix_links.csv`).

### 8.3. Предел времени — безусловный

Причина зависания: тело ответа читалось методом `Response.text()`, который в sync-Playwright
ждёт **без таймаута**; при пустой выдаче страница нужного запроса не делает, и ожидание
не кончалось никогда. Теперь тело не читается этим методом вовсе: ответ перехватывается
на самой странице (init-скрипт наблюдает XHR/fetch по `FastSearch/goods`), а ожидание идёт
через `wait_for_function` с жёстким таймаутом.

- `STEP_MAX_S = 60` — предел на один шаг (выдача по ключу, открытие одной карточки);
- `MODEL_MAX_S = 180` — предел на модель целиком, включая паузы; превышен → в журнал
  «превышен предел времени на модель» и дальше;
- `ctx.set_default_timeout(60 с)` — ни один вызов Playwright не ждёт дольше;
- каждый таймаут вычисляется как остаток до дедлайна: бесконечной ветки не осталось.

**Почему не 60 с на модель, как предлагалось:** в 60 с не помещаются 5 карточек — одни паузы
5–8 с дают 25–40 с. 60 с оставлены на шаг, 180 с — на модель. Числа в шапке файла, менять одной строкой.

В журнале различаются: `выдача не получена (ответ не пришёл)` — наша проблема; `выдача получена,
total=0` — нормальный ответ сайта. Обе строки-ключа логируются раздельно (`article_ответ` /
`article_total` / `oem_ответ` / `oem_total`), добавлена колонка `секунд`.
Resume: модель с «превышен предел» и с «выдача не получена» пройденной **не** считается.

### 8.4. store

В записанных запросах удачного прогона `store=msk-0_1721_1` — другого значения в материале
нет вообще, `all` не встречается ни разу. В адресе скрипта стоит ровно `msk-0_1721_1`,
менять нечего: `store=all` подставил сам сайт (магазин не выбран — нет куки города).
Фактическое значение теперь будет видно в `nix_search_probe.jsonl`.

### 8.5. Проверка (офлайн, к nix.ru с сервера ни одного запроса)

| сценарий | поведение |
|---|---|
| выдача пришла (Q5949A) | 71 ссылка при `total` 75, отобрано 26 |
| выдача пустая, `total=0` | 0 ссылок, итог «total=0», прогон идёт дальше |
| ответа нет вовсе | упирается в предел, «выдача не получена», идёт дальше |
| режим `article`, переключатель найден | в журнал `name=…, id=…, value=…`; строка с артикулом → «прошла», остальные не открываются |
| подтверждение в карточке | `CS-Q5949A` найден; `CS-Q5949AS` — нет (посимвольно, и это правильно) |
| resume | «превышен предел» и «выдача не получена» переигрываются |

Ограничения соблюдены: к nix.ru с сервера ни одного запроса; в WB/Ozon/Яндекс и реестр
ничего не писалось; к API МойСклада не обращались (только выгруженная таблица);
платного не трогали; размеры не додумывались и не усреднялись.
