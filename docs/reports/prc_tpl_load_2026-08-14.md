# Шаблон 14.08.2026 — проверка и загрузка (52 строки)

Источник: `incoming/prc/tpl_2026-08-14.xlsx` (ручной шаблон человека, необработанные строки
поставщиков). Правленая копия: `incoming/prc/tpl_2026-08-14_fixed.xlsx` — присланный файл не тронут
(правило 35). Проверка соотнесения: `docs/reports/prc_tpl_check_2026-08-14.md`.

Поставщики: ООО «ТОНЕРСТОР» 47, ООО «Солюшнс принт» МСК 5. Аббревиатуры кода: `sg` 43,
`sp` 5, `el` 2, `bt` 1, `sl` 1 — сверены с живыми карточками МС (Smart Graphics 1211 `sg`,
Sprint/SP 3927 `sp`, s-Line 1179/1181 `sl`, e-Line 229/229 `el`).

## Занятые коды: 19 строк

| Что | Сколько | Решение Сергея 19.08.2026 |
|---|---|---|
| Артикул отличается ТОЛЬКО разделителями (дефис/пробел/скобка) | 15 | дубль: старую в архив, чистим Артикул/Код/Внешний код (МС ставит свой автокод), заводим новую с верным артикулом |
| Отличие в ЦИФРАХ (`3481sg` номер OEM не дописан, `1798sg` в МС 7 цифр вместо 8) | 2 | так же, как дубли |
| Отличие в доп. БУКВАХ (`1325sg` «SG-SP150HE A_M») | 1 | другой товар: новая карточка, старая живёт (57 шт на Удалённом складе) |
| Другой артикул того же товара (`5781bt` AFRX00B230030 против AFRX00B225010) | 1 | новая партия с новым артикулом — новая карточка |

Перед архивацией у всех 17 проверен остаток по КАЖДОМУ складу (`/report/stock/bystore`) — везде 0.
Бэкап прежних значений: `reports/data/prc_arch_dupes_2026-08-19.json` (id, code, article,
externalCode, archived, name) — откат возможен построчно.

## Правки полей (наши правила, а не как заполнил человек)

Всем 52: НДС 22, ед. «шт», страна Китай, гарантия/срок службы 365, Название WB через `wb_name()`,
группа и Code128 — как у родни по внешнему коду, поставщик — контрагент строки.
Точечно: `6950el` группа «Картриджи Pantum совм.» → «Картриджи Pantum»; `7200sl` (TK-1250,
`AFKYMA3500010`) — новый внешний код **7200**, Code128 по маске семейства `DSQSR0007200`,
хвостовой пробел в названии WB убран, вес пуст (у родни нет — родни нет вовсе).
**Наименование поставщика не правим** (решение Сергея): у `5874sg` в названии остаётся 54 500 стр.,
хотя верный ресурс 160 000 (`5874pl`); «Связь» не проставлена ни у кого — у родни её нет.
В 13 строках чип не назван, у родни «с чипом» — названия оставлены как прислал поставщик.

## Итог

Архивировано 17, создано **52** карточки. `tools/prc/tpl_verify.py` — расхождений 0;
две строки (`1325sg`, `5781bt`) отчёт помечает «карточек в МС 2» — это ожидаемо, у них по решению
Сергея код совпадает со старой карточкой; поля обеих новых сверены вручную и верны.
