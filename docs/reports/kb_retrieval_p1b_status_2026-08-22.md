# P1b — СТАТУС: ЗАМОРОЖЕНО ПОСЛЕ S1–S3 (2026-08-22)

Поток `infra`. Решение Сергея 22.08.2026: **дальнейшая разработка P1b заморожена
до Native Claude Code Memory Capability Audit.** S1–S3 сохранены как экспериментальная
реализация, не как продакшн-путь.

## Что сделано и лежит в git

`/opt/mp-knowledge`:

| коммит | что |
|---|---|
| `26f7bf9` | S0: 3 карточки от сессий 21.08 (`mp-news-api-access`, `ms-supplier-code-vs-external-code`, `project-mp-biz-diary`) + их указатели в индексе + `.gitignore`. Провенанс проверен, содержимое индекса не правилось |
| `5a04989` | S1–S3: `lib/kb_dialog.py` (новый), `lib/kb_retrieval.py` (взвешенный запрос), `tests/test_dialog.py` |

Реализовано ровно по утверждённому дизайну: логические обмены K=2, только та же сессия,
свежесть ≤90 мин, `isSidechain` / `tool_result` / `thinking` исключены, переход от
`set(tokens)` к `dict[stem → weight]` через MAX, отсечка 60 стемов по весу, адаптивная α
от детерминированной самодостаточности, алиасы/owner/теги только от промпта, хвост
никогда не инжектится модели.

**Совместимость:** str-режим бит в бит равен P0 — сверено на 65 запросах (gold + наборы P1),
0 расхождений. Тесты: 32 P0 без правок + 27 новых = **59 OK**.

## Пиннутый корпус эвала (S0)

`scratchpad/p1b/canon_snapshot/` — read-only копия канона на коммите `26f7bf9`:
**91 карточка**, отпечаток тел `ebc2174184d1bbb2`
(sha256 по отсортированным `slug + \0 + body + \n`, формула записана в `SNAPSHOT.json`).
Пересоздаётся командой из `SNAPSHOT.json`. Живой канон с тех пор уже сдвинулся
(+1 untracked карточка) — ровно тот дрейф, от которого пиннинг и защищает.

Поправка к дизайну: в `§0` было «92 карточки» — это счёт файлов, включая симлинк
`memory/MEMORY.md`. `load_cards()` даёт **91**.

## Что НЕ сделано (сознательно, заморожено)

S4 (пересборка наборов 60/40 со стратами и замороженными хвостами), S5 абляции,
S6 выбор конфигурации, S7 перекалибровка порогов, S8 sealed holdout.
Гейты, утверждённые Сергеем, зафиксированы в этом файле и в плане, но **ни один прогон
не выполнялся** — цифр P1b нет и заявлять их нельзя.

## Что не тронуто

- хука `UserPromptSubmit` нет, инъекции нет, P2 не начат;
- `index/MEMORY.full.md` не правился (закоммичены только чужие уже сделанные правки);
- наборы P1 (30/15) остаются выведенными из обращения;
- эмбеддингов/векторной БД нет, LLM query rewrite нет.

## Точка возврата

Возобновление — после Native Claude Code Memory Capability Audit, со стартом с S4
на пиннутом корпусе `26f7bf9` / `ebc2174184d1bbb2`. Дизайн и план не переписывать:
`kb_retrieval_p1b_design_2026-08-22.md`, `kb_retrieval_p1b_plan_2026-08-22.md`.
Утверждённые гейты (агрегат ≥85%, self-contained ≥90%, anaphoric_same_session ≥75%,
collision-heavy ≥80%, уверенно-неверный top-1 = 0, ложная инъекция no-knowledge ≤15%,
p95 построения хвоста ≤250 мс; `anaphoric_cross_session` — только диагностика).
