# BRIEF_<ПОТОК> — поток «<название>»

> Шаблон по политике «Экономия токенов» (CLAUDE.md, п. 1). **Потолок — 150 строк.**
> Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_<ПОТОК>.md» — и больше ничего.
> Полный `docs/HANDOFF.md` (40 тыс. токенов) при старте НЕ читать: только по прямой просьбе
> и только когда реально нужна координация с другим потоком.
> Историю этапов своего потока вынести в `docs/handoff/archive/<поток>_<дата>.md`.

## Где работаю
- Ветка: `<поток>/<...>`  ·  worktree: `.claude/worktrees/<поток>` (если есть)
- Модель сессии: **Sonnet** (механика) / **Opus** (вердикты, архитектура, ревью) — выбрать на
  старте и НЕ менять по ходу.

## Зона ответственности
Одним абзацем: за что этот поток отвечает и чего касается.

## Мои файлы (правлю я)
Коллекторы / витрины / оркестратор / блок миграций. Чужое не трогаю (CLAUDE.md, правило 12).

## Границы
Что читаю read-only и кому принадлежит. Куда писать нельзя.

## Запреты потока
Жёсткие «нельзя» именно этого потока (отправка на площадки, платные ресурсы, запись в чужую
витрину). Общие запреты дублировать не нужно — они в CLAUDE.md.

## Состояние на <дата>
3–7 строк: что сделано и работает, какими цифрами это подтверждено.

## Открытые вопросы
Список с указанием, чьё решение нужно (Сергея / другого потока).

## Следующий шаг
Одна конкретная задача, с которой начинает следующая сессия.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/`, в брифе оставить только живое.
- Закончил кусок — обновил бриф и закрыл сессию. `--resume` вчерашней сессии запрещён:
  протухший кэш переписывается целиком, новая сессия с брифом дешевле.
