# BRIEF_INV — поток «inv» (банковский контур: выписки, платежи, счета)

> Потолок 150 строк (CLAUDE.md, «Экономия токенов», п. 1).
> Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_INV.md» — и больше ничего.
> Детальные карты API — `docs/ALFA_BANK_API.md`, `docs/SBER_BANK_API.md` (читать по нужде).

## Где работаю
- Ветка: `main` (поток исторически коммитит прямо в main; крупные правки — `inv/*`)
- Модель сессии: **Sonnet** (механика: интеграции по документации, прогоны) /
  **Opus** (архитектура контура, спорные решения) — выбрать на старте и НЕ менять.

## Зона ответственности
Деньги на входе и выходе: выписки банков → МойСклад (`paymentin`/`paymentout`), привязка
платежей к приёмкам, черновики платёжных поручений, счета поставщиков и сроки оплаты.
**Два контура, две организации:** Альфа-Банк — ООО «Цифровой Квадрат»; Сбер — ООО «ДИСКВЭР».

## Мои файлы (правлю я)
`collectors/alfa_*.py`, `collectors/sber_*.py`, `invoice_bot/*`, `ops/sber_secret_rotate.py`,
`migrations/2xx`, `docs/ALFA_BANK_API.md`, `docs/SBER_BANK_API.md`.

## Границы
`margin_by_sku` и витрины fin — **read-only**. Веб-дашборд (`web/app.py`) — не мой, изменения
запрашивать у владельца. Секреты только в `.env` и `secrets/` (оба gitignored).

## Запреты потока
- **Боевые платёжки — только черновиками** (без ЭП): банк создаёт документ в статусе «черновик»,
  подписывает человек в вебе. Автоподписание не реализовывать.
- Ключи, токены, `client_secret` — **никогда** в чат, лог и git (в логах только длина и хвост).
- Ротацию/перевыпуск боевых кредов запускать осознанно: последствие отказа — встал весь контур.

## Состояние на 2026-08-03

**Альфа (Цифровой Квадрат)** — в проде. Выписка → МС, привязка платежей к приёмкам,
черновики платёжек включены боевыми 30.07.2026 (первый черновик №519). mTLS + ApiKey.

**Сбер (Дисквэр)** — в проде: авторизация (`collectors/sber_auth.py`, OAuth + mTLS), выписка
(`collectors/sber_statement.py`, счёт `…147717`), запись в МС и привязка платежей к приёмкам
включены боевыми 02.08 (`SBER_MS_APPLY=1`, коммит `8a778b6`). Детали подключения, разрез по
юрлицу и закрытый дефект антидубля — `docs/handoff/archive/inv_2026-08-03.md`.
- `client_secret` живёт **40 дней**: активирован 2026-08-02, дедлайн **2026-09-11**.
  `ops/sber_secret_rotate.py` — автоматическая ротация на 30-й день, предупреждения в TG с 25-го,
  защита от «запрос ушёл, ответ потерялся» через `secret_pending.json` + `--resolve`.
- `po_payment_watch` работает по обеим фирмам (`org_inn`, к банку не обращается — только
  МС→Postgres). Грабли: `process_prepayment_balance` не пересчитывает уже висящий `planned` —
  старый черновик снимать руками. Пачка отсрочки не дублируется: её заказы уходят в `queued`.

**Отправка платёжек — ручной кнопкой, для обеих организаций (сделано 02–03.08).** Тем же приёмом,
что «выписка → МС»: банконезависимое ядро + тонкие драйверы.
- `invoice_bot/payment_draft.py` — ядро сборки платёжки (реквизиты из МС, счета-основания, НДС,
  назначение, `load_drafts` / `mark_sent` / `mark_error`). Три хардкода Цифрового Квадрата ушли
  в `draft["org_inn"]`; ключевое — карточка получателя ищется по условиям оплаты **своего**
  юрлица, иначе Дисквэр брал бы реквизиты из строк Цифрового.
- `invoice_bot/alfa_payment_draft.py` — драйвер Альфы (утоньшён, поведение прежнее: dry-run до и
  после рефактора совпал побайтово, `externalId` не поехал), `invoice_bot/sber_payment_draft.py` —
  драйвер Сбера (`POST /fintech/api/v1/payments`, `urgencyCode="INTERNAL"`, счёт списания из
  `client-info`, статус всегда `sent_prod` — песочницы нет).
- `invoice_bot/payment_send.py` — диспетчер: банк выбирается по `org_inn` черновика.
  Кросс-банк закрыт тремя рубежами (эндпоинт сверяет `org_inn` с БД → выбор драйвера по БД →
  `_check_org` внутри драйвера перед HTTP, fail-closed). Двойной клик — `pg_try_advisory_lock`
  на время HTTP, плюс детерминированный `externalId` = uuid5(банк, id черновика).
- Веб: `POST /api/suppliers/payment-terms/queue/send` + кнопка «в банк» в строках очереди
  (только у `planned`; юрлицо берётся ИЗ СТРОКИ, поэтому кнопка работает и в режиме «Все»).
  Подтверждение перед отправкой прямо говорит, что платёжка уйдёт НЕПОДПИСАННОЙ.
- **Все флаги сняты 03.08 по прямым командам Сергея** («ставь apply=1 всем», «снимай блокировку
  сбера»): `WEB_PAYMENT_SEND=1`, `ALFA_PAYMENT_APPLY=1` + `ALFA_PAYMENT_PROD_READY=1`,
  `SBER_PAYMENT_APPLY=1` + `SBER_PAYMENT_PROD_READY=1`. Оба контура боевые: и клик, и крон
  создают реальный неподписанный документ. Обратного хода нет — лишнее отклоняется в вебе банка.
- Проверено: dry-run Дисквэра (плательщик …147717, получатель из карточки МС Дисквэра, НДС по
  `vat_rate` условий) и Цифрового; кросс-банк отбит; несуществующий id отбит; выключенный
  `WEB_PAYMENT_SEND` даёт понятный отказ; лок на двойной клик срабатывает.
- Статусы очереди в дашборде развели по цвету 03.08: жёлтый `запланирован` → синий **`в банке`**
  (документ создан и НЕ подписан) → зелёный `проведён` (деньги видны в МС). Раньше «отправлен»
  и «проведён» были одинаково зелёными; под таблицей легенда.

**Автоотправка по крону — включена 03.08** (`3b3e414`), решение Сергея: предоплата по счёту
уходит в банк по факту заказа в МС, авансы и отсрочка — следующим шагом после выписки; обе фирмы;
**без потолка суммы** (платёжка без ЭП, деньги без подписи человека не двигаются).
- `invoice_bot/payment_autosend.py` — обвязка над `payment_send.send_draft`: берёт `planned`
  выбранных юрлиц и оснований, шлёт последовательно с паузой 1 с, ошибка строки не роняет прогон,
  сводка ≤20 строк в лог и в TG (молчит, когда отправлять нечего). Рубильник `PAYMENT_AUTOSEND`
  в `.env` — стоп-кран, крон при этом трогать не надо; гейты банков продолжают действовать поверх.
- `po_payment_watch`: `set_org()` + `--org all|<инн>` (крон наполнял очередь **только по ЦК** —
  дыра закрыта) и `--methods`. Вечерний прогон сужен до предоплаты: у `deferred` нет гейта
  «один planned за раз», полный прогон дважды в день дробил бы пачку отсрочки надвое.
  `mark_executed_drafts` под фильтр НЕ попадает — оплаченное руками не должно уйти повторно.
- Крон (UTC): `43 4` поллер обе фирмы → `55 4` (07:55 МСК) авансы+отсрочка; `0 14` (17:00 МСК)
  поллер по предоплате → отправка предоплаты. Лог `/opt/mp-analytics/payment_autosend.log`.
- **Сводка идёт в отдельный бот `@ds_fin_bot`** (`TG_PAY_BOT_TOKEN` / `TG_PAY_NOTIFY_ID` в `.env`,
  `ca1319b`): суммы и получатели не должны попадать в общий канал invoice-bot, который читает
  и Алексей. Отката на `TG_BOT_TOKEN` в `tg()` нет намеренно — бот не настроен, сводка не уходит
  никуда, отправка платежей при этом работает. Канал проверен живым сообщением 03.08.
- **Первый прогон 03.08: ушли все 10 висевших черновиков, 891 902.47 ₽** (ЦК 6, Дисквэр 4),
  ошибок 0; первой отправлена одна сберовская под наблюдением. Повторный прогон отправляет 0,
  дублей `external_id` нет; двойной прогон поллера с фильтром новых пачек не создаёт.

**УПД → Приёмка: пустые страна/ГТД (КТ-000160, 03.08).** Приёмка создалась, но страна и ГТД
не проставились: матчер строк УПД↔позиции заказа сравнивал артикулы токенами на РАВЕНСТВО, а
поставщик пишет их иначе — `BS-TK-865Y/1T02JZAEU0` против нашего `BS-TK865Y` (другая расстановка
дефисов + дописанные OEM-коды через «/»). Различающий цвет тонул в общих слабых токенах серии
(`250CI/300CI`, `12000`) → уникального максимума нет → 0 матчей из 7, и приёмка ушла пустой молча.
- Матчер: шаг **1c** в `match_rows` — артикул заказа без разделителей (`_art_key`) ищется
  ПОДСТРОКОЙ в наименовании строки УПД, берём при единственном попадании. КТ-000160: 0 → 7/7,
  цвет к цвету, количества сходятся. Регресс проверен старым/новым матчером на 12 прежних УПД
  (165 позиций) — расхождений нет.
- Режим **`--fill`**: приёмка на заказ уже есть → не плодить дубль, а дозаполнить страну/ГТД
  построчно (+ те же поля в карточках); количества, цены и шапку не трогает. КТ-000160
  дозаполнена боевым прогоном 03.08 (7 позиций, 7 карточек, проверено чтением из МС).
- Гейт: `matched == 0` теперь даёт громкое предупреждение в отчёте (раньше молча пустая приёмка).

**Счёт → Заказ у того же поставщика: 0 из 7 (КТ-000117, 03.08).** Тот же корень, другая половина
конвейера — `invoice_to_po`. Два дефекта:
- **Регексп артикула Блоссома обрывался на «/»**: `BS-M-TNP-50/51M/A0X5354/A0X5355` → `BS-M-TNP-50`,
  общий огрызок серии, цвет потерян. В `pattern` добавлен `/` (как у Феррета) → артикул берётся
  целиком, дальше срабатывает существующее отсечение хвостов по «/».
- **Точный `filter=article` не находит карточку при иной расстановке дефисов** (`BS-TK-865C` против
  нашего `BS-TK865C`). Добавлен последний рубеж `_find_by_art_key`: пул сужается самым длинным
  сырым префиксом через `article~=` (пустой пропускаем, >300 — сдаёмся), внутри пула наш артикул
  без разделителей должен быть НАЧАЛОМ поставщицкого; берём самое длинное совпадение и только
  если оно одно. КТ-000117: 0 → **7/7**, сумма 13 926.00 ₽ сошлась со счётом и УПД, цвет к цвету.
- Регресс: 20 счетов (КВК, Одиссей, Солюшнс, Колортек) прогнаны старым и новым матчером —
  345 прежних матчей не тронуты, потеряно 0, новые матчи только у КТ-000117.
- Заказ КТ-000117 в МС уже заведён руками — пересоздавать не требуется, правка работает вперёд.

Коммиты: `0cba70c` (карта API + авторизация), `f62dc91` (выписка v2), `d6dc9ef` (ручная кнопка
для обеих фирм), `3b3e414` (автоотправка по крону). Всё в `main`; на GitHub не выложено —
ждёт отдельного «ок».

## Открытые вопросы
1. ~~Прогонять ли ротацию вживую сейчас~~ — **решено Сергеем 2026-08-02: ждать 30-го дня**,
   боевой прогон не делаем. Первое срабатывание автоматическое **2026-09-01** (cron `--cron`,
   тег `SBER_SECRET_ROTATE`, 06:17 UTC), предупреждения в TG с 27.08. **Следить за TG в эти дни:**
   отказ банка по формату кандидата придёт сообщением; контур при этом жив на старом секрете,
   но дедлайн 11.09 не двигается — тогда чинить руками до дедлайна.
2. ~~Префикс платёжных эндпоинтов Сбера~~ — **закрыт 02.08: платежи на `v1`** (выписка на `v2`);
   подробности — `docs/SBER_BANK_API.md` §2.
3. ~~`vat` / `urgencyCode` в теле РПП~~ — **закрыт 02.08: модель их не требует**; НДС пишем
   текстом в `purpose`, как у Альфы.
3a. Живой POST в Сбер сделан 03.08 — банк принял 4 документа без `digestSignatures` (наш статус
   `sent_prod`). **Осталось подтвердить глазами в СберБизнес**, что они лежат черновиками и
   ждут подписи, а не ушли на исполнение. То же по Альфе (6 документов).
4. Есть ли у Сбера push/webhook о зачислениях, или только поллинг выписки, как у Альфы.
5. ~~Мосты «назначение → приёмка» для Дисквэра~~ — **снят 02.08**: состав берём из черновика.
6. ~~Три авансовых черновика Дисквэра на 125 000 ₽~~ — **отправлены 03.08 по прямому решению
   Сергея** («отправить всё первым прогоном»). Природа их прежняя: баланс авансов считается от
   нуля, прежние авансы платились руками и в нашей очереди их нет. Следствие на будущее: пока
   ручные авансы не попадут в очередь, поллер будет предлагать аванс заново — смотреть на
   `note` («баланс 0 ₽ < порог») и снимать лишнее ДО 07:55, теперь отправка автоматическая.

## Следующий шаг
1. **Глазами в Альфе и СберБизнес:** 10 отправленных 03.08 документов должны лежать
   **неподписанными черновиками** (закрывает вопрос 3a). Лишнее — отклонить в вебе банка.
2. Первое автоматическое срабатывание крона — 04.08, 07:55 и 17:00 МСК. Проверить
   `payment_autosend.log` и сводку в TG; при странном — снять `PAYMENT_AUTOSEND` в `.env`
   (крон трогать не надо) и разбираться.
3. Наблюдение по Сберу: первые дни смотреть в `alfa_statement.log` строку «Сбер …» — «создан N»
   означает, что автозапись выписки опередила ручной ввод.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/inv_<дата>.md`, в брифе только живое.
- Закончил кусок — обновил бриф и закрыл сессию. `--resume` вчерашней сессии запрещён.
