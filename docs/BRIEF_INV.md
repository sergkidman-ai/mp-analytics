# BRIEF_INV — поток «inv» (банковский контур: выписки, платежи, счета)

> Потолок 150 строк (CLAUDE.md, «Экономия токенов», п. 1).
> Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_INV.md» — и больше ничего.
> Детальные карты API — `docs/ALFA_BANK_API.md`, `docs/SBER_BANK_API.md` (читать по нужде).

## Где работаю
- Ветка: `main` (поток исторически коммитит прямо в main; крупные правки — `inv/*`)
- Модель сессии: **Sonnet** (механика: интеграции по документации, прогоны) /
  **Opus** (архитектура контура, спорные решения) — выбрать на старте и НЕ менять.

## Зона ответственности
Деньги на входе и выходе: выписки банков → МойСклад (`paymentin`/`paymentout`), привязка
платежей к приёмкам, черновики платёжных поручений, счета поставщиков и сроки оплаты.
**Два контура, две организации:** Альфа-Банк — ООО «Цифровой Квадрат»; Сбер — ООО «ДИСКВЭР».

## Мои файлы (правлю я)
`collectors/alfa_*.py`, `collectors/sber_*.py`, `invoice_bot/*`, `ops/sber_secret_rotate.py`,
`migrations/2xx`, `docs/ALFA_BANK_API.md`, `docs/SBER_BANK_API.md`.

## Границы
`margin_by_sku` и витрины fin — **read-only**. Веб-дашборд (`web/app.py`) — не мой, изменения
запрашивать у владельца. Секреты только в `.env` и `secrets/` (оба gitignored).

## Запреты потока
- **Боевые платёжки — только черновиками** (без ЭП): банк создаёт документ в статусе «черновик»,
  подписывает человек в вебе. Автоподписание не реализовывать.
- Ключи, токены, `client_secret` — **никогда** в чат, лог и git (в логах только длина и хвост).
- Ротацию/перевыпуск боевых кредов запускать осознанно: последствие отказа — встал весь контур.

## Состояние на 2026-08-03

**Альфа (Цифровой Квадрат)** — в проде. Выписка → МС, привязка платежей к приёмкам,
черновики платёжек включены боевыми 30.07.2026 (первый черновик №519). mTLS + ApiKey.

**Сбер (Дисквэр)** — в проде: авторизация (`collectors/sber_auth.py`, OAuth + mTLS), выписка
(`collectors/sber_statement.py`, счёт `…147717`), запись в МС и привязка платежей к приёмкам
включены боевыми 02.08 (`SBER_MS_APPLY=1`, коммит `8a778b6`). Детали подключения, разрез по
юрлицу и закрытый дефект антидубля — `docs/handoff/archive/inv_2026-08-03.md`.
- `client_secret` живёт **40 дней**: активирован 2026-08-02, дедлайн **2026-09-11**.
  `ops/sber_secret_rotate.py` — автоматическая ротация на 30-й день, предупреждения в TG с 25-го,
  защита от «запрос ушёл, ответ потерялся» через `secret_pending.json` + `--resolve`.
- `po_payment_watch` работает по обеим фирмам (`org_inn`, к банку не обращается — только
  МС→Postgres). Грабли: `process_prepayment_balance` не пересчитывает уже висящий `planned` —
  старый черновик снимать руками. Пачка отсрочки не дублируется: её заказы уходят в `queued`.

**Отправка платёжек — ручной кнопкой, для обеих организаций (сделано 02–03.08).** Тем же приёмом,
что «выписка → МС»: банконезависимое ядро + тонкие драйверы.
- `invoice_bot/payment_draft.py` — ядро сборки платёжки (реквизиты из МС, счета-основания, НДС,
  назначение, `load_drafts` / `mark_sent` / `mark_error`). Три хардкода Цифрового Квадрата ушли
  в `draft["org_inn"]`; ключевое — карточка получателя ищется по условиям оплаты **своего**
  юрлица, иначе Дисквэр брал бы реквизиты из строк Цифрового.
- `invoice_bot/alfa_payment_draft.py` — драйвер Альфы (утоньшён, поведение прежнее: dry-run до и
  после рефактора совпал побайтово, `externalId` не поехал), `invoice_bot/sber_payment_draft.py` —
  драйвер Сбера (`POST /fintech/api/v1/payments`, `urgencyCode="INTERNAL"`, счёт списания из
  `client-info`, статус всегда `sent_prod` — песочницы нет).
- `invoice_bot/payment_send.py` — диспетчер: банк выбирается по `org_inn` черновика.
  Кросс-банк закрыт тремя рубежами (эндпоинт сверяет `org_inn` с БД → выбор драйвера по БД →
  `_check_org` внутри драйвера перед HTTP, fail-closed). Двойной клик — `pg_try_advisory_lock`
  на время HTTP, плюс детерминированный `externalId` = uuid5(банк, id черновика).
- Веб: `POST /api/suppliers/payment-terms/queue/send` + кнопка «в банк» в строках очереди
  (только у `planned`; юрлицо берётся ИЗ СТРОКИ, поэтому кнопка работает и в режиме «Все»).
  Подтверждение перед отправкой прямо говорит, что платёжка уйдёт НЕПОДПИСАННОЙ.
- **Все флаги сняты 03.08 по прямым командам Сергея** («ставь apply=1 всем», «снимай блокировку
  сбера»): `WEB_PAYMENT_SEND=1`, `ALFA_PAYMENT_APPLY=1` + `ALFA_PAYMENT_PROD_READY=1`,
  `SBER_PAYMENT_APPLY=1` + `SBER_PAYMENT_PROD_READY=1`. Оба контура боевые: и клик, и крон
  создают реальный неподписанный документ. Обратного хода нет — лишнее отклоняется в вебе банка.
- Проверено: dry-run Дисквэра (плательщик …147717, получатель из карточки МС Дисквэра, НДС по
  `vat_rate` условий) и Цифрового; кросс-банк отбит; несуществующий id отбит; выключенный
  `WEB_PAYMENT_SEND` даёт понятный отказ; лок на двойной клик срабатывает.
- Статусы очереди в дашборде развели по цвету 03.08: жёлтый `запланирован` → синий **`в банке`**
  (документ создан и НЕ подписан) → зелёный `проведён` (деньги видны в МС). Раньше «отправлен»
  и «проведён» были одинаково зелёными; под таблицей легенда.

**Автоотправка по крону — включена 03.08** (`3b3e414`), решение Сергея: предоплата по счёту
уходит в банк по факту заказа в МС, авансы и отсрочка — следующим шагом после выписки; обе фирмы;
**без потолка суммы** (платёжка без ЭП, деньги без подписи человека не двигаются).
- `invoice_bot/payment_autosend.py` — обвязка над `payment_send.send_draft`: берёт `planned`
  выбранных юрлиц и оснований, шлёт последовательно с паузой 1 с, ошибка строки не роняет прогон,
  сводка ≤20 строк в лог и в TG (молчит, когда отправлять нечего). Рубильник `PAYMENT_AUTOSEND`
  в `.env` — стоп-кран, крон при этом трогать не надо; гейты банков продолжают действовать поверх.
- `po_payment_watch`: `set_org()` + `--org all|<инн>` (крон наполнял очередь **только по ЦК** —
  дыра закрыта) и `--methods`. Вечерний прогон сужен до предоплаты: у `deferred` нет гейта
  «один planned за раз», полный прогон дважды в день дробил бы пачку отсрочки надвое.
  `mark_executed_drafts` под фильтр НЕ попадает — оплаченное руками не должно уйти повторно.
- Крон (UTC): `43 4` поллер обе фирмы → `55 4` (07:55 МСК) авансы+отсрочка; `0 14` (17:00 МСК)
  поллер по предоплате → отправка предоплаты. Лог `/opt/mp-analytics/payment_autosend.log`.
- **Сводка идёт в отдельный бот `@ds_fin_bot`** (`TG_PAY_BOT_TOKEN` / `TG_PAY_NOTIFY_ID` в `.env`,
  `ca1319b`): суммы и получатели не должны попадать в общий канал invoice-bot, который читает
  и Алексей. Отката на `TG_BOT_TOKEN` в `tg()` нет намеренно — бот не настроен, сводка не уходит
  никуда, отправка платежей при этом работает. Канал проверен живым сообщением 03.08.
  Формат (`199395b`): ОТДЕЛЬНОЕ сообщение на юрлицо (разные банки и подписанты), в строке только
  получатель · сумма · основание; номер черновика и техстатус — лишь в логе, ошибка пишется явно.
- **Первый прогон 03.08: ушли все 10 висевших черновиков, 891 902.47 ₽** (ЦК 6, Дисквэр 4),
  ошибок 0; первой отправлена одна сберовская под наблюдением. Повторный прогон отправляет 0,
  дублей `external_id` нет; двойной прогон поллера с фильтром новых пачек не создаёт.

**УПД → Приёмка: пустые страна/ГТД (КТ-000160, 03.08).** Приёмка создалась, но страна и ГТД
не проставились: матчер строк УПД↔позиции заказа сравнивал артикулы токенами на РАВЕНСТВО, а
поставщик пишет их иначе — `BS-TK-865Y/1T02JZAEU0` против нашего `BS-TK865Y` (другая расстановка
дефисов + дописанные OEM-коды через «/»). Различающий цвет тонул в общих слабых токенах серии
(`250CI/300CI`, `12000`) → уникального максимума нет → 0 матчей из 7, и приёмка ушла пустой молча.
- Матчер: шаг **1c** в `match_rows` — артикул заказа без разделителей (`_art_key`) ищется
  ПОДСТРОКОЙ в наименовании строки УПД, берём при единственном попадании. КТ-000160: 0 → 7/7,
  цвет к цвету, количества сходятся. Регресс проверен старым/новым матчером на 12 прежних УПД
  (165 позиций) — расхождений нет.
- Режим **`--fill`**: приёмка на заказ уже есть → не плодить дубль, а дозаполнить страну/ГТД
  построчно (+ те же поля в карточках); количества, цены и шапку не трогает. КТ-000160
  дозаполнена боевым прогоном 03.08 (7 позиций, 7 карточек, проверено чтением из МС).
- Гейт: `matched == 0` теперь даёт громкое предупреждение в отчёте (раньше молча пустая приёмка).

**Счёт → Заказ у того же поставщика: 0 из 7 (КТ-000117, 03.08).** Тот же корень, другая половина
конвейера — `invoice_to_po`. Два дефекта:
- **Регексп артикула Блоссома обрывался на «/»**: `BS-M-TNP-50/51M/A0X5354/A0X5355` → `BS-M-TNP-50`,
  общий огрызок серии, цвет потерян. В `pattern` добавлен `/` (как у Феррета) → артикул берётся
  целиком, дальше срабатывает существующее отсечение хвостов по «/».
- **Точный `filter=article` не находит карточку при иной расстановке дефисов** (`BS-TK-865C` против
  нашего `BS-TK865C`). Добавлен последний рубеж `_find_by_art_key`: пул сужается самым длинным
  сырым префиксом через `article~=` (пустой пропускаем, >300 — сдаёмся), внутри пула наш артикул
  без разделителей должен быть НАЧАЛОМ поставщицкого; берём самое длинное совпадение и только
  если оно одно. КТ-000117: 0 → **7/7**, сумма 13 926.00 ₽ сошлась со счётом и УПД, цвет к цвету.
- Регресс: 20 счетов (КВК, Одиссей, Солюшнс, Колортек) прогнаны старым и новым матчером —
  345 прежних матчей не тронуты, потеряно 0, новые матчи только у КТ-000117.
- Заказ КТ-000117 в МС уже заведён руками — пересоздавать не требуется, правка работает вперёд.

Коммиты: `0cba70c` (карта API + авторизация), `f62dc91` (выписка v2), `d6dc9ef` (ручная кнопка
для обеих фирм), `3b3e414` (автоотправка по крону). Всё в `main`; на GitHub не выложено —
ждёт отдельного «ок».

### Выписки → статьи опер. расходов (04.08.2026, задача Сергея)
- **Миграция 209** (208 занял сосед): `bank_txn` — полная выписка обеих фирм (ничего не режем,
  приход тоже); `opex_category` (7 статей сидом); `bank_txn_opex` — статья платежа + разнесение;
  `opex_rule` — память по ИНН контрагента; вью `opex_fact_alloc` / `opex_fact_month`.
- **Сбор:** `collectors/bank_txn_store.py` (`store` — upsert по `(bank, nk)`, `apply_rules` —
  автоматическая разметка по правилам) + врезка в `run_inv.py` обеими фирмами (обёрнута try/except:
  склад расходов не должен ронять «выписка → МС») + `ops/bank_txn_backfill.py` для добора истории.
  Отсечка `OPEX_STMT_SINCE`; бэкфилл передаёт `since=--from`, иначе store режет по умолчанию.
- **Веб:** подтабы в разделе «Опер. расходы» — «📊 Сводка» (`/opex`) и «📥 Выписки»
  (`/opex/statements`, `web/static/opex_statements.html`). API: `/api/opex/statement`,
  `/api/opex/categories` (GET/POST), `/api/opex/statement/assign`, `/api/opex/fact`.
- **Решения Сергея:** (1) факт замещает ручной снапшот; (2) платежи поставщикам НЕ размечаются —
  висят нераспределёнными, позже уедут в «Кэшфлоу»; (3) память правил по ИНН (фолбэк — имя,
  когда ИНН пуст); (4) разнесение равными долями по N месяцев, остаток копеек в последний;
  (5) **04.08: всё раздельно по юрлицам — сводного «обе фирмы» нет ни в Сводке, ни в Выписках**
  (`ORG_ACCOUNTS`: Цифровой = wb_acc1+oz_acc1, Дисквэр = wb_acc2+oz_acc2, «чистая после расходов»
  считается по аккаунтам своей фирмы); (6) выписка добрана **с 01.01.2026** — Сергей размечает год.
- **Переход месяца на факт:** `_opex_is_fact(month)` — месяц ≥ `OPEX_FACT_SINCE` (2026-08-01)
  ИЛИ по нему уже есть разметка. То есть январь–июль сидят на ручном снапшоте, пока Сергей их
  не разметит, и уезжают на факт по мере разметки. Ручной снапшот один на бизнес, по фирмам
  не делится — так и написано на экране.
- Заодно закрыт латентный баг: совет о постоянных расходах суммировал ВСЕ снапшоты ≤ месяца;
  теперь все четыре потребителя ходят через `_opex_total(period)`.
- **Бэкфилл 01.01–03.08.2026 выполнен** (04.08, фоном ~2 мин, ночь не понадобилась):
  Цифровой 881 операция (расход 29.10 млн ₽, приход 29.36 млн ₽), Дисквэр 474 (14.06 / 16.64 млн ₽).
  Правила, созданные Сергеем на августе, тут же разметили 305 исторических платежей.
- **Найдено на бэкфилле — правило по ИНН слишком широко для карточных операций.** У Дисквэра
  контрагент карточных покупок и банковских комиссий = сам ПАО Сбербанк (ИНН 7707083893), поэтому
  правило «Сбербанк → Упаковка» (создано с одной покупки в cargo-avto) раскатилось на 38 платежей
  188 тыс. ₽ — часть из них комиссии банка, а не упаковка.
- **Фрагмент назначения в правиле (миграция 210, решение Сергея 04.08).** `opex_rule.purpose_like`
  необязателен: пусто = правило на всего контрагента (как раньше), заполнено = только на платежи,
  где фрагмент встречается в назначении (регистр не важен, матч через `position(lower…)`).
  Уникальность правила — пара «контрагент + фрагмент», у одного ИНН может быть общее правило
  и несколько уточнённых. При совпадении нескольких **выигрывает самое узкое** — вью
  `opex_rule_match` + `opex_rule_reach` (миграция 211): специфичность меряется ОХВАТОМ
  (сколько расходов правило вообще матчит), тай-брейк — длина фрагмента, затем свежесть.
  Длина сама по себе не годится: «Покупка PURCHASE_CB в» длиннее «KOMUS», но накрывает все
  карточные покупки подряд. Через ту же вью считается счётчик «авто» в панели правил и
  удаление авторазметки, иначе платёж под двумя правилами попадал бы в оба счётчика,
  а удаление общего правила снимало разметку у платежей уточнённого.
  В UI: селект «запоминать: контрагента / + фрагмент назначения / не запоминать» (выбор живёт
  в localStorage) и панель «🔁 правила» — список с числом авторазметок и удалением.
  **Удаление правила снимает только его авторазметку** (`source='rule'`), ручная остаётся,
  затем правила прогоняются заново, чтобы платёж подхватило более точное.
  API: `GET /api/opex/rules`, `POST /api/opex/rules/delete`.
  Проверено вживую и откачено: правило с фрагментом «Требование об уплате пени» разметило 3 из 27
  платежей того же ИНН, удаление сняло ровно их; состояние вернулось к 7 правилам / 317 разметкам.
- **Выписка файлом для банков без API (04.08, задача Сергея «Озон Банк»).** У Озон Банка счета
  есть у ОБЕИХ фирм, API нет вовсе — только ручная выгрузка из ЛК. Новый `collectors/bank_file_import.py`
  разбирает файл в те же нормализованные записи, что `alfa_statement.normalize` / `sber_statement.normalize`,
  и кладёт в общий `bank_txn` (`bank='ozon'`), поэтому правила, разнесение и экран разметки работают
  без изменений. Форматы: **1С** (`1CClientBankExchange`, cp1251/utf-8 — основной, в нём есть ИНН,
  счета, БИК, назначение) и фолбэк **CSV/XLSX** по ключевым словам заголовков; колонку не нашли —
  падаем с перечнем реальных заголовков файла, а не молча грузим мусор. Направление считаем по
  нашему счёту (мы плательщик → DEBIT), фолбэк — по ИНН нашей организации. Идемпотентность —
  штатный `_nk` (хеш «счёт|дата|сумма|номер|назначение»), uuid банк не даёт; повторная загрузка
  того же файла даёт «новых 0». Отсечка по умолчанию `2026-01-01` (как весь бэкфилл), а не
  августовская `OPEX_STMT_SINCE`.
  UI: кнопка «📤 файл выписки» на экране «Выписки» грузит в ВЫБРАННУЮ слева фирму; сперва
  холостой разбор (`dry`) с показом «операций N, период, расход/приход, счёт» и подтверждением,
  затем запись. API `POST /api/opex/statement/import` принимает файл base64 в JSON —
  `python-multipart` в venv нет, ради одной формы его не ставил. CLI:
  `./venv/bin/python collectors/bank_file_import.py <файл> --bank ozon --org <ИНН> [--dry]`.
  Проверено на синтетике обоих форматов + end-to-end через API (запись 2 → повтор 0 новых);
  тестовые строки удалены. **Живой файл Озон Банка загружен Сергеем 04.08** — формат 1С, 363
  операции 02.01–31.07.2026, счёт …2712, Цифровой Квадрат; разбор чистый (0 строк без ИНН/имени/
  назначения, 363 уникальных номера документа), правила сразу разметили 51 расход на 9.86 млн ₽.
  Внутри есть 54 платежа на 45.3 млн ₽ — переводы на свой же счёт в Альфе: **статью им не ставить**,
  иначе расход задвоится (в Альфе он уже учтён как приход и потом трата).
  Не сделано Сергеем: август по Озон Банку и выписка ДИСКВЭРА по его счёту в Озон Банке.
- **Правило по сумме платежа (04.08, ЕНП Казначейства).** Миграция `212_opex_rule_amount.sql`:
  у правила появились `amount_min` / `amount_max`, полуинтервал **[min, max)** — ровно 40 000 ₽
  попадает в верхнее правило. Уникальность правила теперь «контрагент + фрагмент + диапазон»
  (индексы через `coalesce(...,-1)`, иначе NULL ≠ NULL и правила дублировались бы); обе вью
  миграции 211 пересобраны с проверкой диапазона. Победителя по-прежнему выбирает МЕНЬШИЙ охват,
  так что правило с порогом само перебивает общее правило того же ИНН.
  Случай: Казначейство 7727406020 платит «Единый налоговый платеж» — ИНН и назначение одинаковые,
  различает только сумма (порог Сергея 40 000 ₽): < 40 000 → «Налоги ФОТ», ≥ 40 000 → «Налоги и взносы».
  Факт после применения: 11 шт / 4 883 067 ₽ «Налоги и взносы», 40 шт / 433 138 ₽ «Налоги ФОТ».
  UI: в селекте запоминания появился режим «контрагента + сумма платежа», диапазон вводится строкой
  «до 40000» / «от 40000» / «1000-40000»; в панели правил колонка «Условие» показывает фрагмент и диапазон.
  **Побочный эффект, важный:** `assign` теперь перед прогоном правил СНИМАЕТ авторазметку, разошедшуюся
  с текущим победителем (`source='rule'` и `category_id` ≠ вью `opex_rule_match`), — иначе новое правило
  не перебивало старое решение. Ручная разметка не трогается. При первом прогоне так переехало 36 строк,
  из них 25 — карточные платежи АО «АЛЬФА-БАНК» на 97 008 ₽, ушедшие в «Бухгалтерия» по широкому
  правилу id=109 (фрагмент «Расчеты через ТУ», охват 146). Правило Сергея, не трогал.
- **Фильтр по статье внутри месяца (04.08).** `/api/opex/statement` принимает `cat`
  (`''` все / `none` без статьи / id) и отдаёт `by_cat` — разрез месяца по статьям (шт + сумма),
  из него строится селект со счётчиками. Разрез считается по ВСЕМ расходам месяца, а не по
  отфильтрованной выдаче, иначе после выбора статьи из фильтра было бы не выйти. Опция
  «— без статьи —» заменила чекбокс «только без статьи» (два контрола на одно и то же).
  Итоги в подвале по-прежнему по всему месяцу, при активном фильтре сверху строка «Фильтр · N шт».
- **Разнесение НАЗАД — знак в поле «Мес.» у платежа (04.08).** Первый заход (миграция 213) вешал
  направление на СТАТЬЮ — отменено тем же днём: Сергей уточнил, что внутри «Налогов и взносов»
  лежат НДС (относится к месяцу оплаты) и УСН (за три предыдущих месяца), статья их не различает,
  решает человек на платеже. Итог — миграция `214_opex_spread_sign.sql`: колонка
  `opex_category.spread_back` удалена, у `opex_rule` добавлена `spread_back` (правило не помнит,
  на каком платеже родилось), вью `opex_rule_match` отдаёт знак.
  Механика: поле «Мес.» знаковое. `1` — весь платёж в месяц оплаты; `12` — вперёд год;
  `−3` — назад, три месяца ДО оплаты (УСН платежа июля → апрель–июнь), `−1` — прошлый месяц.
  Отдельного флага у разметки нет: направление видно по `start_month < месяца платежа`, вью 209
  как считала доли вперёд от `start_month`, так и считает. Учтено в `_opex_spread`/
  `_opex_start_month` (ручная разметка), в `bank_txn_store.apply_rules` (авторазметка правилом
  берёт знак из правила) и в выдаче `/api/opex/statement` (поле `spread_back` для UI).
  Проверено на живом платеже: 1 → июль; −3 → апрель-май-июнь; 12 → июль…июнь-2027; −1 → июнь;
  сумма долей везде ровно равна платежу. Налоги вернулись в месяц оплаты (15 из 15), Сергей
  разносит их вручную. Попутно чинено: `ON CONFLICT` в assign не обновлял `start_month`,
  сохранение статьи вслепую снимало `archived`.
- **Веб — не территория inv** (см. «Границы»): правки `web/app.py` и `web/static/opex*.html`
  сделаны по прямой задаче Сергея; при мерже смотреть, чтобы не утащить чужое из общего чекаута.
  Часть этих правок уже уехала в main чужим коммитом соседней сессии inv (`bae5a0d`, она
  закоммитила `web/app.py` целиком) — не потеряно, но напоминание коммитить точечно.

## Открытые вопросы
1. ~~Прогонять ли ротацию вживую сейчас~~ — **решено Сергеем 2026-08-02: ждать 30-го дня**,
   боевой прогон не делаем. Первое срабатывание автоматическое **2026-09-01** (cron `--cron`,
   тег `SBER_SECRET_ROTATE`, 06:17 UTC), предупреждения в TG с 27.08. **Следить за TG в эти дни:**
   отказ банка по формату кандидата придёт сообщением; контур при этом жив на старом секрете,
   но дедлайн 11.09 не двигается — тогда чинить руками до дедлайна.
2. ~~Префикс платёжных эндпоинтов Сбера~~ — **закрыт 02.08: платежи на `v1`** (выписка на `v2`);
   подробности — `docs/SBER_BANK_API.md` §2.
3. ~~`vat` / `urgencyCode` в теле РПП~~ — **закрыт 02.08: модель их не требует**; НДС пишем
   текстом в `purpose`, как у Альфы.
3a. ~~Создаёт ли Сбер документ без `digestSignatures`~~ — **закрыт 03.08**: все 10 отправленных
   документов видны в банке (Альфа 6, Сбер 4), подтверждено визуально. Значит, Сбер принимает
   РПП без нашей подписи так же, как Альфа; подписывает человек в вебе банка.
4. Есть ли у Сбера push/webhook о зачислениях, или только поллинг выписки, как у Альфы.
5. ~~Мосты «назначение → приёмка» для Дисквэра~~ — **снят 02.08**: состав берём из черновика.
6. ~~Три авансовых черновика Дисквэра на 125 000 ₽~~ — **отправлены 03.08 по прямому решению
   Сергея** («отправить всё первым прогоном»). Природа их прежняя: баланс авансов считается от
   нуля, прежние авансы платились руками и в нашей очереди их нет. Следствие на будущее: пока
   ручные авансы не попадут в очередь, поллер будет предлагать аванс заново — смотреть на
   `note` («баланс 0 ₽ < порог») и снимать лишнее ДО 07:55, теперь отправка автоматическая.
7. ~~Ключ правила разметки для карточных операций и банковских комиссий~~ — **закрыт 04.08:
   Сергей выбрал фрагмент назначения** (миграция 210). Осталось его действие: широкое правило
   «ПАО Сбербанк → Упаковка» (37 авторазметок на 160 тыс. ₽, среди них комиссии банка) я не
   трогал — удалить его и пересоздать с фрагментом Сергей решает сам на экране «🔁 правила».

## Следующий шаг
1. Первое автоматическое срабатывание крона — 04.08, 07:55 и 17:00 МСК. Проверить
   `payment_autosend.log` и сводку в TG; при странном — снять `PAYMENT_AUTOSEND` в `.env`
   (крон трогать не надо) и разбираться.
2. Наблюдение по Сберу: первые дни смотреть в `alfa_statement.log` строку «Сбер …» — «создан N»
   означает, что автозапись выписки опередила ручной ввод.

### План-дата приёмки — срок доставки поставщика (03–04.08.2026, закрыто)
- План приёмки = через N РАБОЧИХ дней после счёта, приёмка на ту же дату 08:00. Срок больше
  не в коде: `supplier_payment_terms.delivery_days` (миграция 208) + колонка «Срок доставки»
  на `/suppliers/payment-terms`. 1 = завтра (умолчание), 2 = послезавтра; `skip = delivery_days − 1`.
- **Ищется по ГРУППЕ поставщика, а не по ИНН из счёта** — юрлица у поставщика меняются
  (`SG.related_inns`). Строка на «Картридж Трейд» накрывает все 4 юрлица «Блоссома».
  Остальные условия оплаты на группу переводить НЕ надо (решение Сергея 04.08: при смене ООО
  баланс сводится в 0, условия переносятся руками).
- Стоит 2 у групп «Блоссом» и «Колортек» в обоих юрлицах, у остальных 1. Правится в дашборде
  без перезапуска ботов; чужие поставщики вне таблицы идут по «завтра».
- Детали (правило, `workcal`, выравнивание документов 03.08, грабли поиска приёмок по `name`)
  — `docs/handoff/archive/inv_2026-08-04_plan_date.md`.
- ХВОСТ: заказ КТ-000117 03.08 получил +2 дня, когда код считал +1, — не объяснено.
  Проверить на следующем счёте Блоссом/Колортек, что план встал +2, а не +3.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/inv_<дата>.md`, в брифе только живое.
- Закончил кусок — обновил бриф и закрыл сессию. `--resume` вчерашней сессии запрещён.
