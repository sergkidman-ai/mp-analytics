# BRIEF_INV — поток «inv» (банковский контур: выписки, платежи, счета)

> Потолок 150 строк (CLAUDE.md, «Экономия токенов», п. 1).
> Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_INV.md» — и больше ничего.
> Детальные карты API — `docs/ALFA_BANK_API.md`, `docs/SBER_BANK_API.md` (читать по нужде).

## Где работаю
- Ветка: `main` (поток исторически коммитит прямо в main; крупные правки — `inv/*`)
- Модель сессии: **Sonnet** (механика: интеграции по документации, прогоны) /
  **Opus** (архитектура контура, спорные решения) — выбрать на старте и НЕ менять.

## Зона ответственности
Деньги на входе и выходе: выписки банков → МойСклад (`paymentin`/`paymentout`), привязка
платежей к приёмкам, черновики платёжных поручений, счета поставщиков и сроки оплаты.
**Два контура, две организации:** Альфа-Банк — ООО «Цифровой Квадрат»; Сбер — ООО «ДИСКВЭР».

## Мои файлы (правлю я)
`collectors/alfa_*.py`, `collectors/sber_*.py`, `invoice_bot/*`, `ops/sber_secret_rotate.py`,
`migrations/2xx`, `docs/ALFA_BANK_API.md`, `docs/SBER_BANK_API.md`.

## Границы
`margin_by_sku` и витрины fin — **read-only**. Веб-дашборд (`web/app.py`) — не мой, изменения
запрашивать у владельца. Секреты только в `.env` и `secrets/` (оба gitignored).

## Запреты потока
- **Боевые платёжки — только черновиками** (без ЭП): банк создаёт документ в статусе «черновик»,
  подписывает человек в вебе. Автоподписание не реализовывать.
- Ключи, токены, `client_secret` — **никогда** в чат, лог и git (в логах только длина и хвост).
- Ротацию/перевыпуск боевых кредов запускать осознанно: последствие отказа — встал весь контур.

## Состояние на 2026-08-02

**Альфа (Цифровой Квадрат)** — в проде. Выписка → МС, привязка платежей к приёмкам,
черновики платёжек включены боевыми 30.07.2026 (первый черновик №519). mTLS + ApiKey.

**Сбер (Дисквэр)** — подключён самостоятельно в ЛК, бесплатно, сразу промышленный сервис.
- Авторизация: `collectors/sber_auth.py` — OAuth 2.0 (интерактивный вход представителя по СМС),
  одноразовый `refresh_token` с атомарной ротацией, mTLS. Проверено: `client-info` HTTP 200.
- Выписка: `collectors/sber_statement.py` — счета из `client-info` (у Дисквэра 8, действующий
  один: `…147717` OPEN/calculated; 7 закрытых депозитов дают штатный 400 WORKFLOW_FAULT).
  Контроль 28–31.07: **11 операций**, приход 3 095.34 ₽, расход 122 394.45 ₽.
- **Спецификация Сбера врёт в путях:** выписка живёт на `v2`, `/v1/statement/...` = 404,
  при этом `/v1/client-info` жив. Swagger машинно не отдаётся — пути искать перебором.
- Мелочи, ломающие арифметику: `transactionId` у Сбера НЕТ (ключ = `uuid`), суммы приходят
  **строкой** → в нормализации `Decimal`.
- `client_secret` живёт **40 дней**: активирован 2026-08-02, дедлайн **2026-09-11**.
  `ops/sber_secret_rotate.py` — автоматическая ротация на 30-й день, предупреждения в TG с 25-го,
  защита от «запрос ушёл, ответ потерялся» через `secret_pending.json` + `--resolve`.

- **Выписка Сбера → МойСклад — сделано 02.08.** Ядро «выписка → МС» вынесено из `alfa_ms.py`
  в банконезависимый `collectors/bank_ms.py`; `alfa_ms.py` и новый `collectors/sber_ms.py` —
  тонкие обёртки (чья организация, откуда выписка, нужна ли привязка). Прогон включён в
  `run_inv.py` вторым шагом. **Боевая запись ВКЛЮЧЕНА 02.08.2026 по ОК Сергея**
  (`SBER_MS_APPLY=1` в `.env`); выключается тем же ключом. Контроль включения: боевой прогон
  01.07–02.08 (81 операция) создал **0 документов**, счётчики Дисквэра до/после совпали
  (paymentin 18, paymentout 53).
- **Привязка платежей к приёмкам у Дисквэра — включена 02.08** (`8a778b6`), тем же движком,
  что у Альфы: `_link_supplies` переехал в ядро (`bank_ms.link_supplies`), `sber_ms` передаёт
  его в `link_fn`. Порядок разбора общий: **черновик платёжки → номера документов в назначении
  → аванс FIFO**. Отдельные «мосты назначения» под поставщиков Дисквэра НЕ понадобились —
  состав платежа берём из черновика, который сами и собрали (вопрос 5 закрыт).
- **Разрез по нашему юрлицу — несущая конструкция привязки** (миграция 207 параллельной сессии):
  поставщики у двух фирм ОБЩИЕ, суммы совпадают запросто, приёмки у каждой свои. Org-разрез
  добавлен в `alfa_link`: `org_inn(payment)`, `supplier_agents(our_inn)`, `_inn_of(…, our_inn)`,
  `find_draft` (`AND org_inn`), гейт в `link_payment` до разбора. Без него платёж Дисквэра
  разобрался бы черновиком Цифрового и сел на чужие приёмки.
- Контроль привязки (dry-run 452 исходящих с 01.05, запись не велась): ЦК — already 142 /
  not-supplier 140 / no-match 5; Дисквэр — already 92 / not-supplier 66 / no-match 7. Все 12
  no-match — авансы с остатком без неоплаченных приёмок (ждут поставки). Ошибок нет.
- **`po_payment_watch` прогнан по Дисквэру 02.08** (`ALFA_BUYER_INN=7811803918 …`; поллер после
  правки параллельной сессии полностью на `org_inn`, к банку не обращается — только МС→Postgres).
  Заведено: `po_payment_status` 18 заказов (9 pending / 6 queued / 3 waiting_receipt),
  `payment_draft_queue` 4 черновика — отсрочка КВК ТРЕЙД 37 216.58 ₽ на 6 заказов (лимит не резал)
  + 3 аванса. Повторный dry-run привязки: ЦК и Дисквэр без изменений, новые черновики ничего
  лишнего не подхватили.
- **Второй прогон 02.08 после правки таблицы условий Сергеем** (авансы 50/50/25 тыс., лимиты
  платежей у отсрочки): три прежних черновика аванса сняты руками и пересозданы по новым суммам —
  **125 000 ₽ вместо 250 000 ₽**. Грабли: `process_prepayment_balance` не создаёт второй черновик,
  пока висит `planned` — правка таблицы САМА не пересчитывает уже выставленный черновик, старый
  надо снимать. Пачка отсрочки не дублируется по другой причине: её заказы уходят в `queued`.
- **Артефакт холодного старта:** авансы (после правки таблицы 50 000 + 50 000 + 25 000) выставлены потому, что
  баланс авансов Дисквэра посчитан как 0 — прежние авансы платились руками и в нашей очереди их
  нет. Живыми деньгами это НЕ грозит: отправщик `alfa_payment_draft` жёстко режет `q.org_inn` по
  Цифровому Квадрату, а отправщика для Сбера ещё нет. **Но до появления платёжек Сбера эти строки
  надо снять или подтвердить** — иначе первый же прогон отправит 125 000 ₽. Решение за Сергеем.
- **Дефект антидубля, найден и закрыт:** в аккаунте МС ДВЕ организации, а `existing_index`
  фильтровал платежи только по дате → платёж одной фирмы мог «съесть» платёж другой на ту же
  сумму в тот же день. Замер май–авг: 7 таких пар (день+сумма) среди paymentout. Теперь индекс
  всегда с фильтром по организации. Регресса у Альфы нет (июль, 112 операций — 0 расхождений
  старой и новой логики).
- Грабли МС: в `filter=` символ `>` обязан быть percent-encoded, иначе HTTP 400 с пустым телом
  (штатный `urllib.parse.quote()` без `safe` — правильно; `safe=":/;=<>"` ломает запрос).
- Контроль на данных: 01.07–02.08 — 81 операция Дисквэра, **все уже в МС** (68 «номер+сумма»,
  1 «сумма+день», 12 зарплатных отсечены гейтом), к записи 0. Ручной ввод не отстаёт.
- `tokens.json` Сбера привязан к каноническому чекауту: `refresh_token` одноразовый, две копии
  файла (основной чекаут + deploy-worktree крона) гасили бы токен друг друга.

Коммиты: `0cba70c` (карта API + авторизация), `f62dc91` (выписка v2).

## Открытые вопросы
1. ~~Прогонять ли ротацию вживую сейчас~~ — **решено Сергеем 2026-08-02: ждать 30-го дня**,
   боевой прогон не делаем. Первое срабатывание автоматическое **2026-09-01** (cron `--cron`,
   тег `SBER_SECRET_ROTATE`, 06:17 UTC), предупреждения в TG с 27.08. **Следить за TG в эти дни:**
   отказ банка по формату кандидата придёт сообщением; контур при этом жив на старом секрете,
   но дедлайн 11.09 не двигается — тогда чинить руками до дедлайна.
2. Префикс платёжных эндпоинтов Сбера (`POST /payments`): раз выписка на `v2`, `v1` под
   подозрением — проверить перебором ДО первого черновика.
3. Обязателен ли блок `vat` в теле РПП Сбера и какое значение `urgencyCode`
   (`INTERNAL` из примера vs `NORMAL` как у Альфы).
4. Есть ли у Сбера push/webhook о зачислениях, или только поллинг выписки, как у Альфы.
5. ~~Мосты «назначение платежа → приёмка» для поставщиков Дисквэра~~ — **снят 02.08**: состав
   платежа берётся из черновика платёжки, мосты назначения нужны только как запасной путь.
6. Три авансовых черновика Дисквэра на 125 000 ₽ — артефакт холодного старта (баланс авансов
   считается от нуля: прежние авансы платились руками, в очереди их нет). Снять или подтвердить
   ДО появления отправщика платёжек Сбера.

## Следующий шаг
Черновики платёжек Сбера — сначала закрыть вопрос 2 (префикс `v1`/`v2` у платёжных эндпоинтов,
искать перебором как выписку), затем вопрос 3 (`vat`, `urgencyCode`). Наблюдение по Сберу:
первые дни после включения записи смотреть в `alfa_statement.log` строку «Сбер …» — если
появится «создан N», значит банк опередил ручной ввод и автозапись сработала боевым.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/inv_<дата>.md`, в брифе только живое.
- Закончил кусок — обновил бриф и закрыл сессию. `--resume` вчерашней сессии запрещён.
