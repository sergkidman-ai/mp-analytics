# BRIEF_FIN — поток «Финансы / экономика»

> Первым делом прочитай этот бриф + `CLAUDE.md` + `ARCHITECTURE.md`. Ты — сессия домена **fin**.
> Параллельно может идти сессия домена **mkt** (Джем/реклама/воронка/ABC) — не лезь на её территорию.

## Зона ответственности
Себестоимость, COGS, маржа по SKU, выручка/расходы, P&L по площадкам, opex, сверка с эталоном,
модель периода, поставщики/закупки/остатки МС, оборачиваемость, ABC-маржа (часть «маржа»).

## Твои файлы (правишь ты)
- Коллекторы: `collectors/{moysklad, ms_products, ms_demand_cogs, wb, ozon, ozon_postings,
  ozon_products, ozon_fbo_stock, yandex, yandex_monthly, suppliers, supplier_purchases, set_cost}.py`
- Витрины: `reports/{margin_by_sku, margin_ozon_sku, ozon_expenses}.py`
- Оркестратор: `run_daily.py`
- Скрипты: `rebuild_validate_cogs.py`, `phase1_cogs.py`, `cogs_compare.py`, promo/анализ экономики
- Миграции: **блок 0xx** (следующая — `028_*`). НЕ бери 1xx (это mkt).

## Твои таблицы (пишешь)
`products, sales, margin_by_sku, ms_demand_cogs, cogs_actual, opex, stocks, supplier_*,
ms_product, ms_barcode, set_cost, yandex_cost, yandex_monthly, raw_*` (финансовые).

## Граница (важно)
- **`margin_by_sku` — ТВОЯ собственность, ты её ПИШЕШЬ.** Маркетинг её только читает.
- Источник себеста — ТОЛЬКО `report/stock/byoperation` (память `project_mp_analytics_cogs`). Не возвращайся
  к cost_seb/byPrice/усреднению.

## СТОП-зоны (территория mkt — не трогай)
Джем (`wb_jam`, `wb_search_*`), реклама (`wb_ads`, `ozon_ads`, `ozon_bids`, `ad_spend_daily`),
воронка (`wb_funnel`), отзывы/рейтинг (`ozon_reviews`, `ozon_rating`), `drops`, `run_marketing.py`.
Если задача тянет тебя туда по контексту или коду — **СТОП, сообщи пользователю** (это сигнал, что
границу пора пересматривать).

## Параллельная работа (worktree-изоляция)
Так как сессии идут одновременно — работай в СВОЁМ git worktree и доменной ветке:
```bash
git worktree add .claude/worktrees/fin -b fin/<задача> origin/main   # или из текущего main
cd .claude/worktrees/fin && echo fin > .workstream
```
Имя ветки `fin/*` включает флаг территории автоматически.

## Флаг чужой территории
Активен git-хук (`tools/hooks/pre-commit` → `tools/territory_guard.py`). При коммите правок ЧУЖИХ
файлов из ветки `fin/*` — коммит блокируется с пояснением. Самопроверка в любой момент:
```bash
python3 tools/territory_guard.py --status
```
Осознанный обход: `WORKSTREAM_OVERRIDE=1 git commit ...`.

## Дисциплина
Чекпоинты в `docs/` и память (`project_mp_*` твои); локальные коммиты; **push только с ОК**;
секреты не печатать; следить за размером сессии.

## Архив этапа до 2026-08-05

Раздел «Себестоимость» по Ozon и WB (влито в `main`), аудит нагрузки на МойСклад, проверка
«отчёт Ozon сам сторнирует себест?» — `docs/handoff/archive/BRIEF_FIN_2026-08-05.md`.

## Этап 12–13.08 — в архиве

«Себестоимость в Отчётах МП = только реализованный товар», состав внедрённого 12–13.08 и
перевод себеста Маркета на FIFO — `docs/handoff/archive/BRIEF_FIN_2026-08-13.md`. Всё влито в ветку `fin/cogs-realized`.

## FIFO вместо `cost_seb` по всем площадкам (2026-08-13, ветка `fin/cogs-realized`)

Правило Сергея: **единственный источник себестоимости отгрузки — FIFO**; `products.cost_seb`
(средняя по ТЕКУЩЕМУ остатку из карточки МС) себестоимостью списания не является. Вычищено везде,
где считались деньги.

- `reports/fifo_fallback.py` — общий модуль. `unit(article, day)` (себест/шт по артикулу площадки:
  товар → набор как Σ FIFO компонентов), `unit_ms(ms_id, day)`, `impute(pos, day)` (себест документа
  по позициям). FIFO = ближайшая ПРЕДШЕСТВУЮЩАЯ отгрузка того же товара МС (если товар до этой даты
  не отгружался — первая после). Набор/документ отдаём ТОЛЬКО при полном покрытии состава: частичная
  сумма занизила бы себест, а занижение себеста завышает прибыль.
- Подключён в: `collectors/{wb,oz,ya}_cogs_demand.py` (метод `tovar_fifo` перед аварийным `imputed`),
  `web/app.py` (три кнопки «пересчитать»), `reports/margin_by_sku.py` (хвост WB),
  `reports/margin_ozon_sku.py` — слой 3 «FIFO товара» ВПЕРЕДИ старой групповой цены из карточки.
  Метка «FIFO товара» и фильтр по методу добавлены на страницы раздела и в дрилл.
- **Ozon после подключения (пересобран 2025-11…2026-08):** ветка групповой цены `cost_seb` — 4
  постинга за весь 2026 (было ~сотня); покрытие дрилла acc1 95–98 %, acc2 98–99 % (кроме января).
  A/B в одном процессе на одних данных (ветка FIFO выключена / включена): июль acc1 +4 096 ₽,
  acc2 +7 042 ₽, август acc2 +2 167 ₽ — FIFO даёт себест ЧУТЬ ВЫШЕ карточной средней, направление
  верное (карточка занижала). Витрина Ozon за 2025-11/12 была пуста — заполнена (acc1 1.38 / 1.80 млн).
- **Статика вкладки «Отчёты МП · Ozon» обновлена** новой командой
  `python -m reports.ozon_mp_freeze cogs [YYYY-MM …]`: она переносит из витрины ТОЛЬКО COGS и
  производные net/margin. Полный `freeze` для этого не годится — он откатил бы сверенные из Отчёта
  о реализации Продажи/Возвраты/Вознаграждение обратно в оценку по транзакциям.
  Янв–июл 2026: COGS 19 105 908 → 18 752 239, **−353 669 ₽ (чистая на столько же выше)**;
  acc1 −310 238, acc2 −43 431.
- **Починен цикл импорта, занесённый `210fd86`:** `ozon_mp_page → mp_cogs_drill → ya_cogs_page →
  ozon_mp_page` роняла ImportError любой вход, начинающийся с `ozon_mp_page` — то есть CLI
  `reports.ozon_mp_freeze` и его cron на границе месяца (`1 0 1 * *`). В `mp_cogs_drill` импорт
  хелперов страницы сделан ленивым (`_yp()`).
- **Грабли витрины:** ключ `margin_by_sku` включает `period_to`, поэтому пересборка текущего месяца
  окном «по сегодня» НЕ перезаписывает строки run_daily (окно «по конец месяца»), а кладётся рядом —
  август задвоился и дал фантомные +793 793 ₽. Пересобирать месяц ТЕМ ЖЕ окном, что и `run_daily`
  (`_month_bounds`). Дубли удалены, проверка «месяцев с >1 окном `period_to`» = 0.

**Ozon acc2, две аномалии покрытия — разобраны (пункт 3 закрыт):**
- **январь 68→70 %** — не потеря, а FBO: отгрузки на постинг в МС не существует по природе схемы.
  16 FBO-постингов (91 962 ₽) + 2 FBS, где Ozon перенумеровал третий сегмент постинга
  (в отчёте `0153778244-0032-13`, в МС `0153778244-0032-1`, 61 718 ₽). За весь 2026 таких FBO:
  acc1 361 постинг / 2 164 088 ₽, acc2 88 / 411 976 ₽; перенумерованных FBS — 7 / 224 288 ₽.
- **июнь 103 %** — не переполнение, а разная механика сторно: витрина сторнирует по
  всеисторическому `cpu_global`, раздел — долей себеста конкретного документа. Июнь acc2: раздел
  брутто 544 407 ₽, витрина брутто 553 862 ₽, витрина после сторно 519 200 ₽.

**Открыто:** 181 заказ Яндекса на 273 138 ₽ есть в МС, но отсутствует в сырье отчёта (учтены,
считаются счётчиком в логе); заморозить 2025-11/12 кнопкой; WB 82–91 % покрытия (FBO-импутация);
частичные возвраты WB/Ozon (статус ставится на отгрузку целиком); правило 7 `CLAUDE.md` переписать
после мержа. **В `main` не мержил — жду «ок».**

### Хвост `cost_seb` в закрытых месяцах пересчитан (по команде Сергея 2026-08-13)

Разморозил 2025-11…2026-07, пересчитал строки `method='imputed'` тем же путём, что кнопка
«пересчитать» (сначала своё FIFO документа из МС, затем FIFO тех же товаров МС), заморозил обратно
**теми же записями** — `closed_by`/`closed_at` сохранены, снимок в scratchpad-сессии
(`frozen_snapshot*.json`). Скрипт разовый, в репозиторий не кладу.

| площадка | строк | было | стало | Δ | стало `ms_fifo` / `tovar_fifo` |
|---|---|---|---|---|---|
| Ozon | 41 | 50 802 ₽ | 50 192 ₽ | −610 | 1 / 22 |
| WB | 16 | 30 134 ₽ | 26 994 ₽ | −3 140 | 0 / 11 |
| Маркет | 1 | 3 367 ₽ | 3 407 ₽ | +41 | 0 / 1 (это `ms_fifo`) |

Дельта меньше сухой оценки (Ozon −3 651, WB −6 250), потому что оценка была без повторного
обращения к МС и только по 2026 году; здесь окно шире (плюс бэкфилл 2025-11/12, 41 и 16 строк
вместо 37 и 10), и часть документов при перезапросе отдала СВОЙ FIFO.

**Остаток `imputed` (FIFO не существует нигде — товар ни разу не отгружался):** Ozon 18 строк /
25 165 ₽, WB 5 / 10 612 ₽, Маркет 0. Меньше не станет, пока такой товар не отгрузят.
Гейт перед обратной заморозкой проверен: отгрузок `status='done'` с нулевым себестом — 0 по всем трём.
Отчёты МП по WB/Ozon эти правки не двигают (там витрины `margin_*`, не раздел); у Маркета отчёт
читает `ya_cogs_demand` — там +41 ₽ в одном месяце 2025 года, пересборкой месяца ради 41 ₽ не дёргаю API.

## Связи универсальных моделей в цепочке FIFO (`analog_fifo`, 2026-08-14)

Задача Сергея: один картридж продаётся под разными кодами (Q2612A = Canon 703). Покупатель заказал
5335, со склада ушёл 0002 — в приёмках/отгрузках МС кода 5335 нет, 0002 есть. Соседняя сессия
(поток **prc**, миграция 408) залила связи в `prc_tc_link` из каталога TheCartridge.

**Сделано** — в `reports/fifo_fallback.py` добавлен шаг **`analog_fifo`**:
`unit()`: товар → набор → аналог; `impute()`: позиция без своего FIFO ищется по связям (метод
документа тогда `analog_fifo`, правило полного покрытия сохранено); компоненты набора тоже добирают
FIFO по связям. Связи читаются **в обе стороны** (взаимозаменяемость симметрична); из нескольких
связанных кодов с FIFO берём **максимальный** себест — где не знаем точно, ошибаемся в сторону
расхода. Без миграции 408 модуль работает как раньше (проверка `to_regclass`). `prc_tc_link` —
чужая территория, только read-only. Ярлыки методов добавлены в три страницы себестоимости
(заодно закрыт пробел: `nabor_fifo` нигде не был подписан). CHECK-констрейнтов на `method` нет.

**Эффект в деньгах = 0 ₽, и это правильный результат.** Честный A/B (июль, пересборка подряд без
связей и со связями, 4 аккаунта): дельта ровно 0. Причина: в цепочке витрины первым стоит
`cpu_hist` — себест/шт по истории того же nm из НАШИХ отгрузок, и он перекрывает 140 из 141 позиции,
где связи вообще могли бы сработать (это тоже факт FIFO и он точнее аналога). Моя первая оценка
«219 шт ≈ 104 тыс ₽» мерила шаг `ff.unit` в изоляции, без учёта более раннего шага цепочки —
цифра неверна. Шаг остаётся страховкой: он сработает на новом SKU, проданном по FBO раньше, чем
у нас появилась своя отгрузка.

Дельта первого прогона (−87,7 тыс ₽ по 2026) — **дрейф данных** (июль/август добираются ночным
`run_daily`), не связи: A/B на одних данных дал 0. Статика вкладок сверена с витриной по всем
месяцам 2026 — расходились 4 месяца Ozon acc1 (янв +206, фев −433, май +440, июн −79 ₽),
синхронизировано `reports.ozon_mp_freeze cogs`; ВБ расхождений не было.

**Отложено на вечер 14.08 (просьба Сергея беречь МС в рабочее время):** пересчёт хвоста
`method='imputed'` в закрытых месяцах с учётом связей — там `report/byoperation` на каждый документ
(23 запроса). По замеру связи закрывают 1 отгрузку Ozon из 23, WB — 0: там проблема не в кодах,
а в товаре, который ни разу не отгружался.
