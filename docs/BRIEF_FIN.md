# BRIEF_FIN — поток «Финансы / экономика»

> Первым делом прочитай этот бриф + `CLAUDE.md` + `ARCHITECTURE.md`. Ты — сессия домена **fin**.
> Параллельно может идти сессия домена **mkt** (Джем/реклама/воронка/ABC) — не лезь на её территорию.

## Зона ответственности
Себестоимость, COGS, маржа по SKU, выручка/расходы, P&L по площадкам, opex, сверка с эталоном,
модель периода, поставщики/закупки/остатки МС, оборачиваемость, ABC-маржа (часть «маржа»).

## Твои файлы (правишь ты)
- Коллекторы: `collectors/{moysklad, ms_products, ms_demand_cogs, wb, ozon, ozon_postings,
  ozon_products, ozon_fbo_stock, yandex, yandex_monthly, suppliers, supplier_purchases, set_cost}.py`
- Витрины: `reports/{margin_by_sku, margin_ozon_sku, ozon_expenses}.py`
- Оркестратор: `run_daily.py`
- Скрипты: `rebuild_validate_cogs.py`, `phase1_cogs.py`, `cogs_compare.py`, promo/анализ экономики
- Миграции: **блок 0xx** (следующая — `028_*`). НЕ бери 1xx (это mkt).

## Твои таблицы (пишешь)
`products, sales, margin_by_sku, ms_demand_cogs, cogs_actual, opex, stocks, supplier_*,
ms_product, ms_barcode, set_cost, yandex_cost, yandex_monthly, raw_*` (финансовые).

## Граница (важно)
- **`margin_by_sku` — ТВОЯ собственность, ты её ПИШЕШЬ.** Маркетинг её только читает.
- Источник себеста — ТОЛЬКО `report/stock/byoperation` (память `project_mp_analytics_cogs`). Не возвращайся
  к cost_seb/byPrice/усреднению.

## СТОП-зоны (территория mkt — не трогай)
Джем (`wb_jam`, `wb_search_*`), реклама (`wb_ads`, `ozon_ads`, `ozon_bids`, `ad_spend_daily`),
воронка (`wb_funnel`), отзывы/рейтинг (`ozon_reviews`, `ozon_rating`), `drops`, `run_marketing.py`.
Если задача тянет тебя туда по контексту или коду — **СТОП, сообщи пользователю** (это сигнал, что
границу пора пересматривать).

## Параллельная работа (worktree-изоляция)
Так как сессии идут одновременно — работай в СВОЁМ git worktree и доменной ветке:
```bash
git worktree add .claude/worktrees/fin -b fin/<задача> origin/main   # или из текущего main
cd .claude/worktrees/fin && echo fin > .workstream
```
Имя ветки `fin/*` включает флаг территории автоматически.

## Флаг чужой территории
Активен git-хук (`tools/hooks/pre-commit` → `tools/territory_guard.py`). При коммите правок ЧУЖИХ
файлов из ветки `fin/*` — коммит блокируется с пояснением. Самопроверка в любой момент:
```bash
python3 tools/territory_guard.py --status
```
Осознанный обход: `WORKSTREAM_OVERRIDE=1 git commit ...`.

## Дисциплина
Чекпоинты в `docs/` и память (`project_mp_*` твои); локальные коммиты; **push только с ОК**;
секреты не печатать; следить за размером сессии.

## Текущее состояние (2026-08-05)

**Ветка/worktree:** `fin/oz-cogs` в `.claude/worktrees/fin-oz-cogs` (от `main` @ c972f46).
**Влита в `main` 2026-08-05** по «ок» Сергея, дашборд перезапущен.

### Сделано: раздел «Себестоимость» → Ozon (двумя подтабами)
Зеркало ярусa Яндекса для двух юрлиц Ozon; юрлица НЕ смешиваются — у каждого своя таблица,
свой провал в месяц и своё закрытие месяца.
- `migrations/064_oz_cogs.sql` — `oz_cogs_demand` / `oz_cogs_manual` / `oz_cogs_frozen`
  (PK `account+demand_name`, заморозка per-month per-account). Применена.
- `collectors/oz_cogs_demand.py` — наша цена = `demand.sum` из МС; себест = FIFO из кэша
  `ms_demand_cogs` → фолбэк `byoperation` → импутация `cost_seb×qty`; ручной себест побеждает всё.
  Дёшево по API: список отгрузок страницами вместо документа-за-документом.
- `reports/cost_tabs.py` — полоса подтабов раздела (одно место): Яндекс / Ozon·Цифровой / Ozon·Дисквэр.
  Левое меню НЕ растёт (решение Сергея: «два подтаба»).
- `reports/oz_cogs_page.py` — overview + detail, вёрстка Яндекса; `reports/ya_cogs_page.py` тронут
  минимально (перевод на общий `tabs_html`).
- `web/app.py` — `/reports/cost/ozon/{acc}[/{ym}]` (регистрируются ДО `/reports/cost/{ym}`, иначе
  «ozon» съест шаблон месяца) + API `/api/oz-cost/{manual,reset,freeze,unfreeze}` с обязательным
  `account` из белого списка.

**Своё в Ozon (чего нет у Яндекса) — статус `return_ozon` = «Возврат на склад Озон».** Решение Сергея
2026-08-05: **сторнируем**. Логика — раздел показывает движение себеста по НАШЕМУ складу; товар,
вернувшийся на склад FBO, остался на нашем балансе и будет продан оттуда, а себест по FBO-заказам
спишется в момент той продажи. Не сторновать здесь = списать себест дважды.
Итог: сторно — `return_stock`, `unredeemed`, `return_ozon`; убыток (себест остаётся расходом) —
только `return_defect` (брак, перепродать нельзя). Гейт нулевого себеста при закрытии месяца
соответственно ловит только `done` и `return_defect`.
Дельта от переключения (себест ушёл из расхода, продажи не тронуты): **acc1 −195 230 ₽**
(июнь 76.0→77.2 %, июль 77.6→78.3 %), **acc2 −32 718 ₽** (июнь 79.2→80.1 %, июль 79.5→79.9 %);
до мая-2026 эффект пренебрежимо мал.

**Цифры прогона (2026-01…08):** oz_acc1 9388 отгрузок (FIFO 9359, импутация 29),
oz_acc2 2390 (FIFO 2362, импутация 28). Валовая маржа ровная по месяцам: acc1 76–77.6 %,
acc2 77.6–80.4 %. Статусы: done 11132, other 217, return_stock 209, return_ozon 122,
unredeemed 52, return_defect 46.
**Сверка:** по 10 990 совпавшим отгрузкам себест витрины = `ms_demand_cogs` копейка в копейку
(расхождений 0). Ещё 731 отгрузки в кэше не было — себест взят прямым `byoperation` (тот же FIFO,
не оценка). API проверены живьём: гейт нулевого себеста, ручной ввод + откат, заморозка/разморозка.

### Проверка «отчёт Ozon сам сторнирует себест?» — НЕТ (2026-08-05)
Гипотеза не подтвердилась. В «Отчётах МП · Ozon» себест берётся не из отчёта о реализации, а из
`margin_by_sku` (`web/app.py` → `reports/margin_ozon_sku.py`): деньги из транзакций, COGS — тот же
FIFO МС по постингу. Отчёт несёт возвраты только деньгами (`return_commission.quantity`, 39 строк
из 1331 в выборке) — себест из него не считается, автосторно «в пределах отчёта» нет; там сторно
сделано отдельным кодом с гейтом склада `{Брак, Озон}`.
**Мост по доставленным отправлениям:** FBS есть в `oz_cogs_demand` на 100 % (acc1 3599/3599,
acc2 1112/1112), FBO — почти нет (acc1 1/137, acc2 0/36): FBO-отгрузок в МойСклад не создаётся.
Отсюда два следствия:
- в **витрине** FBO-продаже себест ВСЁ РАВНО начисляется (импутация/группа по offer_id,
  `margin_ozon_sku.py:214–259`) → товар, вернувшийся на склад Озон и проданный по FBO, получает
  себест дважды. Двойное списание живёт именно там;
- в **нашем разделе** FBO-продаж нет вовсе → после сторно 227 948 ₽ себеста не спишутся нигде,
  пока FBO-заказы в раздел не заведут. Сторно верное по принципу «товар вернулся на баланс», но до
  появления FBO-продаж раздел на эту сумму занижает себест.

### Открыто / следующий шаг
1. **Завести FBO-продажи в раздел «Себестоимость»** — парная проводка к сторно `return_ozon`.
   Пока её нет, раздел занижает себест на ~228 тыс ₽ (см. выше). Приоритет №1.
2. **Гейт витрины:** убрать «Озон» из не-сторнируемых в `collectors/ms_return_cogs.py` +
   `reports/margin_ozon_sku.py` — там двойное списание уже есть. Правка меняет исторические цифры
   «Отчётов МП» → отдельным прогоном с дельтой по месяцам и сверкой с ЛК Ozon. НЕ трогать без «ок».
3. **35 отправлений с нулевым себестом** среди реализованных (acc1 20, acc2 15) — ровно то, ради чего
   в таблице ручной ввод. Пока они есть, соответствующие месяцы закрыть нельзя.
4. Коллектор пока запускается вручную (`./venv/bin/python -m collectors.oz_cogs_demand`) — в
   `run_daily.py` НЕ подключён; решить, вешать ли в ежедневный прогон.

---

## Себестоимость → WB (влито в `main` 2026-08-05)

Ветка `fin/wb-cogs`, worktree `.claude/worktrees/fin-wb-cogs`. Раздел «Себестоимость» получил ещё
два подтаба: `🟣 WB · Цифровой` и `🟣 WB · Дисквэр` (всего 5, левое меню не растёт).

**Общий рендер вместо третьей копии.** `reports/mp_cogs_page.py` — вёрстка, фильтры, сортировка,
логика сторно и заморозка месяца на все площадки; `oz_cogs_page.py` (был 401 стр.) и новый
`wb_cogs_page.py` — тонкие конфиги (таблицы БД, статусы, подписи, пояснения). Публичный API
(`ACCOUNTS` / `overview_html` / `detail_html`) сохранён, страницы Ozon рендерятся байт-в-байт.

Файлы: `migrations/065_wb_cogs.sql` (wb_cogs_demand / _manual / _frozen, применена),
`collectors/wb_cogs_demand.py`, `reports/{mp,wb}_cogs_page.py`, `reports/cost_tabs.py`,
`web/app.py` (роуты `/reports/cost/wb/{acc}[/{ym}]` до шаблона месяца + API `/api/wb-cost/*`).

**Период = дата отгрузки в МС** (`demand.moment`), НЕ дата заказа и НЕ дата финотчёта. Себест —
FIFO конкретной отгрузки, он определён на момент списания. Таблицы заказов ВБ в базе нет вовсе.

**Статусы (мост `assembly_id` = `demand.name`, финотчёт + возвраты МС):**
`done` (есть «Продажа») · `return_stock` сторно · `return_defect` убыток · `return_wb` (сторно
продажи в отчёте, в МС возврата нет) · `unredeemed` (невыкуп: продажи нет, только логистика
туда-обратно) · `unreported` (в отчёте нет совсем) · `other` (ждём отчёт, лаг до 7 недель).
Сторнируем `return_stock`, `unredeemed`, `return_wb` — денег за товар ВБ не перечислил, единица
возвращается к нам и спишется по FIFO снова при перепродаже (та же логика, что решение по Ozon
от 2026-08-05). Убыток — только `return_defect`. Три «в МС не проведено» статуса помечены флагом.

**Цифры первого прогона (2026-01…08):** acc1 10 166 отгрузок, acc2 5 912; FIFO покрыл 16 068 из
16 078, нулевой себест — 4 строки. Валовая маржа acc1 68–77 %, acc2 61–75 % (ДО комиссии,
логистики, хранения и рекламы ВБ).

### Открыто по WB
1. **Невыкупов 1 472 против 123 возвратов, проведённых в МойСклад** — 4.06 млн ₽ отгрузок и
   1.23 млн ₽ себеста. Себест сторнируем, но приёмки в МС нет: дыра в складском учёте. Список —
   фильтр «📦 Невыкуп» на странице месяца.
2. **Гейт закрытия месяца** ловит только нулевой себест у `done`/`return_defect`/`return_wb`;
   невыкупы закрытию не мешают. Обсуждалось с Сергеем, решения пока нет.
3. **FBO WB (~11 % оборота)** в раздел не входит: отгрузок в МС не создаётся. То же ограничение,
   что и у Ozon (п. 1 выше).
4. Коллектор в `run_daily.py` НЕ подключён — как и озоновский.

### Нагрузка на МойСклад (аудит 2026-08-05)

Проверено по просьбе Сергея: не молотит ли раздел МС лишними запросами.

Нашлась утечка, общая для WB и Ozon: `MDC.byoperation_cogs()` результат никуда не пишет, поэтому
отгрузки, которых нет в кэше `ms_demand_cogs`, дёргались из МС **при каждом прогоне** — WB 2241,
Ozon 674 запроса, и так каждый раз. Починено (`_known_map`): себест, уже посчитанный прошлым
прогоном, берётся из своей же таблицы, в МС не идём. FIFO состоявшейся отгрузки не меняется, так
что пересчитывать нечего; принудительный пересчёт — кнопка «пересчитать» в детализации (1 отгрузка
= 1 запрос) либо разморозка месяца.

Контроль: прогон после фикса — цифры бит-в-бит те же (acc1 10166 строк / 7 036 908.45 ₽ себеста,
acc2 5912 / 5 835 401.46 ₽), «из кэша без запроса в МС» 1340 + 896.

Что осталось в МС за прогон: 4 `_resolve_href` + ~17 страниц `/entity/demand` (по 1000) ≈ **21
запрос**, ~2m40s (упирается в скорость выдачи МС, не в лимит).

### Штуки отгрузок были мусором (найдено 2026-08-12, июнь Ozon вычищен)

`report/stock/byoperation` отдаёт по позиции `cost` (FIFO-себест — ВЕРНО) и `quantity`, которое
**не** количество в документе, а `stock − reserve + inTransit` — доступный остаток товара НА МОМЕНТ
ЗАПРОСА (формула сошлась на всех проверенных позициях). `byoperation_cogs()` кладёт его в
`ms_demand_cogs.qty` / `ms_demand_pos.qty` → колонка «штук» в разделе врёт, а импутация
`cost_seb × qty` при отрицательном qty даёт ОТРИЦАТЕЛЬНЫЙ себест (случай `19412837-0847-1`: −1266.89 ₽).

Масштаб по июню-2026 Ozon: штуки расходились с документом в **916 из 1627** отгрузок, ещё 84
отгрузки вообще отсутствовали в кэше (им коллектор брал `quantity` напрямую).

**Сделано (только данные июня, код НЕ трогали):** штуки в `ms_demand_cogs`/`ms_demand_pos` за июнь
переписаны из фактических позиций `/entity/demand/{id}/positions`; 84 отсутствующие отгрузки заведены
в кэш; коллектор перепрогнан. Итог июня: штук acc1 7048→3537, acc2 1466→919; себест acc1 не изменился
(2 303 674.77 ₽), acc2 546 665.19→561 247.35 ₽ (+14 582.16 — ушла отрицательная импутация);
отрицательных и нулевых штук/себеста не осталось, гейт закрытия месяца чист. Сверка выборки 6/6.

**Код исправлен** (ветка `fin/byop-qty`, коммит `ab31531`, в main НЕ влит): `demand_positions_qty()`
берёт штуки из `/entity/demand/{id}/positions`, `byoperation_cogs()` = себест из отчёта + штуки из
документа; плюс переиспользуемый бэкфилл `ops/ms_qty_backfill.py --platform {ozon,wb}`.

**Январь–май Ozon прогнан 2026-08-12** (бэкфилл 8413 отгрузок: штуки переписаны у 4347, заведено 349
отсутствовавших; затем разморозка янв–май `oz_acc1` → `collectors.oz_cogs_demand 2026-01-01` по обеим
учёткам → заморозка возвращена с исходными `closed_by`/`closed_at`). Штуки: acc1 37 699→17 380,
acc2 5649→3561. Себест acc1 **не изменился ни на копейку** по всем пяти месяцам (FIFO из `cost`
верен), acc2 май 577 961.01→589 497.62 (**+11 536.61** — 9 отгрузок с нулевым себестом добрали FIFO).
Отрицательных штук и нулевого себеста в `done/return_defect` за янв–май не осталось.

**WB прогнан 2026-08-12** (влито в main, ff `ab31531`): бэкфилл 16 319 отгрузок — штуки переписаны у
7573, заведено 2312 отсутствовавших; затем `wb_cogs_demand 2026-01-01` (acc1 10 331, acc2 5988 строк).
Штуки янв–июл: acc1 36 654→13 124, acc2 13 353→6706. Себест FIFO **не изменился ни в одном месяце**;
переоценка только 12 480.79 ₽ у acc2 за май–июнь (по 4 импутации `cost_seb × qty` в месяц — там штуки
и были мусором). Рост августа (+186 448 ₽) — не переоценка, а догрузка свежих отгрузок текущего месяца
(+165 и +76 строк). Отрицательных штук было 200 → стало 0; заморозок у WB нет вовсе.

**Маркет прогнан 2026-08-12**: разморозка янв–июн → `ya_cogs_demand 2026-01-01` (2917 отгрузок,
FIFO 2916, ручной 1) → заморозка возвращена с исходными `closed_by`/`closed_at` (янв–май «сотрудник»,
июнь «rev»). Штуки янв–июн 9194→3737, себест этих месяцев **не изменился ни на копейку**. Июль
+160 261.32 ₽ при 412→537 строк и август +384 876.32 ₽ (0→282 строки) — целиком догрузка, месяцы
раньше были недособраны. Отрицательных штук 3 → 0. **Грабли:** у `ya_cogs_frozen` PK = (account, ym),
в бэкапе разморозки колонку `account` терять нельзя (у Маркета `ya_acc1`).

**Открыто (следствие, ещё не тронуто):** `reports/margin_by_sku.py` берёт из `ms_demand_cogs` пару
(cogs, qty) и считает юнит-себест `cogs/qty` для сторно возвратов. Штуки теперь другие → при
СЛЕДУЮЩЕЙ пересборке витрины сторно поедет. Витрина сейчас `computed_at = 2026-07-17`, контроль
nmID 216421567 май-2026 = **3741.77 ₽** держится. Пересборку не запускал намеренно: это отдельное
решение — сразу после первого прогона сверить контроль и объяснить дельту.
Закрытые месяцы не листаются и не
переписываются, ручной себест не затирается. В cron/`run_daily` коллекторы себеста (ya/oz/wb) НЕ
стоят — фоновой нагрузки нет вовсе, запуск только руками. Теперь прогон достаточно дёшев, чтобы
поставить его в расписание, — решение за Сергеем.

## Отчёты МП · Ozon — классификация строк (2026-08-12, в main `196d39a`)

`reports/ozon_mp_report.py::_resid_line` — два правила:
- `OperationMarketplaceFlexiblePaymentSchedule` (досрочная выплата, «Гибкий график выплат») →
  **penalty** («Другие услуги и штрафы»). Была приходом в `other` и гасила собой переотправки:
  32 353.00 − 18 795.28 = 13 557.72 — та цифра, что Сергей забраковал.
- `OperationMarketplaceServicePartialCompensationToClient` → **other**: компенсация ПОКУПАТЕЛЮ, это
  списание, а не наша компенсация от Ozon. Итог июля acc1: 32 353.00 − 1 738.63 = **30 614.37** ✓.

Обе операции встречаются только в июле-2026. Снапшот `reports/data/mp_ozon_hist.json` пересчитан по
июню и июлю (`ozon_mp_freeze.py freeze/reconcile`, бэкапы в `reports/data/backups/`), дашборд
перезапущен. Июль acc1: penalty 97 425→116 220, other 13 558→30 614, compensation 120 411→88 603,
net 2 136 583→2 103 079 (маржа 20.2→19.9%); acc2: 21 385→29 680 / 1 583→6 713 / 23 935→15 234,
net 544 939→533 083 (22.0→21.5%). Июнь пересчитан ради нового себеста после починки штук:
acc1 cogs 2 307 061→2 236 276 (net 2 184 147→2 254 932, 21.6→22.3%), acc2 526 347→527 928.
Апрель и май пересчёт не двигает. Запуск скрипта требует `PYTHONPATH=/opt/mp-analytics`.
