# BRIEF_FIN — поток «Финансы / экономика»

> Первым делом прочитай этот бриф + `CLAUDE.md` + `ARCHITECTURE.md`. Ты — сессия домена **fin**.
> Параллельно может идти сессия домена **mkt** (Джем/реклама/воронка/ABC) — не лезь на её территорию.

## Зона ответственности
Себестоимость, COGS, маржа по SKU, выручка/расходы, P&L по площадкам, opex, сверка с эталоном,
модель периода, поставщики/закупки/остатки МС, оборачиваемость, ABC-маржа (часть «маржа»).

## Твои файлы (правишь ты)
- Коллекторы: `collectors/{moysklad, ms_products, ms_demand_cogs, wb, ozon, ozon_postings,
  ozon_products, ozon_fbo_stock, yandex, yandex_monthly, suppliers, supplier_purchases, set_cost}.py`
- Витрины: `reports/{margin_by_sku, margin_ozon_sku, ozon_expenses}.py`
- Оркестратор: `run_daily.py`
- Скрипты: `rebuild_validate_cogs.py`, `phase1_cogs.py`, `cogs_compare.py`, promo/анализ экономики
- Миграции: **блок 0xx** (следующая — `028_*`). НЕ бери 1xx (это mkt).

## Твои таблицы (пишешь)
`products, sales, margin_by_sku, ms_demand_cogs, cogs_actual, opex, stocks, supplier_*,
ms_product, ms_barcode, set_cost, yandex_cost, yandex_monthly, raw_*` (финансовые).

## Граница (важно)
- **`margin_by_sku` — ТВОЯ собственность, ты её ПИШЕШЬ.** Маркетинг её только читает.
- Источник себеста — ТОЛЬКО `report/stock/byoperation` (память `project_mp_analytics_cogs`). Не возвращайся
  к cost_seb/byPrice/усреднению.

## СТОП-зоны (территория mkt — не трогай)
Джем (`wb_jam`, `wb_search_*`), реклама (`wb_ads`, `ozon_ads`, `ozon_bids`, `ad_spend_daily`),
воронка (`wb_funnel`), отзывы/рейтинг (`ozon_reviews`, `ozon_rating`), `drops`, `run_marketing.py`.
Если задача тянет тебя туда по контексту или коду — **СТОП, сообщи пользователю** (это сигнал, что
границу пора пересматривать).

## Параллельная работа (worktree-изоляция)
Так как сессии идут одновременно — работай в СВОЁМ git worktree и доменной ветке:
```bash
git worktree add .claude/worktrees/fin -b fin/<задача> origin/main   # или из текущего main
cd .claude/worktrees/fin && echo fin > .workstream
```
Имя ветки `fin/*` включает флаг территории автоматически.

## Флаг чужой территории
Активен git-хук (`tools/hooks/pre-commit` → `tools/territory_guard.py`). При коммите правок ЧУЖИХ
файлов из ветки `fin/*` — коммит блокируется с пояснением. Самопроверка в любой момент:
```bash
python3 tools/territory_guard.py --status
```
Осознанный обход: `WORKSTREAM_OVERRIDE=1 git commit ...`.

## Дисциплина
Чекпоинты в `docs/` и память (`project_mp_*` твои); локальные коммиты; **push только с ОК**;
секреты не печатать; следить за размером сессии.

## Архив этапа до 2026-08-05

Раздел «Себестоимость» по Ozon и WB (влито в `main`), аудит нагрузки на МойСклад, проверка
«отчёт Ozon сам сторнирует себест?» — `docs/handoff/archive/BRIEF_FIN_2026-08-05.md`.

## Этап 12–13.08 — в архиве

Классификация строк Ozon МП (досрочная выплата → штрафы, реестр состава строк,
сверка с ЛК за июль копейка в копейку) — `docs/handoff/archive/BRIEF_FIN_2026-08-13.md`.

«Себестоимость в Отчётах МП = только реализованный товар», состав внедрённого 12–13.08 и
перевод себеста Маркета на FIFO — `docs/handoff/archive/BRIEF_FIN_2026-08-13.md`. Всё влито в ветку `fin/cogs-realized`.

## FIFO вместо `cost_seb` — итог этапа 13.08 (в архиве)

Единственный источник себестоимости отгрузки — FIFO; `products.cost_seb` (средняя по текущему
остатку карточки МС) себестоимостью списания не является. Модуль `reports/fifo_fallback.py`
(`unit` / `unit_ms` / `impute`), подключён в три коллектора `*_cogs_demand`, `web/app.py`,
`margin_by_sku`, `margin_ozon_sku`. Ozon янв–июл: COGS −353 669 ₽. Хвост `imputed` в закрытых
месяцах пересчитан (Ozon −610, WB −3 140, Маркет +41 ₽), остаток «FIFO не существует нигде» —
Ozon 18 строк / 25 165 ₽, WB 5 / 10 612 ₽. Детали, грабли (цикл импорта, ключ `period_to`,
аномалии покрытия acc2) — `docs/handoff/archive/BRIEF_FIN_2026-08-13.md`.


## Связи универсальных моделей в цепочке FIFO (`analog_fifo`, 2026-08-14)

Задача Сергея: один картридж продаётся под разными кодами (Q2612A = Canon 703). Покупатель заказал
5335, со склада ушёл 0002 — в приёмках/отгрузках МС кода 5335 нет, 0002 есть. Соседняя сессия
(поток **prc**, миграция 408) залила связи в `prc_tc_link` из каталога TheCartridge.

**Сделано** — в `reports/fifo_fallback.py` шаг **`analog_fifo`**: `unit()` товар → набор → аналог;
`impute()` добирает позицию без своего FIFO по связям (правило полного покрытия сохранено);
компоненты набора тоже. Связи читаются **в обе стороны**, из нескольких кандидатов берём
**максимальный** себест (ошибаемся в сторону расхода). Без миграции 408 работает как раньше
(`to_regclass`). `prc_tc_link` — чужая территория, read-only. CHECK-констрейнтов на `method` нет.

**Эффект в деньгах = 0 ₽, и это правильный результат** (честный A/B, июль, 4 аккаунта): в цепочке
витрины раньше стоит `cpu_hist` — факт FIFO по истории того же nm, он перекрывает 140 из 141
позиции, где связи могли бы сработать. Ранняя оценка «219 шт ≈ 104 тыс ₽» мерила `ff.unit`
в изоляции — неверна; дельта −87,7 тыс ₽ первого прогона была дрейфом данных, не связями.
Шаг остаётся страховкой для нового SKU, проданного по FBO раньше своей первой отгрузки.

## Себест только «до даты отгрузки» (правило Сергея 2026-08-14, коммит `0d8927e`)

Товар сперва поступает на склад и только потом уходит покупателю → себест ищется по внешнему коду
**до даты отгрузки, но не позже**. Цена из будущего запрещена. Два места нарушали правило:

1. `fifo_fallback._by_ms` брал «первую ПОСЛЕ», если до даты товар не отгружался → теперь `None`,
   управление уходит дальше по цепочке.
2. `margin_by_sku._fallback_sources` (`cpu_hist`) брал себест из САМОГО СВЕЖЕГО месяца витрины
   независимо от собираемого: пересборка января подставляла цену августа. Теперь история режется
   параметром `before` = первый день собираемого месяца.

**Пересобран весь 2026** (8 мес × 4 аккаунта): COGS **+25 831 ₽** (чистая ниже на столько).
Крупнейшие: Ozon acc1 фев −9 473; WB acc2 мар +7 672, апр +5 358; WB acc1 янв +7 409, июн +6 569.
Статика вкладок синхронизирована: ВБ — рефриз из сырья (строки денег НЕ изменились, только
cogs/net), Ozon — `reports.ozon_mp_freeze cogs` (7 месяцев-аккаунтов); сверка после = 0 расхождений.

**Итоговое покрытие 2026 (ответ на вопрос «нужен ли коллектор приёмок»):**

| площадка | база | своя отгрузка МС | FIFO по истории/товару | суррогат карточки |
|---|---|---|---|---|
| WB | 16 055 шт | 90.5 % exact | 8.3 % (cpu_month 5.8, cpu_hist 2.3, tovar 1.2, nabor 0.2, analog 3 шт) | 5 шт (0.03 %) |
| Ozon | 11 375 отправлений | 96.8 % МС | 3.1 % (импутация 236, FIFO товара 109) | группа 6 + ручная 2 (0.07 %) |

Выручка без COGS за 2026 — 17 748 ₽ = **0.0 %**; число SKU без COGS не выросло. **Вердикт: коллектор
приёмок МС не нужен.** Он закрыл бы 11 позиций из 27 430 (0.04 %) и ~19 тыс ₽ по 12 документам,
где товар отгружался только позже продажи; цена — новый коллектор + постоянная нагрузка на МС.
Вернуться к вопросу, если доля суррогата карточки вырастет выше ~1 %.

**Известный зазор (не закрыт, нужно решение):** внутри месяца витрины берут опорную дату
`day = date_to` (конец собираемого месяца), поэтому продажа 03.08 может получить себест отгрузки
от 20.08. Межмесячная «цена из будущего» закрыта, внутримесячная — нет. Строгий вариант — считать
по дате первой продажи nm в месяце.

## Статус «нужна ручная себест» (2026-08-14, коммит `0f933bf`)

Где FIFO нет нигде, себест раньше молча падал в `products.cost_seb` (метод `imputed`) — это средняя
по остатку, а не себест списания. Теперь ровно те же строки помечаются `method='need_manual'`:
оценка в ячейке остаётся (чтобы месяц не поехал), но строка ждёт цифры от человека. Ставят метку
3 коллектора + 3 кнопки «пересчитать» в `web/app.py`; ярлык «НУЖНА РУЧНАЯ СЕБЕСТ», опция фильтра
«Способ», бейдж и поле ввода — в `mp_cogs_page` / `ya_cogs_page` / `mp_cogs_drill`. Ввод цифры
переводит метод в `manual` (побеждает всё, переживает прогоны).

Бэкфилл: **WB 5 отгрузок / 10 612 ₽ (все `unredeemed` → сторно, ввод не даётся и не нужен),
Ozon 19 / 27 809 ₽ (из них 15 «Выполнен» — редактируемые: acc2 05 — 7, acc2 06 — 7, acc1 08 — 1),
Яндекс 0.** Реальная работа человека — 15 строк Ozon.

## Открыто
- 181 заказ Яндекса на 273 138 ₽ есть в МС, но отсутствует в сырье отчёта (учтены счётчиком в логе).
- Заморозить 2025-11/12 кнопкой (ручное действие человека).
- Частичные возвраты WB/Ozon: статус ставится на отгрузку целиком.
- Правило 7 `CLAUDE.md` переписать после мержа. **В `main` не мержил — жду «ок».**
