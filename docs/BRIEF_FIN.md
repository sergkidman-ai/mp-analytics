# BRIEF_FIN — поток «Финансы / экономика»

> Первым делом прочитай этот бриф + `CLAUDE.md` + `ARCHITECTURE.md`. Ты — сессия домена **fin**.
> Параллельно может идти сессия домена **mkt** (Джем/реклама/воронка/ABC) — не лезь на её территорию.

## Зона ответственности
Себестоимость, COGS, маржа по SKU, выручка/расходы, P&L по площадкам, opex, сверка с эталоном,
модель периода, поставщики/закупки/остатки МС, оборачиваемость, ABC-маржа (часть «маржа»).

## Твои файлы (правишь ты)
- Коллекторы: `collectors/{moysklad, ms_products, ms_demand_cogs, wb, ozon, ozon_postings,
  ozon_products, ozon_fbo_stock, yandex, yandex_monthly, suppliers, supplier_purchases, set_cost}.py`
- Витрины: `reports/{margin_by_sku, margin_ozon_sku, ozon_expenses}.py`
- Оркестратор: `run_daily.py`
- Скрипты: `rebuild_validate_cogs.py`, `phase1_cogs.py`, `cogs_compare.py`, promo/анализ экономики
- Миграции: **блок 0xx** (следующая — `028_*`). НЕ бери 1xx (это mkt).

## Твои таблицы (пишешь)
`products, sales, margin_by_sku, ms_demand_cogs, cogs_actual, opex, stocks, supplier_*,
ms_product, ms_barcode, set_cost, yandex_cost, yandex_monthly, raw_*` (финансовые).

## Граница (важно)
- **`margin_by_sku` — ТВОЯ собственность, ты её ПИШЕШЬ.** Маркетинг её только читает.
- Источник себеста — ТОЛЬКО `report/stock/byoperation` (память `project_mp_analytics_cogs`). Не возвращайся
  к cost_seb/byPrice/усреднению.

## СТОП-зоны (территория mkt — не трогай)
Джем (`wb_jam`, `wb_search_*`), реклама (`wb_ads`, `ozon_ads`, `ozon_bids`, `ad_spend_daily`),
воронка (`wb_funnel`), отзывы/рейтинг (`ozon_reviews`, `ozon_rating`), `drops`, `run_marketing.py`.
Если задача тянет тебя туда по контексту или коду — **СТОП, сообщи пользователю** (это сигнал, что
границу пора пересматривать).

## Параллельная работа (worktree-изоляция)
Так как сессии идут одновременно — работай в СВОЁМ git worktree и доменной ветке:
```bash
git worktree add .claude/worktrees/fin -b fin/<задача> origin/main   # или из текущего main
cd .claude/worktrees/fin && echo fin > .workstream
```
Имя ветки `fin/*` включает флаг территории автоматически.

## Флаг чужой территории
Активен git-хук (`tools/hooks/pre-commit` → `tools/territory_guard.py`). При коммите правок ЧУЖИХ
файлов из ветки `fin/*` — коммит блокируется с пояснением. Самопроверка в любой момент:
```bash
python3 tools/territory_guard.py --status
```
Осознанный обход: `WORKSTREAM_OVERRIDE=1 git commit ...`.

## Дисциплина
Чекпоинты в `docs/` и память (`project_mp_*` твои); локальные коммиты; **push только с ОК**;
секреты не печатать; следить за размером сессии.

## Сигнал от prc 14.09.2026: закупочная в карточках МС пересчитывается каждую ночь

С ночи на 15.09 `prc-enter-buyprice.timer` (23:05 МСК) пишет в buyPrice карточки цену её позиции
в актуальном оприходовании на «Удаленный» по ВСЕМ поставщикам, включая внешний API-загрузчик
(раньше — только почтовые прайсы). Цена по УПД перезаписывается ценой прайса (решение Сергея).
Первая ночь ≈8 078 карточек, у 3 151 изменение >20 %, почти всегда ВНИЗ (profiline −34 %,
rapid/ramis −20 %) — это давно не обновлявшиеся закупочные.
- **Что задевает у fin:** `ms_product.buy_price` (после `collectors/ms_products.py`) →
  `set_cost.cost` (min по внешнему коду) → ступени 7–8 себеста в `margin_by_sku`; фолбэк `buy_ms`
  в `collectors/yandex_monthly.py`. FIFO и `cost_seb` не задеты.
- **Ожидание:** на фолбэк-ступенях и в составе наборов себест снизится → маржа этих строк вырастет.
  После 15.09 сверить долю строк на ступенях 7–8 и дельту `set_cost`.
- Кто что поменял: журнал `prc_buyprice_write` (карточка, старая/новая цена). Детали — `BRIEF_PRC.md`.

## Архив этапов (детали — в `docs/handoff/archive/`)
- Сессии сентября (главная на «Отчётах МП» 08.09, УПД по выкупам ВБ 11–12.09, потолок/пол
  наценок Маркета 02–14.09) — `docs/handoff/archive/fin_2026-09_sessions.md`.

`BRIEF_FIN_2026-08-05.md` — раздел «Себестоимость» по Ozon и WB, аудит нагрузки на МС, проверка
«отчёт Ozon сам сторнирует себест?». `BRIEF_FIN_2026-08-13.md` — «Себестоимость = только
реализованный товар», перевод Маркета на FIFO, классификация строк Ozon МП (досрочная выплата →
штрафы, реестр состава строк, сверка с ЛК за июль копейка в копейку), грабли (цикл импорта,
ключ `period_to`, аномалии покрытия acc2). Всё влито в `main`.

`fin_2026-08_sessions.md` — сессии 13–20.08: этап «себест реализации», добор себеста 16.08,
знаменатель сторно ВБ 17.08, аудит вкладки «Отчёты МП» 18.08, единая форма «оборот = наша цена»
19.08 и доводка вида страниц, детализация расходов Ozon и ВБ 20.08, порог значимости подсветки,
структура расходов Яндекса за июль (13.08, коммит `e041a27` — см. «Открыто»).

## Сессия 21.09 — баннеры Пульта на «Отчёты МП» + разрез по юрлицам (закрыта)

Источников ровно два: «Отчёты МП» (`reports/mp_numbers.py`) и «Опер. расходы» (факт по банку).
Всё верхнее на главной и всех трёх вкладках МП переведено на первый; витрины оставлены, но
подписаны и опущены вторым рядом. Коммиты `22d2309` (баннеры + переключатель юрлиц) и
`def5494` (плашки комиссий), оба в `main`, задеплоены, в `origin` не пушились.

- **ВБ**: верхняя пятёрка снята с `margin_by_sku` (август 3 280 518 / 20,2 % → 3 346 067 /
  18,9 %, чистая та же 633 632 — расходился знаменатель). Витрина ушла в блок «Детали ВБ».
- **Маркет**: убрана старая формула (оплата + субсидия минус только денежные услуги), которая
  завышала чистую в 11 раз (август 1 558 439 / 53,1 % → 142 376 / 5,3 %; июль 1 141 347 →
  115 955). Та же база теперь в таблице месяцев и на вкладке «Опер. расходы».
- **Ozon**: числа не менялись (сходились до рубля), переименованы три подписи.
- **Главная**: селектор юрлица. Площадки режутся `ORG_ACCOUNTS` (у Дисквэра Маркета нет),
  опер. расходы — по ИНН плательщика; по площадкам их НЕ разносим (решение Сергея).
  Август: Цифровой +882 491 после ФОТ, **Дисквэр −56 598 — не окупает свои опер. расходы**.
  Закупки и недельная динамика фирмы не знают, подписаны «по всему бизнесу»; ручной снапшот
  опер. расходов до 08.2026 общий на бизнес — карточка сама об этом пишет.
- **Три плашки «Комиссии ВБ/Ozon/Маркета»** = удержания площадки (комиссия + её услуги, БЕЗ
  возвратов; у ВБ вместе с СПП). Ключ `_HOLD` в `mp_numbers`: `wb_exp` / `itog` / `itog`.
  Август: ВБ 1 883 154 (56,3 %), Ozon 5 863 478 (56,5 %), Маркет 1 806 849 (66,6 %).
- Карта баннеров с разбором «что откуда» — артефакт
  `claude.ai/code/artifact/44189272-fb7d-4fbf-adf9-e25a107266f5`.

**Передано в поток fin4** (`docs/BRIEF_FIN4.md`): Ozon отключил `/v3/finance/transaction/list`,
транзакции не грузятся с 09.09, сентябрь по Ozon обрезан (Цифровой 2 626 386 вместо 9 882 937
в ЛК), `margin_by_sku` по Ozon стоит с 08.09. Замена — `/v1/finance/accrual/{types,by-day,
postings}`, доступ у ключей есть, данные для старта выгружены в `docs/reference/ozon_accrual_*`.
Август не пострадал. **В этом потоке Ozon-API не трогать — он теперь за fin4.**
Туда же переданы два хвоста: зависшие копии `run_daily.py` (вечный ретрай финотчёта ВБ,
нужен потолок ретраев + лок-файл) и пуш трёх коммитов fin в `origin` — только с «да» Сергея,
`origin/main` отстаёт на 32 коммита, и 29 из них чужие.

## Открыто
- **Ветка `fin/intramonth-date` (внутримесячная опорная дата фолбэка FIFO, 16.08) в `main`
  не влита — ждёт «ок»**; worktree `.claude/worktrees/fin-intramonth-date`. Эффект копеечный
  (июль −186 ₽, июнь +83 ₽), бьёт только там, где нет прямого себеста отгрузки.
- **Хвост от 13.08 — структура расходов ЯМ** (детали: `docs/handoff/archive/fin_2026-08_sessions.md`).
  Коммит `e041a27` (ветка `fin/ya-services-midmile`, недобор «средней мили» 3 642,65 ₽/мес)
  **в `main` не влит — ждёт «ок»**. Там же открыты: лояльность −70,00 ₽ (0,01 %, причина не
  найдена); агентское 63,36 ₽/мес из `commissions` против «Приёма платежа» 64,56 ₽ в услугах —
  риск двойного счёта; `_hist_series` (`reports/yandex_mp_report.py:277`) строит ряд как
  `revenue + subsidy`, тогда как везде база `revenue` — на деньги не влияет; проверить по акту
  закрытия, сидит ли субсидия в `payout`.
- **Коллектор офферов Маркета не собирался с 21.08** (`raw_yandex_offer`, max `loaded_at`
  2026-08-21 19:32) — снимок прайса протух, сверять таблицу наценок не с чем. Починить.
- **Маркет, сентябрь:** после 10.09 придут акты за первую декаду — пересчитать фактический `g`
  и маржу вместо оценки; после 15.09 проверить, не просел ли спрос (риск не в марже, а в объёме:
  покупатель платит на 15 % больше). Решение по двум правкам таблицы наценок — за Сергеем.
- **Хвост УПД к выгрузке:** acc1 9 шт (06.07–01.09, 111 959,10 ₽), acc2 5 шт (03.08–01.09,
  40 067,79 ₽) — разбирается кнопкой на `/reports/upd`, рассылкой не трогается.
  У Дисквэра всё до 01.08.2026 закрыто пометкой «выгружено»: пока он не платил НДС на УСН,
  обязанности выставлять УПД по уведомлениям не было, она появилась только с 01.08.2026.
- **Уведомление acc1 от 29.12.2025 идёт со ставкой 20 %** из документа ВБ: правило «5 % с 2026»
  на него не распространяется. Если переход был раньше — одна строка в `VAT_FROM`.
- **ВБ ошибается в своём «Итого» на копейку** в 20 уведомлениях из 74 (только по НДС; суммы без
  НДС сходятся везде). Решение Сергея: считать по позициям. Список —
  `scratchpad/upd_itogo_diff.txt`.
- **Показать Сергею `44490459-0066-1`** (2026-06-02, ДИСКВЭР, Ozon) — единственная отгрузка,
  которая после его правки всё ещё без себеста в МС и в его исключения не входила.
- Заморозить 2025-11/12 кнопкой (ручное действие человека). **Отдельно: за 2025 год в
  `ya_cogs_demand`/`raw_yandex_stats_order` картина неполная — см. ниже про 582 заказа.**

## Кампании Яндекс.Маркета — справка (аккаунт «Цифровой квадрат», `YANDEX_API_KEY_ACC1`)

Собираем 7 кампаний из 8 (FBS): 87623061 Москва Звездный, 148691041 Москва наш склад,
99559900 Москва экспресс, 149178498 Москва SUP, 66831595 / 148690992 / 22536936 Полюстровский
(FBS / наш склад / экспресс). Закрыт только FBY-кабинет **21589415** — 403 из-за
`apiAvailability: MANUALLY_DISABLED` (выключено руками в настройках кампании), а не из-за прав
ключа. Кампании «Кантемировская» на аккаунте нет: это склад в МС, отгружался через три магазина
Полюстровского (подтвердил Сергей 17.08).

**Правило:** выручка в «Отчёты МП · Яндекс» берётся строго из `stats/orders` перечисленных
кампаний — заказы вне периметра сбора в отчёт не попадают (так и нашлись 582 заказа на
663 522 ₽ за 2025 год). Добавляя кампанию, проверяй периметр, прежде чем искать дыру в отчёте.

История сессии 16.08 целиком (реклама по договору 354817/19, частичные возвраты с наборами,
внутримесячная опорная дата, правило 7) — `docs/handoff/archive/fin_2026-08_sessions.md`.
