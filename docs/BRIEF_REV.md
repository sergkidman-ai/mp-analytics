# BRIEF_REV — отзывы/вопросы площадок + вкладка «Распродажа остатков ВБ»

> Поток `rev` — движок автоответов (`feedback_bot/`, `collectors/*_feedbacks.py`,
> `reports/feedback_*`) и вкладка «Распродажа остатков ВБ». Действующие правила и запреты —
> `docs/REV_RULES.md`. История: 07–25.08 — `docs/handoff/archive/rev_2026-08-25.md`,
> 26.08–08.09 (блок G, канал ЛК, блок A) — `docs/handoff/archive/rev_2026-09-08.md`,
> 10.09 и блок G — `docs/handoff/archive/rev_2026-09-10.md`, 11–12.09 (дайджест, инцидент
> 067H, исключение A1.1) — `docs/handoff/archive/rev_2026-09-12.md`.

**Где код:** правки идут прямо в `/opt/mp-analytics` на `main` (worktree у потока нет). Файлы
помечены территорией `rev`, а чекаут — `fin`, поэтому хук `[territory]` ругается; проходим
осознанно через `WORKSTREAM_OVERRIDE=1`.
**Модель сессии:** Sonnet (механика: сбор, витрина, вёрстка вкладки).
**После КАЖДОГО коммита** — `systemctl restart feedback-moderation.service`: `rev=` в логе бота
обязан совпадать с HEAD. 25.08 нашли обратное: сервис сутки крутил код от 24.08, и уже влитая
парковка не работала.

## Итоги 21.09.2026 — пакет правок по неделе модерации (12 пунктов, один коммит)

1. **Позитив без человека** (`feedback_today._positive_clean`): ≥4★ с текстом без DEFECT_RX и без
   претензии → `auto` после контролёра (не PASS — назад на вычитку), длина не важна. За 14 дней:
   47 из 52 отзывов ≥4★ с текстом уходят сами, 5 на вычитке.
2. **Ложная претензия на позитиве** (`request_class.is_claim_text`): ≥4★ без дефекта/недостачи по
   текущему товару — не претензия (4542, 5418). На ≥4★ причина гейта переформулирована и в
   NO_OVERRIDE не входит — кнопка «как есть» есть всегда. Недостача/пересорт считаются дефектом
   текущего товара (иначе «недостача в комплекте» на 5★ переставала быть претензией).
3. **DEFECT_RX**: «не пропечатыва», «плохо работа», «до конца» (только рядом с отрицанием/«плохо»),
   «не тот цвет/картридж», «один … не тот».
4. **Без жаргона в генерации**: правило в `SYSTEM` (feedback_llm), хвост «вариант серии из списка
   совместимости карточки» убран из детерминированного `fam_reply` — он и был источником трёх блоков.
5. **Отказ LLM на многосоставном вопросе** (ветка «совместимость + доп. тема») → `_early_human`,
   серийный shortcut больше не подставляется (FS-1120D).
6. **Претензия к составу набора** (`request_class.set_claim`): «один не тот / короче / другой»,
   «жёлтый короче остальных» → претензия; `sku_relations` на претензии не подставляются.
7. **Серия без модели** (`_series_members`): «MF650», «M6700», «C5x90» → «Если у вас MF651Cw,
   MF655Cdw или MF657Cdw — да». Только модели из списка карточки.
8. **Кэш по семейству кода** (миграция `711`, `answer_cache.family_code/lookup_family`): по артикулу
   пусто → (код расходника, класс, ключ); попадание всегда «подтвердите». Карточка не сшита с МС —
   ответ запоминается под `fam:<код>`. Коды проставлены 35 из 35 записей. Кейс 963XL/9010 проверен.
9. **Автопубликация карточных классов** (`_auto_card_q`): характеристики / производитель /
   комплектация / габарит / совместимость (только `compat.exact`), источник карточка или кэш без
   подтверждения, контролёр PASS, гейт без единой причины → `auto`. Кэш-попадания теперь тоже
   проходят контролёр. Уведомление в чат о каждой автоотправке вопроса до **28.09** включительно
   (`FEEDBACK_AUTO_Q_NOTIFY_UNTIL`); выключатель `FEEDBACK_AUTO_CARD_Q=0`. На данных 14 дней — 2 из 47.
10. **Склейка вопросов** (`_thread`): тот же товар/кабинет за 48 ч, до 3 предыдущих с нашими
    ответами — в промпт. Имя не опора (в 0160 первый вопрос «скрыл данные», второй «Валерий К.»),
    склейку отменяют только два явно разных имени.
11. **Источник по факту**: модель возвращает `source` (card/brand_notes/knowledge/mixed), её оговорка
    «не из CARD_DATA» главнее; «модель+карточка» даёт трейс в гейте.
12. **brand_notes**: HP Dynamic Security, Brother чип+флажок, Kyocera MA2101, Canon II-серия,
    Lexmark CS921, Xerox DocuColor — `docs/rev/brand_notes_2026-09-21.md`, загружено.

Регрессия `tools/rev_regression.py` — **86/0** (78 прежних + раздел 12 на живых кейсах недели),
эталон 9/9. Проверка A1.1 в регрессии обновлена под исключение хендоффа от 12.09 (до правки она
падала 1/78). `tests/test_rev_package_0921.py` — 35 тестов.

## Состояние на 08.09.2026

Движок жив: таймеры `feedback-cycle` / `feedback-collect` / `feedback-daily-summary` активны,
`feedback-moderation.service` поднят на HEAD, отправка WB и Яндекса штатная.

- **Ozon-отзывы — канал закрыт навсегда** (Premium Plus не будет ни у одного юрлица,
  `/v1/review/*` → 403): строки снимаются флагом `skipped_no_api` (миграция 700), черновик им не
  генерируется (`OZON_REVIEW_DRAFTS=0`), а `collectors/ozon_feedbacks.py` с 04.09 вообще не зовёт
  `/v1/review/list` — коллектор Ozon строго про вопросы. **Не заводить это заново.**
- **Ozon-вопросы живы и уходят по API.** 400 `invalid argument` = вопрос ИСЧЕЗ на площадке,
  лечится парковкой; не путать с 403 у отзывов — там подписка.
- Яндекс-отзывы старше окна `WINDOW_DAYS=30` припаркованы `skipped_old` (отвечать через полгода
  смысла нет). `failed` разобран; вопрос Ozon `01a05be4` (T2A) ждёт ручного ответа: черновик
  «как правило перезаправляемый» отправлять нельзя.
- Косметический хвост — 225 `queued` по уже отвеченным отзывам и 67 карточек по отзывам Ozon:
  зачищать или ждать ЛК — решение за Сергеем.
- Поток оператора: **8.4 карточки/день**, на человека уходит 100 % вопросов.
- **Классификатор обращений переписан, A1.1 работает по СМЫСЛУ, а не по форме** (`4a674d6`):
  сначала тема, затем `is_claim_text()` переопределяет на «претензию», если покупка совершена И
  описан отказ либо расхождение с заявленным; `publish_gate` проверяет признак сам. Классы
  «производитель», «габарит», «комплектация» — из атрибутов карточки, кэшируются.

Разбор целиком — `docs/handoff/archive/rev_2026-09-08.md`.

## Правила (действующие) — полностью в `docs/REV_RULES.md`

Коротко, по номерам того файла: **1** во внешние LLM уходит только опубликованное на витринах +
текст покупателя, финансовая фактура МС запрещена на уровне сборки промпта (`prompt_privacy.py`);
**2** внутренние складские пометки режет `public_name()` (инцидент OKI C822); **3** факты карточки —
омоглифы чинятся, двойник Ozon только по коду оффера, имя Яндекса с витрины; **4** движок сам
сообщает, что ответы не уходят, балансы проверяются каждый цикл, выздоровление шлёт 🟢;
**5** цвет алерта по классу причины, «Пополнить баланс» не клеить к небалансовой причине;
**6** пустые отзывы отвечаются шаблоном, а не висят; **7** негатив по мёртвой карточке (`DEAD=4.0`,
`MIN_N=5`) уходит в авто с сухим текстом, без рейтинга — человеку; **8** автотекста в чатах
площадок не делаем, только видимость и кнопка; **9** первыми покупателю не пишем.
**Перед правкой движка — прочитать файл целиком, здесь только напоминание.**
## Вкладка «Распродажа остатков ВБ»

`/reports/wb-clearance` (подраздел «Склад»), страница статическая: `reports/wb_clearance_page.py`
→ `web/static/reports_wb_clearance.html`, перегенерация в `run_daily` и в API dismiss/restore.
Список распродажи приходит файлом «Распродажа_стока_ВБ_*.xlsx» через `dropbox_bot` в `dropbox/`
→ `collectors/wb_clearance.py` (юрлицо по имени файла, файл = полный срез: `DELETE` по account
+ upsert, ключ «Артикул WB» = `nm_id`). Живой остаток — `warehouse_remains` → `wb_stocks`;
закрытые сотрудником позиции — `wb_clearance_dismissed`.

Сигналы: остаток ВБ 0 и возврат едет обратно → 🟡 «Возврат в пути»; остаток 0 без возврата →
🔴 «Поднять цену»; остаток ≤ `LOW_THRESHOLD` (=1) → 🟡 «Заканчивается»; больше → 🟢 «Распродажа».

**Запреты вкладки:** транзит в `quantity` НЕ суммировать — «остаток ВБ» это только то, что
физически лежит на складе, иначе 🔴 не сработает вовремя. Псевдосклад «Всего находится на складах»
в `wb_stocks` не писать — двойной счёт. Транзит живёт отдельной строкой `warehouse='(в пути)'`
с `quantity=0` (колонки `in_way_to_client` / `in_way_from_client`); у потребителей `wb_stocks`
счёт SKU ограничен `quantity > 0`. Разбор целиком — в архиве.

## Следующий шаг

**Блок A закрыт 08.09** (`9b6a632`+`cb6bc69`+`701d196`), блок G влит (`344d156`),
**классификатор + A1.1-fix влиты 08.09 (`4a674d6`)**, сервис на HEAD.
**Блок D ПРИОСТАНОВЛЕН 10.09 по команде Сергея, влит частично (`a719b86`).** Сделано и закоммичено:
D1 (веб — не источник ответа о совместимости, любой ответ на вебе → BLOCK; «да» без карточки
и справочника → BLOCK), D3 (допродажа без справочника → BLOCK), D4 (ресурсная версия),
D5 (нужен регион), D6 (белый список источников трейса) в `publish_gate.py`; компат-ветка
`feedback_today.py` спрашивает `compat_ref` до веба и пишет трейс; таблица `compat_ref`
(миграция 703) создана и применена — **пустая, 0 строк**; модуль `reports/compat_ref.py`.
**10.09 сервис уже на HEAD:** причины блока переведены в трейс, D3 отключён (`277853d`) — на пустом
справочнике он только добавлял блоков в очередь.
Остаток блока D: **D2** (наполнение справочника: (а) из одобренных ответов через `answer_cache`,
(б) `/compat_add`, (в) OEM-импорт OKI/Kyocera/HP LJ), юнит-тесты и секции регрессии по D1–D6,
**Codex**, backfill из `approved_answers`, замер baseline закрываемых без веба.
Снимок разбора блока A — `docs/handoff/archive/rev_2026-09-08.md`,
память — `/opt/mp-knowledge/auto-inbox/rev-block-a-2026-09-08.md`.
**Очередь 46 (10.09, срочная задача Сергея) — сделана.** (1) На заблокированной карточке снова
есть кнопка «✅ Отправить как есть» (`ovr:`): отправка без пересуда гейта, причина блока пишется
в `raw_feedback.override_reason` (миграция 704) и в лог, ответ идёт в `approved_answers` и —
если он утвердительный и без конфликта D4/D5 — в `compat_ref` как `approved_answer` (это же D2(а)).
Кнопки НЕТ у обещаний (A1.2), претензий и маркера-заглушки: там только «✏️ Править».
(2) Отчёт по причинам блоков за 14 дней с оценками — `docs/reports/rev_block_reasons_2026-09-10.md`.
(3) Карточные классы (характеристики/производитель/комплектация/габарит и совместимость при точном
совпадении модели) больше не судятся порогом уверенности, `grounded` и полярностью — но только
при источнике «карточка»/кэш без веба. (4) `refresh_markup()` обновляет плашки уже показанных
карточек через `editMessageReplyMarkup` без пересоздания черновика и без LLM.
**Отступление от буквы п.3, требует решения Сергея:** послабление снимает только пометку
«не прямой ответ (полярность не определена)» — там, где `_quality_guard` ВПИСАЛ в текст «Да,»/«Нет,»
и сломал смысл («Нет, технически заправить можно…»), блок остаётся. По замеру 4 из 6 таких блоков
верные. Правильное лечение — перестать дописывать полярность автоматически (отдельная задача).
Автоотправки вопросов по-прежнему нет: п.3 означает «вердикт ALLOW → на карточке снова ✅»,
решение по каждой — оператора.

**Связи номенклатуры (10.09) — сделаны и влиты** (`e2e73a3`), подробности выше в «Итогах 10.09».
Что по ним осталось: замерить на живом потоке, сколько вопросов реально получают блок связей и
не мешает ли он там, где ответ и без него полный; при следующем разборе очереди смотреть карточки
вида «в комплекте?» / «а тонер?» / «с чипом?» — это их зона.

**Блок I — недельный дайджест (пн 09:00 МСК, `docs/reports/digest_YYYY-MM-DD/`, первый прогон за
30 дней, без LLM) — НЕ НАЧАТ.** Первые кандидаты закупщику: мажента — Canon 069H, HP 711M,
Xerox C310 ×2.

## Открытые вопросы (ждут решения Сергея)

- **5422 «заправка» ×3 (067H) — выбрать один текст.** Три разных ответа на один вопрос ушли с
  ozon/oz_acc1, wb/wb_acc2 и wb/wb_acc1 05.09; backfill их не писал, выгружены в
  `docs/reports/rev_cache_conflicts_2026-09-08.md`. Ответить правильным текстом один раз — запись
  встанет в кэш сама.
- **Артикул карточки-ребёнка не сшивается с МС** — 19 из 150 отправленных вопросов мимо кэша.
  Наш `external_code` четырёхсимвольный, площадка даёт пятизначный код варианта (`00818`, `07642`)
  и коды наборов (`4434X6`); `_PLAT_SUFFIX_RX` режет только хвост 6–10 символов, поэтому кандидата
  не рождается вовсе. Префикс из 4 символов сшивает 14 артикулов из 17 (проверено по названиям);
  реально нет в МС только `7136` и два чужих товара (утюг Braun, эпилятор Rowenta). Ведущий ноль
  резать НЕЛЬЗЯ (`03282` → `3282` — чужая карточка). Правка — один кандидат `raw[:4]`
  в `internal_article`, вернёт в кэш ~13 % вопросов; не делал, меняет артикул в карточке оператора.

- **Ozon через ЛК — блокирующий:** чья машина держит логин (ОС, включена ли днём постоянно).
  Схема готова: Playwright с постоянным профилем, меняется только транспорт внутри
  `send_ozon_review()` (`collectors/feedback_send.py:184`), модерация и лимиты как есть.
  До ответа код не пишем.
- **Фильтр «жалоба без конкретики» на живой карточке** — отдавать шаблону вместо человека
  (−0.8 карточки/день, оператору осталось бы 0.9 вместо 1.6). Замер сделан, решения нет.
- **Тихий 403 при сборе Ozon-отзывов** — завести в `health.py` проверку доступности `review/list`
  по аккаунтам, где отзывы РАНЬШЕ собирались (для `oz_acc2` 403 — норма).
- 31.07 на Ozon покупателю ушёл текст телеграм-карточки вместо ответа: причина закрыта коммитом
  `8e21497`, повторов нет, но опубликованный ответ висит — переписать по команде Сергея.

## Ozon через ЛК — ждём канал

Отзывы Ozon по API закрыты навсегда (403), путь только через ЛК. Комплект разведки готов
(`tools/ozon_lk/recon.py`, только чтение), браузер — Chrome под Xvfb на сервере, вход по СМС через
VNC-туннель. Три адреса Proxy Seller (весь пул Билайна `AS16345`) Ozon отбраковал побайтно
одинаково, потрачено 0 ₽; тем же браузером WB открывается — связка рабочая, режет именно Ozon.
**Ждём от Сергея бесплатный тестовый порт другого оператора/ASN**, проверка две минуты:
`./venv/bin/python tools/ozon_lk/probe_ip.py --browser --proxy "<адрес>"`; покупать нечего до
вердикта «канал годится». Очередь — 21 отзыв (`docs/reports/ozon_lk_manual_reviews_2026-08-24.md`).
История замеров, спецификация порта и порядок работ №2–10 из аудита Codex —
`docs/handoff/archive/rev_2026-09-08.md`; эталонный набор — `docs/reports/rev_gold_set.md`.
## Команды

```bash
./venv/bin/python collectors/wb_clearance.py [путь.xlsx]   # загрузить файл распродажи
./venv/bin/python -c "from reports import wb_clearance_page as p; print(p.render())"
systemctl restart mp-dashboard.service

./venv/bin/python feedback_bot/health.py                   # балансы DeepSeek/Anthropic + алерты
./venv/bin/python -m unittest tests.test_prompt_privacy    # приватность промптов (11, офлайн)
./venv/bin/python -m unittest tests.test_health_classify   # классы причин: 529 ≠ пустой баланс
./venv/bin/python -m unittest tests.test_publish_gate      # машинный запрет публикации (7, офлайн)
./venv/bin/python -m unittest tests.test_sku_relations     # связи номенклатуры (8, офлайн)
./venv/bin/python -m reports.sku_relations --stats         # размер связей по типам и аккаунтам
./venv/bin/python -m reports.sku_relations --check 3896    # связи по нашему артикулу
./venv/bin/python -m reports.sku_relations --build         # полная пересборка (~10 мин)
./venv/bin/python -m reports.feedback_digest --days 30      # дайджест: собрать и показать
./venv/bin/python -m reports.feedback_digest --days 7 --send  # + отправить в Telegram
systemctl restart feedback-moderation.service              # ОБЯЗАТЕЛЬНО после каждого коммита
```
