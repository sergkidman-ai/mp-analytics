# BRIEF_MKT — поток «Маркетинг / видимость»

> Первым делом этот бриф + `CLAUDE.md`. Ты — сессия домена **mkt**; параллельно идёт **fin**
> (себест/COGS/маржа/P&L), не лезь. История до 25.08 — `docs/handoff/archive/mkt_2026-08-25.md`.

## ГДЕ МЫ СЕЙЧАС (обновлено 16.09.2026)

**Причина падения выяснена 24–25.08 — она НЕ в рекламе.** Три слоя по убыванию денег:
1. **Наличие.** Правда — живой `totalQuantity` с card.wb.ru, не `wb_stocks`. Замер 25.08: 83 карточки
   на 506 455 ₽ купить нельзя, из них 44 «остаток есть, купить нельзя» (214 521 ₽ — вопрос карточки
   и поставки). `docs/reports/mkt_oos_2026-08-25_*.csv`.
2. **Ширина.** Карточек с ≥50 показов/нед: acc1 цель 328, acc2 цель 150.
3. **Цена и отзывы.** SERP 24.08 (131 запрос, 675 пар): медиана нашей цены **1.74×** топ-10,
   отзывов 2 против 4. **ОСТОРОЖНО:** множитель ≠ «мы дороже» (комплекты 4 шт и оригиналы) —
   массовое снижение цен по нему НЕЛЬЗЯ.

Конверсия упала 3.6 → 2.9 % там, где ставки не трогали → это рынок, не ставки.

### Контракт API рекламы ВБ (выяснено 25.08, действует)
- `POST /adv/v2/seacat/save-ad` **СОЗДАЁТ кампанию**, а не правит состав (бюджет, инвариант 8).
- Состав — `PATCH /adv/v0/auction/nms`, двойная вложенность
  `{"nms":[{"advert_id":N,"nms":{"add":[…],"delete":[…]}}]}` (плоские → ошибка десериализации).
  Лимит 50, `delete` есть, категория обязана совпадать (у нас 1820); новой карточке ВБ ставит
  **минимальную** ставку кампании. Пополнение — `POST /adv/v1/budget/deposit`, `type: 1` = «Счёт».
- Ответы идут из общего кэша ВБ (`camp-api-public-cache`) и могут ссылаться на ЧУЖУЮ кампанию —
  сверять `advert_id`. Статусы 4/9/11. Состав видно только через `wb_ad_nm` и только по карточкам,
  давшим строку статистики. Старт: `GET /adv/v0/start?id=` → 200 с пустым телом, сверять `count`.

### Замер 07.09 (ряды — в `wb_roy_weekly.log`)
Заказы росли на обоих (acc1 152→159→**186**, acc2 80→153→**162**) при падающем расходе рекламы:
восстановление было ЦЕЛИКОМ органическим. **Ширина проседает:** acc1 242 → **191** при цели 328,
acc2 107 → **111** при 150. Сторож остатка живьём: acc1 43 правки, acc2 7.
- **Сторож обвала маржи** `ops/mkt_margin_watch.py`, крон `7 7,14 * * *`, бот PRC (@ds_prc_bot):
  порог 100 в минус ИЛИ (срез у 20 % каталога / медиана ±15 %) при ≥10 перевернувшихся. Дополняет
  `wb_promo_watch` (ev): тот про СОБЫТИЕ, этот про ПОСЛЕДСТВИЕ. **ОРАКУЛ СОСТАВА УМЕР 07.09** —
  место в кампании ищется ТОЛЬКО попыткой добавления (отказ атомарен).

### Автоакции ВБ — разбор 07.09 и перепроверка 11.09 (`mkt_promo_exit_*`, `mkt_promo_recheck_*`)
Ночь 06→07.09 acc2: срезано 58 % каталога на −51 %, **1 199 в минус** (acc1 — 29 % на −30 %, 52);
«ВБ уронил цены» ПОДТВЕРЖДЕНА. Подушка маржи ОДИНАКОВАЯ (34.7 / 35.3 %), дело в глубине среза —
**почему acc2 вдвое глубже, открытый вопрос**. Выход acc2 продержался сутки, ВБ вернул сам; два дня
акции дали **+23 480 ₽** сверх тихого, **«выйти» СНЯТА** (минусовых 11 из 10 347 и 2 из 9 751).

### Маркет: кто на самом деле выбивает товар из акции (20.09.2026)
Из 1 427 заведённых в «Бестселлеры» к 20.09 осталось 87. Поддержка Маркета: «удаляла ваша
интеграция». **По журналу это не так:** наших удалений всего 215 уникальных SKU, у 000211
удалений нет вовсе (только два заведения и трижды «проходит по полу»), цена 2 446 ₽ при поле
1 830 ₽ и себесте 310 ₽ — убыточным он не был. Сейчас его нет даже среди кандидатов акции,
то есть исключили на уровне ТОВАРА, а не акции. Про 3490 поддержка права — это моя проба
по мягкой ступени без проверки пола, сторож его убрал законно.
**Версия Натальи: позиции убирает ТК** (настройки участия в акциях). 20.09 настройки ТК
изменены, сверка — 21.09: ждём совпадения выпавших с исключениями ТК. Параллельно на ВБ
0161 и 0902 (ТК скидок не ставит) — войдут ли они в акцию ночью.
**Полезное из поддержки:** отключить автоучастие можно `POST /campaigns/{campaignId}/offer-prices/updates`
с `delete=true` по товару. Не применялось: метод пишет в ЦЕНЫ магазина, а отправка цены без
`minimumForBestseller` стирает ранее заданный минимум. Сначала проверка на одном SKU и «да» Сергея.

### ВБ: плановые цены акций из выгрузок ЛК (20.09.2026)
`ops/wb_promo_plan_import.py` → `wb_promo_plan_price` (`126_`, `127_`). Файл ЛК «Все товары,
подходящие для акции» — единственный источник и потолка участия, и состава: у автоакций
`nomenclatures` даёт 422, а какая карточка в какой акции — API не говорит вовсе.
Кабинет определяется по колонке «Бренд», акция — по имени файла; ключ таблицы включает акцию.
**Карточка сидит в нескольких акциях сразу** (acc1 в среднем 2,6, acc2 2,0; всего идёт 8 и 12
акций), поэтому потолок подъёма = МИНИМУМ плановых цен по акциям, где товар участвует —
замечание Натальи 19.09, иначе подъём выбьет товар из соседней акции.
Загружено на 19.09: acc1 5 акций (10 392 / 6 275 / 5 502 / 4 600 / 4 177 участий + Новинки 1 390),
acc2 5 акций. Запас до минимального потолка, который берётся ТОЛЬКО уменьшением скидки:
acc1 8 372 карточки / 0,71 млн ₽, acc2 4 272 / 0,77 млн ₽ (на одну штуку каждой). Остальной
запас требует подъёма розничной цены — не трогаем, это ценовая политика.
Выгрузка — за сутки до старта акции, плановая цена держится всю акцию (Сергей 19.09);
замер стабильности — `wb_promo_plan_import --check` по карточкам «ровно на плане»
(acc1 1 042, acc2 424 на 19.09). Подъём до потолка ещё НЕ включён: ждёт проверки устойчивости.
- ⚠️ Наборы проверять только через `cost_map`: у самого набора остатка не бывает, наличие
  считается по компонентам (3097, 3795, 4351 — живые наборы, я ошибочно назвал их мёртвыми).

### Сторожа акций Ozon и Маркета — рабочие контуры (17.09.2026)
Одна семья с ВБ, себестоимость у всех трёх одна: `wb_promo_guard.stock_map/unit_cost` по наличию
СЕЙЧАС (свой склад → поставщик с остатком ≥ 4 шт → закупка ТК → нигде нет = себеста нет).
Пол везде `(себест + max(300 ₽, 10 % себеста)) / доля выручки после удержаний`.
- **Ozon** — `ops/ozon_promo_guard.py`, журнал `oz_promo_guard_log`. Все акции, КРОМЕ белого списка
  лестницы (`ozon_stock_action.py`). Ход: поднять `action_price` до пола, если пол ≤ потолка акции,
  иначе снять поштучно. Крон UTC `30 3` / `30 10` (06:30 / 13:30 МСК), `--apply --limit 300`.
  Ozon переигрывает состав акций ночью сам — утренняя волна за этим и стоит.
- **Маркет** — `ops/ya_promo_guard.py`, журнал `ya_promo_guard_log` (`125_ya_promo_guard.sql`).
  Кабинет один (businessId 879051). Удержания **плоские за закрытый месяц** (66,65 % за август →
  нам 33,35 %), не по диапазонам, как в карантине цен — решение Сергея 17.09. Правим только
  DIRECT_DISCOUNT и BLUE_FLASH, `MARKET_PROMOCODE` не трогаем. Цены каталога не трогаем вовсе:
  `minimumForBestseller` не ставим. Маркет применяет update/delete через **4–6 часов**, поэтому
  отправку сверяет СЛЕДУЮЩИЙ прогон (строка живёт в статусе `sent`). Фильтр `statuses` в запросе
  товаров не использовать: его словарь другой, любой наш статус → 400; отбираем по ответу.
  Крон UTC `35 3` / `35 10` (06:35 / 13:35 МСК). Состояние 17.09: акция одна
  («Бестселлеры», 17–30.09), в ней 2 товара, оба выше пола; потенциальных 2 031, из них у 436
  потолок акции ниже пола — войдут в минус, если Маркет добавит их сам.

### Маркет: заведение в акцию по ступеням — `ops/ya_promo_push.py` (17.09.2026)
У «Бестселлеров» четыре ступени цены (light / best / superBest / topBest); `maxPromoPrice` —
это всегда light, самая мягкая. Чем глубже ступень, тем заметнее товар. Сторож ступени не
выбирает, поэтому рычаг вынесен отдельным инструментом. **Решения Сергея 17.09:** ступень —
самая глубокая, чья цена не ниже пола; запас над полом не требуется; заводим все проходящие.
Заведение = тот же `promos/offers/update` (promoPrice + зачёркнутая цена из `offer-prices`,
`offerIds` там 1–200 за запрос, иначе 400). Журнал общий со сторожем, `mode='add'`.
План 17.09: кандидатов 2 029, завести 1 420 (топбест 1 384, супербест 10, бест 11, лайт 15),
мимо 609 (у 601 все ступени ниже пола, у 8 нет себеста). Медиана скидки от каталога 22,6 %,
медиана запаса над полом 24,2 %. **17.09 заведены первые 100** (`--apply --limit 100 --wave push_0917`,
по прямой команде Сергея): все на топбест, отказов 0, Маркет применит через 4–6 ч, сверит
следующий прогон сторожа. Выборочная проверка Сергея: товары в акции. **Остальные 1 320 залиты тем же вечером
(`--wave push_0917b`), отказов 0** — итого 1 420 позиций: топбест 1 384, супербест 10, бест 11,
лайт 15. Мимо 609: у 601 все ступени ниже пола, у 8 нет себеста.
**Первая отправка применилась лишь частично: из 1 420 встали 86** (почти одни комплекты),
отказов API не было, причина со стороны Маркета не объявлена. **Повтор 18.09 (`push_0918`,
1 325 позиций) применился ПОЛНОСТЬЮ примерно за 1,5 часа** — в акции 1 417 наших товаров.
Вывод: отсутствие отказа ≠ применено, состав акции надо перечитывать и при расхождении слать ещё раз.
**18.09 вечер: Маркет добавил в акцию 74 товара САМ** (статус AUTO) — у всех потолок акции ниже
пола, сторож их убирает (`wave='fix_probe'`, 79 позиций вместе с 5 нашими). Автодобавление у
Маркета реальное, поэтому по решению Сергея прогон сторожа переведён на **каждые 6 часов**:
крон UTC `35 3,9,15,21` (06:35 / 12:35 / 18:35 / 00:35 МСК), `--wave q6`.
⚠️ Проба по мягкой ступени была отправлена БЕЗ проверки пола — 4 товара встали в акцию ниже пола
(3490, 7200, 57581, 57582), убраны сторожем. Любая ручная отправка цены обязана считать пол.
Открыто: досылать неприменённое автоматически (до 3 попыток) — не сделано.
Проба 18.09: 4 товара заведены по «лайту» — ступень на приём не влияет. У 6077 «лайт» оказался
на рубль выше `maxPromoPrice` → `PROMO_PRICE_BIGGER_THAN_MAX`; потолок ступени теперь режется
и по `maxPromoPrice`.
**Свой себест ниже половины рыночной цены — не себест** (правило Сергея 17.09, `OWN_COST_MIN_SHARE`
в `wb_promo_guard.py`, действует у всех трёх площадок): так в МС приходит замена брака за 1 ₽.
3641 считался от 1 ₽ (пол 902 вместо 16 108), 0334 — свой рубль при отсутствии товара у поставщиков,
теперь без себеста и в акции ему не место.

### Сторож автоакций `ops/wb_promo_guard.py` — рабочий контур (16.09.2026)
Правит цену ЗА СЧЁТ СКИДКИ (база цела), сравнивает с `discountedPrice` (после скидки акции, до
СПП). Пол = **максимум из 300 ₽ и 10 % себестоимости**, чистыми до налога и операционки;
удержание **56.9 % Цифровой / 55.4 % Дисквер**. Нет себеста или нет остатка нигде → вывод из
акции, автоматом, без исключений. **Волна start перенесена на 01:00 МСК (крон UTC `0 22`, решение Сергея 21.09):** в 00:30 она
трижды подряд находила ноль — ВБ роняет цены позже. Проверка — на старте следующей акции.
Дневные волны 11:00/15:00/19:00 МСК (крон UTC `0 8,12,16`) введены 19.09: за первый же день
30 правок, ночные волны в тот день нашли ноль — дыра 08:00→00:30 была реальной.
Старт акции 21.09: волна 04:00 подняла 1 064 (acc1) и 3 932 (acc2) карточки, в минусе 1 188,
спасено 1,26 млн ₽ на одну штуку каждой; ошибок 0.
**Без скидки от ТК карточка в акцию не входит** (0161, 0902 на старте 21.09: скидка 0 %,
цена не менялась). Это же объясняет исчезновение товаров из «Бестселлеров» Маркета.
Крон UTC: `10 20` доакционный снимок, `5 21` сбор остатков,
волны `30 21` / `0 1` / `0 5` (00:30 / 04:00 / 08:00 МСК). Правки — ТОЛЬКО по расписанию
(решение Сергея 16.09), руками не применяем.
- **Ночной снимок остатков — только ПОЛНЫЙ (крон правлен 15.09).** `collectors.suppliers --light`
  переносит `buy_price` из прошлого снимка и в МС за ценой НЕ ходит, тяжёлый проход шёл раз в
  неделю → цены жили неделю. Цена: 7 ложных убыточных 15.09 (3502/3503 по 6825 вместо 4167,85;
  не видели Булата 5700,16 по 5075). `--light` из крона убран, полный проход ~4 мин до волны.
- **Пустой ответ `/history/tasks` = «ещё не готово», а не провал (фикс 15.09 в `push()`).** Волна
  00:30 записала 100 ложных ошибок из-за `status=None`. Три волны 16.09: 2/2, 5/5, 5/5, ошибок 0.
- Оба знания — `/opt/mp-knowledge/inbox/external-code-lookup-rules.md`, пп. 14 и 15.
- **`supplier_min` — только реальные предложения поставщиков.** Наша прошлая закупка ТК
  (`tc_buy_price_*`) в него НЕ входит, идёт отдельным `tc_price` (наборам — как своя цена
  комплекта). Цена смешивания — ошибка 11.09 по бандлу 0025X6.
- **`MIN_SUP_STOCK = 4`** (сравнение `>=`): 3 шт и меньше обнуляются. Режет дешёвые хвосты на
  дорогих позициях — 5947: Одиссей 8323,99 / 3 шт против Колортека 12 248,16 / 25 шт, ~10 000 ₽
  запаса скидки. Правило Сергея, не меняли.
- **Картина 16.09 10:42** (`reports/wb_promo_guard_2026-09-16_1042.csv`): Цифровой 20 574 карточки,
  себест. известна 14 543, под правку **4**, в минусе **0**; Дисквэр 20 640 / 14 473 / **0** /
  **0**. Было 12.09 — 46 под правку, 15.09 — 36 с семью убыточными; разница от свежих закупочных.
- **ОТКРЫТО:** наличие проверяем по воронке, а она отдаёт только карточки с активностью за период
  → ФБС слепой (5801 и шесть из восьми 6593 не видны). Замена — `/api/v3/stocks`; скоуп
  «Маркетплейс» есть в ОБОИХ кабинетах, в токенах `WB_TOKEN_RETURNS_ACC1/ACC2` (200 на
  `/api/v3/warehouses`, прочие восемь 403), новых ключей не нужно. Не начато, ждёт «да».
- **ОТКРЫТО:** надбавки к цене поставщика — цифры Сергея стабильно = наши × 1.10 (4227, 2783).

### Три вещи, которые определяют механику (проверено 07–14.09)
- **Витрина затирает ночное дно.** `mkt_margin_control` — строка на `captured_date`, а
  `reports.margin_control` гоняется в **07:40 (только acc2)**, 09:20 и 16:20 МСК: 10.09 в 10:07
  было **938** перевернувшихся, в 17:07 — **22**. Лечится `_ЧЧММ` в имени; у acc1 раннего нет.
- **Выборочного участия в акции нет.** `/calendar/promotions/nomenclatures` → **422** везде;
  работают только `/details?promotionIDs=N` и живая цена ПОСЛЕ старта. Выход — целиком или никак.
- **Рой поднял зелёных 14.09 — впервые.** acc1 230 правок (🟢104 9.96→10.95, 🟤111 в пол, ⚫12),
  acc2 67 (🟢27 9.43→10.38). Фраза «подъём не автоматизирован» в боте врала — снята.
- **Кандидаты в рекламу:** живой остаток > 0 И (продажи с 01.06 ИЛИ «из сумрака») И маржа ≥ 15 %
  И не в рекламе → `docs/reports/mkt_ad_candidates_*` (14.09 по acc1 — 124, предметы 1820/1907).

### Мест в старых кампаниях нет → заводим новыми (сделано 14.09)
Обход всех 103 живых кампаний acc2 (`ops/wb_ad_enroll_sweep.py`, проба одной карточкой): **мест 0**
— кампания, где витрина показывает 1–41 карточку, всё равно отвечает «exceed limit» (`wb_ad_nm`
знает только получавших показы); acc1 так же, 80 из 92 уже ≥50. **Решение — новые кампании**
(`ops/wb_ad_campaign_new.py`, создание бесплатно, лимит 50): КРУТЯТСЯ (статус 9, по 1000 ₽, всего
**7000 ₽**) acc2 `40157978/85/94` = 124 карточки и acc1 `40161426/31/42/45` = 124. **`type: 1` =
«Счёт»** (докум. ВБ врёт про 0); `/budget/deposit` бывает `{"total":0}` при зачисленных деньгах —
проверять `--budget`. **Итог 14–17.09: 5 544 ₽ → 6 заказов (924 ₽/заказ, ДРР 56 %)**, списание
за показы 0,50 ₽ (CPM 500 против 145–218 в старых), Рой их не ведёт, показы у 54 из 248 — способ
ЗАКРЫТ. «~270/~300 в очереди» было без замера (реально 22/17 с продажами). Вместо этого 18.09:
14 старых рабочих кампаний стояли на нуле (90 заказов на 8 269 ₽ за 28 дн) — **ПОПОЛНЕНЫ 18.09
на 15 000 ₽** (по 1000, 39087266 — 2000; **минимум пополнения ВБ = 1000 ₽**), запущены;
3 без заказов (35697894/8187/8366) и 35706004 — пополнены позже по «да» Сергея, +4 000 ₽ (итого 19 000);
**18.09: состав кампаний ЕСТЬ** — `GET /api/advert/v2/adverts?ids=` (nm_settings, payment_type);
мои 7 кампаний ВБ создал как **cpm 500 ₽** (save-ad игнорирует paymentType) — Сергей закрыл все 7
вручную (статус 7). Точно: вне кампаний из продававшихся с 01.06 — acc1 27, acc2 7 (с маржой и
остатком); свободных мест в cpc — acc1 20 (36879647/36880441), acc2 0. acc2: 29 спящих cpm-кампаний
(0 ₽ за 28 дн, 343 позиции) дают 396 из 445 дублей.
**18.09 вечер (всё по «да» Сергея):** acc1 +20 позиций в 36879647/36880441 (состав перечитан, по 50);
`wb_ad_enroll` — дубли/места только по составу; `wb_ad_campaign_new` — создание ЗАКРЫТО, --start только cpc;
рычаг 1 волна 1: `wb_roy_seed_defaults` — acc2 232 позиции (seed) + 191 контроль, протокол
`mkt_roy_seed_w1_2026-09-18_wb_acc2.csv`, разбор ~05.10 разностью разностей (база заказов 22 vs 35);
cron: `wb_ad_budget_guard` 07:30 UTC (PRC-бот), `wb_stock_guard` DRY 07:40 UTC (решение об --apply 25.09).
`wb_stocks` НЕ сломан — он FBO-only, истина остатка — card.wb.ru. НЕ СДЕЛАНО: закрытие 29 cpm acc2 —
классификатор заблокировал, команда у Сергея: `ops/wb_ad_stop_dormant_cpm.py --apply`. Пн 21.09:
проверить прогон Роя (впервые с seed) и куда ушли остатки закрытых 40157994/40161431. признак склейки на платном трафике — шум (рычаги: артефакт b4ad4b48).

### Окупаемость рекламы — замер 4 недель 17.08–13.09 (`mkt_wb_growth_2026-09-14.html`)
Расход / выручка с рекламы / ДРР / CTR / CR клик→заказ / цена заказа: acc1 **45 816 / 674 207 ₽ /
6.8 % / 1.85 % / 4.97 % / 205 ₽**; acc2 **19 541 / 488 659 ₽ / 4.0 % / 1.68 % / 7.43 % / 106 ₽** —
заказ вдвое дешевле, а денег вдвое меньше. **Клик окупается до 35.4 / 39.0 ₽**, платим 10.22 и
7.85; `MAX_CPC = 25 ₽` стоит НИЖЕ порога на обоих. Средняя `booster_pos` ≈ 49 — третья страница.
- **Эффект подъёма измерить НЕЧЕМ — замер смещён отбором.** Когорты 17.08 и 24.08: показы +41 и
  +9 % при контроле −14 / −5 (ставка ПОКУПАЕТ видимость), но заказы −62 % при контроле +15/+133 %.
  Рой берёт 🟢 по пику прошлой недели, и эта же неделя — база «до»: откат к среднему гарантирован,
  «подъём убивает продажи» читать НЕЛЬЗЯ. Закрыто холдаутом (ниже). Подъёмы были только 17.08 (76),
  24.08 (68) и 14.09 (131); 31.08 и 07.09 — 253 и 128 правок, ВСЕ вниз: Рой лето был срезателем.
- **Объём рекламы:** показы за 7 дней у **5 110** карточек acc1 (92 кампании) и **4 632** acc2 (71),
  за 30 дней 8 695 / 8 642. Ставкой из наших рук управляется лишь **1 799 acc1** (1 078 на полу) и
  **380 acc2** (128) — у остальных ~7 500 ставка кампании по умолчанию, Рой их не видит.
- **Маржа acc1 НЕ падала:** WB по `margin_by_sku` 20.7 → 18.8 → 15.8 → 16.4 → 21.5 → **20.2 %**
  (мар–авг), acc2 18.3 → 18.2 → 9.2 → 10.7 → 21.3 → **18.1 %**: середина коридора, выше майско-
  июньской ямы. НО витрина по свежим месяцам тонкая (1709 шт июнь → 605 август) — точность нет.

### Вывод ⚫ ОТМЕНЁН Сергеем 14.09 — из кампаний не удаляем
«Расходы по ним небольшие, а видимость можно срезать из-за этого». Заявка (acc1 31 / acc2 16)
не исполняется, еженедельный `--queue` закомментирован в `wb_roy_weekly.sh`, контур цел и
включается возвратом строки. ⚫ остаются в кампаниях со ставкой на полу. **Живой пробы `delete`
так и не было — за 3 месяца в `wb_bid_log` НИ ОДНОГО удаления, только `api_set`.**

### Холдаут 50/50 — сделан 14.09, первое назначение в прогон 21.09
`wb_roy_apply --holdout 50`: половина 🟢 по детерминированному хэшу `nm_id` НЕ поднимается.
Хэш, а не жребий — карточка всегда в одной группе, когорты копятся. Назначение →
`mkt_roy_holdout_<конец недели>*.csv`, разбор `ops/wb_roy_holdout_eval.py` (в прогоне до
применения) даёт разность разностей по **`заказы_всего`** и выкидывает карточки, которых за
неделю касался не Рой. Сухой прогон по 13.09: acc1 43/61, acc2 16/11. **Первый вердикт 28.09.**

### Следующий шаг
1. Замерить первую неделю 248 новых карточек (закроется 21.09) и 2905/2907 — оба искажают
   сравнение недель, читать вместе. Когорта 07.09 (10 шт) за неделю: 261 показ, 6 кликов, 0 заказов.
2. **Сторож остатка на крон** — сейчас руками, а он уже дважды нашёл десятки SKU.
3. «Остаток есть, купить нельзя» ПЕРЕМЕРЕН 18.09: acc1 27 / acc2 26 позиций, выручка с 01.06
   584 739 ₽ (с 01.08 — 177 554 ₽), у поставщика остаток есть, на ВБ купить нельзя →
   `docs/reports/mkt_oos_2026-09-18_*.csv`. Причину (ФБС-остаток не выгружен? карточка?) не искали.
   **Склейки 18.09:** клеим ЦВЕТОВЫЕ серии (Bk/C/M/Y), каталог уже склеен: 442/592 серий acc1,
   438/592 acc2 целиком; одиночек в сериях 59/83 почти без заказов — рычаг ЗАКРЫТ.
   **Разбор ассортимента 18.09** (артефакт QHTbYEWQxXxJX6yAqukuPB): WB март–авг 6,16 млн прибыли; 45 % штук
   (прибыль <300 ₽/шт) = 8,6 % прибыли; комплекты 21 % штук = 37 % прибыли (755 ₽/шт против 384);
   комплекты в мае −21 % цены → +77 % штук, −37 % прибыли. Невидимым позициям ставку поднимали в
   августе: 290 шт → 0 заказов. **6 076 пачек acc2 (сентябрь) НЕ склеены с одиночными** (живая
   сверка). Пилот поиска ВБ v14 (бесплатно): у топ-моделей 26–74 конкурента. **Склад (Сергей 18.09): 410 000 ₽/мес, ~200 заказов/день →
   67 ₽ на заказ** (на ВБ штука = заказ). Март–авг после склада 5,44 млн (−13 %); ниже черты 409 позиций,
   1 323 шт (11 %), −259 тыс.; июль–авг — 72 позиции, 100 шт, −29 тыс., у 61 из 72 цена уже выросла ≥10 %
   → проблема акционная, не структурная. Список `mkt_below_line_2026-09-18.csv`. Ранний крон acc1 поставлен.
   **Сводка 19.09** (артефакт Y2pqUDxqXpZB7WgCVPvWue): сент.(1–13) против мая — acc1 прибыль/день +36 %
   (после склада +54 %) при −49 % штук; acc2 = май при −39 % штук. КПИ ≥25 %: acc1 89 %, acc2 77 %.
   **Слепое пятно:** `margin_own_live` считает от НАШЕЙ цены до акции — автоакцию не видит; 18.09 acc2
   2905/2907 урезали 6 876 поз. на 46 % (маржа 16,7 %, 3 008 <15 %), сторож поднял 980 (пол 300 ₽ ≠ 25 %),
   к 19.09 урезаны 47. acc1/acc2 юнит-экономика одинакова (3 908 пар: цена ×1,02, маржа 32/33 %).
   Новинки сент.: acc1 17, acc2 123 (~95 копий acc1); остальное — пачки (acc1 4 471, acc2 5 906) и
   пачки фотобарабанов. **`wb_cards_live` для acc1 не обновляется** (нет ни одной сентябрьской позиции).
   Комплект = 0,82 суммы цветов (747 сопост.) → себест 28–35 % цены, маржа % ниже, ₽/шт вдвое выше.
   **Склейки 19.09** (артефакт Thdpsfibx963F82ZAwEArU; всё в предмете 1820 — технически клеится):
   пачки 10 010 → 9 661 без своего одиночного (к 3 002 одиночным, 446 продавались); цветные вне склейки 148
   в 85 сериях; двойные наборы струйников 62 из 352 разорваны; объём std↔XL 203 группы (523 поз.);
   барабан↔картридж 611 можно, 258 уже. Новинки acc2 медиана маржи 38,1 %, у acc1 нет себеста.
   Склейка по принтерам — только тест 20/20 с понедельной воронкой. Скрепер: план и 5 вопросов Сергею.
   **19.09 вечер:** `wb_cards` acc1 не обновлялась с 19.08 — общий токен 403 на content-api; `collect_cards`
   переведён на WB_TOKEN_CONTENT_ACC* (коммит 7ca5adf), acc1 20 542; sku_economics+margin_control пересобраны
   (новинки acc1 с маржой 12/17; 23 новинки не в продаже). Склейки v3 — по характеристикам ВБ + чип из МС
   (бандлы 10 197 → 3 183 одиночных; бандлы между собой разнесены у 2 890 из 2 900 моделей; цвет 103 серии;
   объём 258; барабан↔картридж 339). Ошибка v1: ключ серии склеил Canon 067H/068H/069H — исправлено.
   `ops/wb_glue.py` (moveNm, dry-run по умолчанию, --undo). **Пилот СКЛЕЕН 19.09 по «да» Сергея** (витрина подтвердила):
   349953418→группа 1848015675 (397270419), 1540583098→группа 304797663 (313214639); журнал mkt_glue_journal.jsonl. Скрепер — отдельной сессией: `docs/prompts/mkt_wb_scraper_start.md`.
4. Перемерить расход склада до 2905/2907 (17.09): медиана покрытия 23 дня, 16 карточек <7 дней.
5. Ранний крон acc1 (снимок 07:00) классификатор не дал — ставить руками (отчёт 11.09).

## Сигнал от prc 14.09.2026: закупочная в карточках МС — ПРОВЕРЕНО
С ночи на 15.09 buyPrice карточки МС каждую ночь = цена актуального оприходования на «Удаленный»
(все поставщики). Пол цены НЕ просел: по сторожу автоакций стало ЛУЧШЕ — фантомные убыточные
ушли (16.09 в минусе 0 на обоих кабинетах). Цены ТК (`tc_buy_price*`) не задеты.
Журнал `prc_buyprice_write`, детали `BRIEF_PRC.md`.

## Скрепер выдачи ВБ — этап 1 сделан (19.09.2026)
`docs/prompts/mkt_wb_scraper_start.md`, ветка `mkt/wb-scraper` (worktree `mkt-wb-scraper`).
- **Эндпоинт сменился ТРЕТИЙ раз.** Живой проверкой перед стартом: `search.wb.ru v14` → 200
  (100 товаров), `v7` → 429, старый `u-search.wb.ru v18` (на нём крон `serp_core.log` с 11.08)
  → **403 с 12–18.09 минимум** — крон `40 22 * * *` (`tools/wb_serp_probe.py`) молча пишет пустые
  CSV шестой день, алерта нет. Version у ВБ гуляет: v9(07.08)→v18(08.08)→v14(19.09) — при
  следующем отказе версию менять только после ручной проверки, не наугад.
- **Новый сборщик `collectors/wb_search_scrape.py` → таблица `wb_search_snapshot`**
  (миграция `126_wb_search_snapshot.sql`, applied). Append-only лог: run_id, запрос, регион,
  dest, позиция 1–100, nm_id, продавец/supplierId, бренд, название, цена после СПП и до, рейтинг,
  отзывы, наличие/остаток. Темп 1 запрос/3.5 с, 429 → бэкофф до 3 попыток (не отвечает ≠ пусто,
  повторится в следующем прогоне), суточный потолок 1500 запросов (счётчик по строкам за сегодня).
  Список запросов по умолчанию — существующий курированный `tools/wb_serp_core_keys.txt` (152
  строки); «топ-200 прибыльных моделей» автоматом — не сделано, нужен отдельный проход (сейчас
  модель/код в названии не структурирован, компат-индекс — по принтерам, не по кодам картриджей).
  Проверено вручную: `--limit 3` (без записи) и `--limit 2 --write` — 200/600 строк легли в БД,
  поля разобрались (цена/бренд/продавец/остаток чётко видны).
- **4 вопроса промта — ответы Сергея 19.09:**
  1. IP сервера — ДА (тот же профиль темпа, что у сборщиков цен, риска не увидели).
  2. Регионы — Москва/СПб/Екатеринбург. dest СПб `-1198055` и Екб `-5818883` сняты ЖИВЫМ запросом
     к `user-geo-data.wildberries.ru/get-geo-info?latitude=&longitude=` по координатам города
     (не угаданы); Екб на «картридж 718» уже дал другой топ-3 (локальный «Принтбург»), СПб пока
     совпал с Москвой — регион реально что-то меняет, но не всегда.
  3. Конкуренты — без списка, сигналы по всем продавцам в выдаче (этап 3, не сейчас).
  4. Тест «мы одни → цена +10 %» — НЕ начинать, сначала данные (этап 3+, не сейчас).
- **Крон поставлен 19.09** (по ответам выше): `40 22 * * *` (01:40 МСК) — старая мёртвая строка
  `tools/wb_serp_probe.py` (u-search v18/CSV) ЗАМЕНЕНА на `collectors/wb_search_scrape.py --write`
  с тремя `--dest` (msk/spb/ekb), лог `logs/wb_search_scrape.log`. Снимок crontab до правки —
  `backups/crontab_2026-09-19.txt`. Источник запросов пока старый `wb_serp_core_keys.txt`
  (152 строки) — это НЕ «топ-200 прибыльных моделей» из промта, та выборка не сделана.
- **НЕ СДЕЛАНО (по промту, стадии 2–4):** «топ-200 прибыльных моделей» вместо курированного
  списка; сопоставление «тот же товар»; сигналы «мы одни»/просадка конкурента; список новых
  принтеров; ценовой тест. Первый плановый прогон — ночь на 20.09, проверить `logs/wb_search_scrape.log`
  и рост `wb_search_snapshot` перед тем, как строить сигналы поверх.

## Открытые вопросы к Сергею (нужно прямое «да»)
- **Себест: фолбэк на базовый 4-значный код есть у ТК (`code[:4] in base`), у остатка МС нет** —
  `4328TLQMH10X` = «нет себеста», хотя двойник `4328` даёт 6619 ₽; хвост карантина хуже реального.
- **`MAX_CPC` 25 → 30 ₽ при марже ≥25 %** — потолок ниже порога окупаемости (35.4 / 39.0 ₽) и
  сейчас главный тормоз. Делать ПОСЛЕ холдаута, не одновременно.
- **Перелить бюджет на acc2** (+5 300 ₽/нед): заказ там 106 ₽ против 205 ₽ на acc1.

## Рекламная стратегия (что поднимаем, что опускаем)
Крон `12 8 * * 1` (пн 11:12 МСК) → `ops/wb_roy_weekly.sh`, оба аккаунта, автор `roy`.
Правила: 🟢 +10 % · 🔴 −10 % · 🟤 в пол · ⚫ вывод в пол · 🟡 не трогаем. Константы: пол 7.30 ₽,
потолок 25 ₽, шаг +10 %, срез 0.90, `--max-up 0` = без ограничения, `SEED_GRACE_DAYS = 14`.
Зелёной считается позиция, у которой ДРР профиля ниже СВОЕГО потолка (**маржа − 25 %**): маржа
30 % → потолок ДРР 5 %, 60 % → 35 % — чем прибыльнее, тем агрессивнее.
Сторож остатка (`ops/wb_stock_guard.py`) симметричен: нет живого `totalQuantity` → в пол, вернулся →
ТА ЖЕ ставка из `old_cpc`. Авторы `wb_bid_log`: `ladder, lower, roy, roy-rollback, seed, stock-guard`.
**Разница аккаунтов в материале, не в правилах:** acc1 1 799 SKU со ставкой (998 на полу), acc2 380
(96). **Две дыры:** нет правила по марже («маржа < 0 → пол + `delete`») и нет холдаута — правила
крутятся без контрольной группы, то есть их эффект до сих пор не подтверждён ни одним замером.

## Грабли, за которые уже заплатили
- **`wb_price.market_price` — НЕ цена рынка**, а наша цена после СПП; рынок только из SERP-снимков
  `tools/core_<ГГГГММДД>_detail.csv` (крон `40 22 * * *`), история цен — `mkt_margin_control`.
- **`wb_stocks` — FBO-only**, не признак доступности; молчание card.wb.ru тоже не «остатка нет».
  ФБС: `Удаленный склад`, `Звездный`, `Дисквер`. Мост: 4 цифры `vendor_code` → `external_code`.
- **`wb_funnel.period` — начало МЕСЯЦА, не недели**; недельные ряды сравнивать одинаковыми днями
  недели (пн–чт к пн–чт). Коррелированные подзапросы к `supplier_stock` вешают прогон на 15 мин —
  CTE по `ms_id`. **Посуточные медианы — только на ФИКСИРОВАННОЙ когорте**: состав
  `mkt_margin_control` плавает 9 835…14 102 строк, сырая медиана по дням — артефакт.

## Зона ответственности и твои файлы
Видимость, поисковые запросы, воронка, реклама и ДРР/ROI, отзывы/рейтинг, ABC, точки роста,
детектор просадок (маржу БЕРЁШЬ готовой). Правишь: `collectors/{wb_jam, wb_funnel, wb_ads,
ozon_ads, ozon_bids, ozon_reviews}.py` · ставки ВБ `ops/wb_roy_*.py`, `ops/wb_bid_ladder.py`,
`ops/wb_stock_guard.py`, `ops/wb_ad_enroll*.py`, `ops/wb_ad_campaign_new.py`,
`ops/wb_roy_weekly.sh`, `web/marketing_app.py` (+ `web/static/wb_evict.html`) · `run_marketing.py`
· витрины `reports/{abc,funnel,visibility,search}*` · миграции **блок 1xx** (0xx — fin). **Пишешь
таблицы:** `wb_search_*, wb_jam_may, wb_funnel, wb_ads, wb_ad_nm, ad_spend_daily, ozon_ads,
ozon_bids, ozon_rating, drops, wb_bid_override, wb_bid_log`.

## Граница и СТОП-зоны
Маржу/себест НЕ считаешь — берёшь готовой из `margin_by_sku`/`margin_ozon_sku`, только SELECT;
кажется неверной — не правь, скажи fin/Сергею. Запись на ВБ/Ozon/Яндекс — только по прямой команде
Сергея (инвариант 6), dry-run можно. Территория fin: себест/COGS (`ms_demand_cogs`,
`report/stock/byoperation`, `cost_seb`), `margin_by_sku` на запись, `sales`, `products`,
`run_daily.py`, финколлекторы — тянет туда, **СТОП, сообщи Сергею**.

## Карантин, ведущий ноль, worktree
`price_quarantine.py --apply` в кроне **03:58 · 11:12 · 17:47 · 23:06 МСК**; простой 10–14.09 от
`newPrice=None` починен card (`cd2385f`). Ведущий ноль не резать НИКОГДА; ветка — в worktree.
