# BRIEF_INFRA — поток «сервер и платные API»

> Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_INFRA.md» — и больше ничего.
> Поток выделен 17.08.2026: эти вопросы всплыли в сессии потока rev (отзывы) и были оттуда
> вынесены по требованию Сергея — в сессии про отзывы должны быть только отзывы.

## Где работаю
- Ветка: `main` (юниты systemd, окружение, `.env`) · worktree не нужен
- Модель сессии: **Sonnet** для замеров и прогонов, **Opus** только под вердикт про деньги/лимиты.

## Зона ответственности
Ресурсы машины (память, swap, кто её съедает) и платные API-ключи как биллинг: сколько уходит,
кто ходит по ключу, когда пополнять. Прикладные алерты о балансах живут в потоке rev
(`feedback_bot/health.py`) — этот поток их НЕ переписывает, только пользуется их сигналом.

## Мои файлы (правлю я)
`/etc/systemd/system/*` и drop-in'ы, `.env` (лимиты и пороги), скрипты замеров. Прикладной код
потоков не трогаю.

## Запреты потока
- Пополнение любого баланса, покупка подписок, платные серверные инструменты — **только после
  явного «да» Сергея на конкретную сумму** (CLAUDE.md, «Платные ресурсы»). Наличие ключа
  в `.env` не мандат на трату.
- Гасить чужие сессии Claude Code без разрешения нельзя: 17.08 Сергей решил их не трогать.

История этапов (состояние 17.08, техдолг worktree-settings) —
`docs/handoff/archive/infra_2026-08-24.md`.

## Открытые вопросы
- **Пополнить DeepSeek — решение Сергея.** На нуле встанет ветка ОТЗЫВОВ, то есть основной
  поток движка (вопросы идут на Anthropic, отзывы и веб-проверки — на DeepSeek).
- **Гигиена сессий Claude Code — решение Сергея.** 19 процессов на 5.9 ГБ будут ронять
  тяжёлые прогоны и дальше; варианты: гасить брошенные сессии, поднять swap, лимит памяти
  на процессы Claude Code. Пока не выбрано ничего, стоит только защита цикла.
- Нужен ли отдельный дешёвый учёт расхода по ключу Anthropic в разрезе «движок vs sokol-server»,
  раз баланс общий.

## Следующий шаг
Замерить память после планового цикла 15:04 UTC (`free -m` + `ps` по Claude Code, свести
в файл) и принести Сергею один выбор по гигиене сессий с оценкой, а не список вариантов.

## Auto-mode проекта + SIDE-EFFECT / PRODUCTION GUARD (внедрено 18.08.2026)

* **`.claude/settings.json`** — канонический набор permissions проекта (коммитится, наследуется
  всеми worktree, созданными от этого коммита). `defaultMode: acceptEdits`; 125 allow / 15 ask / 18 deny.
* **`.gitignore`** — закрыты `.env.*` (бэкапы `.env.bak_*` лежали неигнорируемыми),
  `.claude/.credentials.json*`, `*.key/*.pem/*.p12/*.pfx/*.jks/*.keystore`, `logs/`.
* **`tools/hooks/guard_side_effects.py`** — PreToolUse-хук. Работает и под
  `--dangerously-skip-permissions` (ночной режим), т.е. это последняя линия защиты.
  Уровни: AUTONOMOUS → AUTONOMOUS+LOG → ASK → HARD DENY; хук умеет только ПОДНИМАТЬ планку.
  Классы: secret_read, secret_exfil, bank_prod, mp_write, db_destructive, git_dangerous,
  fs_destructive, service_prod, obfuscation, territory, paid_api. Журнал
  `logs/guard_side_effects.jsonl` (команда обеззаражена, значения секретов не пишутся никогда).
* **`tools/hooks/test_guard_side_effects.py`** — 77 тестов (`python3 -m unittest`). Тестам НЕ нужны
  ни `.env`, ни venv, ни боевые данные площадок/банка — проверено прогоном в чистом worktree.
* **`.claude/settings.local.json`** — вычищен с 398 накопленных allow до 5 машинно-специфичных;
  из правил удалён боевой ключ RAPID, лежавший открытым текстом в allow-правиле `curl`.

Запреты: значения секретов не попадают ни в permission-правила, ни в журнал хука; приложению
пользоваться секретом из окружения не мешаем, Клоду читать/печатать/отправлять/коммитить
ЗНАЧЕНИЕ секрета запрещено; ослабление guard'а — только правкой файла и с объяснением, что
именно становится автономным.

Открыто: старые worktree (`fin-*`, `gab`, `inv-*`, `mkt-ozon`, `prc-*`, `ret`) получат
`settings.json` и хук только после rebase/merge на `main`. Ключ RAPID стоит перевыпустить —
он лежал открытым текстом в локальных настройках.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы.
- Дорос до 150 строк — старое в `docs/handoff/archive/infra_<дата>.md`.

## Канон знания `/opt/mp-knowledge` (MVP-1, 2026-08-20)

**Состояние: внедрено, ждёт вердикта Сергея по shadow-периоду.**

Память проекта больше не лежит в скоупе Claude — скоуп это симлинк на отдельный
git-репозиторий `/opt/mp-knowledge` (свой аудит-лог, откат `git revert`).

- 88 карточек в `memory/`, индекс генерируется: `python3 /opt/mp-knowledge/bin/kb index`.
  **Индекс руками не править** — правится карточка, потом `kb index`.
- `kb lint` — битые ссылки на пути, длина строк, размеры тел. `kb stats` — что где лежит.
- Не в работе, но и не потеряно: `quarantine/` (3, не проверяются по коду),
  `inbox/merge_review/` (3 пары на ручную склейку), `archive/stale/` (1).
- Вход в знание проекта — `bin/mpsess` (Claude из каталога проекта). Из голого `/root`
  канон НЕ виден: память привязана к git-репозиторию рабочего каталога.
  Worktree'ы делят скоуп репозитория автоматически — отдельные ссылки им НЕ нужны.
- Откат и все замеры — `docs/reports/kb_migration_2026-08-20.md`.
  Бэкап памяти до миграции: `backups/kb_memory_2026-08-20/`, держать 30 дней после GO.

**K4 — ЗАКРЫТО 2026-08-24** (отчёт `docs/reports/engineering_memory_k4_final_2026-08-24.md`).
Инженерная память переведена на нативные механизмы Claude Code:

- `CLAUDE.md` = только CORE: инфраструктура, аккаунты, точка входа, 11 инвариантов.
  9 433 → 2 545 токенов. Полный прежний текст — `docs/archive/CLAUDE_k4_2026-08-24.md`.
- Условное знание вынесено в `.claude/rules/{fin,ozon,gab,data-layer}.md` с `paths:` —
  на старте стоит **0 токенов**, грузится только при касании своих файлов (проверено
  хуком `InstructionsLoaded`: `path_glob_match`).
- Индекс канона доставляется как `.claude/rules/kb-index.md`, генерируется `kb deploy`
  из `/opt/mp-knowledge/memory/*.md`. **Руками не править.** Целостность — `kb verify`
  (отпечаток по карточкам, не по форматированию).
- Канон и авто-память физически разведены: `autoMemoryDirectory` →
  `/opt/mp-knowledge/auto-inbox` (недоверенный слой), промоушен только человеком
  (`/opt/mp-knowledge/docs/correction-flow.md`). Claude в канон не пишет.
- Три Skill'а: `ms-mass-change`, `codex-delegation`, `gab-box-calc`.
- Контрольная цифра стала тестом: `tests/test_control_margin.py`.
- Хук `guard_side_effects.py` на ASK печатает шесть строк (намерение / масштаб / эффект /
  обратимость / рекомендация / превью-счёт read-only). Вердикты не ослаблены, набор зелёный.

Замер: project-added стартовый контекст **17 981 → 10 280 токенов (−43 %)**, gold-set
GS-A 15/15 без деградации.

**B4 закрыт детерминированно (24.08).** Текстом норма владения держалась неустойчиво (2 прогона
из 3), поэтому вынесена в рантайм: `tools/territory_guard.py` получил единый разрешатель
владения `resolve_ownership()` (явный `.workstream` главнее ветки, расхождение → `conflict` и
fail closed на запись) и манифест `RESOURCES` (владелец `fin`, точки входа пересборки витрины,
таблица `margin_by_sku`); `guard_side_effects.py` — детектор `d_territory` с уровнем **DENY**.
Чтение чужой территории разрешено, запись/пересборка из чужого потока невозможна независимо от
того, вспомнил ли Клод правило. 20 новых тестов.
**Побочный эффект, который надо знать:** общий чекаут сейчас `.workstream=fin` при ветке
`mkt/roy-width-2026-08-24` → статус `conflict`, и в НЁМ пересборка витрины закрыта до
приведения маркера и ветки в согласие (в доменных worktree всё как обычно).

**B3 закрыт детерминированно (25.08).** Инвариант «платные ресурсы — только после явного да»
жил только текстом, и `curl` к платному эндпоинту или запуск батча проходили как обычная работа.
Появился `tools/paid_api_registry.py` — единый источник правды о платных возможностях:
4 провайдера (хосты + ИМЕНА ключей, значений там нет) и 16 точек входа, каждая с доказательством
из кода; тарифы намеренно НЕ хранятся. Детектор `d_paid_api` даёт **ASK, не DENY** (расход бывает
законным, но согласие даёт владелец) и печатает ту же карточку из шести строк, где в строке
«стоимость» стоит «неизвестна заранее» — число не выдумывается. Ловятся: путь к точке входа,
`-m модуль`, тело `python -c`, ключ провайдера в окружении команды, прямой сетевой вызов хоста;
исполнителя не разобрали при платном упоминании → fail closed в ASK. Освобождены офлайновые
тесты (`tests/`, любой `test_*.py`) — иначе прогон набора требовал бы подтверждения. Дрейф ловит
`python3 tools/paid_api_registry.py --audit` (он же тест `test_no_drift_in_repo`); на внедрении
аудит сразу нашёл 5 незамеченных платных точек входа. 16 новых тестов, набор хуков **97/97**,
e2e через реальный бинарь хука 16/16.

**Добор по B5 (25.08).** Пересобранный e2e гигиены контекста нашёл ещё один класс обходов
`guard_bulk_output.py`: построчные фильтры (`paste -s`, `rev`, `fold`, `pr`, `shuf`, `sort`,
`uniq`, `cut`, `tr`, `column`) выдают столько же строк, сколько в файле, — тот же `cat` под
другим именем; плюс `jq`/`column` числились «ограничителями» и разрешали ВЕСЬ конвейер
(`cat big.csv | jq .` проходил). Закрыто списком `LINE_DUMPERS` + проверкой тождественного
фильтра `jq`; `jq '.x' big.json`, `sort -c` и `sort -o file` намеренно остались разрешёнными.
Тестов в наборе стало 48, e2e B5 — 16/16.

Далее: склейка 3 пар из `inbox/merge_review/`;
8.6 % ссылок на несуществующие пути внутри карточек (мягкий порог ≤ 2 % не взят, чинится
редактурой тел); P1b (semantic retrieval) остаётся **заморожен** — триггеры расфриза
перечислены в финальном отчёте K4.
