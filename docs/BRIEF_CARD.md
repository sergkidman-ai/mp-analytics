# BRIEF_CARD — поток «card» (здоровье карточек товара на площадках)

> Потолок 150 строк. Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_CARD.md» — и больше
> ничего. Историю этапов выносить в `docs/handoff/archive/card_<дата>.md`.

## Где работаю
- Ветки `card/ozon-health` (мерж `2632c42`, 23.08) и `card/wb-card-presence` (мерж `6b87d64`,
  25.08) влиты в `main`, оба worktree снесены; крон смотрит в общий чекаут.
- Блок миграций потока: **6xx** (0xx=fin, 1xx=mkt, 2xx=inv, 3xx=ret, 4xx=prc, 5xx=ev).
  Домен `card` зарегистрирован в `tools/territory_guard.py`.
- Модель: **Opus** — архитектура/вердикты; **Sonnet** — прогоны и сводки.
- Отчёт заказчику 31.08 (карточки 52 ч из ≈156): `docs/reports/ozon_cards_promo_work_report.md`
  / `.docx`, артефакт `533fbfe4-6eb0-4171-8895-7d43a9debfea`; детали — `docs/BRIEF_PRC.md`.

## Зона ответственности
Контент карточек пишет **ТК** (внешняя система, ей управляет человек); после её апдейтов часть
карточек нестабильно падает в «Ошибки» и «На доработку». Поток `card` отвечает за **здоровье**:
видеть состояние, классифицировать причину и **дотолкивать до продажи те, которым нужна только
повторная отправка**. Мы не авторы контента — мы «дожиматель». Сюда же — целостность самого
списка карточек: строка в БД без карточки в кабинете такой же дефект здоровья.

## Мои файлы (правлю я)
- `collectors/ozon_card_status.py` — детектор статусов ✅ построен, в cron.
- `tools/ozon_card_push.py` — дожиматель классов A и W ✅ построен, отправка под флагом.
- `tools/card_migrate.py`, `tools/card_selfcheck.py` — применение миграций и self-check.
- Миграции `600` (`card_status` + `card_push_log`), `601` (класс W), `603` (`mark_attr`) ✅.
- `tools/ozon_card_c_report.py`, `tools/ozon_card_marking_codes.py` — разбор класса C и внешние
  коды МС (чтение) ✅.
- `tools/card_wb_presence.py` + `migrations/602_wb_card_presence.sql` — присутствие карточек WB
  в кабинете (чтение площадки, запись только в свою таблицу) ✅ построено 25.08.
- `docs/BRIEF_CARD.md`, `docs/reports/ozon_card_*`, `docs/reports/21_ozon_card_*`.

## Границы
- **Контент карточки принадлежит ТК.** Шлём ровно то, что уже в карточке; текст/атрибуты не
  генерируем и не правим — иначе первый же пуш ТК затрёт правку.
- `margin_by_sku`, витрины fin/mkt — read-only. `collectors/ozon.py` — общий, правки только
  по согласованию (беру оттуда `_headers` и URL).

## Запреты потока
1. **Изменение контента карточек запрещено.** Только повторная отправка существующего значения.
2. **Постоянное разрешение на классы A и W** (A — 22.08.2026, W — 23.08.2026 командой
   «расширяй автомат»): дожиматель ходит по торгующим карточкам сам, включая крон. Разрешение
   покрывает ТОЛЬКО повтор существующего значения. Любое другое действие — снова разовый вопрос.
4. Признак успеха — **не HTTP 200**, а `status` задачи `imported` + очистившийся `status_failed`.
   `skipped` = попытка не засчитана, HTTP 200 при этом приходит.

## Состояние на 2026-09-08

Классы: **A** — «Не обновлён» (`errors[]` пуст), лечится повтором того же значения; **W** —
«На доработку» (`PARTIAL_APPROVED`, торгует, ERROR нет), тот же повтор, но только где значение
в карточке ЕСТЬ, граница по КОДУ (`HEAL_WARN`); **C** — контентный ERROR модерации; **O** —
прочие WARNING на торгующих. Опыт по кодам W и история этапа 22–27.08 (маркировка 611,
три файла Наталье, `mark_attr`/атрибут 23536, ТН ВЭД 22232 `out_of_range`, адресат бота) —
`docs/handoff/archive/card_2026-08-23.md`, `card_2026-08-22.md`, `card_2026-08-25.md`.

**Открыто 50** (было 92 на 27.08): C 36 (acc1 3, acc2 33), W 10, O 4. Торгуют 11.
За ночь 07→08.09 закрылось 23: **вся группа хештегов acc1** (`BR_hashtag_brand` 18 +
`BR_hashtags_symbols_validation` 3 — в кабинете этих кодов больше нет) и `4748WHASUCKT`,
которая висела в `needs_human` после 3 попыток — Ozon снял отбивку сам. Класс A пуст.
Пришло 7 новых у acc2: 2 C и 5 W.

**Новый тренд — `attribute_hierarchy_fail` «Совместимые модели принтеров»:** площадка
перетряхнула справочник, значения протухают волнами (08.09 — 8 карточек acc1 + 6 acc2).
Дожиматель 08.09 не отправил ничего: 10 кандидатов, все отсечены предохранителем «свежий
апдейт ТК (<2 ч)» — ТК правила их в 06:18–06:21, крон стартовал в 08:14. Гипотеза: повтор
того же значения такое замечание НЕ лечит (устарело само значение), проверит крон 09.09.

**Остаток ТК — 36 карточек:** 30 acc2 «упоминается другой бренд» (не двигается с 22.08),
3 acc2 (2 спецсимволы в названии + 1 «получился другой товар»), 3 acc1 спецсимволы в названии.
Не чинится никем: 4 acc2 `SKU_VWC_IS_NOT_EDITABLE` — габариты закреплены площадкой,
только поддержка Ozon (габариты не придумывать, инвариант 5).

## Техника (проверено на живых вызовах)
- `POST /v1/product/attributes/update` — точечно, карточку целиком не шлёт. **`task_id`
  возвращается в КОРНЕ ответа**, не в `result` — на этом первый прогон потерял все id.
- Статус задачи: `/v1/product/import/info` → `imported` / `skipped` / `failed`. HTTP 200 успехом
  не является. Атрибуты карточки — `/v4/product/info/attributes`, ключ `id`, не `attribute_id`.
- Безопасные атрибуты для no-op: `9024` Артикул → `4389` Страна → `10400` Гарантия.
- Витрины `/v3/product/list`: рабочие — `STATE_FAILED` и `PARTIAL_APPROVED`; `BANNED` и
  `IMAGE_ABSENT` возвращают весь каталог и непригодны.

## Автомат (построен 22.08, влит в `main` 23.08; детали — `docs/handoff/archive/card_2026-08-24.md`)

**Миграция 600.** `card_status` — состояние по ключу площадка+аккаунт+`offer_id`; после лечения
строка закрывается (`is_open`+`healed_at`) — видны рецидивы. Журнал попыток `card_push_log` возобновляем.

**Детектор `collectors/ozon_card_status.py`** — только чтение: витрины `STATE_FAILED` и
`PARTIAL_APPROVED` → `/v3/product/info/list` → класс A/W/C/O → upsert; кого площадка больше не
считает проблемным — закрывает. Крон 07:41, лог `card_health.log`; ругается на A/W старше недели.

**Дожиматель `tools/ozon_card_push.py`** — классы A и W, ступень 1 (no-op по атрибуту
`9024`→`4389`→`10400`); отправка только с `--apply`. Крон 08:14, оба аккаунта подряд, партия 500.
Ограничение 50 осталось в СТРАЖЕ (`tools/hooks/guard_side_effects.py`, `is_mass`): РУЧНОЙ вызов
с `--limit >50` режется в DENY; крон через хук не идёт.

**Признак успеха у классов РАЗНЫЙ:** у A — очистившийся `status_failed`, у W — исчезнувший код
замечания; вылеченная W строку не закрывает, класс меняется на `O`. Предохранители: 3 попытки,
backoff 20 ч, пропуск карточки со свежим (<2 ч) апдейтом ТК, живая перепроверка перед записью,
ворота после первых 20 (<50 % — стоп), стоп при `declined` или уходе из продажи, `skipped` →
`needs_human`. **Ступень 2 (round-trip импорт) НЕ реализована** — шлёт карточку целиком.

## Класс C — проба повторной модерации 24.08 + задание для ТК

`tools/ozon_card_c_report.py` (чтение): имя поля лежит ТОЛЬКО в `errors[].texts.attribute_name`,
вид ошибки задаёт связка «код + поле + текст», а НЕ код. Задание — `21_ozon_card_class_c_2026-08-24.md`.
**Проба 24.08 по команде Сергея: 68 контентных карточек прогнали тем же no-op** — приняты все,
отбивка снялась у 37, РОВНО у торгующих, рецидивов детектор не нашёл. Правило: у ТОРГУЮЩЕЙ
карточки контентный ERROR = устаревшее замечание, лечится повтором; у «Не создан» — настоящий
блок, только контент в ТК. В автомат — с фильтром `is_selling` (из 64 открытых C торгуют 5).

**Задания Наталье 24.08** (хештеги 21, ТН ВЭД 58, маркировка 611) **закрыты ТК 25.08**: из 611
предупреждение снялось у всех, детектор закрыл 593; состав файлов и мост offer_id → внешний код
МС — `docs/handoff/archive/card_2026-08-25.md`. Хвост: **не найдены в МС** 0513, 0514, 1760, 1843,
3407, 2496, 5694, 5917 (плюс 85 acc1 и 40 acc2 из файлов 26.08); `price_sent` ≠ «торгует».

## Мёртвые карточки WB (ветка `card/wb-card-presence`, 25.08)

`wb_cards` только копится: удалённую в ЛК карточку коллектор перестаёт видеть, а строка живёт
вечно. `migrations/602_wb_card_presence.sql` — таблица `wb_card_presence` и вьюха **`wb_cards_live`**
для остальных потоков; чужую `wb_cards` не трогаем. `tools/card_wb_presence.py` — кабинет
(активные + корзина) против `wb_cards`, только чтение; непроверенная карточка считается живой,
пустой ответ = сбой и не пишется. **Крон: понедельник 07:11**, лог `card_health.log`. Первый
прогон 25.08: 27 мёртвых (12 acc1 + 15 acc2), `wb_cards_live` — 29 008 из 29 035.

## Акции Ozon — лестница починена и поехала (файл потока mkt, по разрешению Сергея)

`ops/ozon_stock_action.py` переписывал `last_step_on` на КАЖДОМ прогоне, включая keep, а шаг
разрешён при разнице ≥7 дней — отсчёт обнулялся ежедневно, и все 65 товаров с 22.08 по 07.09
простояли на ступени 0 по потолку (за 16 дней продано 5 шт). Коммит `2bf949b`: дата двигается
только вместе со ступенью + товар, которому площадка 3 прогона подряд отвечает `DiscountPercent
must be…`, откладывается на неделю (2297937094 бился 16 суток). Даты входа восстановлены
из `oz_action_log`, бэкап `backups/oz_action_ladder_2026-09-07.csv`.

**Проверено после крона 08.09 09:05** (`docs/reports/oz_action_ladder_2026-09-08.txt`): на
ступень 1 ушли 56 товаров — 45 acc1 (6193 → 5696 ₽) и 11 acc2 (5600 → 5232 ₽); площадка приняла
все 161 изменение, отказов ноль, 2297937094 молчит, ниже пола не ушла ни одна строка. 9 вошли
позже — их шаг в течение недели. До пола 4 ступени, шаг раз в неделю. Отчёт Наталье ушёл prc-ботом.

## Открытые вопросы
- **Сергей / ТК:** остаток класса C после пробы — задание `21_ozon_card_class_c_2026-08-24.md`.
- **Внешние коды без товара в МС** (8 штук, список выше): заводить в МС или гасить карточки?
- Природа `skipped` (гипотеза — наборы) и рецидив класса W после следующего пуша ТК.
- **Вердикт по `wb_cards_live`:** переводить ли на неё выборки mkt/fin или оставить справочной.

## Следующий шаг
1. **09.09 после крона 08:14:** лечится ли `attribute_hierarchy_fail` повтором значения — если
   нет, группа уходит в ТК как контентная. 2. Вердикт по классу C в автомат (просрочен
   с ~31.08) и по `wb_cards_live` — с mkt/fin. 3. 80 карточек в `price_sent` — остаток FBS.

**Ведение:** обновлять в конце блока; дорос до 150 строк — старое в `docs/handoff/archive/`.
