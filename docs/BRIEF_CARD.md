# BRIEF_CARD — поток «card» (здоровье карточек товара на площадках)

> Потолок 150 строк. Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_CARD.md» — и больше
> ничего. Историю этапов выносить в `docs/handoff/archive/card_<дата>.md`.

## Где работаю
- Ветка `card/ozon-health` **влита в `main` 23.08.2026** (мерж `2632c42`, 6 файлов, 568 строк).
  Worktree `.claude/worktrees/card-ozon-health` больше не нужен, крон смотрит в общий чекаут.
- Блок миграций потока: **6xx** (0xx=fin, 1xx=mkt, 2xx=inv, 3xx=ret, 4xx=prc, 5xx=ev).
  Домен `card` зарегистрирован в `tools/territory_guard.py`.
- Модель: **Opus** — архитектура/вердикты; **Sonnet** — прогоны и сводки.

## Зона ответственности
Контент карточек пишет **ТК** (внешняя система, ей управляет человек: каждый перепрогон —
постановка задачи, исполнение, контроль). После апдейтов ТК часть карточек падает в «Ошибки»
и «На доработку», причём нестабильно: один и тот же товар то проходит, то нет. Поток `card`
отвечает за **здоровье**: видеть состояние, классифицировать причину и **дотолкивать до
продажи те, которым нужна только повторная отправка**. Мы не авторы контента — мы «дожиматель».

## Мои файлы (правлю я)
- `collectors/ozon_card_status.py` — детектор статусов ✅ построен, в cron.
- `tools/ozon_card_push.py` — дожиматель классов A и W ✅ построен, отправка под флагом.
- `tools/card_migrate.py`, `tools/card_selfcheck.py` — применение миграций и self-check.
- `migrations/600_card_health.sql` — `card_status` + `card_push_log` ✅ применена.
- `migrations/601_card_class_w.sql` — фиксация класса W ✅ применена.
- `docs/BRIEF_CARD.md`, `docs/reports/ozon_card_*`.

## Границы
- **Контент карточки принадлежит ТК.** Мы отправляем ровно то, что уже лежит в карточке, и
  никогда не генерируем и не правим текст/атрибуты. Иначе первый же пуш ТК затрёт правку.
- `margin_by_sku`, витрины fin/mkt — read-only.
- `collectors/ozon.py` — общий, правки только по согласованию (беру оттуда `_headers` и URL).

## Запреты потока
1. **Изменение контента карточек запрещено.** Только повторная отправка существующего значения.
2. **Постоянное разрешение на классы A и W** (A — 22.08.2026, W — 23.08.2026 командой
   «расширяй автомат»): дожиматель ходит по торгующим карточкам сам, включая крон. Разрешение
   покрывает ТОЛЬКО повтор существующего значения. Любое другое действие — снова разовый вопрос.
4. Признак успеха — **не HTTP 200**, а `status` задачи `imported` + очистившийся `status_failed`.
   `skipped` = попытка не засчитана, HTTP 200 при этом приходит.

## Состояние на 2026-08-23

Полный срез обоих аккаунтов (35 900 карточек) разложил проблему на классы. **A** — «Не обновлён»
(`status_failed=imported`, `errors[]` пуст): причины в API нет, лечится повторной отправкой того
же значения. **C** — контентный ERROR модерации: повтор бесполезен, правится только в ТК.

| аккаунт | карточек | класс A (торгуют) | класс W | класс C | класс O |
|---|---|---|---|---|---|
| acc1 «Цифровой квадрат» | 22 435 | было 150 → **0** | **279** | 30 | 320 |
| acc2 «Дисквэр» | 13 465 | было 258 → **0** | **25** | 160 | 328 |

23.08 добавился третий класс — **W**, «На доработку» (`PARTIAL_APPROVED`): карточка торгует,
ERROR-ошибок нет, висит замечание. Раньше я записал класс в безнадёжные — **и ошибся, Сергей был
прав**. Граница проходит по КОДУ замечания, а не по группе: где значение в карточке ЕСТЬ, повтор
снимает протухший вердикт площадки; где значения НЕТ — повтор его не создаст.

**Класс A вычищен на ОБОИХ аккаунтах: 408 карточек, не вылечилась одна** (`3463`, набор CMYK:
дважды `skipped`, причины в природе не существует). Путь `Не обновлён → Готов к продаже` за ~2
минуты, повторной модерации нет, `declined` ноль, из продажи не вышла ни одна. Сам класс A не
рассасывался: июльские висели больше месяца.

**Опыт по классу W (23.08, 25 карточек acc1, два прогона; замер и перечиткой карточки, и
счётчиком площадки).** Лечатся повтором: `double_without_merger_offer` 5/5,
`erased_attribute_value` 5/5, `pics_http_error`+`some_image_failed` 5/5, `attribute_hierarchy_fail`
3/3. НЕ лечатся (значения нет вовсе, работа ТК): `warning_attribute_values_empty` 0/5,
`…_out_of_range` 0/2. Все 18 вылеченных ушли из «На доработку», продажу не потеряла ни одна.
Белый список — `HEAL_WARN` в детекторе; пополнять **только по факту такого же опыта**, а не по
семейству кода (`cover_unprocessed`, 3 карточки, в опыт не попал и в список не включён).

Подробности — `docs/handoff/archive/card_2026-08-22.md`,
`docs/reports/ozon_card_partial_approved_2026-08-23.md`, `docs/reports/ozon_partial_test*.csv`.

## Техника (проверено на живых вызовах)
- `POST /v1/product/attributes/update` — точечно, карточку целиком не шлёт. **`task_id`
  возвращается в КОРНЕ ответа**, не в `result` — на этом первый прогон потерял все id.
- Статус задачи: `/v1/product/import/info` → `imported` / `skipped` / `failed`. HTTP 200 успехом
  не является. Атрибуты карточки — `/v4/product/info/attributes`, ключ `id`, не `attribute_id`.
- Безопасные атрибуты для no-op: `9024` Артикул → `4389` Страна → `10400` Гарантия.
- Витрины `/v3/product/list`: рабочие — `STATE_FAILED` и `PARTIAL_APPROVED`; `BANNED` и
  `IMAGE_ABSENT` возвращают весь каталог и непригодны.

## Автомат (построен 22.08.2026, ветка `card/ozon-health`, коммиты `fb037c2`, `52b50e8`)

**Миграция 600.** `card_status` — состояние проблемной карточки по ключу
площадка+аккаунт+`offer_id`. Строка после лечения **не удаляется**, а закрывается
(`is_open`+`healed_at`): так видны рецидивы и «сколько висит» — цифра, которой у площадки нет.
Журнал попыток — отдельная таблица, строка на отправку, прогон возобновляем.

**Детектор `collectors/ozon_card_status.py`** — только чтение. `STATE_FAILED` (A/C) и
`PARTIAL_APPROVED` (W) → `/v3/product/info/list` → класс A / W / C / O → upsert; кого площадка
больше не считает проблемным — закрывает. Ругается, если карточки класса A или W висят дольше
недели (значит, дожим не работает). **В cron: ежедневно 07:41**, лог `card_health.log`.

**Дожиматель `tools/ozon_card_push.py`** — классы A и W (`--classes`, по умолчанию оба),
ступень 1 (no-op по атрибуту `9024`→`4389`→`10400`). `--tk-report` выгружает класс C для
программиста ТК. **Отправка по умолчанию выключена**, нужен `--apply`. **В cron: ежедневно
08:14**, партия 50 на аккаунт (потолок стража побочных эффектов), оба аккаунта подряд.
Порядок в пуле: сначала A (карточка сломана и часто не торгует), потом W (торгует, страдает
видимость); внутри класса — сначала неторгующие и давние.

**Признак успеха у классов РАЗНЫЙ, и это принципиально:** у A — очистившийся `status_failed`,
у W — исчезнувший код замечания (у W `status_failed` пуст изначально, общая проверка засчитала
бы вылеченными все подряд). Вылеченная W строку не закрывает — класс меняется на `O`.

Предохранители: потолок 3 попытки на карточку и backoff 20 ч; пропуск карточки со свежим
(<2 ч) апдейтом ТК — иначе гонка; живая перепроверка статуса перед записью; ворота после первых
20 карточек (вылечилось <50 % — стоп); стоп при `declined` или уходе из продажи; `skipped` →
`needs_human`.

**Ступень 2 лестницы (полный round-trip импорт) сознательно НЕ реализована.** Она шлёт
карточку целиком, то есть может утащить её на повторную модерацию — ровно тот риск, от
которого мы уворачивались, а проверить его не на чем: кандидат ровно один (`3463`).

**Проверено боем.** 23.08 в 08:14 крон поднял дожиматель САМ и вылечил 50 карточек acc2 без
участия человека. Ветка `skipped → человеку` проверена на `3463`. SQL-пути (пул A+W, журнал,
попытка, оба вида лечения, выход из пула) прогнаны self-check'ом; прогон возобновляем.

## Открытые вопросы
- **Сергей / ТК:** отчёт по классу C. Выгрузка готова —
  `docs/reports/ozon_card_content_errors.csv`, 190 карточек: 88 `DESCRIPTION_DECLINE`,
  80 `error_attribute_values_empty` (acc2, «Нужен код маркировки»), 16 `BR_hashtag_brand`,
  3 спецсимвола, 1 объём/вес. **Из них 25 на acc1 «Не создан» = прямые потерянные продажи.**
- Природа `skipped`: почему часть карточек не принимает no-op. Гипотеза — вариантные связки
  и наборы.
- Рецидив класса W: снятое замечание может вернуться со следующим пушем ТК (в отличие от A,
  где вылечено — значит вылечено). Первый разбор рецидивов — по журналу через неделю.

## Следующий шаг
**Класс A вычищен на обоих аккаунтах; автомат влит в `main`, работает по крону сам и с 23.08
расширен на класс W.** Очередь W: 279 карточек acc1 и 25 acc2, по 50 на аккаунт в сутки — acc1
разгребётся за ~6 ночей. Вручную партий W не гонял: опыт (25 карточек) — по прямой команде.

Что осталось и чего я НЕ делал:
1. **Класс C — в ТК.** Отдать программисту `docs/reports/ozon_card_content_errors.csv`.
   Наш дожим тут бессилен по устройству: это контентные отбивки модерации. Туда же уходит
   нелечимый остаток «На доработку» (302 `…_empty` + 106 `…_out_of_range` на acc1).
2. **Наблюдение за первой неделей крона.** Детектор ругается, если класс A или W висит дольше
   недели — если ругани нет, автомат держит. Заглянуть в `card_health.log` ~30.08.
   Смотреть отдельно: не растёт ли доля невылеченных W (значит, в белый список попал код,
   который на самом деле не лечится).
3. **`main` опережает `origin/main` на 102 коммита** (накопилось по всем потокам, не мной).
   Пуш на GitHub — не моя территория, но сказать стоит.

---

### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/card_<дата>.md`.
- Закончил кусок — обновил бриф и закрыл сессию; `--resume` вчерашней сессии запрещён.
