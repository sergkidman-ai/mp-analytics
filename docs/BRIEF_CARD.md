# BRIEF_CARD — поток «card» (здоровье карточек товара на площадках)

> Потолок 150 строк. Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_CARD.md» — и больше
> ничего. Историю этапов выносить в `docs/handoff/archive/card_<дата>.md`.

## Где работаю
- Ветка `card/ozon-health` **влита в `main` 23.08.2026** (мерж `2632c42`, 6 файлов, 568 строк).
  Worktree `.claude/worktrees/card-ozon-health` больше не нужен, крон смотрит в общий чекаут.
- Блок миграций потока: **6xx** (0xx=fin, 1xx=mkt, 2xx=inv, 3xx=ret, 4xx=prc, 5xx=ev).
  Домен `card` зарегистрирован в `tools/territory_guard.py`.
- Модель: **Opus** — архитектура/вердикты; **Sonnet** — прогоны и сводки.

## Зона ответственности
Контент карточек пишет **ТК** (внешняя система, ей управляет человек). После её апдейтов часть
карточек падает в «Ошибки» и «На доработку», причём нестабильно: один и тот же товар то
проходит, то нет. Поток `card` отвечает за **здоровье**: видеть состояние, классифицировать
причину и **дотолкивать до продажи те, которым нужна только повторная отправка**.
Мы не авторы контента — мы «дожиматель».

## Мои файлы (правлю я)
- `collectors/ozon_card_status.py` — детектор статусов ✅ построен, в cron.
- `tools/ozon_card_push.py` — дожиматель классов A и W ✅ построен, отправка под флагом.
- `tools/card_migrate.py`, `tools/card_selfcheck.py` — применение миграций и self-check.
- `migrations/600_card_health.sql` — `card_status` + `card_push_log` ✅ применена.
- `migrations/601_card_class_w.sql` — фиксация класса W ✅ применена.
- `tools/ozon_card_c_report.py`, `tools/ozon_card_marking_codes.py` — разбор класса C и
  выгрузка внешних кодов МС по коду маркировки (только чтение) ✅ построены.
- `docs/BRIEF_CARD.md`, `docs/reports/ozon_card_*`, `docs/reports/21_ozon_card_*`.

## Границы
- **Контент карточки принадлежит ТК.** Отправляем ровно то, что уже лежит в карточке, никогда
  не генерируем и не правим текст/атрибуты — иначе первый же пуш ТК затрёт правку.
- `margin_by_sku`, витрины fin/mkt — read-only. `collectors/ozon.py` — общий, правки только
  по согласованию (беру оттуда `_headers` и URL).

## Запреты потока
1. **Изменение контента карточек запрещено.** Только повторная отправка существующего значения.
2. **Постоянное разрешение на классы A и W** (A — 22.08.2026, W — 23.08.2026 командой
   «расширяй автомат»): дожиматель ходит по торгующим карточкам сам, включая крон. Разрешение
   покрывает ТОЛЬКО повтор существующего значения. Любое другое действие — снова разовый вопрос.
4. Признак успеха — **не HTTP 200**, а `status` задачи `imported` + очистившийся `status_failed`.
   `skipped` = попытка не засчитана, HTTP 200 при этом приходит.

## Состояние на 2026-08-24

Срез обоих аккаунтов (35 900 карточек) разложил проблему на классы. **A** — «Не обновлён»
(`status_failed=imported`, `errors[]` пуст): причины в API нет, лечится повторной отправкой того
же значения. **C** — контентный ERROR модерации: повтор бесполезен, правится только в ТК.
**W** — «На доработку» (`PARTIAL_APPROVED`): торгует, ERROR нет, висит замечание; лечится тем же
повтором, но **только где значение в карточке ЕСТЬ** — граница по КОДУ замечания, а не по группе
(белый список `HEAL_WARN`, пополнять только по факту опыта).

**Открыто после крон-прогона и ручной перечитки 24.08** (детектор 09:41, оба аккаунта):
**A — 0 и 0**, **W — 2 acc1 и 0 acc2**, **C — 30 и 71**, **O — 327 и 329**.
Очередь W (178 acc1 + 25 acc2) крон взял за один проход: вылечено 178/178 и 25/25, ворота
открылись 20 из 20 на обоих. Не поддалась одна карточка: `3463`, набор CMYK, дважды `skipped`.
C на acc2 упал 160 → 71 — ТК закрыла группу «Нужен код маркировки» (см. ниже).

История этапа, цифры прогонов и опыт по кодам W — `docs/handoff/archive/card_2026-08-23.md`
и `card_2026-08-22.md`.

## Техника (проверено на живых вызовах)
- `POST /v1/product/attributes/update` — точечно, карточку целиком не шлёт. **`task_id`
  возвращается в КОРНЕ ответа**, не в `result` — на этом первый прогон потерял все id.
- Статус задачи: `/v1/product/import/info` → `imported` / `skipped` / `failed`. HTTP 200 успехом
  не является. Атрибуты карточки — `/v4/product/info/attributes`, ключ `id`, не `attribute_id`.
- Безопасные атрибуты для no-op: `9024` Артикул → `4389` Страна → `10400` Гарантия.
- Витрины `/v3/product/list`: рабочие — `STATE_FAILED` и `PARTIAL_APPROVED`; `BANNED` и
  `IMAGE_ABSENT` возвращают весь каталог и непригодны.

## Автомат (построен 22.08, влит в `main` 23.08; детали — `docs/handoff/archive/card_2026-08-24.md`)

**Миграция 600.** `card_status` — состояние проблемной карточки по ключу площадка+аккаунт+`offer_id`;
после лечения строка не удаляется, а закрывается (`is_open`+`healed_at`): так видны рецидивы и
«сколько висит». Журнал попыток `card_push_log` — строка на отправку, прогон возобновляем.

**Детектор `collectors/ozon_card_status.py`** — только чтение: витрины `STATE_FAILED` и
`PARTIAL_APPROVED` → `/v3/product/info/list` → класс A / W / C / O → upsert; кого площадка больше
не считает проблемным — закрывает. Крон 07:41, лог `card_health.log`. Ругается, если A или W
висят дольше недели.

**Дожиматель `tools/ozon_card_push.py`** — классы A и W, ступень 1 (no-op по атрибуту
`9024`→`4389`→`10400`); отправка только с `--apply`. Крон 08:14, оба аккаунта подряд, партия 500.
Ограничение 50 осталось в СТРАЖЕ (`tools/hooks/guard_side_effects.py`, `is_mass`): РУЧНОЙ вызов
с `--limit >50` режется в DENY; крон через хук не идёт.

**Признак успеха у классов РАЗНЫЙ, и это принципиально:** у A — очистившийся `status_failed`,
у W — исчезнувший код замечания (у W `status_failed` пуст изначально). Вылеченная W строку не
закрывает — класс меняется на `O`. Предохранители: 3 попытки на карточку, backoff 20 ч, пропуск
карточки со свежим (<2 ч) апдейтом ТК, живая перепроверка статуса перед записью, ворота после
первых 20 (вылечилось <50 % — стоп), стоп при `declined` или уходе из продажи, `skipped` →
`needs_human`.

**Ступень 2 (полный round-trip импорт) сознательно НЕ реализована:** она шлёт карточку целиком и
может утащить её на повторную модерацию — ровно тот риск, от которого уворачивались; кандидат
ровно один, `3463`.

## Класс C — задание для ТК (живой остаток 30 acc1 + 71 acc2)

`tools/ozon_card_c_report.py` (только чтение) перечитывает класс C с площадки: имя поля лежит
ТОЛЬКО в `errors[].texts.attribute_name`, в БД его нет. Итог — задание
`docs/reports/21_ozon_card_class_c_2026-08-23.md` и `docs/reports/ozon_card_content_errors.csv`.
Было 190 карточек, 198 ошибок, 10 видов работ; вид задаёт связка «код + поле + текст», а НЕ код
(под одним `DESCRIPTION_DECLINE` — чужой бренд, спецсимволы, реклама: разные правки в разных полях).

**Группа «Нужен код маркировки» (80 карточек acc2) закрыта ТК 24.08** — обновлены 11:50–11:53 МСК,
ошибка снята у всех, ERROR-ошибок нет, `moderate=approved`, `is_created=True`, статус `price_sent`.
Наших отправок по ним не было ни одной (класс C дожиматель не трогает). Детектор закрыл все 80.
`price_sent` — промежуточный статус: «торгует» подтверждается остатком FBS, отдельной проверкой.

**Мост Ozon acc2 → МойСклад:** `offer_id` Дисквэра = 4-значный внешний код МС + 8 случайных
символов (12 знаков у 13 558 из 13 616 карточек; штрихкодов нет ни в v3, ни в v4). По группе
маркировки отдан `docs/reports/ozon_marking_codes_2026-08-23.xlsx`
(`tools/ozon_card_marking_codes.py`, чтение): 80 карточек = 26 внешних кодов, 21 сшит с МС,
**5 кодов (0513, 0514, 1760, 1843, 3407 — 13 карточек) в МойСкладе не найдены вовсе**.

## Открытые вопросы
- **Сергей / ТК:** остаток класса C — 30 acc1 + 71 acc2 по заданию
  `docs/reports/21_ozon_card_class_c_2026-08-23.md` (группа маркировки из него уже закрыта).
- **5 внешних кодов без товара в МС** (0513, 0514, 1760, 1843, 3407 — 13 карточек): на Ozon они
  вылечились, но товара в МойСкладе нет → остатки и себест по ним не поедут. Заводить в МС
  или гасить карточки?
- Природа `skipped`: почему часть карточек не принимает no-op. Гипотеза — вариантные связки/наборы.
- Рецидив класса W: снятое замечание может вернуться со следующим пушем ТК (в отличие от A,
  где вылечено — значит вылечено). Первый разбор рецидивов — по журналу через неделю.

## Следующий шаг
**Классы A и W вычищены на обоих аккаунтах** (24.08: A — 0 и 0, W — 2 и 0; остаток W acc1 — это
две карточки под backoff 20 ч, их возьмёт крон 25.08). Автомат в `main`, крон отработал сам:
детектор 07:41, дожим 08:14–09:09, партия 500 хватило на всю очередь за один проход.

Что осталось и чего я НЕ делал:
1. **Класс C — в ТК.** Наш дожим тут бессилен по устройству: это контентные отбивки модерации.
   Туда же уходит нелечимый остаток «На доработку» (302 `…_empty` + 106 `…_out_of_range` на acc1).
2. **Наблюдение за первой неделей крона.** Детектор ругается, если класс A или W висит дольше
   недели. Заглянуть в `card_health.log` ~30.08; отдельно — не растёт ли доля невылеченных W
   (значит, в белый список попал код, который на самом деле не лечится).
3. **Довести группу маркировки до продажи:** 80 карточек в статусе `price_sent` — проверить
   остаток FBS и что они реально торгуют, а не висят созданными. Проверка чтением, отдельным
   заходом.

---

**Ведение:** обновлять в конце значимого блока (CLAUDE.md, правило 10); дорос до 150 строк —
старое в `docs/handoff/archive/card_<дата>.md`; закончил кусок — закрыл сессию, `--resume` вчерашней запрещён.
