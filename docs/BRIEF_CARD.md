# BRIEF_CARD — поток «card» (здоровье карточек товара на площадках)

> Потолок 150 строк. Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_CARD.md» — и больше
> ничего. Историю этапов выносить в `docs/handoff/archive/card_<дата>.md`.

## Где работаю
- Ветка `card/ozon-health` **влита в `main` 23.08.2026** (мерж `2632c42`, 6 файлов, 568 строк).
  Worktree `.claude/worktrees/card-ozon-health` больше не нужен, крон смотрит в общий чекаут.
- Блок миграций потока: **6xx** (0xx=fin, 1xx=mkt, 2xx=inv, 3xx=ret, 4xx=prc, 5xx=ev).
  Домен `card` зарегистрирован в `tools/territory_guard.py`.
- Модель: **Opus** — архитектура/вердикты; **Sonnet** — прогоны и сводки.

## Зона ответственности
Контент карточек пишет **ТК** (внешняя система, ей управляет человек); после её апдейтов часть
карточек нестабильно падает в «Ошибки» и «На доработку». Поток `card` отвечает за **здоровье**:
видеть состояние, классифицировать причину и **дотолкивать до продажи те, которым нужна только
повторная отправка**. Мы не авторы контента — мы «дожиматель».

## Мои файлы (правлю я)
- `collectors/ozon_card_status.py` — детектор статусов ✅ построен, в cron.
- `tools/ozon_card_push.py` — дожиматель классов A и W ✅ построен, отправка под флагом.
- `tools/card_migrate.py`, `tools/card_selfcheck.py` — применение миграций и self-check.
- `migrations/600_card_health.sql` — `card_status` + `card_push_log` ✅ применена.
- `migrations/601_card_class_w.sql` — фиксация класса W ✅ применена.
- `tools/ozon_card_c_report.py`, `tools/ozon_card_marking_codes.py` — разбор класса C и внешние
  коды МС (чтение) ✅.
- `docs/BRIEF_CARD.md`, `docs/reports/ozon_card_*`, `docs/reports/21_ozon_card_*`.

## Границы
- **Контент карточки принадлежит ТК.** Шлём ровно то, что уже в карточке; текст/атрибуты не
  генерируем и не правим — иначе первый же пуш ТК затрёт правку.
- `margin_by_sku`, витрины fin/mkt — read-only. `collectors/ozon.py` — общий, правки только
  по согласованию (беру оттуда `_headers` и URL).

## Запреты потока
1. **Изменение контента карточек запрещено.** Только повторная отправка существующего значения.
2. **Постоянное разрешение на классы A и W** (A — 22.08.2026, W — 23.08.2026 командой
   «расширяй автомат»): дожиматель ходит по торгующим карточкам сам, включая крон. Разрешение
   покрывает ТОЛЬКО повтор существующего значения. Любое другое действие — снова разовый вопрос.
4. Признак успеха — **не HTTP 200**, а `status` задачи `imported` + очистившийся `status_failed`.
   `skipped` = попытка не засчитана, HTTP 200 при этом приходит.

## Состояние на 2026-08-24

Срез обоих аккаунтов (35 900 карточек) разложен на классы. **A** — «Не обновлён»
(`status_failed=imported`, `errors[]` пуст): причины в API нет, лечится повтором того же значения.
**W** — «На доработку» (`PARTIAL_APPROVED`): торгует, ERROR нет, висит замечание; лечится тем же
повтором, но **только где значение в карточке ЕСТЬ** — граница по КОДУ (белый список `HEAL_WARN`).
**C** — контентный ERROR модерации; считался нелечимым, проба 24.08 это поколебала (ниже).
**O** — прочие WARNING на торгующих карточках.

**Открыто после крон-прогона и перечитки 24.08** (детектор 09:41): **A — 0 и 0**, **W — 2 acc1
и 0 acc2**, **C — 30 и 71**, **O — 327 и 329**. Очередь W (178 acc1 + 25 acc2) крон взял за один
проход: 178/178 и 25/25, ворота 20 из 20 на обоих. Не поддалась одна: `3463`, набор CMYK, дважды
`skipped`. C на acc2 упал 160 → 71: ТК закрыла группу «Нужен код маркировки».
История этапа и опыт по кодам W — `docs/handoff/archive/card_2026-08-23.md`, `card_2026-08-22.md`.

## Техника (проверено на живых вызовах)
- `POST /v1/product/attributes/update` — точечно, карточку целиком не шлёт. **`task_id`
  возвращается в КОРНЕ ответа**, не в `result` — на этом первый прогон потерял все id.
- Статус задачи: `/v1/product/import/info` → `imported` / `skipped` / `failed`. HTTP 200 успехом
  не является. Атрибуты карточки — `/v4/product/info/attributes`, ключ `id`, не `attribute_id`.
- Безопасные атрибуты для no-op: `9024` Артикул → `4389` Страна → `10400` Гарантия.
- Витрины `/v3/product/list`: рабочие — `STATE_FAILED` и `PARTIAL_APPROVED`; `BANNED` и
  `IMAGE_ABSENT` возвращают весь каталог и непригодны.

## Автомат (построен 22.08, влит в `main` 23.08; детали — `docs/handoff/archive/card_2026-08-24.md`)

**Миграция 600.** `card_status` — состояние проблемной карточки по ключу площадка+аккаунт+`offer_id`;
после лечения строка не удаляется, а закрывается (`is_open`+`healed_at`): так видны рецидивы и
«сколько висит». Журнал попыток `card_push_log` — строка на отправку, прогон возобновляем.

**Детектор `collectors/ozon_card_status.py`** — только чтение: витрины `STATE_FAILED` и
`PARTIAL_APPROVED` → `/v3/product/info/list` → класс A / W / C / O → upsert; кого площадка больше
не считает проблемным — закрывает. Крон 07:41, лог `card_health.log`. Ругается, если A или W
висят дольше недели.

**Дожиматель `tools/ozon_card_push.py`** — классы A и W, ступень 1 (no-op по атрибуту
`9024`→`4389`→`10400`); отправка только с `--apply`. Крон 08:14, оба аккаунта подряд, партия 500.
Ограничение 50 осталось в СТРАЖЕ (`tools/hooks/guard_side_effects.py`, `is_mass`): РУЧНОЙ вызов
с `--limit >50` режется в DENY; крон через хук не идёт.

**Признак успеха у классов РАЗНЫЙ:** у A — очистившийся `status_failed`, у W — исчезнувший код
замечания (`status_failed` у неё пуст изначально); вылеченная W строку не закрывает, класс
меняется на `O`. Предохранители: 3 попытки, backoff 20 ч, пропуск карточки со свежим (<2 ч)
апдейтом ТК, живая перепроверка перед записью, ворота после первых 20 (<50 % — стоп), стоп при
`declined` или уходе из продажи, `skipped` → `needs_human`. **Ступень 2 (round-trip импорт)
сознательно НЕ реализована** — шлёт карточку целиком и тащит её на повторную модерацию;
кандидат ровно один, `3463`.

## Класс C — проба повторной модерации 24.08 + задание для ТК

`tools/ozon_card_c_report.py` (чтение) перечитывает класс C с площадки: имя поля лежит ТОЛЬКО
в `errors[].texts.attribute_name`, в БД его нет. Задание — `docs/reports/21_ozon_card_class_c_2026-08-24.md`
+ `ozon_card_content_errors.csv` (срез: 101 карточка, 109 ошибок, 9 видов). Вид задаёт связка
«код + поле + текст», а НЕ код: под `DESCRIPTION_DECLINE` и чужой бренд, и спецсимволы, и реклама.
Сводная картина ошибок обоих аккаунтов — `docs/reports/22_ozon_card_errors_2026-08-24.md`.

**Проба 24.08 по команде Сергея: 68 контентных карточек прогнали тем же no-op (66 «чужой бренд»
acc2 + 2 «получился другой товар»).** Все 68 приняты (`imported`). Через полчаса перечитка:
**отбивка снялась у 37 — и это РОВНО все, кто торговал** (36 acc2 + 1 acc1); у 30 карточек
«Не создан» вердикт держится, плюс одна торгующая. Вывод, который стоит проверить детектором:
у ТОРГУЮЩЕЙ карточки контентный ERROR ведёт себя как устаревшее замечание (лечится повтором),
у «Не создан» — как настоящий блок создания (только правка контента в ТК). Строки `card_status`
проба не закрывает: вердикт модерации асинхронный, итог покажет детектор 25.08 в 07:41.

Режим пробы в дожимателе: `--classes C` разрешён ТОЛЬКО вместе с `--offers <файл со списком>`
(массово класс C гонять нельзя), внутри отключены пропуск «стала контентной» и ворота, `_verify`
не вызывается. Ручной прогон режется хуком `is_mass` при `--limit >50` — 67 карточек шли двумя
пачками.

**Хештеги — задание Наталье отдано 24.08:** `docs/reports/ozon_card_hashtags_2026-08-24.xlsx`
(21 карточка: 18 «чужие бренды в хештегах» + 3 «недопустимые символы»), колонки — внешний код МС,
товар в МС, offer_id, аккаунт, текст отбивки, список хештегов из карточки. Ушло в дропбокс-бот
на её chat_id `1231747786` (@Pro_natalya, подтверждён `getChat`, а не взят из списка допуска).

**Группа «Нужен код маркировки» (80 карточек acc2) закрыта ТК 24.08** (11:50–11:53 МСК, наших
отправок по ним не было ни одной); мост `offer_id` Дисквэра → внешний код МС и разбор группы —
в архиве `docs/handoff/archive/card_2026-08-24.md`. Оттуда живой хвост: **5 внешних кодов
(0513, 0514, 1760, 1843, 3407 — 13 карточек) в МойСкладе не найдены**, а `price_sent` —
промежуточный статус, «торгует» подтверждается остатком FBS.

## Открытые вопросы
- **Сергей / ТК:** остаток класса C после пробы — ориентировочно 29 acc1 + 35 acc2 (из них 30
  «Не создан»), подтвердит детектор 25.08; задание — `21_ozon_card_class_c_2026-08-24.md`.
- **5 внешних кодов без товара в МС** (13 карточек): на Ozon вылечились, но в МойСкладе товара
  нет → остатки и себест по ним не поедут. Заводить в МС или гасить карточки?
- Природа `skipped`: почему часть карточек не принимает no-op. Гипотеза — вариантные связки/наборы.
- Рецидив класса W: снятое замечание может вернуться со следующим пушем ТК; разбор по журналу
  через неделю.

## Следующий шаг
**Классы A и W вычищены на обоих аккаунтах** (24.08: A — 0 и 0, W — 2 и 0; остаток W acc1 — под
backoff 20 ч, возьмёт крон 25.08). Автомат в `main`, крон отработал сам за один проход.

Что осталось:
1. **Сверить пробу класса C по детектору 25.08.** Если 37 снятых отбивок держатся — правило
   «торгующая карточка с ERROR лечится повтором» можно вносить в автомат отдельным классом;
   если вернулись — проба закрыта, класс C целиком уходит в ТК. Остальное в ТК в любом случае:
   контент + 611 пустых «Нужен код маркировки» и 55 ТН ВЭД на торгующих карточках (класс O).
2. **Наблюдение за первой неделей крона.** Детектор ругается, если класс A или W висит дольше
   недели. Заглянуть в `card_health.log` ~30.08; отдельно — не растёт ли доля невылеченных W
   (значит, в белый список попал код, который на самом деле не лечится).
3. **Довести группу маркировки до продажи:** 80 карточек в `price_sent` — проверить остаток FBS
   и что они реально торгуют, а не висят созданными (чтение, отдельным заходом).

---

**Ведение:** обновлять в конце значимого блока (CLAUDE.md, правило 10); дорос до 150 строк — старое
в `docs/handoff/archive/card_<дата>.md`; закончил кусок — закрыл сессию, `--resume` запрещён.
