# BRIEF_CARD — поток «card» (здоровье карточек товара на площадках)

> Потолок 150 строк. Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_CARD.md» — и больше
> ничего. Историю этапов выносить в `docs/handoff/archive/card_<дата>.md`.

## Где работаю
- Ветки `card/ozon-health` (мерж `2632c42`, 23.08) и `card/wb-card-presence` (мерж `6b87d64`,
  25.08) влиты в `main`, оба worktree снесены; крон смотрит в общий чекаут.
- Блок миграций потока: **6xx** (0xx=fin, 1xx=mkt, 2xx=inv, 3xx=ret, 4xx=prc, 5xx=ev).
  Домен `card` зарегистрирован в `tools/territory_guard.py`.
- Модель: **Opus** — архитектура/вердикты; **Sonnet** — прогоны и сводки.

## Зона ответственности
Контент карточек пишет **ТК** (внешняя система, ей управляет человек); после её апдейтов часть
карточек нестабильно падает в «Ошибки» и «На доработку». Поток `card` отвечает за **здоровье**:
видеть состояние, классифицировать причину и **дотолкивать до продажи те, которым нужна только
повторная отправка**. Мы не авторы контента — мы «дожиматель». Сюда же — целостность самого
списка карточек: строка в БД без карточки в кабинете такой же дефект здоровья.

## Мои файлы (правлю я)
- `collectors/ozon_card_status.py` — детектор статусов ✅ построен, в cron.
- `tools/ozon_card_push.py` — дожиматель классов A и W ✅ построен, отправка под флагом.
- `tools/card_migrate.py`, `tools/card_selfcheck.py` — применение миграций и self-check.
- `migrations/600_card_health.sql` — `card_status` + `card_push_log` ✅ применена.
- `migrations/601_card_class_w.sql` — фиксация класса W ✅ применена.
- `tools/ozon_card_c_report.py`, `tools/ozon_card_marking_codes.py` — разбор класса C и внешние
  коды МС (чтение) ✅.
- `tools/card_wb_presence.py` + `migrations/602_wb_card_presence.sql` — присутствие карточек WB
  в кабинете (чтение площадки, запись только в свою таблицу) ✅ построено 25.08.
- `docs/BRIEF_CARD.md`, `docs/reports/ozon_card_*`, `docs/reports/21_ozon_card_*`.

## Границы
- **Контент карточки принадлежит ТК.** Шлём ровно то, что уже в карточке; текст/атрибуты не
  генерируем и не правим — иначе первый же пуш ТК затрёт правку.
- `margin_by_sku`, витрины fin/mkt — read-only. `collectors/ozon.py` — общий, правки только
  по согласованию (беру оттуда `_headers` и URL).

## Запреты потока
1. **Изменение контента карточек запрещено.** Только повторная отправка существующего значения.
2. **Постоянное разрешение на классы A и W** (A — 22.08.2026, W — 23.08.2026 командой
   «расширяй автомат»): дожиматель ходит по торгующим карточкам сам, включая крон. Разрешение
   покрывает ТОЛЬКО повтор существующего значения. Любое другое действие — снова разовый вопрос.
4. Признак успеха — **не HTTP 200**, а `status` задачи `imported` + очистившийся `status_failed`.
   `skipped` = попытка не засчитана, HTTP 200 при этом приходит.

## Состояние на 2026-08-24

Срез обоих аккаунтов (35 900 карточек) разложен на классы. **A** — «Не обновлён» (`errors[]` пуст,
причины в API нет): лечится повтором того же значения. **W** — «На доработку» (`PARTIAL_APPROVED`,
торгует, ERROR нет): тот же повтор, но **только где значение в карточке ЕСТЬ** — граница по КОДУ
(`HEAL_WARN`). **C** — контентный ERROR модерации (ниже). **O** — прочие WARNING на торгующих.

**Детектор 25.08 закрыл 596 карточек** (300 acc1 + 296 acc2) — ТК в этот день пушнула всю группу
маркировки: из 611 закрылось 593, осталось 16 в O (ровно те 8+8 ТН ВЭД) и 2 в A. Свежий осыпь
после пуша ТК: **A 14/14, W 24/23** — это постоянное разрешение, крон возьмёт сам 08:14.
Крон 08:14 в этот день дожал 3 W acc1 (3/3), группы маркировки он не касался.
**26.08:** `cover_unprocessed` (видеообложка) добавлен в `HEAL_WARN` — карточка 0104 acc1 вылечилась
повтором (задача imported, замечание ушло, продажа цела), Сергей: падает регулярно. Коммит 5c87899.
Открытых O 387, из них **333 — «Нужен код маркировки»** на карточках, которые ТК правила 25–26.08
(рецидива нет: из закрытых 25.08 не открылась ни одна). Файлы для ТК Наталье ушли ботом:
`docs/reports/ozon_marking_codes_2026-08-26_{cifrovoy,diskver}.xlsx` (184 + 149, пересечение 0).
История этапа и опыт по кодам W — `docs/handoff/archive/card_2026-08-23.md`, `card_2026-08-22.md`.

## Техника (проверено на живых вызовах)
- `POST /v1/product/attributes/update` — точечно, карточку целиком не шлёт. **`task_id`
  возвращается в КОРНЕ ответа**, не в `result` — на этом первый прогон потерял все id.
- Статус задачи: `/v1/product/import/info` → `imported` / `skipped` / `failed`. HTTP 200 успехом
  не является. Атрибуты карточки — `/v4/product/info/attributes`, ключ `id`, не `attribute_id`.
- Безопасные атрибуты для no-op: `9024` Артикул → `4389` Страна → `10400` Гарантия.
- Витрины `/v3/product/list`: рабочие — `STATE_FAILED` и `PARTIAL_APPROVED`; `BANNED` и
  `IMAGE_ABSENT` возвращают весь каталог и непригодны.

## Автомат (построен 22.08, влит в `main` 23.08; детали — `docs/handoff/archive/card_2026-08-24.md`)

**Миграция 600.** `card_status` — состояние проблемной карточки по ключу площадка+аккаунт+`offer_id`;
после лечения строка не удаляется, а закрывается (`is_open`+`healed_at`): так видны рецидивы и
«сколько висит». Журнал попыток `card_push_log` — строка на отправку, прогон возобновляем.

**Детектор `collectors/ozon_card_status.py`** — только чтение: витрины `STATE_FAILED` и
`PARTIAL_APPROVED` → `/v3/product/info/list` → класс A/W/C/O → upsert; кого площадка больше не
считает проблемным — закрывает. Крон 07:41, лог `card_health.log`; ругается на A/W старше недели.

**Дожиматель `tools/ozon_card_push.py`** — классы A и W, ступень 1 (no-op по атрибуту
`9024`→`4389`→`10400`); отправка только с `--apply`. Крон 08:14, оба аккаунта подряд, партия 500.
Ограничение 50 осталось в СТРАЖЕ (`tools/hooks/guard_side_effects.py`, `is_mass`): РУЧНОЙ вызов
с `--limit >50` режется в DENY; крон через хук не идёт.

**Признак успеха у классов РАЗНЫЙ:** у A — очистившийся `status_failed`, у W — исчезнувший код
замечания (`status_failed` у неё пуст изначально); вылеченная W строку не закрывает, класс
меняется на `O`. Предохранители: 3 попытки, backoff 20 ч, пропуск карточки со свежим (<2 ч)
апдейтом ТК, живая перепроверка перед записью, ворота после первых 20 (<50 % — стоп), стоп при
`declined` или уходе из продажи, `skipped` → `needs_human`. **Ступень 2 (round-trip импорт)
НЕ реализована** — шлёт карточку целиком; кандидат один, `3463`.

## Класс C — проба повторной модерации 24.08 + задание для ТК

`tools/ozon_card_c_report.py` (чтение) перечитывает класс C с площадки: имя поля лежит ТОЛЬКО
в `errors[].texts.attribute_name`, в БД его нет. Вид ошибки задаёт связка «код + поле + текст»,
а НЕ код. Задание — `21_ozon_card_class_c_2026-08-24.md` (101 карточка, 109 ошибок, 9 видов).

**Проба 24.08 по команде Сергея: 68 контентных карточек прогнали тем же no-op** (66 «чужой бренд»
acc2 + 2 «получился другой товар»). Все приняты (`imported`), отбивка снялась у 37 — РОВНО у всех
торгующих; **детектор 25.08 рецидивов не нашёл, все 37 держатся закрытыми**. Правило «у ТОРГУЮЩЕЙ
карточки контентный ERROR = устаревшее замечание, лечится повтором; у „Не создан“ — настоящий блок
создания, только контент в ТК» держится вторые сутки; в автомат — после недели без рецидива
(~31.08), и только с фильтром `is_selling` — из 64 открытых C торгуют 5. Режим пробы: `--classes C`
лишь с `--offers <файл>`, пропуск «стала контентной» и ворота отключены, `_verify` не зовётся.

**Задания Наталье отданы 24.08** (хештеги 21, ТН ВЭД 58, маркировка 611) и **закрыты ТК 25.08**:
из 611 предупреждение снялось у всех, детектор закрыл 593. Состав файлов, мост offer_id →
внешний код МС и обе популяции маркировки — в архиве `docs/handoff/archive/card_2026-08-25.md`.
Хвост: **не найдены в МС** 0513, 0514, 1760, 1843, 3407, 2496, 5694, 5917 (плюс 85 acc1 и 40 acc2
из файлов 26.08); `price_sent` — промежуточный статус, «торгует» = остаток FBS.

## Мёртвые карточки WB (ветка `card/wb-card-presence`, 25.08)

`wb_cards` только копится: удалённую в ЛК карточку коллектор перестаёт видеть, а строка живёт
вечно и всплывает во всех выборках как настоящая (вскрылось на списке mkt «Карточки без наличия»:
7 мёртвых из 234). `updated_at` доказательством НЕ является — это правка на стороне ВБ.

`migrations/602_wb_card_presence.sql` — своя таблица `wb_card_presence` (account+nm_id,
`in_cabinet`/`in_trash`/`first_missing`) и вьюха **`wb_cards_live`** для остальных потоков;
чужую `wb_cards` не трогаем. `tools/card_wb_presence.py` — полный список кабинета (активные +
корзина) против `wb_cards`, к площадке только чтение. Непроверенная карточка считается живой,
пустой ответ кабинета трактуется как сбой и не пишется — иначе один 200 с пустым телом пометил
бы мёртвыми все 14 тыс.

**Крон: понедельник 07:11**, лог `card_health.log` (чтение, оба аккаунта, ~7 мин).
**Первый полный прогон 25.08:** 27 мёртвых строк (12 acc1 + 15 acc2), 9 из них в корзине;
`wb_cards_live` отдаёт 29 008 из 29 035. Все 7 карточек из списка mkt легли ровно как кабинет:
5 удалены, `5530WQ4NN7SNdel` в корзине, `68604G6DL42S` активна — она одна реально без наличия.

## Открытые вопросы
- **Сергей / ТК:** остаток класса C после пробы — задание `21_ozon_card_class_c_2026-08-24.md`.
- **Внешние коды без товара в МС** (8 штук, список выше): остатки и себест по ним не поедут.
  Заводить в МС или гасить карточки?
- Природа `skipped`: почему часть карточек не принимает no-op (гипотеза — наборы).
- Рецидив класса W: снятое замечание может вернуться со следующим пушем ТК; разбор через неделю.
- **Вердикт по `wb_cards_live`:** переводить ли на неё выборки mkt/fin (их территория) или
  оставить справочной. Пока `wb_cards` у всех читается как есть, вместе с мёртвыми строками.

## Следующий шаг
1. **Решить с mkt/fin, кто переходит на `wb_cards_live`** — и проверить первый крон-прогон
   присутствия в понедельник 01.09.
2. **Первая неделя крона** (`card_health.log` ~30.08): не висят ли A/W дольше недели и не растёт
   ли доля невылеченных W. Там же ~31.08 — вердикт по классу C в автомат.
3. **Довести группу маркировки до продажи:** 80 карточек в `price_sent` — остаток FBS (чтение).

**Ведение:** обновлять в конце значимого блока; дорос до 150 строк — старое в
`docs/handoff/archive/card_<дата>.md`; закончил кусок — закрыл сессию, `--resume` запрещён.
