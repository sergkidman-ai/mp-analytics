# BRIEF_GAB — поток «Габариты»

> Стартовая команда сессии: «прочитай CLAUDE.md и BRIEF_GAB.md» — и больше ничего.
> `docs/HANDOFF.md` (40 тыс. токенов) при старте НЕ читать. Потолок этого файла — 150 строк.

> **2026-07-31: направление сброшено.** Всё покрытие габаритов до этой даты признано
> недостоверным и пересобрано с нуля. Разделы ниже читать по порядку.

## Признано недостоверным — НЕ переиспользовать

Ничего из перечисленного нельзя брать как вход, «поправить», «отфильтровать» или
«взять оттуда хорошие строки». Файлы оставлены только как улики.

1. **`docs/FINAL_gabarity.xlsx` и все производные** — `PROSMOTR_gabarity.xlsx`,
   `FINAL_gabarity.bak-2026-07-31.xlsx`, `docs/waves/*`, `nosource_breakdown.csv`,
   `truth_coverage_candidates.csv`, листы «Волны», «Занижённые на ВБ», «Брак_размера».
2. **`docs/coverage.xlsx` всех версий** — листы «Покрыто», «Наборы_расчёт», «Ручной разбор»,
   и `docs/queue_v2/coverage_additions.csv`.
3. **Механизм «короб-близнец» — 6963 строки.** Размер брался из реальной строки прайса, но
   ДРУГОГО товара: критерий совпадения — размер коробки, а не код (rapid `9×7×1.8` — 114 строк,
   solutionsprint `33.5×6.6×6.3` — 196). OEM-код карточки не подтверждает ни одну строку.
4. **Веб-находки без статуса VERIFIED — 553 строки.** Не проверял никто; аудит
   `docs/web_audit_v2` (2026-07-24) охватил 74 записи: 34 VERIFIED / 40 REJECTED.
5. **Харвестер карточек конкурентов — закрыт, не возобновлять.** Калибровка 300 моделей:
   попадание 24 %, макс. ошибка по объёму 2094 %
   (`docs/reports/17_kalibrovka_harvestera_wb_2026-07-30.md`).
6. **`supplier_dims` (миграция 036) и всё, что из неё выведено** — в том числе
   `docs/web_search_v2/calibration/calibration_truth.csv` (1141 «эталон») и
   `tools/gab_calibration_set.py`: собран из `supplier_dims`, наследует её слепоту.

## Установленные факты по прайсам (проверка происхождения, 2026-07-31)

Закрыт пересборкой 21–22.08, вынесен в `docs/handoff/archive/gab_2026-08-22.md` (`supplier_dims` не бралась).

## Корневая причина

`coverage.xlsx` никогда не хранил ссылку на строку прайса — только имя поставщика и признак
«прямой» / «наследование от NNNN». Поэтому постфактум проверить происхождение размера было
физически невозможно, и всё, что построено сверху, унаследовало эту слепоту.

## Правило пересборки

**Источник — это указатель, а не ярлык.** Допустимых значений ровно пять:

1. `строка прайса` — файл + номер строки;
2. `наследование от материнской` — артикул материнской карточки;
3. `сумма набора` — компоненты и их источники;
4. `веб` — URL;
5. `НЕ НАЙДЕНО`.

Шестое значение = дефект сборки. Правило проверяется кодом: строка без рабочего указателя
**выбрасывается скриптом**, а не помечается.

## Что остаётся достоверным

- Выгрузка карточек WB (`wb_cards`).
- Шесть исходных файлов поставщиков как таковые (`incoming/gab/`) — кроме solutionsprint,
  у которого файла нет.
- Продажи за 12 месяцев (`sales`, окно 2025-08-01 … 2026-07-31).
- Откат синтетики подтверждён: 124/124 перечитаны, **в WB забракованное не уходило**.

## Состояние на 2026-08-22 — волна 1 записана, старые снесены, регламент МС оформлен

Пересборка построчная из шести прайсов (`tools/gab_price_dims_parse.py`, `# поток: gab`),
`supplier_dims` не использовалась. `docs/rebuild/`: `dims_registry.csv` 16 198 строк (у каждой
указатель `src_file` + `src_row`), `dims_to_write.csv` 9296 карточек, `dims_conflicts.csv` 88
в ручной разбор. Отчёт — `docs/reports/18_prays_gabarity_ms_2026-08-20.md`. Установлено:
у sakura единицы — метры; profiline размеров не содержит; мост Спринта — `SP`+код, `SP`+код+`_MSK`.

**Самопроверка по контрольному полю файла** (`tools/gab_dims_selfcheck.py`): строка сверяется
с полем ИЗ ТОЙ ЖЕ строки того же файла, выводимым из габаритов (свой объём короба; место
в мастер-картоне = МК / кратность). Итог: `dims_wave1_clean.csv` **8965**, `dims_errors.csv`
**331**. Допуск 10 %; у solutionsprint не меньше 0.05 л (его `vol_l` округлён до 0.1 л);
МК засчитывается только при min(сторона) > 10 мм. Грабли: заголовок «объем, мл» у струйных —
это чернила, а не короб, контролем не считать.

**Записано 21.08** (`tools/gab_dims_apply.py`, фазы clear → write): очищено 8961, записано
**8961**, отказ 4 (`СS-TN-321*` — удалены в МС после нашего слепка, `apply_failed.jsonl`).
Легли Д/Ш/В в см (строкой), `volume` в **литрах**, `weight` в кг. Прогон возобновляемый:
`apply_log.jsonl` хранит отправленные id по фазам. Темп 100 карточек / 5 с — НЕ образец.

**Старые значения снесены 21.08 по всему каталогу** (решение Сергея, объём тоже). Основание:
вне волны 77 % заполненных карточек несли шаблоны (`20×37×15` — 9342, `8×10×5` — 2458), то есть
заглушку, неотличимую от замера. Снесено 17 191 карточка + 2 «сироты» с объёмом без размеров.
Срез до чистки — `dims_backup_2026-08-21.csv`, 26 127 строк, откат возможен.

**Сверка 22.08 перечиткой всего каталога** (`tools/gab_dims_verify.py`): 44 683 не архивных
карточки; вне волны с размерами — **0**; наши видны 8936, совпали с записанным **8936**,
расхождений 0; 29 карточек волны ушли в архив/удалены. Решения Сергея: объём в литрах;
вес вместе с размерами; **тип полей Д/Ш/В остаётся строкой** (на числовой не меняем).

**Инцидент 21.08:** ~35 тыс. изменений за вечер забили очередь вебхуков МС — заказы МС→ТК
и статусы обратно частично не дошли. Итог — регламент массовых изменений в МС в `CLAUDE.md`,
общий для всех потоков: темп по сущностям, немые/значимые поля, окно 23:00–05:00, канарейка.

**Знаменатель задан 22.08** (`docs/reports/19_gabarity_ostatok_2026-08-22.md`): считаем по разделу
**Картриджи** (38 416 активных), со стороны поставщиков — только товары из **Оприходований**
(797 док., 18 004 товара). Рабочий знаменатель **17 631**, закрыто **5569** (31,6 %), осталось
**12 062**. Из них кандидат в шести прайсах есть лишь у 267 — прайсы исчерпаны.

**rapid отложен** (2028 строк): единственный нетронутый внутренний резерв — разобрать до любых
разговоров о внешних источниках. Второй резерв — разрыв мэтчинга: реестр 16 199 строк, легло 9296.

### Следующий шаг

Второй проход: 331 строка `dims_errors.csv` — вывести правила и разобрать. Затем 13 дублей
(8 технических, один `code` — писать в обе карточки; 5 разных товаров — ручной разбор):
правка `code` — ЗНАЧИМОЕ поле для ТК, вебхуки не гасить, медленная ветка регламента.
Затем rapid. Открытый вопрос по изи: порядок сторон в `350*100*160` взят из заголовка
колонки «Размеры мм» как Д×Ш×В — допущение уровня файла, поштучно не подтверждено.

## Где работаю

- Ветка `gab/dims`, worktree `.claude/worktrees/gab` (`venv` и `.env` — симлинки на общий
  чекаут). `docs/` в git не входит — данные лежат только в `/opt/mp-analytics/docs/`.
- Общий чекаут `/opt/mp-analytics` — на чужой ветке, переключать нельзя: 30.07 такой switch
  убил идущий прогон.
- Модель: **Opus** для вердиктов и разбора происхождения, Sonnet для механической работы.
  Выбирается на старте и по ходу не меняется.

## Мои файлы

`tools/gab_*.py`, `tools/competitor_harvester.py`, `tools/wb_dims_validator.py`,
`docs/web_search_v2/**`, `docs/reports/NN_*`, `docs/rebuild/**`. Новое помечаю `# поток: gab`.
Чужие файлы не трогаю. Read-only: `raw_wb_*`, `raw_ozon_*`, витрины fin/mkt.

## Запреты потока

1. **Никаких оценок, медиан, прокси и коэффициентов вместо замера.**
2. **CONFIRMED ставит только независимый валидатор**, не сборщик.
3. **Отправка на ВБ / Ozon / Яндекс — только по прямой команде Сергея на конкретную пачку.**
4. **Платные ресурсы (Icecat Full, `web_search` релея) — заморожены** до решения Сергея:
   смета до запуска, молчание согласием не считается.
5. Мастер-короба (объём > 25 л) под карточкой расходника — брак, в загрузку не пускать.

## Открытые вопросы

- **Сергей:** размораживать ли Icecat Full и `web_search` релея — после закрытия харвестера
  и сброса `supplier_dims` это единственные масштабируемые источники паспортных габаритов.
- **Сергей:** прогон Ozon-сбора ждёт теста с домашнего IP.
- **Владелец интеграции ТК:** ловит изменения карточек на лету или ночным прогоном — от этого
  зависит, чья очередь легла 21.08; и есть ли у заказов добор пропущенного опросом.
- **Владелец `tools/territory_guard.py`:** добавить домен `gab` (сейчас на `gab/*` печатает
  «домен сессии не определён», коммит пропускает).

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы.
- Дорос до 150 строк — старое в `docs/handoff/archive/gab_<дата>.md`.
- Закончил кусок — обновил бриф и закрыл сессию. `--resume` вчерашней сессии запрещён.
