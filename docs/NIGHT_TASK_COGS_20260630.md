# Ночная автономная задача (2026-06-30 → утро): перестроить пайплайн COGS

> Ты запущен из `/opt/mp-analytics` в автономном режиме. Сначала прочитай `CLAUDE.md`,
> `ARCHITECTURE.md` и индекс памяти проекта (особенно `project_mp_analytics_cogs`,
> `project_mp_wb_data_model`, `project_mp_moysklad_products`). Не дублируй уже известное.

## Цель

Себестоимость **конкретной отгрузки** должна считаться по **`report/stock` на ДАТУ отгрузки
по каждой позиции**, а НЕ по усреднённому `report/profit` за период. Это открытый TODO,
последний вывод мёртвой сессии a11ce059 (см. память `project_mp_analytics_cogs`).

Доказанный факт-ориентир: отгрузка `4747758355` (11.03) — набор T036 iBk 121.98 + T037 iCl 154.00
= 275.98 ≈ МС 284 ✓ (усреднение давало 269). Усреднённый report/profit (даже мартовский, 127) ≠
факт отгрузки (284).

## Что сделать

1. **Изучить текущий путь COGS** в `reports/margin_by_sku.py` и `phase1_cogs.py` (а также
   `collectors/wb.py`, `collectors/moysklad.py`): где сейчас берётся себест/шт и как усредняется.
2. **Перестроить** расчёт себеста отгрузки на источник `report/stock` на дату отгрузки по позициям.
   Сохрани идемпотентность (UPSERT по натуральному ключу) и принцип «сырьё отдельно от расчётов».
3. **Сверить с эталоном** `incoming/saleout.xlsx` (выгрузка реализации) и с контролем nmID
   216421567 за май 2026 (чистая ≈3909 ₽). COGS после перестройки должен приблизиться к эталону.
4. Если перестройка требует новой миграции/витрины — добавь по нумерации (`migrations/0NN_*.sql`).

## Дисциплина (обязательно)

- **Чекпоинты по ходу:** ключевые выводы сразу выписывай в `docs/` (рабочий лог этой задачи) и в
  память проекта (`/root/.claude/projects/-opt-mp-analytics/memory/`, обнови `project_mp_analytics_cogs`
  и индекс `MEMORY.md`). Память под git — коммить изменения памяти.
- **Коммить код локально** осмысленными порциями (ветка `main` или новая feature-ветка на твоё
  усмотрение). **НЕ пушить на GitHub без моего ОК утром.**
- **Секреты не печатать** (`.env`). Чужие проекты не трогать (`/opt/sokol-server` и др.).
- **Следи за размером сессии:** не уходи в бесконечный лог. Если задача упирается в большой объём —
  доведи до осмысленного промежуточного результата, выпиши состояние в память и остановись, а не
  раздувай контекст до смерти сессии (урок a11ce059).
- К утру оставь в `docs/` короткое резюме: что сделано, что сверено, что осталось, нужен ли push.
