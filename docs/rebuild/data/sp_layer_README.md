# поток: gab
# Слой Solutions Print (sp_layer.csv)

Указатель источника: `src_url` + `src_code` на каждую запись — правило «источник это указатель»
соблюдено. Метка слоя в первом столбце: `SP`.

**Оговорка про слабый мост.** Внутренний артикул Solutions Print — не наш артикул МойСклада
(прямых совпадений 36). Связка строится по OEM-коду из головы наименования (до слова «для»)
и засчитывается только при попадании ровно в одну материнскую модель:

- 1:1 легло — **2 286** из 6 115 записей с полными ДхШхВ;
- неоднозначно (код ведёт на несколько моделей) — **45**;
- без привязки — **3 784**.

Записи со статусом `неоднозначно` и `без_привязки` сохранены в файле целиком и ничего не
закрывают: они лежат в сыром слое до разбора. Ничего не усреднялось и не достраивалось.

Сырьё: `incoming/gab/solutionsprint_dims.jsonl`
(sha256 6781e489b21be11b493cc0288596f056384bf7c59fa01f300bc430aeb5a44c31, 10 054 строки).
Загрузчик в `supplier_dims`: `tools/load_solutionsprint_dims.py`.
