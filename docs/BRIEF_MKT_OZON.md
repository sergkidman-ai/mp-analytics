# BRIEF_MKT_OZON — поисковые запросы Ozon (подзадача потока `mkt`)

> Читать вместе с `CLAUDE.md` и `docs/BRIEF_MKT.md` (границы домена mkt — там).
> Домен = **mkt**. Отдельный бриф заведён потому, что сессия `mkt` параллельно занята Джемом WB.

## Где работаю
- Worktree: `/opt/mp-analytics/.claude/worktrees/mkt-ozon`
- Ветка: `mkt/ozon-search-collector` (запушена, `origin/mkt/ozon-search-collector`)
- tmux-сессия: `mkt-ozon` (сессию `mkt` НЕ трогать — она занята Джемом WB)
- `.env` и `venv` в worktree — симлинки на общий чекаут (gitignored, за веткой не переезжают)

## Состояние на 2026-08-04
Сборщик написан и **прогнан один раз** (30.07). Витрины и отчёта поверх данных нет.

Коммиты: `6c63c1d` (сборщик + недельная история), `d6146ce` (уточнение семантики).

## Что собрано (лежит в БД `mp_analytics`, от worktree не зависит)
| Таблица | Строк | Назначение |
|---|---|---|
| `ozon_search_query` | 746 673 | запрос × SKU × неделя |
| `ozon_search_product` | 107 062 | сводка по SKU за неделю |
| `ozon_search_run` | 12 | журнал прогонов |

- Недели: **22.06 → 29.07.2026**, оба аккаунта (`oz_acc1`, `oz_acc2`).
  Полные: 22–29.06, 29.06–06.07, 06–13.07, 13–20.07, 20–27.07. Хвост 27–29.07 — **неполный**.
- Последняя запись: **30.07.2026 11:58 UTC**.
- Ключ идемпотентности: `account + period_start + period_end + sku + query`.

## Важно про семантику (коммит d6146ce)
Повторный забор той же недели даёт **другой срез**, а не досчёт: Ozon пересчитывает витрину
задним числом. Поэтому перезабор недели ПЕРЕЗАПИСЫВАЕТ строки, а не суммирует. Сравнивать
недели между собой можно, «уточнять» старую неделю новым прогоном — нельзя без пометки.

## Открытые вопросы / что не закончено
1. ~~**Коллектор не подключён к оркестратору**~~ — закрыто 06.08: вызов добавлен в
   `run_marketing.py` (цикл по `OZON_ACCOUNTS`, гейт `t0.weekday() == OZON_SEARCH_DOW`, среда).
   Проверено подстановкой даты: ср → 2 шага (оба аккаунта), чт → 0. Коллекторы при проверке
   не запускались.
2. Витрины нет: сырьё есть, отчёта «спрос/видимость по запросам Ozon» не построено.
3. Данные не обновлялись с 30.07 — недели после 29.07 не собраны.
4. ~~Ветка отстала от `main`~~ — закрыто 06.08: `main` влит в ветку (39 коммитов, без конфликтов).
5. ~~В `main` ветка не влита~~ — закрыто 06.08 по «ок» Сергея: мерж-коммит `07f14bf` в общем
   чекауте. В `main` приехали `collectors/ozon_search_queries.py`, миграция 109,
   `tools/ozon_search_summary.py`, проводка в `run_marketing.py`, territory_guard. **Не запушено**
   на `github-mp` — push отдельным «ок».

## Сделано 06.08 (автозабор поисковых запросов, индекс цен, проверка юнит-экономики)

Детали вынесены → `docs/handoff/archive/BRIEF_MKT_OZON_2026-08-06.md`.

## Следующий шаг
1. Первый автозапуск — **среда 12.08, 03:23 UTC** по cron. Проверить `ozon_search.log`:
   должен быть не `can't open file`, а нормальный прогон обоих аккаунтов.
2. Добор пропущенных недель (после 29.07) — отдельным осознанным прогоном, не «до кучи»
   с первым автозапуском: перезабор недели ПЕРЕЗАПИСЫВАЕТ строки. Учесть, что глубина Ozon
   ~5–6 недель: недели старше уже не вернуть.
3. **Б. Витрина «спрос/видимость по запросам Ozon»** (следующая сессия) — поверх 842k строк
   `ozon_search_query` + `ozon_search_product`, уже на свежем срезе после А.
   Учесть проверенное: наивный отчёт «спрос есть — показов нет» даёт мусор (топ фраз —
   «товары за 1 рубль», «для», «дождевик»); нужен фильтр релевантности профилю.
4. ✅ ЗАКРЫТО 06.08: cron на индекс цен стоит — `11 4 * * *`, `collectors/ozon_price_index.py all`
   → `/opt/mp-analytics/ozon_price_index.log`. Проводка в `run_marketing.py` остаётся спящей
   (оркестратор в cron не стоит) — это дубль на случай, если `run_marketing` заведут целиком.
   Ветка `mkt/ozon-price-index` влита в main по «ок» Сергея (мерж `cc92e95`), файл виден
   из общего чекаута — первый автозапуск 07.08 в 04:11 UTC (07:11 МСК), проверить лог.
   **В origin НЕ запушено** (push — по отдельному «ок»).
5. ✅ ЗАКРЫТО 07.08: **цена покупателя по всему каталогу** (шаг 1 плана `docs/MKT_OZON_PLAN.md`).
   Прямого источника в Seller API нет (проверено 5 эндпоинтов) — восстановлена из фактических
   продаж: `buyer = наша цена × k`, `k = customer_price / price`. Витрина `mkt_ozon_buyer_price`
   (миграция 113, `reports/ozon_buyer_price.py`): acc1 22 413 строк, медиана k 0.572; acc2 13 456,
   0.549. Заморозка 285 SKU снята: субсидия Озона — процент, устойчива, наши снижения не гасит.
   Применять после пилота; на Озон без прямой команды ничего не отправлять.
6. ✅ ЗАКРЫТО 07.08: **контроль маржи Ozon** — шаг 2 плана. Витрина `mkt_ozon_margin_control`
   (миграция 114, `reports/ozon_margin_control.py`): наша цена × payout − расходы площадки −
   себест (живая `tc_buy_price` + FIFO рядом). **`other` у Ozon уже включает рекламу, баллы,
   подписку, эквайринг, штрафы** — отдельно не добавлять. Дашборд :8092: левое меню разбито
   на «Маркетинг · WB» / «Маркетинг · Ozon», страницы `/ozon-economics`, `/ozon-margin-control`,
   `/ozon-bids` — **только чтение**. Ветка `mkt/ozon-buyer-price`, в main НЕ влито (нужен «ок»);
   пока не влито, страницы в проде (:8092 из `/opt/mp-analytics`) не видны. Цифры — см. п. 14.
7. ✅ ЗАКРЫТО 07.08: **сбор рекламы Ozon был огрызком** — Сергей был прав, кампаний
   много и товаров в них тысячи. Ветка `mkt/ozon-ads-coverage` (миграция 115,
   `collectors/ozon_ads.py`, `collectors/ozon_bids.py`, страница ставок), в main НЕ влито.
   - `GET /campaign/{id}/v2/products` БЕЗ пагинации молча отдаёт ровно 30 строк; собирались
     только `RUNNING` (9 из 33 acc1). Ставки: acc1 209 SKU → **10 552**, acc2 145 → **4 334**.
   - `SEARCH_PROMO`/`ALL_SKU_PROMO` на `/v2/products` дают 400 → `POST /campaign/search_promo/v2/products`
     (отдаёт весь пул аккаунта, таблица `ozon_search_promo`). Продвижение в поиске не используется
     вовсе (0 из 3 302 acc1 / 4 448 acc2), индекс видимости «10+» у 3 035 / 4 077.
   - Расход — `GET /statistics/daily/json` (ZIP для «Оплаты за заказ» не строится), сходится
     с транзакциями до рубля в закрытых месяцах. Отсюда `covered_to`.
   - **Дырка шага 3 открыта:** выручки по «Оплате за заказ» нет ни в одном отчёте
     (`daily/json`, `orders/generate/json`, `products/generate/json`) — развести только экспериментом.
8. ✅ ЗАКРЫТО 07.08: **cron на рекламу Ozon.** `OZON_ADS_MKT` — ежедневно 05:47 UTC
   (`ozon_ads.py all` + `ozon_bids.py all`, лог `ozon_ads.log`); `OZON_ADS_MKT_CLOSE` —
   3-го числа 05:23 UTC пересбор прошлого месяца (`$(date -d "last month")`), иначе месяц,
   собранный в середине, навсегда остаётся половиной. Обе команды прогнаны руками.
   Там же — ускорение страницы ставок (37 с → 0.35 с, миграция 116).
9. ✅ ЗАКРЫТО 07.08: **витрина фраз** (шаг 5 плана). `mkt_ozon_query_econ`
   (миграция 117, `reports/ozon_query_econ.py`) + страница `/ozon-queries` на :8092:
   спрос и наша позиция из сборщика фраз × маржа × текущая ставка → решение по каждой
   паре. Рабочий список «поднять_ставку»: acc1 68 фраз / 236 пар, acc2 28 / 76.
   Грабли (спрос — свойство ФРАЗЫ, широкие фразы, случайные совпадения моделей) —
   в разделе «Шаг 5» плана; повторить их = получить мусорный список.
   Там же поправлена формулировка: выключен только СТАРЫЙ инструмент «Продвижение
   в поиске», а «Вывод в топ» (6 кампаний acc1) и «Трафареты» (1) работают.
10. Следующий шаг — пересчёт списка на снижение цены уже с пределом маржи: из
   «можно_снижать» продавались в июле acc1 134 SKU (1 875 231 ₽), acc2 59 (476 210 ₽).
   Затем шаг 4 (ставки) — только предложения, отправка в Ozon без команды Сергея запрещена.
11. Стратегия рекламы — `docs/MKT_OZON_STRATEGY.md` (07.08). Главное: ставка «оплаты за
   заказ» ЕДИНА на аккаунт (acc1 подняли 5→7 % в июле, acc2 остался 5 % = контроль);
   июльская доплата +135 тыс ₽ дала ≈90 тыс ₽ валовой маржи — не окупилась; 9 % с отключением
   кликов — худший вариант (70 % оборота в чеках 5 тыс+, где процент дороже клика в 4–8 раз).
   Кампании оправданы: факт CPC 8–13 ₽ против потолка 18–308 ₽ (запас 2–27×), пол ставки —
   правильная тактика. Слабые места: «от 0 до 1000» acc1 (порог инкрементальности 49 %),
   «Эксперимент1» (63 % бюджета, CR 5.18→3.66 %), «Все» acc2 (CR 2.4 %). Двойная оплата
   ≥32 % оборота = ≥235 609 ₽ процента за июль.
12. Потолок CPC по SKU — миграция 118 (`cr_bucket`, `bid_ceiling`), считается в
   `reports/ozon_query_econ.py` (константа `PPO_RATE` — правит человек при смене ставки
   в кабинете), выведен колонкой на `/ozon-queries`. Список «поднять_ставку» без этой
   колонки не применять.
13. ЖДЁТ: (а) Сергей запускает `web/nginx_ozon_routes.py` + `nginx -t && systemctl reload nginx`,
   иначе страниц Ozon на дашборде не видно (на :8092 они живы, nginx их не маршрутизирует);
   (б) мерж ветки в main — без него :8092 отдаёт 404 на `/ozon-queries` (нужен явный ОК).
14. ✅ ЗАКРЫТО 07.08: **расходы площадки в контроле маржи — по модели, а не медианой**
   (коммит `d057076`, миграция 119: `volume_l`, `logistics_source`, `other_rate`).
   - Корневая бага: `mkt_ozon_margin_control.sku` был NULL у **всех** строк — `mkt_ozon_buyer_price`
     его не заполняет, поэтому факт по SKU (payout, расходы, габариты) не находился ни разу
     и всё считалось медианой аккаунта. Мост `sku_bridge()` из `ozon_product` (offer_id → sku).
   - Логистика Ozon = **≈75 ₽ + 7.0 ₽/л** (acc1; acc2 73 + 8.7) — МНК по медианам объёмных
     вёдер на 2 902 однопозиционных отправлениях за 200 дней (сырой МНК тянут вверх наборы 34 л).
   - «Прочее» — **не рубли, а % от нашей цены** (Premium Pro + Звёздные + эквайринг + реклама
     + штрафы): 9.8 % acc1, 13.0 % acc2. Возвраты — резерв на обработку (44.3 / 30.1 ₽ на штуку);
     гигантский старый `returns_u` был сторно выручки, а не расход.
   - Итог: отрицательная маржа acc1 **254 → 24**, acc2 → **8**; медиана 28.9 % / 24.9 %.
     На странице в ячейке расходов — разбивка по наведению и бейдж «модель».
15. ✅ ЗАКРЫТО 07.08: **аудит платных инструментов Ozon за 6 месяцев** →
   `docs/reports/ozon_services_strategy.md`. Оборот март→июль +0.6 %, управляемые расходы
   277 тыс → 1 462 тыс ₽/мес (13.8 % оборота). Кандидаты на отключение: оплата за заказ
   541 тыс (берётся с 73–80 % ВСЕГО оборота, атрибуции 0), Premium Pro 286 тыс (комиссия
   не упала: 39.8 → 40.5 %), Звёздные 160 тыс, ускоренный сбор отзывов 74 тыс (бесплатно шло
   473 отзыва/мес, платно — 444). Клики оставляем (ДРР 2.7 %). Габариты — всего ≈15 ₽
   на отправление (тариф пологий). Итог до **1.29 млн ₽/мес**. В кабинете НИЧЕГО не менял —
   решение и переключение за Сергеем.
16. **ВОПРОС ПОТОКУ `fin` (чужая территория, не трогал):** `margin_by_sku` завышает чистую
   по Ozon — июль acc1 3 056 411 ₽ против 1 935 474 ₽ по транзакциям (разрыв растёт
   253 тыс. в январе → 1.12 млн в июле, acc2 июль 260 тыс.). Причина: реклама, оплата за заказ,
   подписка, Звёздные и штрафы не попадают в «прочее» витрины (500 393 ₽ против 1 523 061 ₽
   по транзакциям). Реальная маржа Ozon июль ≈18.3 %, а не 28.9 %. Детали — §4 отчёта.

17. 07.08 **доказательная база по платным инструментам** (часть 2 отчёта
   `docs/reports/ozon_services_strategy.md`). Premium Pro = `PremiumMembershipCommission`
   ВНУТРИ заказа с 23.06: 88 % заказов, 2.5 % их оборота; прежние баллы
   (`...PremiumCashbackInd`) стоили 48 511 ₽ за 4.5 мес, теперь 261 тыс./мес — в 26 раз дороже;
   позиции по парам SKU 56→69 (acc1 с Pro) против 53→65 (acc2 без Pro), конверсия −33 % против
   −12 % → буста нет. Звёздные = `ItemAgentServiceStarsMembership`, 91 % заказов acc1 и 100 %
   acc2 по 1.5 % — процент со ВСЕХ продаж, а не выделение товаров. Штрафы за слот идут все
   7 месяцев (в янв-фев под старыми именами) — постоянные 90–110 тыс./мес, не новость.
   Габариты: заполнены у 27 976 из 27 979 карточек, но 8 значений покрывают 60 % каталога
   (11.1 л × 9 360 = шаблон 20×37×15 с ВБ) — ведёт отдельная ветка Сергея.
18. **РЕШЕНИЕ СЕРГЕЯ 07.08:** выключаем Premium Pro, 2 % оплаты за заказ (7→5) и Звёздные
   товары; досрочных выплат больше не будет; габариты — в его параллельной ветке. Просадку
   компенсируем рекламой. План — §10 отчёта: сначала разгон (ставки 8→18–20 ₽ при потолке
   91.9 ₽, лимиты ценовых кампаний с ДРР 0.9–2.6 %, урезать «Эксперимент1» — 64 % бюджета
   при ДРР 5.0 %/CR 3.66 %), затем отключения по одному в неделю с контролем и триггером
   отката −7 % заказов две недели. Освобождается 709 тыс./мес; закрытие 5 % просадки стоит
   26–38 тыс. В кабинете Ozon НИЧЕГО не менял — переключения делает Сергей.

19. 07.08 **план ставок** — `tools/ozon_bid_plan.py` → `docs/reports/ozon_bid_plan.md` +
   три CSV (`raise` 1 204 / `cut` 1 851 / `drop` 15 558). Потолок считается уже под будущие
   условия (ППО 5 %, +3.5 п.п. маржи после отключения Pro и Звёздных), ставим 35 % потолка,
   рост ≤×3. Поднять: acc1 786 поз = 26.4 млн ₽ выручки за 90 дн, 10 → 24 ₽ при потолке 102 ₽.
   Жрёт: «Эксперимент1» (8 093 SKU, 86 % без продаж, 64 % бюджета, CR 3.66 %, 3 465 SKU
   пересекаются с ценовыми — торгуемся сами с собой); 11 769 позиций без продаж 90 дн;
   921 со ставкой выше потолка (869 из них маржа < KPI). Порядок: чистка → подъём ставок →
   отключения допов по одному в неделю. В Ozon ничего не отправлялось.
20. **Габариты проверены прямым сравнением:** топ-8 комбинаций ДхШхВ = 57 % каталога Ozon
   и 58 % WB, 20×37×15 = 11.1 л на 8 269 карточках Ozon и 8 688 WB; мост по 4-значному коду
   даёт 6 516 пар, объём совпадает у 78 %. Это ОДИН набор габаритов, заведённый в обе площадки,
   а не наша правка (июльская синтетика WB откачена). Ведёт отдельная ветка Сергея.
21. 07.08 **обе заявленные выгоды Premium Pro посчитаны** → `docs/reports/ozon_premium_pro_value.md`
   (коммит `30dc942`). Цена подписки: 24 990 фикс + **2.4 % оборота** = 286 178 ₽ за июль
   (Plus стоил 24 990 → Pro дороже на **261 тыс./мес**). (а) «Выше в поиске»: бонус кроют
   6 820 карточек SUPER+GREEN (720 продающихся, 18.2 млн ₽ = 60 % выручки 90 дн), НО средняя
   позиция SUPER 90.4, в топ-10 всего 18 из 5 317 → +20 % = переезд с 90-й на 72-ю. Кривая
   «GMV на показ» падает как позиция^−1.15 → потолок +29 % поискового GMV = +522 тыс. ₽
   выручки = **+167 тыс. ₽ маржи против 286 тыс. цены → −119 тыс. ₽/мес**, и оценка завышена
   дважды (кривая снята поперёк каталога; прирост посчитан на все 6 820, включая 6 100
   непродающихся). Для выхода в ноль нужно +894 тыс. ₽ выручки (+14.7 %). (б) «До +25 % баллов
   за скидки» = **0 ₽**: механика `carrots` выключена (3 301 из 3 302 строк
   `CARROTS_STATUS_DISABLED`, `carrots_additive` = 0), в транзакциях начислений нет вовсе.
   Вывод подтверждает решение п.18; остаток бонусного счёта виден только в кабинете
   (Финансы → Баллы). Поисковый GMV acc1 = 3.01 млн ₽/28 дн ≈ 28 % оборота.

22. 08.08 **план по ставкам acc1 пересобран под KPI 17 % и окно 180 дней** (`tools/ozon_acc1_plan2.py`):
   1 156 позиций на подъём, отсев по марже упал 140 → 42. Волна 1 (нет продаж ни за 180, ни за 90 дн)
   освобождает 58 121 ₽/мес. Списки — `docs/reports/ozon_acc1_{raise,cut,wave1,wave2,cannibal,add}.csv`.
   Каннибализация подтверждена: 3 476 товаров в 2+ кампаниях (79 375 ₽), 1 232 семьи по 4-значному коду,
   103 семьи в прямой конкуренции (20 347 ₽ на 3.7 млн ₽ выручки).
23. **Позиция решает всё** (данные `mkt_ozon_query_econ`, 138 тыс. запросов июнь–август): топ-10 → 19 заказов
   и 51.5 тыс. ₽, 11–30 → 9 заказов, 31–100 → 2, ниже 100 → 1 при спросе 454 тыс. Ниже 30-й строки
   покупателей нет. Отсюда приоритет разгона: 238 карточек стоят на 11–30 (спрос 85 тыс.), из них 88 вообще
   вне рекламы → `ozon_acc1_ramp_priority.csv`, поле `mkt_ozon_bid_ramp.priority` (миграция 121).
   Мид-тейл 52 запроса, где мы релевантны и ниже 20-й → `ozon_acc1_keys_midtail.csv`: это правка ЗАГОЛОВКОВ,
   ставкой не лечится (на Ozon нельзя купить конкретный запрос).
24. **ЗАПИСЬ СТАВОК РАБОТАЕТ.** `PUT /api/client/campaign/{id}/products` тело `{"bids":[{"sku","bid"}]}`,
   ставка — СТРОКА в микрорублях. `POST` по тому же пути = добавление товаров, на ставку не влияет
   (отвечает 200 и молча ничего не меняет — ловушка). `/v2/products` на любую запись 405 (Allow: GET).
25. 08.08 по прямой команде Сергея **запущены все 4 пункта**:
   (1) шаг разгона +10 % применён на **920 позиций** в 7 кампаниях, проверен обратным чтением 920/920;
   (2) создана кампания **35269704 «Вне РК — спрос без рекламы»** — 111 товаров с подтверждённым спросом
   (6.10 млн ₽ за 180 дн) без рекламы, старт по полу 15 ₽, цель = расчётная ставка от потолка;
   (3) создана **35269713 «Бандлы x2/x6 — старт»** — 200 карточек по 100 моделям, чьи одиночки продаются;
   (4) группа B (продаёт сама, реклама в июле дала 0 заказов при 17.1 тыс. ₽) — 236 позиций на паузе,
   тест оставлен на 50. Дубль 35269726 создан по ошибке скрипта и деактивирован, товаров в нём нет.
26. **Схема создания кампании** (подобрана по ответам API, образец — живая РК 10604674):
   `POST /api/client/campaign/cpc/v2/product`, обязательны `placement=PLACEMENT_TOP_PROMOTION` (скаляр,
   хотя на чтении массив), `productCampaignMode=PRODUCT_CAMPAIGN_MODE_AUTO`,
   `productAutopilotStrategy=TARGET_BIDS`, `PaymentType=CPC`, `expenseStrategy=DAILY_BUDGET`,
   `budgetType=PRODUCT_CAMPAIGN_BUDGET_TYPE_WEEKLY`, `weeklyBudget`, дата старта — только сегодняшняя.
   Правка бюджета — `PATCH /api/client/campaign/{id}` `{"weeklyBudget": "<микрорубли>"}`.
   Запуск — `POST /api/client/campaign/{id}/activate`, остановка — `/deactivate`.
   **Потолок бюджета обязателен: 2 000 ₽/нед на КАЖДЫЙ товар** (111 → 222 тыс., 200 → 400 тыс.).
   Это норматив-потолок, а не план расхода. **Пол ставки в новой кампании — 15 ₽** (ниже «вне диапазона»).
27. **Стоп-кран разгона:** `ozon_bid_ramp.py step` не делает шаг, если расход за последний снятый день
   превысил `--max-spend` (по умолчанию 8 000 ₽ при базе 3 238 ₽/день на 07.08).
   Ежедневный цикл — `ops/ozon_bid_ramp_daily.sh`. **Таймер установлен 08.08.2026**:
   `mp-ozon-bid-ramp.timer` (`OnCalendar=06:17`, `Persistent=true`), лог `/opt/mp-analytics/logs/ozon_bid_ramp.log`.
28. **Первая волна удалена 08.08.2026** — `tools/ozon_wave1_delete.py --apply`: 11 943 связки
   товар×кампания (8 834 уникальных SKU) в 7 кампаниях, ошибок 0, сверка чтением — ни одной не осталось.
   Эндпоинт: `POST /api/client/campaign/{id}/products/delete` `{"sku":[...]}` → 200 `{}`.
   Чтение состава — `GET /api/client/campaign/{id}/v2/products`, **пагинированное** (по умолчанию 30 на
   страницу, нужен `page`/`pageSize`; без пагинации кажется, что в кампании 30 товаров). Пересечения
   с разгоняемыми позициями нет (проверено до отправки). Экономия — 58 121 ₽/мес.
29. **Каннибализм** (`tools/ozon_cannibal.py`, отчёты `ozon_acc1_cannibal_*.csv`). По 16 663 запросам
   в позициях 1–30 стоят 2+ наших карточки ОДНОГО товара (1 375 товаров, 8 003 карточки, спрос
   104 653 чел.). **В деньгах рекламы это почти ничего**: после удаления волны 1 обе карточки
   одновременно в рекламе лишь по 742 запросам (266 SKU), их расход за 30 дней — 181 ₽.
   Вывод: каннибализм у нас не рекламный, а **органический — дубли карточек одного товара**.
   **РЕШЕНИЕ СЕРГЕЯ 08.08.2026: ничего не трогаем.** Много карточек в выдаче = мы занимаем полку и
   оттесняем конкурентов, а платим за это 181 ₽/30 дней — это выгодно. Ни склейку дублей, ни снятие
   тех 7 SKU с рекламы НЕ делать. Пересматривать только если расход на спорных парах вырастет
   заметно (следить по `ozon_acc1_cannibal_action.csv`, колонка `ad_spend_30d`).

30. **10.08 таймер разгона был мёртв со дня установки.** `mp-ozon-bid-ramp.service` падал с
   `203/EXEC` 09.08 и 10.08: у `ops/ozon_bid_ramp_daily.sh` не был выставлен бит `+x`
   (лога `logs/ozon_bid_ramp.log` не существовало вовсе). Итог: после ручного шага 08.08
   разгон не двигался **два дня**. Починено `chmod +x`, проверено dry-run'ом: снимок за 09.08
   — 1 823 позиции, стоп-кран 2 001 ₽ при потолке 8 000, шаг на 10.08 — 1 223 позиции
   в 9 кампаниях. На площадку ничего не отправлялось; догоняющий шаг за пропущенные дни
   НЕ делался (ждёт решения Сергея), штатный запуск — 11.08 06:17 UTC.
   **Хрупкость:** `ExecStart` указывает внутрь worktree `mkt-ozon`; удаление worktree или
   ветки снова убьёт таймер — при мерже в `main` переставить путь на общий чекаут.

31. **10.08 замер эффекта 08.08 (удаление волны 1 + шаг ставок).** Добраны посуточные снимки
   27.07–09.08 (`snapshot --date`, 12 дней) — иначе сравнение упиралось в «пятница против
   воскресенья». Сравнение **выходные к выходным** (01–02.08 → 08–09.08), весь acc1:
   расход 6 111 → 4 012 ₽ (−34 %), клики 560 → 311 (−44 %), заказы 16 → 17, рекламная выручка
   73 205 → 85 798 ₽ (+17 %), ДРР 8.3 % → 4.7 %.
   - Волна 1 на 07.08 (последний день до удаления): 8 232 позиции, 37 298 показов, 177 кликов,
     **1 320 ₽ расхода = 34 % бюджета дня, 0 заказов, 0 ₽**. Оставленные 1 687 позиций — 2 514 ₽
     и все 14 заказов (141 531 ₽). Удаление подтверждено как чистая экономия.
   - Разогнанные 920 позиций: заказы 9 → 12, выручка 56 721 → 62 762 ₽, расход 2 551 → 1 885 ₽.
     Направление верное, но **2 дня и 9 против 12 заказов — это не значимость**, а намёк.
   - Новые кампании фактически стоят: 35269704 — 38 ₽ и 6 кликов, 35269713 — 29 ₽ и 3 клика
     за два дня, заказов 0; показы получили 41 из 111 и 166 из 200 товаров. Пол 15 ₽ аукцион
     не берёт — разбирать отдельно.
32. 🔴 **Волна 1 — не «мёртвый» товар, а ОТМЕНЯЕМЫЙ.** За 01.05–09.08 SKU волны 1 попали
   в **46 заказов, 38 отменены**: май 8 из 8, июнь 13 из 13, июль 15 из 15 — **100 % отмен**
   при среднем по acc1 9–11 %. Критерий «нет продаж 180/90 дней» отработал верно: продаж
   не было потому, что все заказы отменялись (почти наверняка нет стока).
   - После удаления из рекламы 08–09.08 они дали 8 заказов на 106 224 ₽ (54 183 и 18 423 ₽
     в том числе): 2 уехали (`delivering`, 2 641 ₽), 6 висят в `awaiting_packaging` на 103 583 ₽.
     Это **не потерянная реклама**: единственный ad-attributed заказ (sku 873313495, 18 423 ₽,
     кампания 10659516) пришёл при **0 кликов и 0 расхода**.
   - **Проверить 13–14.08**, чем кончились те 6 заказов. Если отменятся — вопрос не рекламный:
     карточки надо либо обеспечить стоком, либо снимать с продажи (отмены бьют по рейтингу).
33. **Долги закрыты 10.08** (задачи ревизии «доделай хвосты»):
   - Ветка `mkt/ozon-ads-coverage` слита с `main` без конфликтов (`3042e03`), отставание 0.
   - `MARGIN_UP` 3,5 → **2,4** во всех трёх планировщиках: пока в допущении только
     Premium Pro (2,47 п.п., тариф кончается 23.08). Звёздные — см. п. 36. План пересчитан, цели
     перезалиты — 1219 позиций; цель упала у 350 из 1126 в среднем на 7 %, медиана 27 → 26 ₽.
     **Перебора нет: выше новой цели только 8 позиций.**
   - Недели фраз: пробела НЕТ. 20–27.07 и 27.07–03.08 собраны 06.08. Неделя 03–10.08
     закрывается сегодня, лаг Ozon 2 дня → появится 12.08, ровно когда сработает кран.
   - Маршруты nginx `^~ /ozon-` и `^~ /api/ozon-` уже стояли; `nginx -t` ok, reload сделан,
     проверка через nginx даёт 401 (basic auth сработал), в финансовый :8090 не проваливаются.
   - **Новые кампании не «стоят»**: обе `RUNNING`, недельный бюджет 400 000 и 222 000 ₽ при
     расходе 38 и 29 ₽. Все 311 товаров на полу 15 ₽ при фактическом CPC acc1 10–14,6 ₽ —
     проигрывают аукцион TOP_PROMOTION. После перезалива у всех 311 есть цели 34 и 30 ₽.
     Единственный рычаг — разгон, а он заморожен с 08.08.
34. 🔴 **`margin_by_sku` завышает прибыль acc1 на 1,12 млн ₽ за июль** (10,6 п.п. маржи:
   3 056 386 ₽ / 28,9 % против 1 935 449 ₽ / 18,3 % по транзакциям). Разложено без остатка:
   1 012 116 ₽ — счета уровня аккаунта, которые модель не разносит (ППО 541к, Premium Pro 286к,
   клики 170к, Звёздные 160к, досрочная выплата 114к, слот 91к, отзывы 74к, эквайринг 61к),
   плюс 108 820 ₽ расхождения базы. `other_rate = 9,81 %` против 14,34 % по факту.
   Вопрос оформлен: `docs/QUESTION_FIN_MARGIN_BY_SKU.md`. **Ставка вопроса: потолок CPC берётся
   из `margin_own_live` той же модели — если верны 18,3 %, цели разгона надо резать на треть.**
   Отдельный вопрос к `fin`: не вычитается ли ППО дважды (в `other_rate` и явно в формуле `ceil`).

36. **Звёздные товары — ОТКЛЮЧЕНЫ Сергеем 09–10.08, подтверждено данными 13.08.** Последнее
   списание `StarsMembership` относится к заказу от **10.08**; по заказам с 11.08 начислений
   **ноль** (проверено по `payload->posting->order_date`, не по дате списания — она тянется
   хвостом 2–4 дня и создаёт иллюзию, что программа жива). За 01–12.08 успело уйти 45 239 ₽.
   `PremiumMembership` при этом продолжает списываться по заказам 11–12.08 — подписка Pro
   оплачена по тариф до 23.08, это ожидаемо. Ниже — расчёт, по которому принималось решение.

   **Звёздные товары — отключать.** 10.08 выяснилось, что тумблер всё-таки есть (раньше
   в брифе стояло «нельзя» — неверно). Расчёт: `docs/reports/ozon_stars_verdict.md`.
   Платим 159 900 ₽/мес = 1,51 % всей выручки; программа касается оборота 934 100 ₽ (8,8 %
   нашего), то есть **эффективная цена 17,1 %** от того оборота, который трогает.
   Видимость даёт мизерную: 24 469 просмотров за 30 дн против 2 634 012 показов рекламы =
   **0,9 %**. Порог окупаемости по инкрементальности: 59 % при марже 28,9 % и **93,5 %**
   при реальной 18,3 % — то есть почти каждый рубль программы должен быть новым.
   При реалистичных 20 % программа стоит −106…−126 тыс ₽/мес. 21,26 % активных покупателей
   дают 8,8 % оборота → чек участника в 2,8 раза ниже среднего; комиссия 17,1 % бьёт как раз
   по мелким заказам. «3 млрд» из кабинета = 24 года наших продаж, это GMV программы целиком.
   - После выключения: `MARGIN_UP` 2,4 → **3,9**, но одним пересчётом вместе с ответом `fin`
     по `other_rate`, чтобы не гонять цели разгона дважды.
37. **Догоняющий шаг разгона сделан 10.08** по прямой команде Сергея: 1232 позиции в 9 кампаниях,
   +10 % к текущей ставке, все кампании ответили 200. Проверено перечиткой с площадки —
   в контрольных кампаниях 35269704 и 10659294 ставка совпала у 103 из 103 и 58 из 58
   (тот самый случай, когда 200 мог оказаться пустышкой, — не оказался).
   Стоп-кран прошёл: расход 09.08 = 2 001 ₽ при потолке 8 000 ₽.
35. **Список правок заголовков готов** — `reports/ozon_title_gaps.py`,
   `docs/reports/ozon_oz_acc{1,2}_title_gaps.csv`. В Ozon ничего не отправлялось.
   acc1: 57 фраз «не видны», 48 чинятся заголовком (спрос 1319/нед, 36 карточек, 29 в рекламе);
   acc2: 46 / 36 / 1066 / 27. Чаще всего не хватает слова **«принтера» — 18 фраз, спрос 686**;
   у 7 фраз (спрос 424, 6 карточек) это единственное недостающее слово. Далее «картридж» (9),
   «pantum» (5). Это правка контента, ставкой не лечится.

38. **13.08 РАЗГОН ЗАМОРОЖЕН по команде Сергея; замеры починены.**
   - Причина заморозки: цели ставок (`bid_target`, в среднем 30,1 ₽) считаются от `margin_own_live`,
     которая завышает маржу на 10,6 п.п. (п. 34). При средней текущей 17,0 ₽ мы уже у границы
     «исправленного» потолка (≈20 ₽), а +10 %/день брали цель за ~6 дней. Ставки НЕ откатывали —
     замерли на достигнутом.
   - Механика заморозки: флаг **`/etc/mp-ozon-ramp-frozen`** (вне репозитория). Проверяется
     И в `ops/ozon_bid_ramp_daily.sh`, И в обёртке `/usr/local/sbin/mp-ozon-bid-ramp` — чтобы шаг
     не ушёл на площадку, даже если запустится старая копия скрипта из другого дерева.
     Пока флаг есть: снимки собираются, ставки не трогаются. Снять = `rm` файла, **только по
     команде Сергея**.
   - Час таймера **06:17 → 09:17 UTC** (`/etc/systemd/system/mp-ozon-bid-ramp.timer`): в 06:17
     Ozon ещё не закрывал вчерашний день, из-за этого снимок за 11.08 вышел пустым (0 позиций),
     и стоп-кран судил по позавчера.
   - **acc2 заведён в посуточные снимки** (`snapshot --account`): разгона на нём нет и ППО всегда
     было 5 % → это контрольная группа. Добраны 27.07–12.08, плюс закрыта дыра acc1 за 11.08.
   - ППО 7 → 5 % (переключено Сергеем 09–10.08) в данных видно: ППО/GMV по дате заказа
     20.07–09.08 в среднем ~5,1 %, 10–11.08 — 3,9 и 3,1 % (отношение 0,69 ≈ 5/7).
   - Замер эффекта 08.08 на 13.08: расход acc1 3 834 (07.08) → 4 213 (10.08) → 5 075 ₽ (12.08),
     кохорта разгона +115 % расхода, ДРР 2,07 → 5,94 %; заказы по всему acc1 будни-к-будням
     44,4 → 44,0 в день, GMV 394 → 453 тыс. ₽. Рост эффективности рекламы есть, роста заказов нет.
   - **Следующий шаг (не требует `fin`):** пересчитать НАШ `other_rate` эмпирически по транзакциям
     с 11.08 (Звёздные уже 0, ППО 5 %), затем `MARGIN_UP` и потолки → и только потом решать,
     снимать ли заморозку. Вопрос к `fin` (`margin_by_sku`, `docs/QUESTION_FIN_MARGIN_BY_SKU.md`) —
     про ИХ витрину, для разгона не блокер.
39. **Волна 1: контрольная точка 13–14.08 закрыта — отмен не было.** Заказы 08–09.08, висевшие
   в `awaiting_packaging` на 103 583 ₽, уехали: 54 183, 46 842 и 18 423 ₽ (тот самый
   ad-attributed sku 873313495) — все `delivering`; отмен за 08–09.08 ноль, в ожидании остался
   один заказ на 1 257 ₽. Сценарий «100 % отмен» не повторился, снимать карточки с продажи
   пока не за что. Оговорка: сопоставление шло по суммам из п. 32, потому что
   `docs/reports/ozon_acc1_wave1.csv` — это 209 позиций плана, а удаляли 8 834 SKU: полного
   списка удалённых в файле нет, при следующей чистке сохранять его отдельно.

### Что ждёт команды или даты
- ⏸ **Разгон заморожен с 13.08** (флаг `/etc/mp-ozon-ramp-frozen`). Снимать — только после
  пересчёта `other_rate`/потолков и по прямому «да» Сергея.
- 📅 **после 23.08** — Premium Pro отваливается, `other_rate` −2,53 п.п. → потолки вверх,
  второй проход по ставкам.
- ✅ 13.08 закрыто: даты от Сергея — Звёздные и ППО 7→5 % переключены 09–10.08 (п. 36, 38).
- ✅ 12.08 закрыто: первый автопрогон сборщика фраз прошёл (04:31 UTC), добраны недели
  27.07–03.08 и 03–10.08 по обоим аккаунтам; нули в хвосте лога — неполная неделя 10–11.08.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/`, здесь оставить только живое.
