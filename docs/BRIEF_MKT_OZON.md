# BRIEF_MKT_OZON — поисковые запросы Ozon (подзадача потока `mkt`)

> Читать вместе с `CLAUDE.md` и `docs/BRIEF_MKT.md` (границы домена mkt — там).
> Домен = **mkt**. Отдельный бриф заведён потому, что сессия `mkt` параллельно занята Джемом WB.

## Где работаю
- Worktree: `/opt/mp-analytics/.claude/worktrees/mkt-ozon`
- Ветка: `mkt/ozon-search-collector` (запушена, `origin/mkt/ozon-search-collector`)
- tmux-сессия: `mkt-ozon` (сессию `mkt` НЕ трогать — она занята Джемом WB)
- `.env` и `venv` в worktree — симлинки на общий чекаут (gitignored, за веткой не переезжают)

## Состояние на 2026-08-04
Сборщик написан и **прогнан один раз** (30.07). Витрины и отчёта поверх данных нет.

Коммиты: `6c63c1d` (сборщик + недельная история), `d6146ce` (уточнение семантики).

## Что собрано (лежит в БД `mp_analytics`, от worktree не зависит)
| Таблица | Строк | Назначение |
|---|---|---|
| `ozon_search_query` | 746 673 | запрос × SKU × неделя |
| `ozon_search_product` | 107 062 | сводка по SKU за неделю |
| `ozon_search_run` | 12 | журнал прогонов |

- Недели: **22.06 → 29.07.2026**, оба аккаунта (`oz_acc1`, `oz_acc2`).
  Полные: 22–29.06, 29.06–06.07, 06–13.07, 13–20.07, 20–27.07. Хвост 27–29.07 — **неполный**.
- Последняя запись: **30.07.2026 11:58 UTC**.
- Ключ идемпотентности: `account + period_start + period_end + sku + query`.

## Важно про семантику (коммит d6146ce)
Повторный забор той же недели даёт **другой срез**, а не досчёт: Ozon пересчитывает витрину
задним числом. Поэтому перезабор недели ПЕРЕЗАПИСЫВАЕТ строки, а не суммирует. Сравнивать
недели между собой можно, «уточнять» старую неделю новым прогоном — нельзя без пометки.

## Открытые вопросы / что не закончено
1. ~~**Коллектор не подключён к оркестратору**~~ — закрыто 06.08: вызов добавлен в
   `run_marketing.py` (цикл по `OZON_ACCOUNTS`, гейт `t0.weekday() == OZON_SEARCH_DOW`, среда).
   Проверено подстановкой даты: ср → 2 шага (оба аккаунта), чт → 0. Коллекторы при проверке
   не запускались.
2. Витрины нет: сырьё есть, отчёта «спрос/видимость по запросам Ozon» не построено.
3. Данные не обновлялись с 30.07 — недели после 29.07 не собраны.
4. ~~Ветка отстала от `main`~~ — закрыто 06.08: `main` влит в ветку (39 коммитов, без конфликтов).
5. ~~В `main` ветка не влита~~ — закрыто 06.08 по «ок» Сергея: мерж-коммит `07f14bf` в общем
   чекауте. В `main` приехали `collectors/ozon_search_queries.py`, миграция 109,
   `tools/ozon_search_summary.py`, проводка в `run_marketing.py`, territory_guard. **Не запушено**
   на `github-mp` — push отдельным «ок».

## Сделано 06.08 (автозабор поисковых запросов, индекс цен, проверка юнит-экономики)

Детали вынесены → `docs/handoff/archive/BRIEF_MKT_OZON_2026-08-06.md`.

## Следующий шаг
1. Первый автозапуск — **среда 12.08, 03:23 UTC** по cron. Проверить `ozon_search.log`:
   должен быть не `can't open file`, а нормальный прогон обоих аккаунтов.
2. Добор пропущенных недель (после 29.07) — отдельным осознанным прогоном, не «до кучи»
   с первым автозапуском: перезабор недели ПЕРЕЗАПИСЫВАЕТ строки. Учесть, что глубина Ozon
   ~5–6 недель: недели старше уже не вернуть.
3. **Б. Витрина «спрос/видимость по запросам Ozon»** (следующая сессия) — поверх 842k строк
   `ozon_search_query` + `ozon_search_product`, уже на свежем срезе после А.
   Учесть проверенное: наивный отчёт «спрос есть — показов нет» даёт мусор (топ фраз —
   «товары за 1 рубль», «для», «дождевик»); нужен фильтр релевантности профилю.
4. ✅ ЗАКРЫТО 06.08: cron на индекс цен стоит — `11 4 * * *`, `collectors/ozon_price_index.py all`
   → `/opt/mp-analytics/ozon_price_index.log`. Проводка в `run_marketing.py` остаётся спящей
   (оркестратор в cron не стоит) — это дубль на случай, если `run_marketing` заведут целиком.
   Ветка `mkt/ozon-price-index` влита в main по «ок» Сергея (мерж `cc92e95`), файл виден
   из общего чекаута — первый автозапуск 07.08 в 04:11 UTC (07:11 МСК), проверить лог.
   **В origin НЕ запушено** (push — по отдельному «ок»).
5. ✅ ЗАКРЫТО 07.08: **цена покупателя есть по всему каталогу** — шаг 1 глобального плана
   (`docs/MKT_OZON_PLAN.md`, разделы 1.4 и «Шаг 1»). Прямого источника в Seller API нет
   (проверены `/v5/product/info/prices`, `/v3/product/info/list`, отчёт по товарам,
   `/v1/actions/products`, `/v1/product/info/discounted`), восстановлена из фактических
   продаж: `buyer = наша цена × k`, `k = customer_price / price`. Витрина
   `mkt_ozon_buyer_price` + журнал `_run` (миграция 113, `reports/ozon_buyer_price.py`),
   ветка `mkt/ozon-buyer-price`. Снимок 06.08: acc1 22 413 строк (k по своему SKU у 415),
   медиана k 0.572; acc2 13 456 (141), медиана 0.549.
   **Заморозка списка 285 SKU СНЯТА:** субсидия Озона — процент (корр. суммы с ценой +0.97),
   устойчива (|Δk| медиана 0.032) и наши снижения цены НЕ гасит (передача Δ медиана 1.17,
   корр. +0.76) → формула `1 − 1.05/индекс` рабочая. Применять всё равно только после
   шага 2 (маржа KPI 25 %) и пилота; на Озон без прямой команды ничего не отправлять.
6. ✅ ЗАКРЫТО 07.08: **контроль маржи Ozon** — шаг 2 плана (раздел 2 `MKT_OZON_PLAN.md`).
   Витрина `mkt_ozon_margin_control` (миграция 114, `reports/ozon_margin_control.py`):
   наша цена × payout (из фактических продаж) − логистика/хранение/приёмка/возвраты/прочее
   (на штуку из `margin_by_sku`, штуки из постингов — `qty` у Ozon NULL) − себест
   (живая `tc_buy_price` + FIFO рядом). **`other` у Ozon уже включает рекламу, баллы,
   подписку, эквайринг, штрафы** — отдельно не добавлять.
   Итоги: acc1 22 413 карточек / маржа у 16 719 / медиана 35.1 % / ниже KPI 1 308 /
   минус 254 / в GREEN укладывается 1 993 против 187; acc2 13 456 / 10 583 / 37.5 % /
   594 / 10 / 3 351 против 44. Сверка с фактом fin за июль: +0.7 п.п. acc1, +0.5 п.п. acc2.
   Дашборд :8092 — левое меню разбито на «Маркетинг · WB» и «Маркетинг · Ozon», три
   страницы Ozon (`/ozon-economics`, `/ozon-margin-control`, `/ozon-bids`), **только чтение**.
   Ветка `mkt/ozon-buyer-price`, в main НЕ влито (нужен «ок»); пока не влито, страницы
   в проде (:8092 из `/opt/mp-analytics`) не видны.
7. ✅ ЗАКРЫТО 07.08: **сбор рекламы Ozon был огрызком** — Сергей был прав, кампаний
   много и товаров в них тысячи. Ветка `mkt/ozon-ads-coverage` (миграция 115,
   `collectors/ozon_ads.py`, `collectors/ozon_bids.py`, страница ставок), в main НЕ влито.
   - `GET /campaign/{id}/v2/products` БЕЗ пагинации молча отдаёт ровно 30 строк; собирались
     только `RUNNING` (9 из 33 acc1). Ставки: acc1 209 SKU → **10 552**, acc2 145 → **4 334**.
   - `SEARCH_PROMO`/`ALL_SKU_PROMO` на `/v2/products` дают 400 → `POST /campaign/search_promo/v2/products`
     (отдаёт весь пул аккаунта, таблица `ozon_search_promo`). Продвижение в поиске не используется
     вовсе (0 из 3 302 acc1 / 4 448 acc2), индекс видимости «10+» у 3 035 / 4 077.
   - Расход — `GET /statistics/daily/json` (ZIP для «Оплаты за заказ» не строится), сходится
     с транзакциями до рубля в закрытых месяцах. Отсюда `covered_to`.
   - **Дырка шага 3 открыта:** выручки по «Оплате за заказ» нет ни в одном отчёте
     (`daily/json`, `orders/generate/json`, `products/generate/json`) — развести только экспериментом.
8. ✅ ЗАКРЫТО 07.08: **cron на рекламу Ozon.** `OZON_ADS_MKT` — ежедневно 05:47 UTC
   (`ozon_ads.py all` + `ozon_bids.py all`, лог `ozon_ads.log`); `OZON_ADS_MKT_CLOSE` —
   3-го числа 05:23 UTC пересбор прошлого месяца (`$(date -d "last month")`), иначе месяц,
   собранный в середине, навсегда остаётся половиной. Обе команды прогнаны руками.
   Там же — ускорение страницы ставок (37 с → 0.35 с, миграция 116).
9. ✅ ЗАКРЫТО 07.08: **витрина фраз** (шаг 5 плана). `mkt_ozon_query_econ`
   (миграция 117, `reports/ozon_query_econ.py`) + страница `/ozon-queries` на :8092:
   спрос и наша позиция из сборщика фраз × маржа × текущая ставка → решение по каждой
   паре. Рабочий список «поднять_ставку»: acc1 68 фраз / 236 пар, acc2 28 / 76.
   Грабли (спрос — свойство ФРАЗЫ, широкие фразы, случайные совпадения моделей) —
   в разделе «Шаг 5» плана; повторить их = получить мусорный список.
   Там же поправлена формулировка: выключен только СТАРЫЙ инструмент «Продвижение
   в поиске», а «Вывод в топ» (6 кампаний acc1) и «Трафареты» (1) работают.
10. Следующий шаг — пересчёт списка на снижение цены уже с пределом маржи: из
   «можно_снижать» продавались в июле acc1 134 SKU (1 875 231 ₽), acc2 59 (476 210 ₽).
   Затем шаг 4 (ставки) — только предложения, отправка в Ozon без команды Сергея запрещена.
11. Стратегия рекламы — `docs/MKT_OZON_STRATEGY.md` (07.08). Главное: ставка «оплаты за
   заказ» ЕДИНА на аккаунт (acc1 подняли 5→7 % в июле, acc2 остался 5 % = контроль);
   июльская доплата +135 тыс ₽ дала ≈90 тыс ₽ валовой маржи — не окупилась; 9 % с отключением
   кликов — худший вариант (70 % оборота в чеках 5 тыс+, где процент дороже клика в 4–8 раз).
   Кампании оправданы: факт CPC 8–13 ₽ против потолка 18–308 ₽ (запас 2–27×), пол ставки —
   правильная тактика. Слабые места: «от 0 до 1000» acc1 (порог инкрементальности 49 %),
   «Эксперимент1» (63 % бюджета, CR 5.18→3.66 %), «Все» acc2 (CR 2.4 %). Двойная оплата
   ≥32 % оборота = ≥235 609 ₽ процента за июль.
12. Потолок CPC по SKU — миграция 118 (`cr_bucket`, `bid_ceiling`), считается в
   `reports/ozon_query_econ.py` (константа `PPO_RATE` — правит человек при смене ставки
   в кабинете), выведен колонкой на `/ozon-queries`. Список «поднять_ставку» без этой
   колонки не применять.
13. ЖДЁТ: (а) Сергей запускает `web/nginx_ozon_routes.py` + `nginx -t && systemctl reload nginx`,
   иначе страниц Ozon на дашборде не видно (на :8092 они живы, nginx их не маршрутизирует);
   (б) мерж ветки в main — без него :8092 отдаёт 404 на `/ozon-queries` (нужен явный ОК).
14. ✅ ЗАКРЫТО 07.08: **расходы площадки в контроле маржи — по модели, а не медианой**
   (коммит `d057076`, миграция 119: `volume_l`, `logistics_source`, `other_rate`).
   - Корневая бага: `mkt_ozon_margin_control.sku` был NULL у **всех** строк — `mkt_ozon_buyer_price`
     его не заполняет, поэтому факт по SKU (payout, расходы, габариты) не находился ни разу
     и всё считалось медианой аккаунта. Мост `sku_bridge()` из `ozon_product` (offer_id → sku).
   - Логистика Ozon = **≈75 ₽ + 7.0 ₽/л** (acc1; acc2 73 + 8.7) — МНК по медианам объёмных
     вёдер на 2 902 однопозиционных отправлениях за 200 дней (сырой МНК тянут вверх наборы 34 л).
   - «Прочее» — **не рубли, а % от нашей цены** (Premium Pro + Звёздные + эквайринг + реклама
     + штрафы): 9.8 % acc1, 13.0 % acc2. Возвраты — резерв на обработку (44.3 / 30.1 ₽ на штуку);
     гигантский старый `returns_u` был сторно выручки, а не расход.
   - Итог: отрицательная маржа acc1 **254 → 24**, acc2 → **8**; медиана 28.9 % / 24.9 %.
     На странице в ячейке расходов — разбивка по наведению и бейдж «модель».
15. ✅ ЗАКРЫТО 07.08: **аудит платных инструментов Ozon за 6 месяцев** →
   `docs/reports/ozon_services_strategy.md`. Оборот март→июль +0.6 %, управляемые расходы
   277 тыс → 1 462 тыс ₽/мес (13.8 % оборота). Кандидаты на отключение: оплата за заказ
   541 тыс (берётся с 73–80 % ВСЕГО оборота, атрибуции 0), Premium Pro 286 тыс (комиссия
   не упала: 39.8 → 40.5 %), Звёздные 160 тыс, ускоренный сбор отзывов 74 тыс (бесплатно шло
   473 отзыва/мес, платно — 444). Клики оставляем (ДРР 2.7 %). Габариты — всего ≈15 ₽
   на отправление (тариф пологий). Итог до **1.29 млн ₽/мес**. В кабинете НИЧЕГО не менял —
   решение и переключение за Сергеем.
16. **ВОПРОС ПОТОКУ `fin` (чужая территория, не трогал):** `margin_by_sku` завышает чистую
   по Ozon — июль acc1 3 056 411 ₽ против 1 935 474 ₽ по транзакциям (разрыв растёт
   253 тыс. в январе → 1.12 млн в июле, acc2 июль 260 тыс.). Причина: реклама, оплата за заказ,
   подписка, Звёздные и штрафы не попадают в «прочее» витрины (500 393 ₽ против 1 523 061 ₽
   по транзакциям). Реальная маржа Ozon июль ≈18.3 %, а не 28.9 %. Детали — §4 отчёта.

---
### Правила ведения
- Обновлять в конце каждого значимого блока работы (CLAUDE.md, правило 10).
- Дорос до 150 строк — старое в `docs/handoff/archive/`, здесь оставить только живое.
