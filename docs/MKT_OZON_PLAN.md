# MKT · Ozon: юнит-экономика и глобальный план

Аналог `MKT_WB_UNIT_ECONOMICS.md` для Ozon. Порядок разделов = порядок работ:
юнит-экономика → контроль маржи → ставки → анализ спроса.
Составлен 07.08.2026, все цифры проверены по нашей БД, источник указан у каждой.

---

## 0. Golden rules

1. **`payout` и `commission_amount` в постингах — суммы НА СТРОКУ, `price` — НА ШТУКУ.**
   Считать только по строкам `quantity = 1` либо делить на количество. Иначе получишь
   комиссию 64 % и выплату 92 % — проверено, первый прогон был именно такой.
2. **`qty` у Ozon в `margin_by_sku` не заполнен** (только суммы). Всё считать в рублях.
3. **Мост к продажам:** `ozon_price_index.offer_id → ozon_product.sku → margin_by_sku.article`.
   Прямого совпадения `price_index.sku` с `margin_by_sku.article` НЕТ (0 строк, проверено).
   В постингах `financial_data.products[].product_id` = `ozon_product.sku`, НЕ offer_id.
4. **`financial_data.products[].actions` — массив строк**, а не объектов с `action_name`.
5. Запись на площадку — только по отдельной прямой команде (CLAUDE.md, правило 7 габаритов
   распространяется на все потоки).

---

## 1. Юнит-экономика: из чего складывается рубль (июль 2026, оба аккаунта)

### 1.1 Цепочка цены

```
old_price (зачёркнутая)
   ↓  наша скидка
price = НАША цена  ──────────────────── от неё считаются комиссия и выплата
   ↓  скидка за счёт Озона (баллы, кредитка Озон Банка, AI benefit system)
customer_price = что платит покупатель  ── от неё считается ИНДЕКС ЦЕН
```

Факт июля, строки `quantity = 1`, доставленные:

| | acc1 | acc2 |
|---|---|---|
| наша цена | 7 986 238 ₽ | 1 931 392 ₽ |
| заплатил покупатель | 4 567 410 ₽ (**57 %**) | 1 661 653 ₽ (86 %) |
| доплатил Озон | **3 418 828 ₽** | 269 739 ₽ |
| выплата нам | **59 % от нашей цены** | **59 % от нашей цены** |

**Вывод:** выплата не зависит от того, сколько заплатил покупатель. Механика тождественна
СПП на ВБ: скидку покупателю финансирует площадка, наша база — наша цена.

### 1.2 Статьи расходов (транзакции июля, % от `accruals_for_sale`)

| Статья | acc1 | acc2 | Комментарий |
|---|---|---|---|
| комиссия за продажу | 40.5 % | 40.5 % | главный расход |
| реклама | 7.6 % | 7.6 % | см. раздел 3 |
| логистика | 2.8 % | 2.8 % | |
| подписка Premium | 2.8 % | 1.1 % | `PremiumMembershipCommission` |
| **баллы «Звёздные товары»** | **1.5 %** | **1.5 %** | см. 1.3 |
| эквайринг | 0.6 % | 0.6 % | |

### 1.3 Баллы — как считаются (ПРОВЕРЕНО 07.08)

- Приходят **отдельной транзакцией**, а не строкой внутри продажи. Сервис
  `ItemAgentServiceStarsMembership` («Звёздные товары»), привязка к продаже — по
  `posting.posting_number` и `items[].sku`.
- **Ставка ровно 1.5 % от суммы продажи, на 100 % отправлений** (1464 из 1467 = 1.5 %,
  3 отправления = 0.8 %). Не 20 %.
- **От зоны индекса НЕ зависят** — проверено по всем зонам:

| Зона | SKU | Выручка | Баллы | % от выручки |
|---|---|---|---|---|
| SUPER | 405 | 9 306 188 | 140 023 | **1.5 %** |
| YELLOW | 108 | 4 775 538 | 71 649 | **1.5 %** |
| RED | 162 | 3 112 670 | 46 883 | **1.5 %** |
| GREEN | 107 | 2 737 897 | 41 377 | **1.5 %** |
| WITHOUT_INDEX | 52 | 1 984 960 | 29 775 | **1.5 %** |

**Вывод: баллы — плоский налог на оборот, а не рычаг.** Зелёный индекс баллами не
«оплачивается» и на их размер не влияет. 20 % на тарифе Premium — это кэшбэк
на стороне ПОКУПАТЕЛЯ, его платит Озон; в наших транзакциях этой суммы нет вовсе.

### 1.4 Цена покупателя — ЗАКРЫТО 07.08 (шаг 1 выполнен)

**Прямого источника в Seller API нет.** Проверены пять эндпоинтов, все отклонены:

| Источник | Что отдаёт | Вердикт |
|---|---|---|
| `/v5/product/info/prices` | поля `marketing_price` в схеме больше нет; `marketing_seller_price` = 0.969 × `price` | наша же скидка |
| `/v3/product/info/list` | тот же блок `price` | цены покупателя нет |
| отчёт по товарам (async) | «Текущая цена с учетом скидки» = 1.049 × наша; «Цена Premium» пуста у всех 22 389 строк | не то |
| `/v1/actions/products` | `action_price` ≈ 0.90 × наша | цена в акции, не витрина |
| `/v1/product/info/discounted` | пусто | только уценённые |

**Восстанавливаем по факту продаж:** `buyer = our_price × k`, где
`k = customer_price / price` из `raw_ozon_posting.financial_data`. Точная цена покупателя
есть в постингах и закрывает **92 % выручки acc1 / 87 % acc2** за июль — остальному
каталогу k присваивается по медиане аккаунта.

**Почему модель законна (замеры 07.08):**
* субсидия Озона — **процент, а не фиксированная сумма**: корреляция суммы субсидии
  с ценой **+0.97**, корреляция k с уровнем цены **−0.09** (k ≈ 0.54…0.62 во всех корзинах);
* k устойчив во времени: `|k(июль) − k(июнь)|` медиана **0.032**, у 91 % SKU < 0.10;
* **снижение нашей цены доходит до покупателя**: на 59 SKU с изменением цены июнь→июль
  передача Δ медиана **1.17** (p25 0.96), корреляция **+0.76**.

**Следствие — заморозка списка 285 SKU СНЯТА.** Прежний вывод «снижение нашей цены
не обязано двигать индекс» опровергнут: Озон свою скидку в ответ не убирает, формула
`скидка = 1 − 1.05/индекс` рабочая и скорее консервативная. Условие применения теперь
одно — проверка по марже (KPI 25 %, шаг 2) и пилот, а не отсутствие данных.

**Витрина:** `mkt_ozon_buyer_price` (миграция 113, `reports/ozon_buyer_price.py`),
журнал прогонов `mkt_ozon_buyer_price_run`. Снимок 06.08: acc1 22 413 строк (415 с k
по своему SKU), медиана k 0.572; acc2 13 456 (141), медиана k 0.549.

**Честная точность (holdout: k по продажам старше 14 дней → проверка на свежих):**

| | n | медиана предсказ./факт | в ±10 % |
|---|---|---|---|
| k по своему SKU (acc1) | 142 | 0.999 | 66 % |
| k = медиана аккаунта (acc1) | 89 | 0.949 | 49 % |
| k по своему SKU (acc2) | 30 | 0.958 | 53 % |
| k = медиана аккаунта (acc2) | 42 | 0.916 | 45 % |

Смещения нет (медиана ≈ 1.0), но попадание в ±10 % ограничено сверху **самой природой
цены**: при ОДНОЙ и той же нашей цене у одного SKU разные покупатели видят разные цены —
размах k внутри пары (SKU, цена) медиана **0.080**, коэффициент вариации 5.5 %, совпадают
все продажи лишь у 3 % пар. То есть цена покупателя — не число, а распределение
(Premium, кредитка Озон Банка, персональные скидки). Точность на уровне медианы —
предел, а не недоработка; для управления ценой этого достаточно.

---

## 2. Контроль маржи — ЗАКРЫТО 07.08 (шаг 2 выполнен)

Витрина `mkt_ozon_margin_control` (миграция 114, `reports/ozon_margin_control.py`),
KPI — чистая ≥ 25 % от НАШЕЙ цены (база комиссии Ozon), не от цены покупателя.

**Модель на штуку.** `to_pay = наша цена × payout_ratio`, где `payout_ratio = payout / price`
измерен по фактическим продажам (`financial_data`, `payout = цена − комиссия`, логистика в
payout НЕ входит). Дальше минус логистика, хранение, приёмка, возвраты, прочее — все на штуку
из витрины fin `margin_by_sku` (read-only), делённые на штуки из доставленных постингов:
у Ozon `qty` в витрине NULL by design. **`other` у Ozon уже содержит рекламу, баллы, подписку,
эквайринг и штрафы** (`reports/margin_ozon_sku.py`) — отдельно НЕ добавлять, будет двойной счёт.
Две себестоимости рядом, как на ВБ: живая `tc_buy_price` (решения) и FIFO (справочно) + дельта.
Маппинг живой себести: прямой `offer_id`, иначе 4-значный префикс кода товара (`buy_map_source`).

**Предел снижения** (главный ответ шага): `price_at_threshold = (постоянные на штуку + себест) /
(payout_ratio − порог)` — цена, при которой маржа падает ДО 25 %, а не до нуля;
`discount_limit_pct` — сколько процентов от текущей цены это даёт. Допущение: постоянные на
штуку не зависят от цены (реклама внутри `other` на деле частично пропорциональна цене →
оценка консервативна, в нашу пользу).

**Пересечение с индексом:** `price_for_target` из `mkt_ozon_buyer_price` (цена для GREEN) против
`price_at_threshold`. `verdict`: `можно_снижать` / `не_укладывается` / `уже_зелёный` /
`нет_индекса` / `нет_себеста`.

**Сверка с фактом fin:** воспроизводит реализованную маржу июля с точностью +0.7 п.п. (acc1,
541 SKU) и +0.5 п.п. (acc2, 219 SKU) — систематического перекоса нет.

| | acc1 | acc2 |
|---|---|---|
| карточек | 22 413 | 13 456 |
| маржа посчитана | 16 719 | 10 583 |
| медиана маржи-live | 35.1 % | 37.5 % |
| ниже KPI 25 % | 1 308 | 594 |
| отрицательная | 254 | 10 |
| GREEN укладывается в KPI | 1 993 | 3 351 |
| GREEN не по карману | 187 | 44 |

Где GREEN не по карману — это не скидка, а смена поставщика или вывод позиции.

**Дашборд** (:8092, только просмотр, на Ozon ничего не отправляет): левое меню разделено на
«Маркетинг · WB» и «Маркетинг · Ozon», у Ozon три страницы — `/ozon-economics` (юнит-экономика
за последний полный месяц), `/ozon-margin-control` (вкладки: ниже порога / отрицательная /
можно снижать / не укладывается / нет себеста / все), `/ozon-bids` (кампании + ставки по SKU
рядом с маржой и пределом снижения).

---

## 3. Реклама: что мы видим и чего не видим — ПЕРЕСОБРАНО 07.08

Прежние цифры этого раздела были неверны: собирался огрызок кабинета. Что чинили —
в `migrations/115_ozon_bids_coverage.sql` и в шапках обоих коллекторов.

**Расход теперь сходится с деньгами до рубля.** Источник — `GET /statistics/daily/json`
(асинхронный ZIP для «Оплаты за заказ» не строится вовсе, а прежний фильтр по
`RUNNING` терял кампании, остановленные внутри месяца). Сверка с транзакциями
(`raw_ozon_transaction`, «Продвижение с оплатой за заказ»): июль acc1 541 441 ₽,
acc2 97 783 ₽, июнь acc2 106 724 ₽ — **delta 0 ₽** во всех закрытых месяцах.
В августе расхождение −22 001 / −1 449 ₽ — транзакции отстают в открытом месяце.

Было: июль acc1 «410 867 ₽». Стало: **711 089 ₽**. Разница не в методике — строки
июля были собраны 17.07 и покрывали 01–16.07, то есть половину месяца, неотличимую
от целого. Отсюда колонка `covered_to`: по какой день период реально закрыт.

### 3.1 Два вида кампаний, июль (пересобрано)

| Аккаунт | Тип | Кампаний | Расход | Приписанная выручка | ДРР |
|---|---|---|---|---|---|
| acc1 | Трафареты (`SKU`) | 7 | 169 648 ₽ | 6 205 672 ₽ | 2.7 % |
| acc1 | Оплата за заказ (`ALL_SKU_PROMO`) | 1 | **541 441 ₽** | **не отдаётся** | — |
| acc2 | Трафареты | 5 | 62 610 ₽ | 1 432 555 ₽ | 4.4 % |
| acc2 | Оплата за заказ | 1 | 97 783 ₽ | **не отдаётся** | — |

ДРР по кабинету (расход / выручка `margin_by_sku`): acc1 июнь 5.7 %, июль 6.7 %,
август-по-07 8.1 %; acc2 6.9 / 6.5 / 7.5 %. Тренд на acc1 — вверх, при том что
чистая июля (3 056 411 ₽) ниже июньской (3 182 965 ₽) на почти ту же величину,
на какую вырос расход. Это первый кандидат в работу по шагу 4.

**Дырка №1 (открыта): на «Оплату за заказ» уходит 76 % рекламного бюджета acc1
(541 441 из 711 089 ₽), и выручки по этим деньгам нет ни в одном источнике.**
76 % — это ДОЛЯ БЮДЖЕТА, а не ставка; сама ставка модели — **7 % с заказа**.
Проверены три пути, все пустые: `daily/json` отдаёт `orders=0, ordersMoney=0`
при реальном расходе; `POST /statistic/orders/generate/json` строит пустой отчёт;
`POST /statistic/products/generate/json` отдаёт нули и `PromotionStatus: "Выключено"`.
Оценка «выручка ≈ расход / 0.07» остаётся оценкой — как факт её не подавать.

**Дырка №2 (открыта): органику посчитать нельзя.** Трафареты приписывают себе
6 205 672 ₽ = 59 % всей выручки acc1 при расходе 1.6 % от неё. «Оплата за заказ»
по своей ставке 7 % набирает 541 441 / 0.07 ≈ 7.7 млн ₽ — ещё 73 %. В сумме
**132 % выручки кабинета** (acc2: 58 % + 56 % = 114 %), то есть один и тот же заказ
считают обе кампании, а сколько продалось бы само, не видно вовсе. Развести можно
только опытом: выключить кампанию на группе SKU и смотреть выручку — это шаг 4.

### 3.2 Покрытие ассортимента — противоречие снято

Снимок `ozon_bids` от 07.08 берёт ВСЕ кампании (кроме архивных и завершённых)
и весь их состав постранично:

| | было (17.07) | стало (07.08) | продавалось в июле | из них в кампаниях |
|---|---|---|---|---|
| acc1 | 209 SKU | **10 552 SKU** (14 035 строк, 7 кампаний) | 749 | **704** |
| acc2 | 145 SKU | **4 334 SKU** (5 239 строк, 5 кампаний) | 292 | **223** |

Прежнее «Трафареты приписывают 63 % выручки, но продавались всего 19 их SKU»
объяснялось не атрибуцией, а обрезанным снимком: в кампаниях лежат 704 из 749
продававшихся SKU acc1, на них 9 661 453 ₽ = **91 % выручки месяца**. Причина
обрезки — `GET /campaign/{id}/v2/products` без пагинации молча отдаёт ровно
30 строк (в кампании 12704286 реально 8 093 товара) и фильтр по `RUNNING`
(9 кампаний из 33 на acc1).

### 3.3 Продвижение в поиске — не используется вовсе

`SEARCH_PROMO` и `ALL_SKU_PROMO` на `/v2/products` отвечают 400: у них
`POST /campaign/search_promo/v2/products`, и он отдаёт не состав кампании,
а весь пул товаров аккаунта (выдача одинакова для любого `campaignId`, включая
несуществующий) → отдельная таблица `ozon_search_promo`.

acc1: 3 302 SKU в пуле, доступны к продвижению 2 547, **участвуют 0**, ставки 0.
acc2: 4 448 / 3 906 / **0**. Оттуда же приходит индекс видимости (строка, бывает
ведро «10+»): в «10+», то есть за пределами топ-10 выдачи, сидят 3 035 SKU acc1
и 4 077 acc2 — сырьё для шага 5.

---

## 4. Поисковые фразы: что они дают и чего не дают

Собрано: `ozon_search_query` — 842 435 строк, недели 22.06–03.08, acc1 113 028
уникальных фраз / 12 345 SKU, acc2 86 329 фраз / 9 083 SKU.

**Что видим:** фразы, которые Озон привязал к НАШИМ карточкам, с позицией, показами,
конверсией в просмотр, заказами и GMV. В том числе строки с `position = 0` —
7 % acc1, 12 % acc2: фраза к карточке привязана, но мы по ней не ранжируемся.
Это и есть ближайший к «где нас нет» слой, который у нас есть.

**Чего НЕ видим:**
- фразы, по которым наш товар Озон вообще не привязал к выдаче — то есть спрос,
  где нас нет как класса. Эндпоинт отдаёт запросы **по нашим товарам**, не рынок целиком;
- **по каким фразам ранжируются конкуренты** — таких данных Ozon Seller API не отдаёт
  ни на одном тарифе. Косвенно: наш `position = 0` при живом спросе = там кто-то другой;
- связки «фраза → расход рекламы». В Трафаретах нет отчёта по фразам, поэтому
  «мусорные фразы, которые едят бюджет» **напрямую не видны**. Реконструкция возможна
  только через `position`/`order_count` из поиска против расхода кампании — грубо.

**Ловушка (проверено):** наивный отчёт «спрос есть — показов нет» даёт мусор,
топ фраз — «товары за 1 рубль», «для», «дождевик». Нужен фильтр релевантности профилю
до любых выводов.

---

## 5. Свежесть источников (на 07.08)

| Таблица | Последнее | Регулярность |
|---|---|---|
| `ozon_search_query` | 06.08 | cron, среда 03:23 UTC |
| `ozon_price_index` | 06.08 | cron, ежедневно 04:11 UTC (первый автозапуск 07.08) |
| `ozon_ads` | июнь–август, `covered_to` 07.08 | cron, ежедневно 05:47 UTC |
| `ozon_bids` | 07.08 (весь состав кампаний) | cron, ежедневно 05:47 UTC |
| `ozon_search_promo` | 07.08 | cron, ежедневно 05:47 UTC (тем же коллектором) |
| `ozon_rating` | 17.07 | **вручную** |

Месяц, собранный в его середине, выглядит как целый — поэтому в cron не только ежедневный
прогон текущего месяца, но и **пересбор прошлого месяца 3-го числа** (`OZON_ADS_MKT_CLOSE`,
23:05 UTC → 3-е число, период `$(date -d "last month")`): иначе повторяется история июля —
410 867 ₽ вместо 711 089 ₽. Контроль — `covered_to`. Лог общий: `ozon_ads.log`.

---

## 6. План работ (порядок обязателен)

### Шаг 1. Юнит-экономика — цена покупателя ✅ СДЕЛАНО 07.08
Прямого источника в API нет (пять эндпоинтов проверены), цена восстановлена
из фактических продаж: `mkt_ozon_buyer_price`, миграция 113,
`reports/ozon_buyer_price.py`. Подробности и точность — раздел 1.4.
**Заморозка списка 285 SKU снята**: субсидия Озона процентная и наши снижения цены
не гасит (передача Δ 1.17, корреляция +0.76).

### Шаг 2. Контроль маржи ✅ СДЕЛАНО 07.08
Витрина `mkt_ozon_margin_control` (миграция 114, `reports/ozon_margin_control.py`) + три
страницы Ozon в дашборде :8092. Даёт ответ «на сколько реально можем упасть» — по каждому
SKU, от KPI 25 %, а не от нуля. Цифры и сверка с фактом fin — раздел 2.
**Открытый хвост:** список 285 SKU на снижение ещё не пересчитан с пределом маржи
(пересечение `price_for_target` × `price_at_threshold` уже лежит в витрине как `verdict`),
и на Озон по-прежнему ничего не отправляем без прямой команды.

### Шаг 3. Реклама — частично СДЕЛАНО 07.08
1. **выручка «Оплаты за заказ» — не закрыто, и похоже, что закрыть нечем:** три пути
   проверены (`daily/json`, `orders/generate/json`, `products/generate/json`), выручки
   нет ни в одном. Вслепую тратится 76 % бюджета acc1 — это доля бюджета, ставка самой
   модели 7 % с заказа. Дальше — либо запрос в поддержку Озона, либо косвенная оценка,
   честно помеченная как оценка;
2. развести пересекающуюся атрибуцию, посчитать органику — **не закрыто**, чистого
   пути в API нет: только эксперимент на группе SKU (это шаг 4);
3. ✅ состав кампаний перезабран полностью, противоречие 3.2 снято (704 из 749
   продававшихся SKU acc1 — в кампаниях, 91 % выручки); расход сходится с транзакциями
   до рубля; добавлены `state`, `covered_to`, `orders` и пул `ozon_search_promo`;
4. ✅ cron поставлен 07.08: `OZON_ADS_MKT` — ежедневно 05:47 UTC реклама + ставки + пул
   поиска; `OZON_ADS_MKT_CLOSE` — 3-го числа 05:23 UTC пересбор закрытого месяца.
   Обе команды прогнаны руками end-to-end, июль пересобран (711 089 / 160 393 ₽).

### Шаг 4. Ставки
Только после шага 3. Логика как на ВБ: поднимать там, где маржа после рекламы
держит 25 %, резать там, где ДРР съедает guardrail. Ставки — расчёт и предложение;
**отправка на площадку — по отдельной команде.**

### Шаг 5. Спрос и видимость
Витрина Б: `position = 0` + живой спрос + релевантность профилю → список
«где нас нет, а спрос есть». После шага 4, потому что часть таких дыр закрывается
ставкой, а не контентом.
