# Рабочий лог — ночная задача COGS (2026-06-30)

ТЗ: `docs/NIGHT_TASK_COGS_20260630.md`. Цель — себест КОНКРЕТНОЙ отгрузки из МС, не усреднение.

## КЛЮЧЕВОЙ РАЗВОРОТ (проверено эмпирически, подтверждено клиентом)

Мёртвая сессия a11ce059 заключила: «себест отгрузки = `report/stock` на ДАТУ по позициям»
(дало 275.99 на эталон-отгрузке 4747758355 при «МС 284»). **Это оказалось приближением, а не
фактом.** Проб на той же отгрузке:

| Источник | Σ себест отгрузки 4747758355 |
|---|---|
| `report/stock/all?moment=11.03` (stock-на-дату) | **275.99** (приближение, ремейн-сток avg) |
| `report/profit/byproduct` усреднён за март | 285.00 |
| **`report/stock/byoperation?operation.id=<demand>`** | **284.00** ✓ = эталон «МС 284» |

**ВЕРНЫЙ ИСТОЧНИК (клиент 2026-06-30: «в отгрузке себест уже рассчитана, возьми ровно её»):**
`GET /report/stock/byoperation?operation.id=<demand_id>` — «Остатки по документу». Возвращает
`rows[0].positions[].cost` (копейки, ИТОГ с учётом quantity) — себест FIFO **на `moment` документа
отгрузки** (для Отгрузки/Розн.продажи/Приёмки/Возвратов считается на moment док-та; док.
МойСклад подтверждает). Σ positions.cost/100 = себест всей отгрузки = ровно то, что МС показывает
в документе. Эталон 4747758355: T036 12600коп + T037 15800коп = 28400коп = **284.00 ₽** ✓.

- `report/profit/byoperation` — НЕ существует (404 «Неопознанный путь»). Не путать с byproduct.
- Дубль-позиции в byoperation агрегируются — нам ОК, т.к. суммируем cost по документу.

## Объём бэкофилла
distinct FBS отгрузок (=assembly_id): wb_acc1 (Цифровой) 10 002, wb_acc2 (Дисквэр) 6 005.
1 call/отгрузка → ~16k вызовов, ~20–30 мин при лимите МС 45/3с. Кэшируем (идемпотентно).

## План реализации
1. Миграция `027_ms_demand_cogs.sql`: `ms_demand_cogs(demand_id PK, demand_name, org, moment, cogs, qty, loaded_at)`.
2. `collectors/ms_demand_cogs.py`: list demand (org+agent «Покупатель ВБ», окно moment) → name→id;
   для нужных/некэшированных → `stock/byoperation` → UPSERT. Сырьё отдельно от расчётов.
3. `reports/margin_by_sku.py`: источник COGS заказа = `ms_demand_cogs` (матч assembly_id=demand_name
   НАПРЯМУЮ, не cost_seb, не окно). FBO-импутация без изменений.
4. Сверка: эталон `cogs_actual` (WB Цифровой янв-апр) + контроль nm 216421567 май.

## Статус
- [x] Источник найден и подтверждён (284 ✓)
- [x] Миграция 027 (ms_demand_cogs)
- [x] Коллектор collectors/ms_demand_cogs.py + бэкофилл (acc1 10002, acc2 6005, потеряшек 0)
- [x] Перестройка margin_by_sku (источник COGS = ms_demand_cogs, покрытие 96–99%)
- [x] run_daily: шаг сбора себеста отгрузок
- [x] Сверка с эталоном

## РЕЗУЛЬТАТ СВЕРКИ (WB Цифровой, acc1)

| мес | старый cost_seb | НОВЫЙ byoperation | эталон | Δнов% |
|---|---|---|---|---|
| Янв | 946 050 | 1 062 711 | 946 376 | +12.3% |
| Фев | 1 060 372 | 1 150 170 | 1 284 369 | −10.4% |
| Мар | 925 276 | 976 954 | 1 113 371 | −12.3% |
| Апр | 885 199 | 944 305 | 852 235 | +10.8% |
| **ИТОГ Я–А** | 3 816 897 (**−9.0%**) | **4 134 140 (−1.5%)** | 4 196 351 | |

- **По СУММЕ перестройка дала цель: −9.0% → −1.5% к эталону.** Себест отгрузки теперь точный.
- Контроль nm 216421567 (май): net 3583 ≈ ориентир 3909; cogs/шт 272–293 стабильно.
- bckfill: ms_demand_cogs acc1 Σ6.77М (qty=0 edge 289), acc2 Σ6.18М (edge 247).

## ОТКРЫТО (требует решения клиента — НЕ дефект себеста)
Помесячная разбивка ±10–12% в РАЗНЫЕ стороны (Янв/Апр перебор, Фев/Мар недобор) = вопрос
**атрибуции месяца**: наш пайплайн → месяц реализации WB (по rr_dt, как выручка); файл-эталон →
по дате документа МС (отгрузки). Лаг отгрузка→выкуп сдвигает COGS между соседними месяцами.
Решение — финализировать «период = дата формирования отчёта» (согл. модель, см. project_mp_wb_data_model)
и пересобрать И выручку, И COGS на ней. Это отдельный шаг, не входил в «сменить источник себеста».

## ПРОДОЛЖЕНИЕ 2026-07-06: январь по МОДЕЛИ ФОРМИРОВАНИЯ сошёлся −1.0%

Проверка клиента: себест по КАЖДОМУ отчёту ВБ января-2026 по дате формирования (10 отчётов).
- FBS: Σ ms_demand_cogs по assembly_id отчёта, покрытие 100% (1126/1126) = **813 545**.
- FBO (160 шт): импутация **nm_id → cpu из margin_by_sku** (janCPU 129шт → cpu др.мес 26 → cost_seb 1,
  непокрыто 4) = покрытие **98%** = **123 213**. ⚠️ Баркод-путь для FBO МЁРТВ (баркодов нет в
  ms_barcode, 0/8), cost_seb-путь давал 29% — НЕ использовать.
- **ИТОГО 936 758 vs эталон 946 376 = −1.0%** (календарная модель rr_dt давала +12.3%).
- ВЫВОД: модель «период=дата формирования» + byoperation + FBO-по-nm-cpu ПОДТВЕРЖДЕНА эталоном.
- Инфраструктура: миграция 028 ms_demand_pos (позиции отгрузок), коллектор пишет позиции;
  бэкофилл позиций сделан только для января acc1 (1126) — остальное по мере надобности.
- Скрипты анализа: scratch_jan_cogs.py (FBS по отчётам), scratch_jan_fbo3.py (FBO v3, рабочий),
  scratch_backfill_pos.py (докачка позиций: jan|all). Не в git (scratch-конвенция).

## PUSH
НЕ пушено. Ветка cogs-byoperation, локальные коммиты. Ждём ОК.
