# rev — архив брифа (перенесено 12.09.2026)

## Итоги 10.09.2026

- **Справочник знаний по брендам `brand_notes` — влит (`370b40c`), сервис на HEAD.** Миграция 707
  (`brand, series_pattern, topic, text, source, updated_at`; `brand='*'` — общие правила магазина),
  модуль `reports/brand_notes.py`, загружено 46 записей из выгрузки Сергея (в задаче значилось 60 —
  в файле их 46: 39 по брендам + 7 общих). Зачем: контролёр резал как выдумку то, что мы про серию
  знаем достоверно («чип 067H одноразовый», «гарантия на заправку не распространяется»), потому что
  этих фактов не было ни в карточке, ни в одном источнике промпта, а движок добирал их из веба.
  Приоритет источников закреплён порядком блоков промпта: **CARD_DATA > brand_notes >
  approved_answers > веб**; справочник уходит и во ВХОДНЫЕ ДАННЫЕ контролёра (лимит inputs
  2000 → 4000 символов). Запись цепляется по бренду + коду серии из CARD_DATA или вопроса; записи
  без серии — по теме; чужая серия в промпт не попадает (тест). Веб-поиск не зовём вовсе, если по
  бренду и теме запись уже есть. Пополнение с телефона — `/note_add бренд | серия | тема | текст`.
  **Замер на тех же 20 строках oz_acc2 (dry-run, в БД не писалось): FIX контролёра 6 → 4**, пять из
  шести прежних снялись (одноразовый чип, гарантия на заправку, «в теории можно», Canon GX4040),
  остался №274 «страна производства Китай». Появились 3 новых FIX на других строках — генерация
  между прогонами не детерминирована, и каждый раз находится своё: «артикул M21-750-499 не из
  карточки», публичное обещание замены, снова «китайское производство». **Вывод: тема «страна
  производства / производитель» не закрыта ничем — ни карточкой, ни справочником; это кандидат
  №1 на следующую партию записей.**

- **Связи номенклатуры `sku_relations` — влиты (`e2e73a3`), сервис на HEAD.** Миграция 705,
  `reports/sku_relations.py`, шаг **1d** цикла (гейт по `compat_index`: связи строятся из индекса,
  значит протухают вместе с ним). 78 393 строки на 29 575 лотах: `chip_pair` 1 938 (879 лотов),
  `drum_toner` 24 851, `kit_component` 51 604. Правила: chip_pair — бесчиповый → тот же код с чипом,
  **только в одну сторону** (предлагать вместо готового лота бесчиповый — вредный совет);
  drum_toner — семейство кода (DR/DK-#### ↔ TN/TK-####) плюс пересечение списков совместимости там,
  где коды из разных серий (HP, Xerox, Konica); kit_component — комплект по названию, составные по
  кодам внутри него. Связь всегда внутри канала покупателя (площадка+аккаунт), как
  `catalog._same_channel`; не больше трёх соседей одного типа на лот; в паре «барабан→тонер» первым
  идёт одиночный картридж, а не комплект. В ответах: блок «СВЯЗАННЫЕ ЛОТЫ» в `CARD_DATA` и
  детерминированная строка «Если оригинала нет — есть версия с чипом: <ссылка>» в `_store` — обе
  подставляются, только когда спросили именно про соседний лот; **связи нет → не добавляется ничего**.
  Проверено: 3815 (W2122X) → 4764 во всех пяти каналах, 3896/38962 (W1360A) → 6816,
  2750 (DR-2085) → одиночный TN-2085. Регрессия 78/0, `tests/test_sku_relations.py` (8 тестов).
- **`_CODE_RX` в `card_facts.py` расширен:** HP W-коды (`W1360A`, `W2122X`), Xerox `\d{3}R\d{3,5}`,
  Samsung `ML-####`, Kyocera `DK-`, Brother `DU-`. Голые цифровые коды Canon (067H, 069) в общую
  регулярку НЕ брать — «069» в тексте значит что угодно; их ловит парсер названия по нашей
  конвенции (`sku_relations.code_of_title`), покрытие кода по названию 55 % против 22 % регуляркой.
- **Отдельного атрибута «чип» у площадок нет** — доказано перебором: Ozon пишет его в rich-контент
  (11 254), хештеги (23 171), аннотацию (4 191) и комплектацию (4 384), ВБ — в характеристику
  «Комплектация», у Яндекса чип не размечен вовсе. Единственный детектор — `_classify_chip` по
  тексту; «без чипа» в НАЗВАНИИ сильнее описания (описание шаблонное и врёт «готов к печати»).
- **Причины блока на вопросах переведены в трейс, D3 отключён** (`277853d`).
- **Каталог ищет по обоим аккаунтам Ozon** (`_LOOKUP_ACCOUNTS`), сторож `check_after_run`,
  cron коллектора атрибутов — вс 04:15. Разбор — артефакт «Каталог по аккаунтам».
- **Артефакты дня:** «Каталог по аккаунтам» (cron + `_LOOKUP_ACCOUNTS`), «Очередь вопросов»
  (9 живых карточек модерации с полными текстами вопросов и черновиков).

## Блок G — кэш утверждённых ответов ВЛИТ 08.09.2026 (`344d156`)

`approved_answers` (миграция 702) по ключу «наш артикул + класс + ключ вопроса»: ✅ и правка →
запись, чтение в `feedback_today` ДО генерации; маршрут остаётся `review`. Инвалидация —
`/cache_drop <артикул> [класс]` и отпечаток карточки; текст из кэша всегда проходит A1.2,
`_qa_guards`, `_scrub_urls`. ЧЕСТНАЯ ЦИФРА: за 8 недель закрыл бы **4 вопроса из 177 (2 %)** —
покупает единообразие ответа по артикулу (067H ×3), не экономию. Разбор целиком —
`docs/handoff/archive/rev_2026-09-08.md`.

**Дальше по порядку части 2:** D (совместимость) → B (парсер моделей) → E → F → C → H;
I и J — последними. Ревью Codex после каждого блока, регресс-набор до и после.

