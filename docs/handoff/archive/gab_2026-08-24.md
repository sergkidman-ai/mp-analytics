# Архив потока gab — 24.08.2026

## Волна 1: запись 21.08, чистка старых значений, инцидент очереди вебхуков

**Записано 21.08** (`tools/gab_dims_apply.py`): 8961 карточка, отказ 4. Д/Ш/В в см строкой,
`volume` в литрах, `weight` в кг; журнал возобновляемый. Темп 100 карточек / 5 с — НЕ образец.
**Старые значения снесены по всему каталогу** (17 191 + 2 сироты): вне волны 77 % заполненных
несли шаблоны. Откат — `dims_backup_2026-08-21.csv`. **Сверка 22.08 перечиткой** (44 683
карточки): вне волны с размерами 0; наши 8936 из 8936 совпали. Детали — в архиве 22.08.

**Инцидент 21.08:** ~35 тыс. изменений за вечер забили очередь вебхуков МС — заказы МС→ТК
и статусы обратно частично не дошли. Итог — регламент массовых изменений в МС в `CLAUDE.md`,
общий для всех потоков: темп по сущностям, немые/значимые поля, окно 23:00–05:00, канарейка.

## Инцидент 23.08 — двойное касание при записи rapid (закрыт 24.08)

**Инцидент 23.08 — двойное касание (п.3 регламента нарушен).** В журнале 825 строк на
413 карточек: вечером остался жить процесс со СТАРОЙ пачкой 413 (до сужения), рядом запустили
второй с 412 — оба ждали окна и в 23:00 пошли вместе. Отсюда «объект уже изменён» на пачке 8
и два счётчика в логе. Каждая из 412 карточек тронута дважды; темп 825 изменений за 22 мин
≈ 2250/час — под потолком 3000/час, поля немые, повтора 21.08 не было.
Сверх пачки записалась `SFR-F6V18AE` — исключённый брак строки (плотность 1.13 г/см³).
**Закрыто 24.08:** габариты этой карточки снесены (ДШВ пусто, `volume` 0; **вес 0.19 остался** —
старого значения нет ни в одном срезе, число из той же бракованной строки, решить отдельно);
в `tools/gab_rapid_apply.py` поставлен flock-лок одного экземпляра (коммит `c00fcbf`), берётся
до `gate()` и держится всё ожидание окна, фаза `snapshot` тоже под локом.
