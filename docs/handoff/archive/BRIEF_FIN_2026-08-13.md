# Архив BRIEF_FIN — этап 2026-08-12/13 (ветка `fin/cogs-realized`)

## Себестоимость в «Отчётах МП» = только реализованный товар (2026-08-13, ветка `fin/cogs-realized`)

**Решение Сергея.** В отчётах МП строка «Себестоимость» считает себест ТОЛЬКО того товара, по
которому маркетплейс начислил выручку в этом же отчёте. Критерий — **факт начисления выручки в
отчёте**, а НЕ статус отгрузки и НЕ склад возврата. Гейты «Брак» / склад «Озон» к этой строке
отношения не имеют: они живут в разделе «Себестоимость», где брак остаётся убытком.
МП сторнировал выручку → сторнируем себест. Потерь от брака в отчётах МП быть не должно.

**Что нашли (замеры янв–июл 2026, зонды в scratchpad сессии):**

- Недосторнированный себест при старой логике (гейт `sell_budget`/склад):
  WB acc1 79 662 ₽, acc2 50 592 ₽, Ozon acc1 374 611 ₽, acc2 104 385 ₽ — **итого 609 252 ₽**
  (сторнируется лишь 35 % положенного). Маркет — отдельно ≈98,8 тыс. ₽ за один июль.
- «Товарные» возвраты Озона без денежного сторно — **артефакт помесячной нарезки**: из 172 таких
  отправлений (acc1, продажа в отчётах есть) у 164 денежное сторно ЕСТЬ, у 30 — в более раннем
  месяце. Вывод: триггер сторно = ТОЛЬКО денежная операция (`accruals_for_sale < 0`), товарная
  (`OperationReturnGoodsFBSofRMS`) как триггер не нужна, дублирует. Сторно ложится в месяц
  денежного сторно. 8 отправлений без денег нигде — себест остаётся в COGS (выручку не вернули).
- **Рассинхрон `status='done'` ↔ выручка (фев–июл):** WB 0; **Ozon acc1 204 отгрузки / 404 662 ₽,
  acc2 61 / 74 950 ₽** (2,9 % и 2,5 % себеста без единого рубля выручки); Яндекс 5 CANCELLED +
  2 PICKUP / 8 624 ₽. Поэтому берём выручку отчёта, а не статус.
- Витрина Яндекса фев–июл: cogs 2 850 024 ₽ против 2 426 617 ₽ по отгрузкам с выручкой → −423 407 ₽.

**Схема (согласована):**

1. Себест отчёта = `Σ cogs` отгрузок из `wb_/oz_/ya_cogs_demand`, у которых в ЭТОМ отчёте есть
   начисление выручки, **+ FBO-импутация** (только WB: FBO ~11–12,5 % оборота, документа
   «Отгрузка» в МС не создаёт). Разрыв 11–12,5 % во всех месяцах — это ровно FBO, не возвраты.
2. Сторно — по строкам возврата САМОГО отчёта (WB `supplier_oper_name='Возврат'`,
   Ozon `accruals_for_sale < 0`), без гейта склада и без требования документа `salesreturn` в МС.
3. Клик по строке «Себестоимость» → таблица отгрузок отчёта (отгрузка / дата / штуки / наша цена /
   себест / метод / статус, фильтр по статусу) — тем же рендером, что `/reports/cost`
   (`reports/mp_cogs_page.py`), но выборка по месяцу ОТЧЁТА, а не по месяцу отгрузки.
4. Возвраты без моста к себесту отгрузки (187 шт) — фолбэк «себест/шт по истории SKU» (ОК Сергея).

**Мосты `demand_name` (проверены):** WB = `assembly_id` строк финотчёта; Ozon = постинг, первые
2 сегмента (`48742608-0134-2` → `48742608-0134`); Яндекс = `orderId` (НЕ `id`).

**Долить до внедрения:** `*_cogs_demand` декабрём-2025 (иначе январский отчёт WB недосчитает 520
отгрузок: acc1 284, acc2 236); `raw_yandex_order` за янв–май (сейчас с мая, до этого мост пуст).

**`other`/`unreported` — статус НЕ врёт:** из 341 залипшей отгрузки WB ни одна не имеет строки
«Продажа» в финотчёте, новое правило отсекает их само. Нужен не фикс статуса, а **флаг по
возрасту**: 85 отгрузок acc1 (82 481 ₽) и 60 acc2 (70 238 ₽) старше нормального лага 7 недель.

**Следующий шаг:** правка `reports/margin_by_sku.py`, `reports/margin_ozon_sku.py`,
`collectors/yandex_monthly.py` + drill-down. Правило 7 CLAUDE.md (сторно COGS) меняется по всем
трём площадкам — после прогона сверить дельту в рублях по каждому месяцу.

## Внедрено 2026-08-12/13 (ветка `fin/cogs-realized`, worktree `.claude/worktrees/fin-cogs-realized`)

Себестоимость в отчётах МП = только реально реализованный покупателю товар; критерий — факт
начисления выручки В ОТЧЁТЕ, а не статус отгрузки. Потери брака в отчётах МП больше не возникают.

- `479d92b` **Ozon** (`reports/margin_ozon_sku.py`): триггер сторно = реверс выручки
  (`accruals_for_sale < 0` / `OperationAgentStornoDeliveredToCustomer`), товарная
  `OperationReturnGoodsFBSofRMS` больше не триггер; склад возврата и `salesreturn` в МС не гейт.
  Кап — бюджет «начислено штук минус сторнировано ранее» (`storno_budget`), только против двойного
  сторно. Зонд: за янв–июл acc1 199 пар (отправление, SKU) с денежным возвратом, все с продажей.
  Там же доводка статуса `other` старше 60 дней → `unreported` на рендере (`_ST` в
  `reports/ya_cogs_page.py`, используется `mp_cogs_page`, метка/фильтр у `oz_cogs_page`).
- `5ac91bc` **Яндекс** (`collectors/yandex_monthly.py`): себест только по заказам, которым Маркет
  выручку начислил; CANCELLED_*/RETURNED отброшены (мост `customerorder.name` = `id` stats/orders,
  2774/2918 = 95 %). Отброшено 379 заказов на 498 466 ₽.
- `210fd86` **drill-down**: `reports/mp_cogs_drill.py` (+ `/api/mp-cogs/detail`, JS на трёх
  страницах отчётов) — клик по ячейке месяца в строке «Себестоимость» раскрывает отгрузки отчёта.

**Дельта COGS янв–авг 2026 (минус COGS = плюс к чистой):** WB −135 370 ₽, Ozon −374 641 ₽
(acc1 помесячно −58 187 / −57 954 / −46 084 / −64 169 / −44 018 / **+18 060** / −39 806 / −8 031;
acc2 **+10 683** / −7 140 / −2 113 / −9 490 / −15 888 / −8 728 / −31 391 / −10 385 — плюсы там, где
сторно переехало в месяц реверса денег), Яндекс −324 365 ₽ (−5 032 / −8 241 / −24 839 / −40 416 /
−54 628 / −55 133 / −84 547 / −51 529). **Итого −834 376 ₽ COGS.**

**Покрытие списка drill-down vs строка COGS отчёта** (фев–июл ровное): WB acc1 89–91 %,
acc2 82–91 % (разрыв = FBO-импутация); Ozon acc1 96–98 %, acc2 98–103 %; Яндекс 81–91 %
(себест отчёта Маркета считается по `cost_seb` карточки МС от заказов, а раздел «Себестоимость» —
по FIFO отгрузок; расхождение источников — **открытый вопрос**).

**Бэкфилл ноября–декабря 2025 (`5f9d7a7`).** `_eff_since` в трёх коллекторах больше не подавляет
осознанный бэкфилл: если запрошенная дата раньше `min(ym)` в таблице, пропуск закрытого префикса
не поднимает `since`. Закрытые месяцы защищены фильтром `frozen` при записи, как и раньше.
Собрано за 2025-11/12: WB 5 890 отгрузок (2.60 млн ₽), Ozon 3 340 (5.37 млн ₽), Яндекс 438
(0.45 млн ₽ — вопреки прежней оценке «отгрузок нет», агент у Маркета за эти месяцы нашёлся).
Январь-2026 считается по отгрузкам, а не импутацией: покрытие WB acc1 63→**87 %**, acc2 51→**88 %**,
Ozon acc1 75→**97 %**, acc2 59→**68 %**. Остаток по Озону — 73 постинга, из них 63 FBO, где
отгрузки на постинг в МС не бывает по природе схемы (штатная импутация, не потеря).
**Побочный эффект:** ноябрь и декабрь 2025 теперь незаморожены, поэтому обычный прогон с дефолтным
`since='2026-01-01'` больше не поднимает границу до 2026-08 и будет тяжелее. Экономия вернётся,
когда человек закроет эти два месяца кнопкой в разделе «Себестоимость» (заморозка — ручное
действие, сам не делаю).

**Уточнение мостов:** `oz_cogs_demand.demand_name` = ПОЛНЫЙ постинг (3 сегмента, `48742608-0134-2`);
двухсегментный ключ — только внутри `margin_ozon_sku` для группировки. `ya_cogs_demand.demand_name`
= `id` заказа stats/orders.

## Себест Маркета переведён на FIFO (`7481a72`, 2026-08-13)

Решение Сергея: **единственный источник себестоимости отгрузки — FIFO**. Прежний расчёт строки
«Себестоимость» Маркета (`_ms_cogs_monthly`) брал Σ `products.cost_seb` × qty по позициям
МС-ЗАКАЗА — то есть среднюю себестоимость текущего остатка из карточки, а не стоимость
конкретного списания. Теперь читаем раздел «Себестоимость» (`ya_cogs_demand`, метод `ms_fifo`),
в расход идёт только `status='done'`. Месяц — по дате ЗАКАЗА (мост `demand_name` = `stats/orders.id`),
потому что по ней же нарезана выручка в `_fold_order`; нарезка по месяцу отгрузки роняла покрытие
до 69–115 %. `run_daily`: шаг `collectors.ya_cogs_demand` переставлен ПЕРЕД помесячной сборкой.

**Дельта COGS янв–авг 2026: −210 182 ₽** (чистая на столько выше). Покрытие drill-down 87–100 %,
`cogs_cov_pct` 100 %. Витрина пересчитана, 2026-01..07 переморожены, статика перерисована.
`_cost_map()`/`yandex_cost` (цепочка на `cost_seb`) остались в файле как аварийный фолбэк
`map_cogs` — срабатывает, только если `ya_cogs_demand` пуст (тогда в лог идёт явное предупреждение).

**Аудит «где ещё считается через `cost_seb`» (2026, деньги):**
- раздел «Себестоимость», метод `imputed` (FIFO вернул 0, обычно FBO): WB 10 отгрузок / 20 404 ₽,
  Ozon 37 / 45 161 ₽, Яндекс 0; плюс `manual` Ozon 20 / 14 882 ₽, Яндекс 1 / 511 ₽;
- `margin_by_sku` (WB), шаги 3–4 цепочки фолбэков (группа `cost_seb` по артикулу / по префиксу) —
  верхняя оценка по SKU без единого FIFO-матча: acc1 40 972 ₽ из 6.86 млн, acc2 11 022 ₽ из 5.76 млн
  = **0.4 % COGS**;
- `margin_ozon_sku`: acc1 8 694 ₽ из 16.38 млн, acc2 2 309 ₽ из 3.25 млн = **0.06 % COGS**.
Всё это — FBO/непокрытая ветка, где отгрузки на постинг в МС не существует и FIFO физически нет;
шаги 1–2 цепочки (cpu месяца, cpu истории nm) — производные того же FIFO, не карточки.
**Решение по этому хвосту — за Сергеем** (молча не менял).

## FIFO вместо cost_seb по всем площадкам + хвост закрытых месяцев (2026-08-13, перенесено из брифа 14.08)

## FIFO вместо `cost_seb` по всем площадкам (2026-08-13, ветка `fin/cogs-realized`)

Правило Сергея: **единственный источник себестоимости отгрузки — FIFO**; `products.cost_seb`
(средняя по ТЕКУЩЕМУ остатку из карточки МС) себестоимостью списания не является. Вычищено везде,
где считались деньги.

- `reports/fifo_fallback.py` — общий модуль. `unit(article, day)` (себест/шт по артикулу площадки:
  товар → набор как Σ FIFO компонентов), `unit_ms(ms_id, day)`, `impute(pos, day)` (себест документа
  по позициям). FIFO = ближайшая ПРЕДШЕСТВУЮЩАЯ отгрузка того же товара МС (если товар до этой даты
  не отгружался — первая после). Набор/документ отдаём ТОЛЬКО при полном покрытии состава: частичная
  сумма занизила бы себест, а занижение себеста завышает прибыль.
- Подключён в: `collectors/{wb,oz,ya}_cogs_demand.py` (метод `tovar_fifo` перед аварийным `imputed`),
  `web/app.py` (три кнопки «пересчитать»), `reports/margin_by_sku.py` (хвост WB),
  `reports/margin_ozon_sku.py` — слой 3 «FIFO товара» ВПЕРЕДИ старой групповой цены из карточки.
  Метка «FIFO товара» и фильтр по методу добавлены на страницы раздела и в дрилл.
- **Ozon после подключения (пересобран 2025-11…2026-08):** ветка групповой цены `cost_seb` — 4
  постинга за весь 2026 (было ~сотня); покрытие дрилла acc1 95–98 %, acc2 98–99 % (кроме января).
  A/B в одном процессе на одних данных (ветка FIFO выключена / включена): июль acc1 +4 096 ₽,
  acc2 +7 042 ₽, август acc2 +2 167 ₽ — FIFO даёт себест ЧУТЬ ВЫШЕ карточной средней, направление
  верное (карточка занижала). Витрина Ozon за 2025-11/12 была пуста — заполнена (acc1 1.38 / 1.80 млн).
- **Статика вкладки «Отчёты МП · Ozon» обновлена** новой командой
  `python -m reports.ozon_mp_freeze cogs [YYYY-MM …]`: она переносит из витрины ТОЛЬКО COGS и
  производные net/margin. Полный `freeze` для этого не годится — он откатил бы сверенные из Отчёта
  о реализации Продажи/Возвраты/Вознаграждение обратно в оценку по транзакциям.
  Янв–июл 2026: COGS 19 105 908 → 18 752 239, **−353 669 ₽ (чистая на столько же выше)**;
  acc1 −310 238, acc2 −43 431.
- **Починен цикл импорта, занесённый `210fd86`:** `ozon_mp_page → mp_cogs_drill → ya_cogs_page →
  ozon_mp_page` роняла ImportError любой вход, начинающийся с `ozon_mp_page` — то есть CLI
  `reports.ozon_mp_freeze` и его cron на границе месяца (`1 0 1 * *`). В `mp_cogs_drill` импорт
  хелперов страницы сделан ленивым (`_yp()`).
- **Грабли витрины:** ключ `margin_by_sku` включает `period_to`, поэтому пересборка текущего месяца
  окном «по сегодня» НЕ перезаписывает строки run_daily (окно «по конец месяца»), а кладётся рядом —
  август задвоился и дал фантомные +793 793 ₽. Пересобирать месяц ТЕМ ЖЕ окном, что и `run_daily`
  (`_month_bounds`). Дубли удалены, проверка «месяцев с >1 окном `period_to`» = 0.

**Ozon acc2, две аномалии покрытия — разобраны (пункт 3 закрыт):**
- **январь 68→70 %** — не потеря, а FBO: отгрузки на постинг в МС не существует по природе схемы.
  16 FBO-постингов (91 962 ₽) + 2 FBS, где Ozon перенумеровал третий сегмент постинга
  (в отчёте `0153778244-0032-13`, в МС `0153778244-0032-1`, 61 718 ₽). За весь 2026 таких FBO:
  acc1 361 постинг / 2 164 088 ₽, acc2 88 / 411 976 ₽; перенумерованных FBS — 7 / 224 288 ₽.
- **июнь 103 %** — не переполнение, а разная механика сторно: витрина сторнирует по
  всеисторическому `cpu_global`, раздел — долей себеста конкретного документа. Июнь acc2: раздел
  брутто 544 407 ₽, витрина брутто 553 862 ₽, витрина после сторно 519 200 ₽.

**Открыто:** 181 заказ Яндекса на 273 138 ₽ есть в МС, но отсутствует в сырье отчёта (учтены,
считаются счётчиком в логе); заморозить 2025-11/12 кнопкой; WB 82–91 % покрытия (FBO-импутация);
частичные возвраты WB/Ozon (статус ставится на отгрузку целиком); правило 7 `CLAUDE.md` переписать
после мержа. **В `main` не мержил — жду «ок».**

### Хвост `cost_seb` в закрытых месяцах пересчитан (по команде Сергея 2026-08-13)

Разморозил 2025-11…2026-07, пересчитал строки `method='imputed'` тем же путём, что кнопка
«пересчитать» (сначала своё FIFO документа из МС, затем FIFO тех же товаров МС), заморозил обратно
**теми же записями** — `closed_by`/`closed_at` сохранены, снимок в scratchpad-сессии
(`frozen_snapshot*.json`). Скрипт разовый, в репозиторий не кладу.

| площадка | строк | было | стало | Δ | стало `ms_fifo` / `tovar_fifo` |
|---|---|---|---|---|---|
| Ozon | 41 | 50 802 ₽ | 50 192 ₽ | −610 | 1 / 22 |
| WB | 16 | 30 134 ₽ | 26 994 ₽ | −3 140 | 0 / 11 |
| Маркет | 1 | 3 367 ₽ | 3 407 ₽ | +41 | 0 / 1 (это `ms_fifo`) |

Дельта меньше сухой оценки (Ozon −3 651, WB −6 250), потому что оценка была без повторного
обращения к МС и только по 2026 году; здесь окно шире (плюс бэкфилл 2025-11/12, 41 и 16 строк
вместо 37 и 10), и часть документов при перезапросе отдала СВОЙ FIFO.

**Остаток `imputed` (FIFO не существует нигде — товар ни разу не отгружался):** Ozon 18 строк /
25 165 ₽, WB 5 / 10 612 ₽, Маркет 0. Меньше не станет, пока такой товар не отгрузят.
Гейт перед обратной заморозкой проверен: отгрузок `status='done'` с нулевым себестом — 0 по всем трём.
Отчёты МП по WB/Ozon эти правки не двигают (там витрины `margin_*`, не раздел); у Маркета отчёт
читает `ya_cogs_demand` — там +41 ₽ в одном месяце 2025 года, пересборкой месяца ради 41 ₽ не дёргаю API.
