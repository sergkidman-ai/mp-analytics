# Поток rev — отработанное 25–26.08.2026

Вынесено из `docs/BRIEF_REV.md` 27.08.2026: всё сделано и не требует действий.

**Боевая публикация ВКЛЮЧЕНА обратно 26.08.2026 12:56 по команде Сергея** (`FEEDBACK_LIVE_SEND=1`,
все 5 аккаунтов), после работы №1. Была выключена 25.08 13:47 на время аудита.

**Работа №1 сделана 26.08.2026** — `reports/publish_gate.py`, проверка серверно в `post_answer()`
до всех прочих условий + вердикт в боте (кнопка ✅ и баннер причин). Прогон по истории: 81 из 128
черновиков-вопросов (63 %) удержаны, все 30 с `grounded=false` — удержаны, отзывы (4561) не тронуты.
7 из 8 эталонных кейсов удержаны; G-006 проходит — врёт сама карточка (B3), машинного сигнала нет.

**Дыра dry-run закрыта:** `sent` на `(True, "dry-run")` съел 9 одобренных ответов (25.08 13:47 —
26.08 11:01); теперь dry-run → `deferred` + `flush_deferred()`. Разбор — карточка памяти
`rev-dryrun-sent-trap`.

**Досыл 26.08 сделан:** 7 из 9 ушли покупателям, 2 гейт удержал — mod=500 (wb, «источник только
модель» + `grounded=false`) и mod=407 (ozon, чужой лот, дефект D4). Обе в `queued` с причиной в
`error`: отвечает оператор вручную, кнопки ✅ на них нет.

