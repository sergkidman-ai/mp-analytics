# Архив потока inv — этап 28.07…03.08.2026

Вынесено из `docs/BRIEF_INV.md` 03.08 (бриф перерос 150 строк). Здесь улёгшееся: детали
подключения Сбера, выписка → МойСклад и привязка платежей к приёмкам. Живое состояние —
в брифе.

## Сбер (Дисквэр): подключение и выписка

- Подключён самостоятельно в ЛК, бесплатно, сразу промышленный сервис (песочницы нет).
- Авторизация: `collectors/sber_auth.py` — OAuth 2.0 (интерактивный вход представителя по СМС),
  одноразовый `refresh_token` с атомарной ротацией, mTLS. Проверено: `client-info` HTTP 200.
- Выписка: `collectors/sber_statement.py` — счета из `client-info` (у Дисквэра 8, действующий
  один: `…147717` OPEN/calculated; 7 закрытых депозитов дают штатный 400 WORKFLOW_FAULT).
  Контроль 28–31.07: 11 операций, приход 3 095.34 ₽, расход 122 394.45 ₽.
- **Спецификация Сбера врёт в путях:** выписка живёт на `v2`, `/v1/statement/...` = 404, при этом
  `/v1/client-info` жив; платежи — на `v1`. Swagger машинно не отдаётся, пути искать перебором.
  Признак: «маршрута нет» → `NOT_FOUND`/`urlPath`, «документа нет» → `WORKFLOW_FAULT`.
- Мелочи, ломающие арифметику: `transactionId` у Сбера НЕТ (ключ = `uuid`), суммы приходят
  строкой → в нормализации `Decimal`.
- `tokens.json` привязан к каноническому чекауту: `refresh_token` одноразовый, две копии файла
  (основной чекаут + deploy-worktree крона) гасили бы токен друг друга.

## Выписка → МойСклад и привязка платежей к приёмкам (02.08)

- Ядро вынесено из `alfa_ms.py` в банконезависимый `collectors/bank_ms.py`; `alfa_ms.py` и новый
  `collectors/sber_ms.py` — тонкие обёртки. Прогон включён в `run_inv.py` вторым шагом.
  Боевая запись включена 02.08 по ОК Сергея (`SBER_MS_APPLY=1`). Контроль: боевой прогон
  01.07–02.08 (81 операция) создал 0 документов, счётчики Дисквэра до/после совпали — ручной
  ввод не отстаёт.
- Привязка платежей к приёмкам у Дисквэра включена 02.08 (`8a778b6`), тем же движком
  (`bank_ms.link_supplies`, `sber_ms` передаёт его в `link_fn`). Порядок разбора общий:
  черновик платёжки → номера документов в назначении → аванс FIFO. Мосты назначения под
  поставщиков Дисквэра не понадобились: состав берём из черновика.
- **Разрез по нашему юрлицу — несущая конструкция привязки** (миграция 207): поставщики у двух
  фирм ОБЩИЕ, суммы совпадают запросто, приёмки у каждой свои. Org-разрез добавлен в `alfa_link`:
  `org_inn(payment)`, `supplier_agents(our_inn)`, `_inn_of(…, our_inn)`, `find_draft`
  (`AND org_inn`), гейт в `link_payment` до разбора. Без него платёж Дисквэра разобрался бы
  черновиком Цифрового и сел на чужие приёмки.
- Контроль привязки (dry-run 452 исходящих с 01.05): ЦК 142 already / 5 no-match, Дисквэр
  92 already / 7 no-match. Все 12 no-match — авансы с остатком без неоплаченных приёмок.
- **Дефект антидубля, найден и закрыт:** `existing_index` фильтровал платежи только по дате →
  платёж одной фирмы мог «съесть» платёж другой на ту же сумму в тот же день (7 таких пар за
  май–авг). Теперь индекс всегда с фильтром по организации; регресса у Альфы нет.
- Грабли МС: в `filter=` символ `>` обязан быть percent-encoded, иначе HTTP 400 с пустым телом
  (штатный `urllib.parse.quote()` без `safe` — правильно; `safe=":/;=<>"` ломает запрос).
