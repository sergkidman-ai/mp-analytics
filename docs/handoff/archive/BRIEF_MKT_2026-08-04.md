# Архив BRIEF_MKT — состояние на 2026-08-04

Вынесено из брифа 07.08.2026 (правило диеты: бриф ≤150 строк). Живое состояние — docs/BRIEF_MKT.md.

## Текущее состояние (2026-08-04)
**Ветка:** работаем в основном чекауте на `main` (не worktree). Фича «Ставки WB».
**Сделано в этой сессии:**
- Восстановил в памяти слой управления ставками WB (`project_mp_wb_ads_endpoint`): CPC per-nmID,
  `PATCH /api/advert/v1/bids` (смена), `/bids/min` (пол 7.3₽ поиск/5.54₽ реко), **геттера текущей
  ставки НЕТ** → «текущая» = реконструкция факт.CPC=расход/клики из `wb_ad_nm` (только кликнутые SKU).
- **Починил сбор `wb_ads`:** причина простоя — `run_marketing.py` (единственный вызов `wb_ads`) не был
  ни в cron, ни в systemd; ежедневный mkt-трекер собирал только Джем+воронку. Добавил cron-строку
  `wb_ads` обе площадки (`52 6 * * *`, лог `wb_ads.log`). Бэкфилл август: acc1 87 камп/3250 nm,
  acc2 66 камп/2952 nm. Фильтр берёт статусы 9(действ.)+11(пауза) — архив не опрашивается.
- **Новый токен `WB_TOKEN_ADS_ACC1`** (Продвижение, write-enabled, acc1) в `.env` — под живую смену
  ставок. Чтение подтверждено (200, 198 камп); write подтвердится первым реальным PATCH.
**Фича «Ставки WB» — ГОТОВА и задеплоена (2026-08-04):**
- Миграция 110 применена: `wb_bid_override` (ручные/api текущие ставки per-nmID) + `wb_bid_log`
  (журнал смен + baseline Джема на момент смены).
- Эндпоинты в `marketing_app.py`: `GET /api/wb-bids` (картина: COALESCE(оверрайд, реконструкция
  из последних 2 периодов — берём свежий период С КЛИКАМИ, т.к. ставка держится per-nm) + свежий
  Джем; виды all/no_data/floor/raised/override), `POST /api/wb-bids/manual` (ручной ввод факта
  ставки + снимок Джема), `POST /api/wb-bids/change` (dry-run: пишет в журнал, в WB НЕ шлёт;
  гейт пола 7.3₽), `GET /api/wb-bids/log` (журнал + дельта позиции/заказов из свежего Джема).
- Вкладка `web/static/wb_bids.html` → пункт «💰 Ставки WB» в меню Маркетинг; таблица SKU с текущей
  ставкой+бейджем источника (восст./ручная/нет данных), инпут новой ставки, кнопки «Сохранить»
  (факт) и «dry», плюс журнал последствий внизу.
- nginx: `location = /wb-bids` + `location /api/wb-bids` (префикс — есть под-пути) → :8092;
  `nginx -t` ок, reload сделан. Сквозной smoke-тест пройден (ручной ввод/оверрайд/dry/гейт/журнал),
  тестовые строки удалены → журнал открывается пустым.
- Покрытие: acc1 3250 SKU в рекламе, на полу 468, без ставки (ручной ввод) 2644, ручных 0.
**Маржа-гейтед вид — ГОТОВ (2026-08-04), это основной полезный режим вкладки:**
- Причина: сортировка по позиции Джема бесполезна для решений; статистики per-SKU для микро-оптимизации
  ставок нет (0 SKU с ≥3 заказами/мес, ДРР общий 4.5% — реклама маржу не ест). Инструмент развёрнут
  под стратегию Сергея: маржа-гейт 25% (KPI от нашей цены) + потолок ДРР 10% (задан Сергеем).
- `GET /api/wb-bids?mode=margin` (по умолчанию) → `_margin_gated()`: решение по КАЖДОМУ рекл. SKU.
  **МАРЖА-ГЕЙТ = ЖИВАЯ** (`mkt_margin_control`, SELECT-only, `margin_own_live`): текущая цена × себест
  «купим сегодня» (TheCartridge). Помесячную `margin_by_sku` как гейт НЕ используем — реклама крутит
  сегодняшние цены, а помесячная маржа врёт на тонких месяцах (216421567 июль qty=1 → −22%, живая +34%).
  ГРАБЛИ: в `mkt_margin_control` ~14× дублей строк (166978/11927 уникальных) → джойн через
  `DISTINCT ON (nm_id)`, иначе всё размножается. `buy_status` no_price/unmapped → маржу не судим (⚪).
  `margin_by_sku` (SELECT-only, `article=nm_id::text`) остался ТОЛЬКО для флага «продавался» (⚫ dead).
  Рекл. агрегаты (spend/rev/clicks/orders → ДРР, CPO) — за ПОСЛЕДНИЙ ПОЛНЫЙ месяц (`_decision_period`;
  август MTD тонкий) + свежий Джем (позиция справочно).
- Вердикт `_verdict(sold,mp,drr,spend,ad_orders,repriced)`: 🟢 grow (маржа≥25 + реклама конвертит + ДРР<10 →
  поднять ставку) · 🟡 expensive (маржа ок, ДРР≥10 → вниз) · 🔵 keep (маржа ок, 0 рекл.заказов, цена ~прежняя
  → держать на полу) · 🟣 repriced (маржа ок, 0 рекл.заказов, но цена ≥25% ВЫШЕ той, по которой продавался —
  активность была из-за низкой цены → тест рекламой + смотреть склейку, не пойдёт — снять) · 🔴 cut
  (продаётся, маржа<25 → снять/поднять цену) · ⚫ dead (0 продаж/окно, реклама жгла → редкий-держим/снять)
  · ⚪ hold (нет сигнала).
- **Окно активности 3 мес** (`WB_ACTIVITY_MONTHS`, май–июль): продавался = qty>0 за окно (не только decision-мес).
- **Флаг repriced с порогом qty** (`WB_REPRICE_MIN_QTY=5`): дрейф = our_price(live) / (rev3/qty за окно). Базис q3<5
  = шум (продалась 1–2 шт по «кривой» цене возврата → дрейф +1000% мусор) → НЕ флагуем, уходит в keep.
  Из ~650 дрейф-кандидатов надёжных (q3≥5) только 79.
- **Склейка подмешана** (`raw_wb_card_content.payload->>'imtID'`): колонка ⛓ размер + продажи СОСЕДЕЙ за окно
  (`sib_q`). Сигнал: repriced с живой склейкой (соседи продают) — склейка вытянет, держим; мёртвая — снимаем.
- **⚠ Живая цена была ПРОТУХШЕЙ (снимок 20.07, разбор 2026-08-04 по 216448553):** в cron гонялся только
  `thecartridge_prices;margin_control` (себест свежая), а `wb_prices+wb_market_price+sku_economics` (ЦЕНА) НЕ в
  расписании → `wb_price`/`mkt_sku_economics` застыли на 20.07. Маржа = свежая себест ÷ старая цена = мусор
  (216448553: цена 9572 vs полка 4100, «маржа» 41% → после обновления 21.4% = 🔴 cut). Перекос по вселенной
  медиана ×1.36 (часть — реальное подорожание, часть была протухшая цена). ФИКС: цены обновлены, витрина
  пересобрана. **CRON-строка обновления цены применена (2026-08-04):** в `20 6,13` перед `thecartridge_prices`
  добавлены `wb_prices wb_acc1; wb_market_price wb_acc1; sku_economics` → теперь 2×/день освежаются ОБА входа
  живой маржи (цена И себест). Порядок: цена→economics→себест→margin_control, префикс `cd /opt/mp-analytics &&`.
- **Итог по июлю acc1 (8022 SKU, расход 13449₽, свежие цены):** ⚫ dead 554 = **6045₽ = 45% бюджета жгут SKU
  с 0 продаж**; 🔵 keep 713; 🟣 repriced 79; 🔴 cut 260 (1780₽); 🟢 grow 55 (1372₽); 🟡 0; ⚪ hold 6361 (~0).
- Фронт `wb_bids.html`: переключатель режима (🎯 Решения / 💰 Ставки), таб-бар вердиктов со счётчиками,
  сортировка по деньгам-в-игре (расход/ад-выручка/маржа/ДРР/заказы), колонки маржа%·маржа/шт·зак(рекл)·
  расход·ДРР·CPO·тек.CPC·Джем поз. Живая запись в WB отсюда НЕ идёт — только картина для отбора.
- nginx правок не потребовалось (`mode`/`sort` — query-параметры на существующих путях). Smoke-тест HTTP
  обоих режимов пройден.
**Сделано 2026-08-04 (живое управление ставками в mode=margin):**
- **Контракт `PATCH /api/advert/v1/bids` ПОДТВЕРЖДЁН** прямой пробой (не докой): тело
  `{"bids":[{"advert_id":<id>,"nm_bids":[{"nm_id":<nm>,"bid_kopecks":<коп>,"placement":"search"}]}]}`,
  auth-заголовок `Authorization: <token>` (без Bearer), значения в КОПЕЙКАХ, 200 эхом отдаёт ставку.
  `placement:"combined"` аукцион отклоняет — только `"search"`. **`WB_BID_LIVE_ENABLED=True`.**
- **Снятие nmID с рекламы — API НЕТ** (все пути 404, `/bids` = PATCH-only, DELETE→405 Allow=PATCH,
  namespace состава кампании за антиботом). Деградировано в **очередь**: `/remove` пишет
  `wb_bid_log action='remove' applied=false`, фактическое исключение — руками в ЛК.
- Бэкенд `marketing_app.py`: `_rec_cpc` — **мягкий шаг +10%** (`WB_STEP_PCT`), НЕ прыжок к потолку
  (Сергей 08-04: прыжок к ДРР 10% давал 40₽/клик → слив бюджета за часы). grow: `cpc×min(1.10, 10/drr)`
  пол 7.3; expensive: вниз к потолку `cpc×10/drr`; keep/repriced → пол «тест». Оценка в логе — через
  `WB_STEP_DAYS=2` дня (шагнул → ждём реакции → снова). `POST /api/wb-bids/apply` (живой PATCH+лог+override),
  `POST /api/wb-bids/remove` (очередь), `/log` расширен `age_days`/`due_review`/`eval_verdict`
  (оценка через 7д: «сработало / дороже впустую / нет эффекта»).
- Фронт `wb_bids.html`: 14-я колонка «действие → WB» (тек.CPC+источник · rec-кнопка · инпут ·
  применить/dry · снять для cut/dead), цветочип маржи (≥25 зел / [15,25) жёлт / [0,15) красн / <0 бордо),
  блок оценки в логе (⏰ пора оценить / «через Nд»). Деплой `mp-marketing`, read-smoke ✅.
- **Джем ×2 колонки (движение позиции)**: «Джем пред»/«Джем посл» = два последних суточных сбора
  + дельта (улучшение зел ↑ / ухудшение красн ↓). ID кампании (РК) в ячейке действия для ручной
  правки в ЛК. grow без факт.CPC → рекомендация = пол (старт теста).
- **Разбор «джем встал»**: НЕ вставал. `wb_jam.py` гоняется кроном ежедневно (06:42 UTC) и пишет
  свежее. `piso = cur["start"]` помечает строку датой НАЧАЛА скользящего 7-дн окна = `сегодня−7`,
  поэтому max(period_start) всегда выглядит на неделю назад — это норма, метка сдвигается на день
  ежедневно. Ярлык в UI переведён на реальную дату сбора (period_start+7). Позиция = средняя за
  7-дн окно → суточное движение сглажено (~1/7), годится для тренда, не для резких скачков.
**Сделано 2026-08-04 (боевой smoke + UI-полировка, всё в main):**
- **Живой end-to-end PATCH-smoke ПРОЙДЕН** (по «да» Сергея): один PATCH на ПРИОСТАНОВЛЕННОЙ
  кампании 35701146 / nm 199332171 на полу 7.3₽ (расход не меняется) → applied=true, resp 200,
  запись в `wb_bid_log` (action=api_set). Контракт замкнут end-to-end. **Фичу можно юзать в боевую.**
- Полировка вкладки (коммиты `2bde071 917d095 6ade9ec d42f2ac 89bd7b7 80740c0 febd40c`):
  таблица режима «Решения» ужата до 7 колонок (SKU · вердикт · марж% · ДРР · Джем пред · Джем посл ·
  действие); дубль CPC убран (осталась «текущая» в ячейке действия); название картриджа расширено 20→40.
  **Кнопка «dry» удалена** (живая запись подтверждена — предпросмотр не нужен). Клик по **артикулу ВБ**
  и по **РК** копирует в буфер (флеш-подсветка). **РК перенесён в колонку SKU** (справа от артикула).
  **Баг «после боевого применения следующая позиция не срабатывала до F5» починен:** после «применить»
  строка обновляется НА МЕСТЕ (зелёная подсветка) вместо полной перезагрузки таблицы; слушатель кликов
  на стабильном `#tbl`; блокирующий alert успеха убран.
**Сделано 2026-08-04 (МАССОВЫЕ ПРАВКИ — готовы, в main):**
- Бэкенд `marketing_app.py`: `POST /api/wb-bids/apply-bulk` (`WB_BULK_MAX=500`) — группирует items по
  `advert_id`, шлёт по одному `_wb_patch_bids_group` на кампанию (`nm_bids` = массив), гейт пола (ниже
  7.3 → skip), по каждому nm — лог `action='api_set' author='dashboard-bulk'` + baseline Джема + апсерт
  `wb_bid_override`; возвращает applied/error/skipped/results. `_old_cpc_src(account,nm)` = старая CPC
  (override→реконструкция). Live-гейт тот же (`WB_BID_LIVE_ENABLED` + токен); при live off — намерения
  в журнал applied=false, в WB ничего.
- Фронт `wb_bids.html` (mode=margin): колонка-чекбокс (только у строк с рекомендацией И активной РК),
  select-all в шапке, панель `#bulkbar` («выбрать все 🟢 растить» / «все с рекомендацией» / «снять» +
  «применить рекомендации к выбранным (N)»). Кнопка активна только при live on; предпросмотр (до 12 nm)
  + один `confirm` ДО отправки; строки обновляются НА МЕСТЕ (зел/красн подсветка), сводка applied/skip.
  Item = `{nm_id, advert_id, new_cpc:rec_cpc}`; cut/dead/hold (rec=null) — без чекбокса.
- Smoke: маршрут (пустой список → ошибка) + skip-путь (ниже пола → пропуск, PATCH НЕ уходит) ✅;
  живой массовой отправки НЕ делал (ждёт «да» Сергея на объём). `node --check` JS ✅, страница 200.
**Фикс 2026-08-04 (ошибки смены ставок — 400 "not found in advert"):**
- В журнале 8 отказов `400 "nomenclature <nm> not found in advert"`. Корень: `_margin_gated.dm`
  брал кампанию (`adverts`) из **decision-периода = ИЮЛЬ**, а часть nm в августе мигрировала в другую
  РК (216360541: июль 35698453 → авг 35700718; 200166545: июль 35697812 → авг 35703066). PATCH в
  июльскую РК → WB верно отвечает 400. Плюс пачка у WB **атомарна** — один протухший nm валил валидных
  соседей (216453663/192569274/…), им в журнал писался чужой resp.
- **ПРАВКА A:** добавлена CTE `cur` — `adverts`/`any_active` берутся из ТЕКУЩЕГО периода wb_ad_nm
  (max period, август), упорядочены `(status=9) DESC, clicks DESC` → `adverts[0]` = активная РК сейчас.
  Метрики решения (spend/rev/drr) по-прежнему из июля. nm без августовской РК → `adverts=[]` → нет
  чекбокса, в пачку не попадёт (9 grow таких). Режим «Ставки» этим не болел (там `l.period=l.maxp`).
- **ПРАВКА B:** `apply-bulk` — при групповом не-200 и >1 позиции добивает по одному (`_wb_patch_bid`),
  чтобы валидные соседи применились, а сбойный получил свой ответ.
- Проверено (без живой записи): 216360541→[35700718], 200166545→[35703066], сосед 216453663 остался
  [35698453]. 8 старых отказов = applied=false; 4 валидных nm (216453663/192569274/200163811/200167236)
  ставку так и НЕ получили — **при желании перевыставить их Сергей жмёт заново** (теперь уйдёт в верную РК).
**Сессия 2026-08-05 — тест-фронт + дневной трек показов:**
- **Миграция 111** `wb_ad_nm_daily` (per-nmID дневной трек: views/clicks/orders/spend/cpc/booster_pos).
  `wb_ads.py` парсит `days[].apps[].nms[]` + `boosterStats[]` → апсерт. `wb_ad_nm` = месячный MTD,
  эта таблица даёт СУТОЧНУЮ гранулярность (реакция на смену ставки; Джем — органика с лагом ровно 7д).
- **Колонка «показы/сут»** на витрине (CTE `dly/dcur/dprev` в `_margin_gated`): рекл. показы за
  последний день + стрелка день-ко-дню. **Колонку ДРР убрал** (KPI-плашка «ДРР общий» оставлена).
- **🧪 Тест-фронт (вердикт `test`)** — стратегия Сергея «широкий фронт +10%/сутки, максимум позиций».
  Условие: маржа≥25 + продавался(3 мес) + в АКТИВНОЙ кампании + кликов ≤2 + ставка <20₽ (`WB_TEST_CAP`).
  `on_ramp = cur_cpc<20` (НЕ «на полу» — иначе после 1-го шага 8.03₽ выпадал бы из когорты, рампа рвётся).
  `_rec_cpc`: +10% за шаг от текущей (со дна 7.3×1.10=8.03). Выход: clicks>2 (зацепилось→grow/дорого)
  или ставка упёрлась в 20₽. Когорта **553**, все реально на полу (rec=8.03). ГРАБЛИ: «cpc» на витрине =
  реконструкция расход/клики; по 1-2 кликам это цена клика в аукционе, НЕ назначенная ставка → база
  шага тест-фронта берётся ТОЛЬКО из `override` (наш PATCH), иначе пол. Табы/pill/кнопка «выбрать все
  🧪» на фронте. repriced-ярлык переименован «🟣 подорожал» (был «тест», путался).
- **157 хороших SKU без активной РК** (keep+repriced, продаются) — список ушёл в ТГ (@Pro_Dropbox_bot,
  chat_id 1031321444). Фаза 2: добавить их в кампании (контракт WB «add nmID to campaign» НЕ подтверждён).
**Сессия 2026-08-06 — первый боевой массовый прогон + вскрыт контракт создания РК:**
- **ТЕСТ-ФРОНТ ПОДНЯТ (по прямой команде Сергея):** 554 SKU, пол 7.3 → 8.03 ₽ (+10%), двумя пачками
  (500+54, `WB_BULK_MAX`). Результат **554/554 applied=true, resp 200**, 68 кампаний, ошибок 0,
  skip 0. В `wb_bid_log` 554 строки `action=api_set author=dashboard-bulk`; `wb_bid_override`
  вырос 57 → 611. Оценивать через `WB_STEP_DAYS=2` по колонке «показы/сут».
- **КОНТРАКТ СОЗДАНИЯ КАМПАНИИ ВСКРЫТ:** `POST /adv/v2/seacat/save-ad`, тело `{"nms":[<nm>,...]}`,
  ответ 200 + **новый advert_id** в body (голое число, не JSON). Поле именно `nms` (`nm`/`nmIds` →
  400 «you must provide at least one nm id»). **`advertId` в теле ИГНОРИРУЕТСЯ** — эндпоинт всегда
  СОЗДАЁТ новую РК, добавить nm в существующую им нельзя.
- **Добавление nmID в СУЩЕСТВУЮЩУЮ кампанию — по-прежнему НЕ найдено.** Пробито 34 пути в двух
  неймспейсах: в `/api/advert/v1/` живут только `bids` (PATCH) и `bids/min` (POST); в `/adv/` —
  только `v2/seacat/save-ad` (POST) и `v1/budget/deposit` (POST). Остальные 404. Вывод: фаза 2
  делается НЕ добавлением в старые РК, а созданием новых с нужной номенклатурой.
- **⚠ ПОБОЧНЫЙ ЭФФЕКТ (моя ошибка):** схему полей выяснял POST-ом с заведомо несуществующим
  `advertId=999999999`, ожидая отбой валидации. Валидации нет — создались **две реальные РК
  39047221 и 39047222** (type=9, **status=4 «готова к запуску»**, обе с nm 199332171).
  Бюджет обеих проверен: `cash=0 netting=0 total=0` → **денег не тратят и не могут**, пока не
  пополнены и не запущены. Решение по ним (удалить/переиспользовать) — за Сергеем.
  **УРОК: у WB write-эндпоинты без dry-run; вслепую POST-ить нельзя даже «невалидным» телом.**
- Не выяснено до запуска фазы 2: лимит nm на одну РК; какие имя/настройки получает РК по умолчанию
  (в теле их не передавали); стартовая ставка новой РК. Запуск требует `budget/deposit` = ЖИВЫЕ
  ДЕНЬГИ → отдельное «да» Сергея на конкретную сумму (правило 13).
**ФАЗА 2 УПЁРЛАСЬ В ТУПИК (2026-08-06): создать CPC-кампанию через API НЕЧЕМ.**
- Созданные через `save-ad` 5 РК оказались **CPM** (заметил Сергей в ЛК, подтверждено независимо:
  `PATCH /api/advert/v1/bids` на такую РК → 400 **«CPM bid must be a whole number»**).
  Нам нужен CPC — **все 8 созданных РК удалены** (5 рабочих + 3 мусорные), аккаунт вернулся
  к исходным 198 кампаниям, проверено `promotion/count`. Денег не потрачено (бюджет всех был 0).
- **Удаление РК через API ЕСТЬ: `GET /adv/v0/delete?id=<advert_id>`** → 200. Также живы (все GET!)
  `/adv/v0/start`, `/adv/v0/stop`, `/adv/v0/pause`, POST `/adv/v0/rename`. **ОСТОРОЖНО: это GET —
  «пробный» запрос с реальным id ЗАПУСТИТ кампанию.** Пробовать только через OPTIONS или с
  несуществующим id. (В ЛК у Сергея кнопки удаления нет — удаление доступно только этой ручкой.)
- CPC-создания нет: пробито **52 пути**, единственный живой создающий — `/adv/v2/seacat/save-ad`,
  и он делает CPM. Параметра модели оплаты у него не видно (проверены `type/paymentType/bidType/
  cpc/instrument/payment_type/model` — проверка лимита срабатывает первой и маскирует остальное).
- **Следствие: фаза 2 целиком ручная.** Даже если Сергей создаст CPC-РК в ЛК, дозаписать в неё
  номенклатуру нечем (add-nm не существует). Готовый список с нарезкой под лимиты WB —
  `docs/MKT_WB_NEW_CAMPAIGNS_2026-08-06.md` (5 групп: 41/41/40/44 картриджи + 10 чернила).
- **ДВА ЖЁСТКИХ ЛИМИТА `save-ad`, выяснены на живых отказах (не из доки):**
  1) **≤50 номенклатур на кампанию** (`no more than 50 of products in the campaign`);
  2) **все nm одного ПРЕДМЕТА** (`all nomenclatures must belong to the same category`) — поэтому
     нарезка идёт по `subject`, а не по марже; 10 «Чернил» обязаны были уйти отдельной РК
     (план был 4 РК × 44, факт 5 — ограничение WB, не выбор).
  Отказ по лимиту = 400 и **кампания НЕ создаётся** (мусора не плодит).

**★ НАЙДЕН ПОЛНЫЙ РИДЕР КАМПАНИЙ (2026-08-06): `GET /api/advert/v2/adverts`** — без параметров
отдаёт ВСЕ кампании (189 из 200; завершённые частью не попадают). На кампанию:
`id, status, bid_type (unified|manual), settings{name, payment_type (cpc|cpm!), placements},
restrictions{can_change_nms}, timestamps, nm_settings[]{nm_id, subject{id,name}, bids_kopecks{search,
recommendations}}`. Закрывает СРАЗУ ТРИ старых дырки: **состав кампании**, **модель оплаты**
и **назначенная ставка по каждому nmId** (реконструкция CPC=расход/клики из fullstats больше
не нужна, см. память `project_mp_wb_ads_endpoint`). `nm_settings` бывает `null` — обрабатывать.
Старые пути `/adv/v1/promotion/adverts` (GET и POST) — 404, это тупик, не они.

**КОНФЛИКТ СОСТАВОВ (2026-08-06, вскрыл Сергей в ЛК) — мой список «176 без рекламы» БЫЛ НЕВЕРЕН.**
- Сергей завёл руками 2 CPC-РК: **39050343** «Кампания 86» (81 SKU, ставка 24 ₽, **status 9 — идёт**)
  и **39050589** «Кампания 87» (85 SKU, ставка 7.3 ₽, status 11 — пауза). Обе `payment_type=cpc`,
  `bid_type=manual`, площадка только «поиск». Обе набраны из моих 176.
- Пересечение: **142 из 166 SKU уже лежат в ИДУЩИХ (status 9) чужих кампаниях**, ещё 21 — в РК
  на паузе, и лишь **3 SKU реально нигде нет**. Конкурентов **47 РК**, и все **смешанные**
  (78–139 SKU каждая, там же остальной ассортимент) → «остановить старую РК» не вариант.
- **Причина ошибки:** когорту «без рекламы» я считал по АКТИВНОСТИ (`wb_ad_nm_daily`: нет показов/
  расхода), а не по ЧЛЕНСТВУ в кампании. SKU с нулём показов сидит в РК и в выдаче не участвует —
  выглядит как «без рекламы», хотя место занято. Ридер выше даёт членство напрямую: **впредь
  когорты строить по `nm_settings`, а не по фактам открутки.**
- Ставки этих SKU в старых идущих РК: **95 шт на полу 7.3 ₽**, 36 — 8.0 ₽, остальные 8.8–9.7 ₽.
  То есть они не «без рекламы», а **заведены и придушены полом** → рычаг не новая РК, а подъём
  ставки там, где они уже лежат (механика отработана на 554).
- **МУСОР К УДАЛЕНИЮ В ЛК: 39047221, 39047222, 39048140** — побочный эффект пробы контракта
  (см. урок выше). Статус 4, бюджет 0, вреда не несут. API удаления НЕ искал: у WB write-пути
  боевые без dry-run, вслепую POST-ить нельзя.
**СДЕЛАНО 06.08 (по прямой команде Сергея):** 2 новые РК он удалил сам; вместо них подняли ставку
там, где SKU уже лежат. Два прогона `apply-bulk`, оба сверены с WB через новый ридер **1-в-1**:
- **173 из 176** «молчащих» (+10%; 10 из них стояли ниже поискового пола 4.47–5.4 ₽ → подняты
  сразу до пола 7.3 ₽; 23 сидят в РК на паузе — ставка записана, крутиться начнёт после запуска РК;
  3 SKU не в кампаниях вообще — сделать нечем);
- **88** оставшихся кандидатов «продавались за 90 дн, но в РК молчат» (+10%, все выше пола).
**Тест-фронт = `wb_bid_override` 872 SKU** (554 + 173 + 88 + 57 старых), тег `dashboard-bulk`.
Вселенная «продавалось за 90 дн» — 1076 SKU — закрыта полностью, кроме 60 SKU, которых нет
ни в одной кампании (им нужна РК руками в ЛК; 35 из 60 продали по 1 шт — не окупится).

**ПЛАН РЕЗКИ РАСХОДА — `docs/MKT_WB_AD_CUT_PLAN_2026-08-06.md`** (база: июль, полный месяц).
Расход 13 449 ₽ → выручка от рекламы 232 525 ₽, но **вся отдача с 63 SKU** (1 737 ₽, ДРР 0.77%);
остальные **11 664 ₽ (87%) — ноль рекламных заказов**. Резать нечем, кроме паузы кампаний:
ставку вниз нельзя (158 из 179 уже на полу), ручки состава РК в API нет (`/api/advert/v2/adverts`
только GET). Очереди: **А** 14 РК сейчас (390 ₽/мес, тест не задевают), **Б** 33 РК после вердикта
по тест-фронту (3 060 ₽/мес — внутри 246 SKU, которым ставку подняли именно там, пауза сейчас
убьёт тест), **В** 36 смешанных РК (7 714 ₽/мес — только руками в ЛК, там же все 63 окупаемых).
**Открыто/следующий шаг:**
- Ждать ответ тест-фронта: снять «показы/сут» и заказы через `WB_STEP_DAYS` от 06.08.
- Пауза очереди А — только по прямой команде (`GET /adv/v0/pause?id=`, не запускал).
- `wb_ad_nm_daily` покрывает лишь с 01.08 — месячные выводы строить по `wb_ad_nm`, не по ней.
- Оценить тест-фронт 554 (поднят 06.08): реакция по «показы/сут» через 2 дня.
- Массовые прогоны прочих когорт — только по прямой команде Сергея на конкретный объём.
- Известный мелкий разрыв: на 1-е число `wb_ads` пропускает сбор (endDate=вчера в прошлом месяце) —
  последний день месяца недобирается. Не блокер; при желании — добор «вчера» на стыке месяцев.
**Запрет:** любая запись ставки в WB (в т.ч. массовая) — только по прямой команде Сергея на конкретный
объём (данные ≠ разрешение писать). Массовая отправка требует отдельного явного «да».

