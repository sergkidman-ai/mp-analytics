# Архив потока card — этап 22–23.08.2026

Вынесено из `docs/BRIEF_CARD.md` при подрезке до 150 строк 23.08.2026.

## Срез каталога и появление класса W

Полный срез обоих аккаунтов — 35 900 карточек (acc1 «Цифровой квадрат» 22 435,
acc2 «Дисквэр» 13 465). Исходные объёмы проблемных: класс A 150 acc1 / 258 acc2,
класс W 279 / 25, класс C 30 / 160, класс O 320 / 328.

23.08 добавился третий класс — **W**, «На доработку» (`PARTIAL_APPROVED`): карточка торгует,
ERROR-ошибок нет, висит замечание. Раньше я записал класс в безнадёжные — **и ошибся, Сергей
был прав**. Граница проходит по КОДУ замечания, а не по группе: где значение в карточке ЕСТЬ,
повтор снимает протухший вердикт площадки; где значения НЕТ — повтор его не создаст.

## Класс A: как вычищали

**408 карточек на обоих аккаунтах, не вылечилась одна** (`3463`, набор CMYK: дважды `skipped`,
причины в природе не существует). Путь `Не обновлён → Готов к продаже` за ~2 минуты, повторной
модерации нет, `declined` ноль, из продажи не вышла ни одна. Сам класс A не рассасывался:
июльские висели больше месяца. Канарейка 4/5, волна 1 — 27/27 неторгующих, волна 2 —
118/118 торгующих.

## Опыт по классу W (23.08, 25 карточек acc1, два прогона)

Замер и перечиткой карточки, и счётчиком площадки. Лечатся повтором:
`double_without_merger_offer` 5/5, `erased_attribute_value` 5/5,
`pics_http_error` + `some_image_failed` 5/5, `attribute_hierarchy_fail` 3/3.
НЕ лечатся (значения нет вовсе, работа ТК): `warning_attribute_values_empty` 0/5,
`warning_attribute_values_out_of_range` 0/2. Все 18 вылеченных ушли из «На доработку»,
продажу не потеряла ни одна.

Белый список — `HEAL_WARN` в детекторе; пополнять **только по факту такого же опыта**, а не по
семейству кода (`cover_unprocessed`, 3 карточки, в опыт не попал и в список не включён).

Подробности прогонов — `docs/reports/ozon_card_partial_approved_2026-08-23.md`,
`docs/reports/ozon_partial_test*.csv`, более ранний этап — `docs/handoff/archive/card_2026-08-22.md`.

## Класс C — раскладка по видам работ (срез 23.08.2026)

190 карточек, 198 строк ошибок, 10 видов. Крупное: **80** «Нужен код маркировки»
(acc2, обязательный атрибут пуст), **75** чужой бренд в названии/описании (в названиях 75
карточек — Samsung), **18** бренды в хештегах, **10** спецсимволы в названии, **3** символы
в хештегах, **2** ТН ВЭД вне справочника, **2** «получился другой товар», **1** реклама
в тексте, **1** ОВХ.

Задание для ТК — `docs/reports/21_ozon_card_class_c_2026-08-23.md`, построчно —
`docs/reports/ozon_card_content_errors.csv`.
