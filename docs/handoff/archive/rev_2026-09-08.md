# Архив брифа rev — снимок 08.09.2026

Вынесено из `docs/BRIEF_REV.md` при сжатии до 150 строк. Здесь полный разбор блока G,
история канала «Ozon через ЛК» (три смены IP, вердикт по Proxy Seller) и порядок работ
из аудита движка ответов. Действующее состояние — в самом брифе.

## Блок G — кэш утверждённых ответов ВЛИТ 08.09.2026 (`344d156`)

`approved_answers` (миграция 702) по ключу «наш артикул + класс + ключ вопроса»; запись из
модерации (✅ → `send`, правка → `edit`), чтение в `feedback_today` ДО генерации — попадание, и
модель не зовётся. Маршрут остаётся `review`: автопубликации вопросов в движке нет вовсе, кэш
убирает повторную генерацию, а не человека («после — ALLOW» из брифа реализовано как вердикт
`publish_gate`, не как кнопка ✅). Инвалидация: `/cache_drop <артикул> [класс]` и автоматически
по отпечатку карточки — устаревшую запись гейт не пускает («кэш устарел»). Стоп-лист A1.2,
`_qa_guards` и `_scrub_urls` прогоняются по тексту из кэша всегда.

ЧЕСТНАЯ ЦИФРА ЭФФЕКТА: за 8 недель кэш закрыл бы **4 вопроса из 177 (2 %)**, у oz_acc2 — 1 из 28.
Ключуется вообще половина вопросов (88 из 177), но повторы редки. То есть блок покупает
ЕДИНООБРАЗИЕ ответа по артикулу (ради чего и заводился — 067H ×3), а не экономию генераций;
ждать от него снижения нагрузки на оператора не надо.

Backfill с 15.07: 150 отправленных вопросов → 18 ключей, записано 17. Не ключуется 130: 48
запрещено гейтом, 39 «прочее», 19 артикул не подтверждён МС, 15 «претензия», 5 «характеристики»
без атрибута, 4 «доставка». Единственный конфликт — **5422 «заправка» ×3** (те самые 067H,
`docs/reports/rev_cache_conflicts_2026-09-08.md`): три разных текста, правильный выбирает человек —
ответить один раз, и запись встанет в кэш сама. **Это открытый пункт, ждёт Сергея.**

Отступления от буквы брифа (осознанные): «ресурс» кэшируется как атрибут (классификатор делает
его отдельным классом, бриф перечисляет среди атрибутов); артикул для ключа берётся только
подтверждённый МойСкладом — из-за этого ключуется на 19 строк меньше, но чужой ответ не уйдёт.

**Дальше по порядку части 2:** D (совместимость) → B (парсер моделей) → E → F → C → H;
I и J — последними.
Открытый хвост блока A: 67 карточек модерации по отзывам Ozon в `queued` (✅ даст 403) — зачищать
или оставить до ЛК, решение за Сергеем.

**07.09 — часть 1 брифа (исследование авто-отзывов) ЗАКРЫТА, дальше стоп до решения Сергея.**
Отчёты: `docs/reports/rev_auto_reviews_baseline_2026-09-07.md`,
`rev_auto_templates_2026-09-07.md`, `rev_auto_gap_examples_2026-09-07.md`; память —
`/opt/mp-knowledge/auto-inbox/rev-auto-reviews-baseline-2026-09-07.md`; сводка — HANDOFF «Отзывы».
BASELINE: негатив ≤3★ с содержанием — 71, из них 6 (8.5 %) без человека, 1 опубликован;
11 авто-отзывов с маркером проблемы, 7 закрыты позитивной ротацией.
Порядок части 2 (не начата): A (претензии/обещания) → G (кэш одобренных) → D (совместимость) →
B (парсер моделей) → E (инструкции/заправка) → F (источники фактов) → C (ресурс по компонентам) →
H (шаблоны отзывов); I (накопитель обращений) — отдельной сессией; J (обратный матчинг) — последним.
Ревью Codex после каждого блока, коммит по блокам, регресс-набор до и после блока.


**Сделано 25–26.08 и закрыто** (разбор — `docs/handoff/archive/rev_2026-08-26.md`): боевая
публикация включена обратно 26.08 12:56 по команде Сергея; гейт `reports/publish_gate.py`
проверяет серверно в `post_answer()` до прочих условий (81 из 128 черновиков-вопросов
удержаны, все 30 с `grounded=false`, отзывы не тронуты); дыра dry-run закрыта переводом в
`deferred` + `flush_deferred()`; досыл отправил 7 из 9, 2 удержаны гейтом (mod=500, mod=407)
и ждут оператора вручную.

**ЛК Ozon — активная задача (решения Сергея 26.08.2026).** Отзывы Ozon по API закрыты навсегда
(403, подписки не будет), путь только через личный кабинет. Решено: браузер с постоянным профилем
на **машине сотрудника** (сервер в дата-центре Beget, смена IP/ASN — главная причина срыва сессии
по разведке 23.08); первый шаг — **разведка вживую, без записи**. Готов автономный комплект
`tools/ozon_lk/` (`recon.py` + `README.md`): ни БД, ни `.env` не нужны, ввода и нажатий по кнопкам
в коде нет — `_assert_readonly()` роняет прогон, если появятся. Снимает: жив ли профиль, селекторы
списка и формы ответа, кнопки текстом, внутренние запросы ЛК (метод/URL/статус, без тел).
**Закрыты 26.08:** прямой адрес сервера (`155.212.189.66`, Beget — антибот `fab_chlg`, разбор в
карточке памяти `ozon-antibot-blocks-server-ip`) и релей в Нидерландах (`403 Filtered`, белый
список только под OpenAI, SSH-ключа на релей нет). `api-seller.ozon.ru` не затронут, коллекторы
живы. Мерялка любого канала — `tools/ozon_lk/probe_ip.py` (`--proxy` / `--proxy-file`, с
`--browser` — настоящий Chrome): две минуты и вердикт, пускает адрес или нет.

**Курс 26.08: автономия, а не машина сотрудника.** Браузер остаётся на сервере (Chrome под Xvfb,
постоянный профиль), наружу ходит через операторский адрес; человек нужен один раз — вход по СМС
через VNC-туннель (`session_view.sh`, экран :99, x11vnc только на 127.0.0.1). Входить и работать
обязательно ЧЕРЕЗ ОДИН адрес — `recon.py` умеет `--proxy`/`--proxy-file`. Месяц приватного порта
до 2 500 ₽ — только после вердикта «канал годится», оплата за Сергеем. Спецификация порта и
регламент — `docs/reports/ozon_lk_proxy_test_plan_2026-08-26.md`; адрес порта кладётся в `.env`
ключом `OZON_PROXY_URL`, в чат не передаётся.

**Тест проведён 27.08: канал не годится, но подход подтверждён.** Бесплатный порт Proxy Seller
(Билайн, Одинцово) Ozon отбраковывает так же, как сервер. Меняли по одной переменной — адрес,
браузер (поставлен настоящий Chrome 152), окружение (Windows-отпечаток, ANGLE, `webdriver=false`),
сайт (`seller` и покупательский `www`): ответ побайтно тот же — 403 и застревание на `?__rr=1`.
Решающая улика: проверочный вызов антибота `/abt/result` тоже 403, куки не ставятся вовсе — ответ
челленджа не принимают к рассмотрению, до оценки отпечатка дело не доходит. Вывод: маскировка
браузера бесполезна, нужен другой мобильный IP. **Второй адрес (`128.71.53.197`, Билайн,
Краснодар) — тоже блок**, все четыре замера. **Контроль пройден:** тем же браузером через тот же
прокси `www.wildberries.ru` открылся полностью, WB поставил куку с нашим адресом — связка «Chrome
под Xvfb + мобильный порт» рабочая, отбраковывает именно Ozon. Подробности —
`docs/reports/ozon_lk_proxy_test_result_2026-08-27.md`.

**Три смены IP исчерпаны 27.08 — Proxy Seller на Билайне не годится** (Одинцово, Краснодар,
Москва — блок побайтно одинаковый; весь пул `AS16345`, а не отдельный адрес). Потрачено 0 ₽.

Ждём от Сергея: бесплатный тестовый порт ДРУГОГО оператора (МТС / Мегафон / Теле2 — другой ASN)
или другого провайдера. Проверка канала — две минуты:
`./venv/bin/python tools/ozon_lk/probe_ip.py --browser --proxy "<адрес>"`. Покупать нечего до
вердикта «канал годится». Дальше — транспорт внутрь
`send_ozon_review()` (`collectors/feedback_send.py:185`); модерация, лимиты, алерты и `publish_gate`
остаются сверху. Очередь: 21 отзыв с готовыми ответами —
`docs/reports/ozon_lk_manual_reviews_2026-08-24.md`; отдельно — восстановить СБОР отзывов Ozon
(встал 23.08 06:46) и переписать ответ по инциденту 31.07 (правится только в ЛК).

Аудит движка ответов на вопросы завершён и отревьюен независимо (Codex):
`/opt/mp-knowledge/auto-inbox/rev-qa-audit-2026-08-25.md`, эталонный набор кейсов —
`docs/reports/rev_gold_set.md` (G-001…G-008, пополняется каждой ручной правкой оператора).

Порядок работ (из §7 отчёта, после ревью):
1. ~~`publish_verdict`~~ — сделано (`reports/publish_gate.py`, `tests.test_publish_gate`, 7 тестов).
   Текст, отличный от черновика, идёт как `override` с записью в лог: судить сигналами о ЧЕРНОВИКЕ,
   который человек переписал, нечего, но факт обхода фиксируется.
2. Выходные стоп-листы D1 (обобщения) / D2 (возврат-замена-гарантия) / D3 (внутренний жаргон) → `hold`.
3. Регрессионный набор по всем путям отправки: авто · ✅ · правка · отложенное · dry-run.
4. Причина эскалации и класс обращения — колонками в БД (сейчас причина не хранится нигде).
5. Атрибуты `oz_acc2` (0 строк при 296 вопросах) и `article` у вопросов Ozon (пуст у 100 %).
6. Тип товара и список моделей — из номенклатуры, а не regex по названию.
7. Политика по классам обращения → 8. токенайзер модели → 9. справочник принтер→серия→артикул
   (OKI, Kyocera, HP) → 10. сверка карточек против справочника.

Прежние планы — `docs/reports/rev_chats_moderation_2026-08-24.md`, разбор негатива —
`docs/reports/rev_negatives_2026-08-24.md`. Оценка «после справочника 40–50 % вопросов можно в авто»
аудитом **не подтверждена**: без работы №1 автопубликацию включать нельзя ни в каком классе.


## Снимок «Следующий шаг» на 08.09 (блок A)

### Снимок раздела «Следующий шаг»

**08.09 — БЛОК A ЗАКРЫТ полностью, влит в `main`, сервис на HEAD (`rev=701d196`, `live=True`).**
A0+A2 — `9b6a632`, A1 — `cb6bc69`, A3 — `701d196`. Регрессия `tools/rev_regression.py`:
70 проверок, 0 провалов; эталон `rev_gold_set` 9/9 без ослаблений; гейт на 60 опубликованных
вопросах 22/38 (было 27/33). Ревью Codex прогнано после A1 и после A3, замечания закрыты в тех же
коммитах («вернёмся с ответом» ≠ обещание; зачин «Да» при отказе не считается утверждением;
недоказанной считается каждая неподтверждённая модель, а не только пустой `matched`; черновик,
изменённый лишь регистром, остаётся нашим; догон `backfill_review_drafts.py` закрыт тем же флагом).
Два отступления от буквы брифа, оба зафиксированы: стоп-лист A1.2 сужен до глаголов первого лица
(иначе блокировал «замена чипа простая»), а Canon LBP646 держит qa-guard, а не A1.3 — его ответ
отказной.
**LBP646 → блок D (допродажа только из каталога); до D временно держится на qa-guard, хрупко.**
Правило A1.3 ловит утверждения о совместимости, а тут ответ отказной («не подойдут») с подстановкой
другого лота — его удерживают qa-guard «не прямой ответ» и источник «модель». Пропадёт любой из
этих двух сигналов — кейс поедет покупателю без запрета. Память — `/opt/mp-knowledge/auto-inbox/rev-block-a-2026-09-08.md`.
**A1.1-fix (до блока D): претензия в форме вопроса.** Покупка совершена и описан отказ или
расхождение с заявленным — класс «претензия» независимо от формы обращения. Маркеры: прошедшее
время про товар, фактический ресурс против заявленного, «пустой», «не работает», «как такое
возможно». Кейсы: Xerox C310 ×2 — `3Jy9OKAByX-W7wzvhu5A` и `bijIOKABMJha5m81zPTh` (#3 и #4 в
`docs/reports/rev_g0_flipped_2026-09-08.md`): `rc.classify` даёт им 'ресурс' и 'характеристики',
и держит их сейчас только стоп-лист A1.2, а не правило A1.1.

**Блок I, первые кандидаты закупщику:** мажента — Canon 069H, HP 711M, Xerox C310 ×2.
TN-14 снят с кандидатов: MFC-L2715DW в карточке `3762RAYGQ2NL` нет (совместимость —
DCP-L2551dn, HL-L2371DN, MFC-L2751DW, Brother 2551), покупатель ошибся моделью.


## Состояние 07.09 — перевыпуск карточек и разбор failed

- **07.09 — все висящие карточки перевыпущены.** 15 карточек (msg 85…628, от 24.07 до сегодня)
  тонули в истории чата: оператор видел внизу только 3 последних и считал, что остальное потеряно.
  Бот карточки не удаляет — их просто разносит по ленте. Старые погашены правкой текста (кнопки
  сняты, пометка «перевыпущена ниже»), очередь переслана пакетом: 16 живых карточек подряд,
  msg 629…644. Окно `_pending()` расширял вручную (`send_batch(limit=25, days=60)`) — иначе
  вопрос от 24.07 не проходит 30-дневный фильтр. Две отправки сорвались на `Network is
  unreachable` (сеть сервера), досланы повтором.
- **Разбор `failed` (07.09).** Было 5: 3 яндекс-вопроса с багом `parentEntityId` — ответ ушёл
  повтором, строки висели мусором, переведены в `sent`; отзыв `454822818` — 404 `Feedbacks not
  found`, удалён на Яндексе, остаётся единственной строкой `failed`; вопрос Ozon `01a05be4`
  задержан `publish_gate` (`no_card=True`, `article=None` — карточка не сматчена с номенклатурой,
  ответ собран по вебу про аналог Cactus CS-T2A). Возвращён оператору карточкой msg=644 с одной
  кнопкой «✏️ Ответить вручную». Черновик «как правило перезаправляемый» отправлять нельзя.
- Косметический хвост — 225 строк `queued` по уже отвеченным отзывам (`posted_ok=true`).
- Поток оператора по факту: **8.4 карточки/день** = позитив с текстом 4.2 + вопросы 2.5 +
  жалобные 1.8 (+4★ 0.2). Сейчас на человека уходит 100 % вопросов.

