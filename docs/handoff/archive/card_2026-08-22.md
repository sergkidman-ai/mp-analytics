# card — этап 22.08.2026: разбор ошибок карточек Ozon и постройка автомата

Вынесено из `docs/BRIEF_CARD.md` по регламенту (бриф ≤150 строк). Живое состояние — там.

## Состояние на 2026-08-22

**Снят полный срез каталога** — 35 900 карточек обоих аккаунтов через `/v3/product/list`
(visibility) + `/v3/product/info/list`. Файл: `scratchpad/oz_cards_full.csv`.

Ozon **acc1 = «Цифровой квадрат»** (подтверждено атрибутом 85 «Бренд»), 22 435 карточек,
из них проблемных **180**, двумя непересекающимися классами:

| класс | шт. | суть | лечится |
|---|---|---|---|
| **A** | 150 | «Не обновлён»: `status_failed=imported`, `errors[]` пуст, `moderate=approved`, `validation=success` | повторной отправкой |
| **C** | 30 | контентный ERROR модерации | только в ТК |

Класс C: **25 карточек «Не создан»** — новый товар вообще не попал в продажу (15 — бренды
в хештегах `BR_hashtag_brand`, 7 — `DESCRIPTION_DECLINE` «упоминается другой бренд», 3 —
спецсимволы в хештегах), плюс 5 торгующих с отбитым апдейтом по бренду. Это прямые
потерянные продажи, и это единственное, что реально нужно править на стороне ТК.

Класс A сам не рассасывается: 67 карточек висят с июля, за месяц ни одна не вышла.
Из 150: 118 торгуют (деньги не теряются, устарел только контент), 32 не продаются.

acc2 (Дисквэр) для сравнения: 13 465 карточек, 419 проблемных, там своя болячка —
80 ERROR + 253 warning на незаполненном обязательном атрибуте «Нужен код маркировки».

**Прогон детектора 22.08 разложил acc2 точнее, и картина оказалась другой, чем казалось:**
418 проблемных = **258 класса A** (из них **165 торгуют**) + 160 класса C. То есть на
Дисквэре дожимаемых карточек в 1,7 раза больше, чем было на Цифровом, и никто их не
дожимал. Отправку туда не делал — разрешения не было.

**Канарейка 22.08 подтвердила механику лечения класса A.** 5 неторгующих карточек, no-op:
атрибут 9024 «Артикул» отправлен обратно со своим же значением через
`/v1/product/attributes/update`. Результат — **4 из 5 вылечились за ~2 минуты**:
`Не обновлён → Обновляется → Готов к продаже`, `status_failed` очистился,
`moderate_status` остался `approved`.

**Волна 1 (22.08, вечер) — 27 оставшихся неторгующих карточек класса A: 27 из 27 вылечены.**
Все задачи импорта вернулись `imported`, ни одного `skipped`. Итог по всем 27:
`Готов к продаже | Нет на складе`, `status=price_sent`, `moderate=approved`, `declined` — ноль.
Файлы: `scratchpad/classA_result.json`, срез до отправки `scratchpad/classA_snapshot.json`.

**Волна 2 (22.08, по прямой команде Сергея) — 118 ТОРГУЮЩИХ карточек класса A: 118 из 118
вылечены.** Пул пересобран живьём из API перед стартом (не из старого среза), карточек со
свежей правкой ТК (<2 ч) не было. Шли двумя партиями с воротами: 20 → перечитка → 98.
Все 118 задач импорта вернулись `imported`, **`declined` — ноль, из продажи не вышла ни одна**.
Файлы: `scratchpad/wave2_result.json`, журнал отправок и срезов `scratchpad/wave2_journal.jsonl`.

**Итог по классу A на acc1: было 150 → осталась 1** (`3463`). Контрольная перечитка
`STATE_FAILED` после волны 2: 31 карточка = 30 класса C + `3463`.

Два вывода:
- **Повторная модерация не запускается** — карточки шли `offer_validated → price_sent`,
  ни одна не заходила в «На модерации». На торгующих (118 шт.) риск подтвердился нулевым:
  ни одна не потеряла продажу и не ушла на модерацию.
- **Есть подкласс, которому одного повтора мало.** Карточка `3463` (набор CMYK
  W2210X-W2213X) дважды вернула задачу со статусом **`skipped`** и пустым списком ошибок —
  Озон не отбивает её с ошибкой, а просто не принимает к исполнению. Текста причины
  не существует в природе.

