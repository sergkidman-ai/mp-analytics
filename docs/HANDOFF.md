# HANDOFF — состояние проекта между сессиями

> **Читать ПЕРВЫМ делом при старте сессии.** Обновлять в конце значимого блока работы.
> Домены разнесены — см. также `docs/BRIEF_FIN.md` / `docs/BRIEF_MKT.md`. Обновлено: 2026-07-21.

## Текущее состояние
- **Финансы** `:8090` — `web/app.py`, ветка `eng/deepseek-answer-engine` (грязный чекаут, активная сессия).
  Дашборд `bi.metaverseworld.ru` (nginx → :8090, basic-auth `.htpasswd-bi`).
- **Маркетинг** `:8092` — ОТДЕЛЬНЫЙ сервис `web/marketing_app.py` (systemd `mp-marketing`), nginx `/marketing`→:8092.
  Развязан от `app.py` финансов намеренно (иначе роут затирался правками eng → 404). Витрина `mkt_sku_economics` (`reports/sku_economics.py`).
- **Эталон сверки:** nmID 216421567, май-2026 = **3741.77 ₽** (НЕ 3909 — устаревшее).

## Сделано за последние дни (mkt)
- Страница «Маркетинг» (юнит-экономика SKU): 3-ценовой стек, форвард-маржа через payout-ratio, KPI, сценарий акции.
- Себест: фолбэк на факт отгрузок (`buy_price` МС дырявый у ~половины); **наборы → BOM** компонентов (⊕).
- **Маржа считается от НАШЕЙ ПРОМО-цены** (не list) — по решению клиента; добавлена колонка факт-маржи месяца.

## Активные ветки
- `eng/deepseek-answer-engine` — финансы/дашборд (главная рабочая).
- `mkt/start` — маркетинг (эта линия). ВНИМАНИЕ: её `CLAUDE.md` был stale (эталон 3909) — синхронизировать с main.
- `fin/ozon-sales-3way` (worktree `fin-night`) — Ozon 3-way.
- `main` — канон (CLAUDE.md свежий: эталон 3741.77, Codex, сторно возвратов).

## Открытые вопросы к пользователю
- Разрыв «прогноз vs факт-июнь» объяснён: ВБ давил нашу цену дек→июнь, в июле подняли (>2×). Как показать динамику:
  спарклайн цены в строке / свежий период вместо июня / флаг «ВБ давит цену»? (ждём выбор)

## Следующий шаг
- Реализовать выбранный вид ценовой динамики; либо витрина `funnel×margin` (единственная открытая mkt-задача).
