# Габариты, логистика, переобмеры площадок — сводный контекст

> Собрано 2026-07-18 из docs/, кода, миграций, git-истории и рабочих сессий. Тема: логистика
> маркетплейсов считается по ОБЪЁМУ задекларированного короба; раздутые карточки = переплата.
> Домен: **fin**. Файл — рабочая память по теме, обновлять по мере продвижения.

В MEMORY.md и CLAUDE.md отдельных записей строго по «габаритам» нет (проверено grep'ом) —
контекст жил в docs/ и коде; этот файл его консолидирует.

---

## 1. СУТЬ ПРОБЛЕМЫ

Обе площадки берут логистику по объёму, вычисленному из ДхШхВ, которые МЫ сами вводим в карточку:
- **WB (FBS):** логистика ~пропорциональна объёму в литрах (`volume_l = Д×Ш×В / 1000`).
- **Ozon:** логистика по **объёмному весу** = `объём_см³ / 5000` (кг).

Раздутый/незаполненный короб в карточке → систематическая переплата за каждую отгрузку.
Исправляется коррекцией ДхШхВ в карточке (Контент WB / атрибуты Ozon) — пересчёт тарифа
происходит у площадки автоматически со следующих поставок.

---

## 2. СДЕЛАНО (код + данные в БД)

### Модель данных (миграции)
- `006_wb_cards_dims.sql` — в `wb_cards`: `length_cm/width_cm/height_cm, weight_kg, volume_l, dims_valid`.
- `036_supplier_dims.sql` — таблица `supplier_dims` (габариты ЗАКУПОЧНОЙ упаковки поставщиков);
  ключ связки `article` = `products.article`; объём/вес приведены к `volume_l`/`weight_kg`.
- `037_ozon_dims.sql` — таблица `ozon_dims` (задекларированные габариты карточек Ozon);
  `depth/width/height_mm, weight_g, volume_l`; связка с продажами по `sku`/`offer_id`.

### Коллекторы
- `collectors/wb_card_content.py` / `collectors/wb.py` — габариты карточек WB → `wb_cards`.
- `collectors/supplier_dims.py` — РУЧНЫЕ выгрузки прайсов поставщиков (`incoming/size.zip → incoming/gab/`).
  У каждого поставщика свой формат единиц (приводим к см/кг/л):
  - **Изи (T2)** — «Размеры мм» одной ячейкой `Д*Ш*В`, вес кг;
  - **Cactus** — Длина/Высота/Ширина (мм) по колонкам, вес брутто кг;
  - **Сакура** — Д/Ш/В в МЕТРАХ + объём м³, вес кг (листы Лазерная/Струйная/Матричная);
  - **Профилайн** — только Вес и Объём (м³), БЕЗ ДхШхВ и штрихкода.
  - **RAPID (b2b-rapid1.ru)** — API JSON, см. ниже 4bis(5); НЕ ручной, отдельный коллектор.
- `collectors/rapid_dims.py` — габариты поставщика RAPID по API (`GET export.php?authkey=…&type=json`,
  ключ `RAPID_API_KEY` в `.env`). Поля: `CodeID`=OEM-модель→article (98%), `Ean`→barcode (39%),
  `Gabarity{Dlina,Shirina,Visota}` Д/Ш/В в МЕТРАХ (полные у 24%), `Volume` м³ (расширяет покрытие),
  `Weight` кг. Санитайзеры единиц переиспользованы из `supplier_dims`. Пишет `supplier='rapid'` в
  ту же таблицу. Сырьё дампится в `incoming/gab/rapid_<date>.json`.
- `collectors/ozon_dims.py` — габариты карточек Ozon из `GET /v4/product/info/attributes`
  (ДхШхВ мм + вес г) → литры + объёмный вес.

### Аналитика переплаты
- **WB** (`docs/wb_logistics_overpay.md` + `.csv`, 406 строк): регрессия факт-логистики на объём
  карточки дала эмпирический тариф **логистика/шт ≈ 46 + 29,8 × объём_л** (1 л→76 ₽, 5 л→195 ₽,
  11 л→374 ₽). Каждый лишний литр ≈ **30 ₽/шт**.
- **Ozon** (`docs/ozon_dims_overpay.csv`, 159 SKU): переплата по объёмному весу, декл. объём vs реальный.
- Коммиты: `feat(fin): габариты поставщиков → переплата по логистике WB` и
  `83c43e9 feat(fin): габариты Ozon из API → точная переплата по объёму` (оба 2026-07-15).

### Дашборд (web/app.py)
- Коммиты `31fa716` (2 маржи, габариты/плотность, ФБО-стоимость), `7788e64` (в убыточных —
  рекомендация «поправить габариты» вместо цены при подозрительном объёме).
- Флаг плотности `кг/л` на продаваемых карточках, пороги **DENS_LOW=0.05, DENS_HIGH=0.7**
  (медиана ≈0.14, p05≈0.04, p95≈0.61):
  - `< 0.05` = «крупн./лёгкий» → раздут объём, лишние литры логистики;
  - `> 0.7` = «тяжёлый/мелкий» → занижены габариты (часто опечатка, напр. длина 1 см);
  - `dims_valid=false` = «невалидные» (WB пометил).
- Блок «Подозрительные габариты» + подсказка «N убыточных позиций — из-за габаритов, а не цены».
- `analyze_return_plan.py` тоже использует `volume_l`/`dims_valid` (реальный объём карточки WB) в
  плане «не в минус».

---

## 3. ВЫЯСНЕНО (ключевые цифры и факты)

### WB
- Медиана раздутости `объём_карточки / реальный_объём_поставщика` = **2,30×**.
- **148 карточек** висят на дефолтных **~11 л**; реальный медианный объём картриджа — **4,58 л**.
- WB-логистика за июнь 2026: **893 595 ₽** на 3004 шт (сред. **297 ₽/шт**).
- **Потенциал экономии ~133 000 ₽/мес** (≈15% всей логистики WB) по покрытой части; из них
  **116к на 237 карточках**, раздутых >2×. По всему ассортименту при типичности — до ~2× выше.
- Покрытие матчинга nm↔поставщик: **406 nm из 1342** продающих (~30%) — ограничено 4 поставщиками.
- Топ переплат (₽/мес): nm 323345021 (карт.20.5л/реал 9.5л, 52шт, 17 088 ₽); 200167236 (6 397);
  192569274 (6 248); 313183571 (34.3л→3.7л, 4 561); 324993492 (3 142). Полный список — в CSV.

### Ozon
- Переплата всего **≈35 773 ₽/мес** по 159 SKU: **oz_acc1 31 635 ₽**, **oz_acc2 4 138 ₽**.
- Массовый дефолт декл. объёма **34.27 л** при реальных ~3–6 л (см. `ozon_dims_overpay.csv`).
- Топ: sku 930613826 (декл 34.27л/реал 3.93л, 16шт, 3 689 ₽); 907612978 (18.5→2.51, 3 038);
  864225717 / 866443793 (34.27→3.95, по 2 765).

---

## 4. ГИПОТЕЗЫ И ОГОВОРКИ (почему это оценка СВЕРХУ)

- Габариты поставщика = закупочная упаковка ОДНОГО картриджа — прокси реального размера. Наш
  ОТГРУЗОЧНЫЙ короб может быть больше (несколько штук, наполнитель) → фактическая экономия ниже.
- Покрыты только 4 поставщика (~30% продаж WB). Остальное появится по мере добавления прайсов.
- WB-тариф выведен ЭМПИРИЧЕСКИ из наших данных (регрессия), не из офиц. сетки WB — годится для
  ПРИОРИТИЗАЦИИ, не для точного биллинга.
- Ozon: та же болезнь и та же оговорка (декларация vs реальный размер отгрузки).

---

## 4bis. СДЕЛАНО В НОЧЬ 2026-07-18 (приоритеты сессии)

### (1) Проверка: внесены ли уже правки габаритов WB — НЕТ
Свежий фетч текущих ДхШхВ по всем 406 nm из CSV (content-api) и сравнение со снимком
`wb_cards` (07-14/15): **0 карточек изменили габариты** с момента анализа. Все рекомендации
остаются неактированными. (History-API у WB нет; вывод — current-vs-snapshot, baseline 3 дня.)
72 карточки помечены «текущий объём уже ≈ рекомендации» — это хвост с малой раздутостью
(напр. 11,1→9,6 л), в топ переплат не попадают. Скрипт: `scratch_wb_dims_check.py`,
снимок текущих габаритов → `scratchpad_wb_current_dims.json`.

### (2) Официальная тарифная сетка WB (вместо эмпирики) — ПОЛУЧЕНА
`GET common-api.wildberries.ru/api/v1/tariffs/box` (скрипт `scratch_wb_tariffs.py`, сырьё →
`scratchpad_wb_box_tariffs.json`). Формула box-логистики FBS (за единицу склада 100%):
**base0 = 46 ₽ (первый литр) + liter0 = 14 ₽ × (ceil(V)−1)**, всё × коэффициент склада.
API отдаёт base/liter УЖЕ умноженными на коэфф. (46×2,2=101,2). Коэффициенты складов
75…445%, медиана 150%, среднее 156%.
- **Наш реальный эффективный коэффициент** (обратный расчёт из факта июня, 3242 строки
  логистики × объём карточки): **медиана 1,54** (совпала с медианой сетки), среднее 1,43.
- Эмпирический наклон 29,8 ₽/л ≈ liter при коэфф. 213% → **эмпирика завышала на ~27%**.
  Официальный маржинальный тариф при нашем складе = 14 × 1,54 ≈ **21,6 ₽/лишний литр**.

**Пересчёт потенциала WB по офиц. формуле** (`scratch_wb_potential_official.py`, покрытые 406 nm):

| Оценка | ₽/мес |
|---|---|
| Эмпирика (29,8/л, старый CSV) | 133 439 |
| **Офиц. coef=1,54 (медиана) — центральная** | **96 977** |
| Офиц. coef=1,43 (среднее) | 90 050 |
| Офиц. coef=1,15 (p25, консерв.) | 72 418 |

Честный потенциал по покрытой части ≈ **97к/мес** (диапазон 72–97к), а не 133к.

### (3) Топ-50 черновик правок (WB+Ozon) — ГОТОВ, карточки НЕ менялись
`docs/wb_ozon_dims_edit_draft.csv` + `.md` (скрипт `scratch_build_edit_draft.py`).
Топ-50 по переплате = **82 486 ₽/мес** (37 WB + 13 Ozon). Колонки: текущие ДхШхВ (свежий фетч),
рекомендуемые ДхШхВ, объём до/после, метод, шт/мес, переплата по офиц. тарифу.
Метод формы: **supplier** (27) — точные ДхШхВ из прайса; **derived** (23) — форма неизвестна,
короб сжат пропорц. до целевого объёма (для FBS-тарифа важен только объём, не форма).
Все 13 Ozon — derived (barcode-матч ozon_dims→supplier_dims не прошёл — пробел покрытия).

### (4) Профилайн без ДхШхВ — методика закрытия
`supplier` = `profiline` (латиницей!), **10 624 позиции: 0 с ДхШхВ, 0 со штрихкодом**, объём
у 9203 но НЕНАДЁЖНЫЙ (фотобарабан 29 л vs TK-1150 0,13 л — разнобой единиц). Связь без barcode:
OEM-модель картриджа зашита в артикул/титул (`OG_TK-1150`). **Лучшее закрытие — не по своим
данным Профилайна, а перенос ДхШхВ той же модели картриджа от cactus/изи/sakura** (физ. размер
модели один). Feasibility (матч OEM-токена из титула): **~63% позиций** закрываемо (верхняя граница —
часть матчей на модель принтера, чистых по картриджу меньше). Остаток: derived-из-объёма (где
объём вменяемый) или ручной обмер топ-муверов. Своя `volume_l` Профилайна для derived НЕ годится
(шум единиц) — брать целевой объём из аналога-поставщика.

### (6) ПОЛНОЕ ПОКРЫТИЕ габаритов + переплата ВБ пересчитана (2026-07-19)
Пулинг ВСЕХ поставщиков (cactus/rapid=КВК/изи/sakura/profiline) + кросс-филл по **OEM-модели**
(та же модель = тот же размер, матч на товары любого контрагента) → покрытие габаритов
**96,7% проданных штук** (реальный матч) + 3,3% оценка по медиане категории = **100%**.
Скрипты: `scratch_dims_coverage.py` (доли поставщиков + покрытие, → `docs/dims_coverage_report.md`,
`docs/dims_coverage_by_supplier.csv`), `scratch_wb_overpay_full.py` (переплата ВБ).
- Ключ матча габаритов RAPID: `norm(article)==norm(CodeID)` НАПРЯМУЮ (не снимать SF/OEM-префикс —
  у совместимых CodeID='SF-…'/'OEM …', объём заполнен у 90%; стрип уводит на оригинал, 19% vs 75%).
- **Переплата ВБ на полном покрытии (87% карточек, 1166/1340): ~186к ₽/мес** (офиц. тариф,
  С поправкой на НАБОРЫ). Консервативно (без profiline и >8×): ~144к. Прогресс оценки:
  294к сырьё → 238к (базовая поправка наборов) → **186к** (плюрал-эвристика «Картриджи»/диапазон = 4 CMYK).
  Против прежних 97к на 30% покрытии — **примерно ×2** (гипотеза «по всему ассортименту вдвое» подтвердилась).
- Наборы (CMYK 4-паки) давали 27% завышения: матч на объём ОДНОГО картриджа → умножаем на pack=4
  (детект: диапазон моделей / «Картриджи» мн.ч. / ключевые слова), объём капим карточкой.
- Вклад источников в переплату: cactus 60%, profiline 16% (ненадёжный объём), sakura 11%, rapid 9%, изи 4%.
- **Доли поставщиков за месяц (sold_30d):** ОДИССЕЙ ~49%, КВК ТРЕЙД=RAPID ~13%, Изипринт ~12%,
  Солюшнс ~10%, ФЕРРЕТ 5%. Габариты реально знаем: у большинства 95–100%, слабее Солюшнс 83%, КПД 74%, Спринт 46%.

### (7) ПЕРЕПЛАТА ПО СУЩНОСТЯМ × МАРКЕТПЛЕЙСАМ (2026-07-19)
Сущности: **Цифровой квадрат** = wb_acc1 + oz_acc1 + Yandex(ya_acc1); **Дисквэр** = wb_acc2 + oz_acc2.

| Сущность | WB (офиц.тариф) | Ozon (7.6₽/л) | Yandex | Итого |
|---|--:|--:|--:|--:|
| Цифровой квадрат | 107 156 | 46 611 | ~15–30к (оценка) | ~169–184к |
| Дисквэр | 79 232 | 8 193 | — (нет акк.) | 87 425 |
| **Итого** | **186 386** | **54 804** | ~15–30к | **~256–271к ₽/мес** |

- WB: полное покрытие 87%, офиц. тариф box, с поправкой на наборы (`scratch_wb_overpay_full.py`).
- Ozon: покрытие ~90% (`ozon_dims` declared vs резолвер real), тариф эмпир. 7.6₽/л (пологий).
  ⚠ Часть Ozon-объёма — FBO (Ozon сам меряет короб) → переплата от declared может не реализоваться.
- Yandex: только Цифровой, доставка июнь 119.8к/470 заказов, ступенчатый тариф — точно НЕ измерено, оценка.
- **Итог по всем МП ≈ 256–271к ₽/мес (~3.1М ₽/год)**; консервативно (WB без profiline/>8×, Ozon как есть) ~215к.
- Изипринт «4% vs 11% продаж»: 4% — атрибуция по ИСТОЧНИКУ габаритов (чей ПРАЙС дал короб), товар
  Изипринта чаще матчится по OEM-модели к cactus (60% источника) → это не про качество коробов Изи.

### (5) Новый поставщик RAPID по API — ПОДКЛЮЧЁН
`collectors/rapid_dims.py` тянет прайс b2b-rapid1.ru (8483 позиции) → `supplier_dims` под
`supplier='rapid'`: **8266 артикулов, 2026 с полными ДхШхВ, 3263 с объёмом, 3343 со штрихкодом**.
Единицы: габариты в метрах (×100→см), `Volume` в м³. Ключ `RAPID_API_KEY` в `.env`.
- **Связка с нашим каталогом — по АРТИКУЛУ (OEM-модель `CodeID`), НЕ по штрихкоду**: rapid.barcode
  ∩ `ms_barcode` = всего 10 (RAPID отдаёт OEM-EAN, у нас другие), а rapid.CodeID ∩ `products.article`
  = **5393** (из них 1557 с полными ДхШхВ, 2786 с объёмом).
- **Инкремент покрытия ДхШхВ:** прежние 4 поставщика давали габариты для 6133 `products.article`;
  RAPID добавляет **+1510 net-new** товаров (полностью непересекающийся набор, +25%).
- **Перематч переплаты с RAPID (2026-07-18) — прироста НЕТ** (`scratch_rematch_overpay.py`).
  Проверено тремя способами (точный article-джойн / воронка nm→МС→поставщик / инкрементальные nm):
  - WB: только **+14 nm** получают объём от rapid, суммарная переплата этих строк = **195 ₽/мес**
    (все qty=1, копейки). Ozon: **+0** (штрихкоды rapid — OEM-EAN, не совпадают с внутренними
    баркодами МС/Ozon: пересечение с `ms_barcode` = 10, с `ozon_dims.barcode` = 0; offer_id Ozon —
    внутренние коды, не OEM-модели).
  - Причина: dim-модели rapid (886 новых external_codes с объёмом) **почти не пересекаются с нашими
    продающими SKU**; там, где пересекаются (напр. W2413A), объём уже дают cactus/изи → нет нетто-выгоды.
  - Узкое место покрытия — звено **nm→МС** (external_code ловит 67% продающих nm), а НЕ поставщик;
    rapid это звено не улучшает. Модель-токен из названия ненадёжен (ловит модели ПРИНТЕРА: nm
    199574646 → rapid «MF4410» = 48,6 л = принтер, мусор) — авто-филл по титулу запрещён.
  - **Вывод:** ценность RAPID — не в текущей переплате, а в широте каталога (+1510 net-new товаров
    с ДхШхВ для будущих SKU/анализа). Официальный потенциал WB остаётся ~97к/мес (на прежних матчах).
    Слабый ре-деривативный CSV (external_code-only, 222/406 совпало со старым) НЕ сохранён — старый
    `docs/wb_logistics_overpay.csv` остаётся авторитетным.

## 5. ОТКРЫТЫЕ ВОПРОСЫ

1. ✅ **Применены ли коррекции карточек?** ЗАКРЫТО (07-18): 0 из 406 топ-nm изменены — правок нет,
   рекомендации актуальны. См. 4bis(1).
2. **Реальный отгрузочный короб** vs закупочная упаковка — надо подтвердить фактические размеры хотя
   бы по топу (иначе экономия завышена). Источник истины — собственные обмеры/фото упаковки.
   ⚠ Для FBS-тарифа важен только ОБЪЁМ (литры, ceil), не форма — но короб должен физически вмещать SKU.
3. ✅ **Официальная сетка WB** ЗАКРЫТО (07-18): base0=46 + liter0=14 × коэфф.склада; наш коэфф.≈1,54;
   потенциал пересчитан → ~97к/мес (было 133к). См. 4bis(2). Ozon-сетка (объёмный вес /5000) — TODO.
4. **Покрытие поставщиков WB** — матчинг ~30%; Профилайн (63% закрываемо переносом модели) поднимет долю.
5. ✅ **Профилайн** — методика закрытия описана (4bis(4)): перенос ДхШхВ по OEM-модели от cactus/изи/sakura,
   ~63% feasibility. Реализация коллектора-обогатителя — TODO.
6. **Мониторинг** — карточки со временем снова «расплываются» (новые SKU на дефолтах); нужен
   регулярный отчёт-детектор, а не разовый разбор.

---

## 6. СЛЕДУЮЩИЕ ШАГИ

- [ ] Прогнать топ WB-CSV: по каждому nm сверить реальные габариты картриджа с задекларированными;
      начать со 148 «дефолтных ~11 л» (явно незаполненные, а не осознанный короб).
- [ ] То же по Ozon: топ `ozon_dims_overpay.csv`, дефолт 34.27 л — исправить атрибуты.
- [ ] Внести правки в карточки → зафиксировать дату; через 1–2 поставки замерить фактическое
      снижение логистики/шт (сверка до/после по `margin_by_sku`).
- [ ] Подтвердить реальный отгрузочный короб по топ-позициям (обмер) → уточнить оценку экономии.
- [ ] Достать офиц. тарифную сетку → пересчитать потенциал точно.
- [ ] Автоматизировать детектор «подозрительных габаритов» в регулярный отчёт (пороги плотности
      уже есть в дашборде: DENS 0.05 / 0.7).
- [ ] (опц.) Завести memory-запись по теме, чтобы контекст не терялся между сессиями.

---

## 7. ГДЕ ЛЕЖИТ (файлы и таблицы)

| Что | Путь |
|---|---|
| WB разбор переплаты | `docs/wb_logistics_overpay.md`, `docs/wb_logistics_overpay.csv` (406 строк) |
| Ozon разбор переплаты | `docs/ozon_dims_overpay.csv` (159 SKU) |
| Габариты карточек WB | таблица `wb_cards` (мигр. 006), коллектор `wb_card_content.py`/`wb.py` |
| Габариты поставщиков | таблица `supplier_dims` (мигр. 036), коллектор `supplier_dims.py`, вход `incoming/gab/` |
| Габариты карточек Ozon | таблица `ozon_dims` (мигр. 037), коллектор `ozon_dims.py` |
| Дашборд-логика | `web/app.py` (плотность/флаги/рекомендация, пороги `DENS_LOW/HIGH`) |
| План «не в минус» | `analyze_return_plan.py` (использует `volume_l`/`dims_valid`) |

---

## 5. Итог сессии 2026-07-19 — узкий срез сделан, осознан масштаб полного аудита

**Что посчитано (по ИЮНЬСКИМ продажам):** переплата WB за габариты = **185 937 ₽/мес**, 716 карточек,
взвешено по продажам июня. Цифровой 107к / Дисквэр 79к. Тариф `1,54 × 14 × (⌈Vкарт⌉−⌈Vреал⌉) × qty`.
Артефакт-список к правке (3 шаблона блоком вверху ①②③, колонки «артикул ВБ nm» + «наш артикул» из МС,
сорт по потере): https://claude.ai/code/artifact/e5d6408b-3bd8-4089-90a3-5d0f6d619fb8
Скрипты: `scratch_wb_overpay_full.py`, `scratch_fix_list.py` → `docs/dims_fix_list.csv`,
`scratch_dims_fix_artifact.py` (+ scratchpad `_tpl.html`).

**Главный рычаг (по июню):** 3 дефолтных короба = 63% потерь (309 карт, 117 229 ₽/мес):
`36×34×28`=34,3л (①, 62 карт, 55 080₽), `20×37×15`=11,1л (②, 156 карт, 35 766₽),
`37×20×15`=11,1л (③, 91 карт, 26 383₽). Это не 716 ошибок, а несколько шаблонов, размноженных по каталогу.

### ⚠️ Осознание: нужен аудит ВСЕГО ассортимента, а не только июньских продаж
- Всего карточек WB: **28 911** (Цифровой 14 486 + Дисквэр 14 425), у всех проставлены габариты.
- Продавались в июне: только **1 342** → текущие 186к = верхушка айсберга.
- Три шаблонных короба стоят на **10 254 карточках всего каталога**: `20×37×15`=8688, `37×20×15`=1046,
  `36×34×28`=520. Латентная переплата на порядок больше июньской (сработает, когда эти карточки продадутся).
- **Настоящая («большая фундаментальная») работа:** сверить каждую из 28 911 карточек с реальными габаритами
  поставщиков, зафиксировать расхождения, оценить годовую переплату по скорости продаж каждого SKU.

### ⚠️ Корректировка pack-count: «просто 4» — ошибка
Наборы разные: тонерные — обычно **4** (CMYK); **фотобарабан + тонер-картридж = 2**; струйные — **2/4/6 и больше**.
Текущий `pack_count()` хардкодит 4 для любого набора. Правильно: (1) лучше брать реальный короб НАБОРА из
прайса поставщика по точному артикулу (pack не нужен); (2) иначе честно считать штуки — парсить диапазон
(`CF410A-CF413A`→4, `106R01335-106R01338`→4), считать цвета в названии, ловить «фотобарабан+картридж»=2.

### Габариты Цифровой vs Дисквэр совпадают (система одна)
На 5205 товаров, заведённых на обоих аккаунтах, размеры совпадают на **100%** (различаются лишь 18 —
ручные опечатки: `BS-DV-460` 16×52×**18** vs 16×52×**180**=150л; `BS-OD-1600` 5×35×5 vs 37×44×45=73л — их
стоит поправить в любом случае). Товар всплывает в списке лишь у одного аккаунта НЕ из-за размеров, а из-за
продаж/ассортимента: карточка заведена в одном кабинете или продавалась в июне только там.

### Можно ли править габариты самим
Технически да — тем же ключом WB Content API (метод обновления карточки принимает `dimensions{length,width,height}`
в см). НО только по явному ОК пользователя. Безопасно: dry-run (показать diff) → батч → лог до/после → откат.
Начинать с 3 шаблонов. Ozon — через attributes API (не проверялось).

### Следующие шаги (приоритет)
1. **Полный аудит каталога** (28 911 карточек) vs реальные габариты поставщиков — фундаментальная задача.
2. Исправить pack-count (не «×4», а по факту набора / по коробу поставщика).
3. Ozon FBO/FBS-разбивка переплаты (данные: `raw_ozon_posting.delivery_method`).
4. Прайс ГалаПринт с ДхШхВ; коллектор габаритов Солюшнс (B2B, нет API).
5. 18 опечаток-расхождений Ц/Д — точечно поправить.

### ⚠️ Структура каталога материнский/дочерний — КЛЮЧ к атрибуции габаритов (пользователь 2026-07-19)
Каталог = материнский картридж + дочерние «по принтеру» (тот же картридж под конкретный принтер).
Материнские = короткий свой артикул (в осн. 4 цифры, 125 шт в products); дочерние = `материнский/вариант`
(МС-модификации; со слэшем в products.article — 2816). **У дочерних одной семьи габариты должны совпадать.**
Ключ семьи в общем виде = текст ДО первого `/` (для брендовых: `NV-CF280A`, `SACE278A`).
Проверка: 222 семьи с 2+ детьми → габариты совпадают в 103, различаются в 13 (мало данных 106).
Виновник расхождений почти всегда дефолт `20×37×15=11,1 л`. Примеры: `NV-CF283X/737` (верно 34×13×10=4,4л /
ошибка 11,1л); `NV-CC531A/718C` (верно 36×12×14=6,0л ×2 / ошибка 11,1л); `NV-CC532A/718Y`; `NV-CF211A/731C`.
**Следствие: каталог частично чинит себя сам** — внутри семьи правильный размер = консенсус сестёр, выброс
присвоить по сёстрам без поставщика (дешёвый слой поверх сверки с прайсами). Ничьи (BS-M-TNP-50: 1,89 vs 0,21л
50/50) — нужен поставщик. Примитив: группировать products.article по split('/')[0], сравнить volume_l.

### ⚠️ УТОЧНЕНИЕ (перекрывает предыдущий раздел про слэш): ключ семьи = external_code
Пользователь уточнил: материнский артикул = 4 цифры с ведущими нулями (`0002`=Q2612A); дочерние по принтеру =
5 цифр, первые 4 = материнский (`00025`=HP LJ1020, `00027`=HP LJ1022N). В БД это поле **`products.external_code`**:
`external_code='0002'` = 33 SKU всех поставщиков Q2612A (18 по 7,28 л); `code`=external_code+суффикс поставщика
(0002cs/0002nv/0002sk…). Более широкая семья = первые 4 цифры external_code. У всех в семье габариты ОДИНАКОВЫ.
Проверка: 4780 external_code-семей с габаритами → согласованы на 98%, только 91 (2%) с разными объёмами = ошибки
(виновник дефолт 11,1 л). ФУНДАМЕНТ АУДИТА: WB-карточка → external_code → консенсус семьи; выброс чинить по семье
без поставщика (точный ключ надёжнее фаззи-матча по модели). Примитив: group by external_code (или left(ec,4)).

### ПОЛНЫЙ АУДИТ (весь каталог 28911 карточек) — scratch_full_audit.py → docs/full_dims_audit.csv
Истина real_vol по приоритету: (1) реальный короб поставщика (supplier_dims по арт/OEM-модели), (2) консенсус
семьи external_code, НО только если НЕ дефолтный шаблон. Важно: 46% семей имеют консенсус=дефолт (products.volume_l
тоже заражён 11,1 л) → семья одна не истина, нужен поставщик. Результат: **10 608 раздутых карточек к правке**
(было 716 = только июнь). Продавались в июне 727 → 185 316 ₽/мес (≈2,22 млн/год, совпало с прежней оценкой);
**латентных 9 881** (раздуты, 0 продаж в июне — future bleed). Истина: поставщик 18 865 + семья 4 704; без истины 5342.
По сущности: Цифровой 105 629 ₽/мес (5363 карт) / Дисквэр 79 687 ₽/мес (5245 карт).

### Подсчёт наборов исправлен (scratch_packcount.py) — не «всегда ×4»
Приоритет: явное «N шт/x2/набор из N» → цвета (cmyk, +lclm=6; C/M/Y=3) → диапазон по номерам
(CF410A-CF413A=4, 106R01082-01085=4) → барабан+тонер=2 → перечисление кодов через дефис → иначе 4.
Распределение: ×2=219, ×3=31, ×4=1700, ×5=8, ×6=60, ×7=4. Подключён в scratch_full_audit.py вместо
жёсткого ×4. Влияние на деньги мало (185 316→186 674 ₽/мес): real_vol капается объёмом карточки, но
отдельные наборы теперь честные (барабан+тонер был ×4→стал ×2, переплата выросла). Полный аудит теперь:
10 646 раздутых карточек, 186 674 ₽/мес кровит (Цифровой 106 470 / Дисквэр 80 204), латентных 9 916.

### «Без истины» = 5342 карточки, для которых НЕ знаем реальный короб (безопасно не считаем переплату):
Разбивка: 3342 — консенсус семьи САМ дефолт (вся семья в МС проставлена 11,1/34,3 л, правды в каталоге нет);
1980 — vendor_code не стыкуется ни с одним external_code (не нашли семью); 20 — семья без большинства.
Это НЕ «пропущенная переплата», а честно неизвестные — закрываются реальными прайсами (ГалаПринт, Солюшнс)
или добором связки vendor_code→external_code. Пока по ним переплата не заявляется (безопасная деградация).

### ⚠️⚠️ ПРИНЦИП ИСТИНЫ (пользователь 2026-07-19) — аудит переделать
1. Правим/сверяем габариты ВБ (в карточке WB) — единственный предмет аудита.
2. МС-объём (products.volume_l) НЕ истина — заражён дефолтом 11,1 л у всех поставщиков семьи. Текущий аудит
   ошибочно поднял «консенсус семьи МС» в истину (tier «семья», 4704 карт) — УБРАТЬ этот tier.
3. Реальный короб — ТОЛЬКО из прайсов поставщиков (supplier_dims: cactus/rapid/sakura/изи), по модели матери.
4. МС = только словарь «карточка→модель матери» (external_code→название TN-114), не источник размера.
Конвейер: карточка ВБ →(баркод) МС →external_code →модель →реальный короб в прайсе →сверить с ДхШхВ ВБ.
Охват: WB↔МС по баркоду (raw_wb_card_content.sizes[].skus[]→ms_barcode→products.ms_id) = 43%; хвост 57% — модель
из названия/vendor_code+фаззи-матч. НЕ использовать «первые 4 цифры vendor_code=external_code» (случайна, мусор).
Эталон: nm964681606 Bizhub 250 (vc 20083, карточка 11,1л) = мать TN-114 ext1292; в МС семья=11,1л(дефолт), реально
из прайсов cactus 37×9×9=3,0л/rapid 28×8×8=2,0л/изи 2,3л → TN-114≈2-3л, раздут ×4-5. (баркодом не линкуется, нашли по названию).

### АУДИТ v2 (scratch_audit_v2.py → docs/audit_v2.csv) — по схеме пользователя, ИСТИНА только из прайсов
Связка «внешний код»: WB.vendorCode = ВНЕШНИЙ КОД (есть прямо в ВБ). Цифровой — чистые цифры (vendorCode==external_code);
Дисквэр — vendorCode = внешний_код + буквенный АНТИКОПИ-хвост (5364MC4EHYNL→5364), снимаем ведущими цифрами.
Схема справочника МС: products.code = external_code + буква поставщика (0002cs=cactus, 0002gp=galaprint, 0002nv=nv,
0002sf=superfine/rapid, 0002sk=sakura…); products.article в строке = АРТИКУЛ ПОСТАВЩИКА → supplier_dims (реальный короб).
Конвейер: WB.vendorCode → external_code → артикулы поставщиков (МС-словарь) → supplier_dims → медиана НЕ-дефолтного короба.
Хвост (внешний код не в словаре) → фаззи-матч модели из названия к supplier_dims. Объём МС (products.volume_l) НЕ используется.
РЕЗУЛЬТАТ: истина найдена у 18 740 карт (внешний код 6460 + модель 12280); без истины 10 171 (нужны прайсы ГалаПринт/Солюшнс).
**Раздутых 9 592, переплата 169 162 ₽/мес (~2,03 млн/год): Цифровой 99 204 / Дисквэр 69 958.** Ниже прежних 186к,
потому что убрали заражённый МС-консенсус — теперь каждый рубль подкреплён реальным коробом поставщика. Пример модели
в ВБ: карточка Bizhub 250 в description содержит «TN-211» (не TN-114!) — модель достаётся из полей ВБ, не угадывается.

### Список правок пересобран (scratch_dims_fixlist.py → docs/dims_fixlist.csv, 2026-07-22) — дефолтные первыми
9 738 раздутых карт, 170 411 ₽/мес (~2,04 млн/год). Дефолтные шаблоны блоком сверху: 5 153 карт / 108 084 ₽/мес
(36×34×28: 323 карт/43 631₽; 20×37×15: 4143 карт/34 276₽; 37×20×15: 687 карт/30 177₽). Цифровой 102 288 / Дисквэр 68 123.
Артефакт (дефолтные сверху, nm+внешний код+рекоменд.короб): https://claude.ai/code/artifact/e5d6408b-3bd8-4089-90a3-5d0f6d619fb8

### ДОСТУП к WB для массовой правки габаритов (проверено по JWT .env, 2026-07-22)
Метод: WB Content API `POST content-api.wildberries.ru/content/v2/cards/update` — принимает карточку целиком с
`dimensions{length,width,height}` (см). Токен нужен категории **«Контент»** с ПРАВОМ ЗАПИСИ (не «Только чтение»).
Текущие токены (декод JWT, поле s = битовая маска, бит 30 = read-only):
- **WB_TOKEN_ACC1 (Цифровой): s=228 → Контент ЕСТЬ, read-only=НЕТ → ЗАПИСЬ УЖЕ ДОСТУПНА.** Можно править сразу.
- **WB_TOKEN_ACC2 (Дисквэр): s=1073750118 → Контент есть, но бит30 read-only=ДА → только чтение.**
  Нужен НОВЫЙ токен Дисквэра с категорией «Контент» и снятым флагом «Только на чтение».
Как выдать (Дисквэр): ЛК WB → Настройки → Доступ к API → создать токен, категория «Контент», НЕ ставить
«Только чтение» → положить в .env как WB_TOKEN_CONTENT_ACC2 (или заменить ACC2). Безопасно: dry-run→батч→лог до/после→откат.

### МЕТОД КОРОБА НАБОРА (уточнён пользователем 2026-07-22) — компоненты РАЗНЫЕ, суммировать каждый
Картриджи в наборе разного размера (струйный: цвета≠чёрный; барабан+тонер: обе части разные). НЕЛЬЗЯ «один × N».
Правильно: (1) разложить набор на компоненты — диапазон CF410A-CF413A→410/411/412/413, CMYK→4 цвета, барабан+тонер→2 модели;
(2) найти РЕАЛЬНЫЙ размер КАЖДОГО компонента в supplier_dims (объём + ДхШхВ где есть);
(3) объём короба = СУММА объёмов компонентов (точная величина для тарифа ВБ);
(4) стороны: компоненты в ряд одной ориентацией → длина=max(Lᵢ), высота=max(Hᵢ), ширина=Σ(Wᵢ); нет сторон → Тир B (ужать текущий короб до Σобъёма).
Проверено: CF410A-CF413A = 2,47(bk)+3,87+3,87+3,87 = 14,1 л (НЕ 24 л как медиана-один×4 → переплата по наборам РАСТЁТ);
Brother DR-2335(барабан 6,27л)+TN-2375(тонер 3,43л)=9,7 л. Заменяет прежний pack_count ×N — теперь суммировать компоненты.

### ПИЛОТ 15 карт ① — запись ОТКЛОНЕНА (2026-07-22): WB 401 «token scope not allowed»
Разбор JWT был НЕВЕРЕН: read-only-бит не отражает реальный скоуп записи. WB_TOKEN_ACC1 (Цифровой) ПО ФАКТУ НЕ пишет в Контент.
Вывод: ОБА аккаунта требуют НОВОГО токена «Контент» с правом записи (ЛК→Настройки→Доступ к API→категория Контент, снять «Только чтение»).
Карточки НЕ тронуты (проверено: nm199334449 в ВБ по-прежнему 36×34×28). Скрипт пилота scratch_pilot_dims.py (dry-run без флага / --execute).
Обновление dimensions: content/v2/cards/update, объект = полная карточка минус read-only поля (imtID,nmUUID,createdAt,updatedAt,photos,video,needKiz,kizMarked,subjectName), меняем только dimensions{length,width,height}.

### МЕТОД СУММЫ КОМПОНЕНТОВ ВСТРОЕН (утверждён 2026-07-22) + универсум = продажи 6 мес
Наборы: разложить по диапазону в названии (CF410A-CF413A→CF410A/411A/412A/413A, зеропад для Xerox 106R01443..446),
найти объём КАЖДОГО компонента в supplier_dims, **rec_vol = СУММА объёмов компонентов** (стороны = ужать текущий короб
до этого объёма; ДхШхВ компонентов от разных поставщиков несогласованы, поэтому объём — истина, форма — пропорции ВБ).
Не разложенные наборы (C-EXV49, CMYK без кодов) → fallback «оценка ×N». Скрипт scratch_dims_fixlist_6mo.py → docs/dims_fixlist_6mo.csv.
Пример: CF410A-CF413A=2,47(bk)+3,87×3=14,1л (было 24л). Проверка: 72 набора по сумме, 94 fallback, 960 одиночных «короб поставщика».
УНИВЕРСУМ = карточки с продажами за ПОСЛЕДНИЕ 6 МЕС (янв–июн 2026), переплата по СРЕДНИМ месячным продажам (÷6).
**ИТОГ: 1 574 карт, 145 548 ₽/мес (~1,75 млн/год): Цифровой 102 153 / Дисквэр 43 395. Дефолтные 3 шаблона: 671 карт / 85 896 ₽/мес (59%).**
Артефакт (та же ссылка) обновлён. Ждём токен «Контент» с записью (оба аккаунта) для правки. Метод короба набора утверждён пользователем.

### ПРОФИЛАЙН ИСКЛЮЧЁН ИЗ ИСТИНЫ (2026-07-22)
Проверка провенанса (scratch_prov_audit.py): в supplier_dims настоящие ДхШхВ есть только у
cactus/rapid/sakura/изи. У profiline 10624 строк, но **0 с полными ДхШхВ** — только объём
(чужой/дефолтный). ГалаПринт и Солюшнс в данных ОТСУТСТВУЮТ полностью (0 строк).
Решение (одобрено пользователем): профилайн исключён из резолвера габаритов (EXCLUDE={'profiline'}).
Эффект на активный ассортимент (продажи янв–июн 2026):
  с профилайном  1574 карт / 145 548 ₽/мес (~1,75 млн/год)
  БЕЗ профилайна 1372 карт / 119 754 ₽/мес (~1,44 млн/год)
На профилайне висело 202 карты / 25,8к ₽/мес, но 82% экономии держится на надёжных поставщиках.
Файлы: scratch_dims_samples.py + scratch_dims_samples_html.py — выборка «сейчас→станет» по 5 категориям
(наборы/струйники/тонерные/чернила/фотобарабаны) с источником каждого размера, docs/dims_samples.csv,
артефакт https://claude.ai/code/artifact/2f308cc9-0a5b-4c5f-941d-1f2c9e7022a9.
Пробел: нужен прайс ГалаПринта (осн. поставщик) и Солюшнс с габаритами — закроет хвост «низкой уверенности».

### ПОКРЫТИЕ ПРАЙСАМИ + правка про «основного» (2026-07-22)
Ошибка исправлена: ГалаПринт НЕ основной поставщик (пользователь), Солюшнс ~10%.
Покрытие активного ассортимента (3096 карт / 2394 шт-мес, scratch_supplier_coverage.py):
реальный короб — cactus 64.9% / rapid 41.2% / изи 32.9% / sakura 28.7% / profiline 0%.
Совокупно реальный короб 74.7% карт (72.2% штук); хоть какой-то объём 88.7%; не покрыто ничем 11.3%.
Костяк габаритов = cactus. Профилайн касается 82% карт, но только объёмом без ДхШхВ → надёжного не добавляет.
Это покрытие ПРАЙС-ФАЙЛАМИ (широта ассортимента), не доля закупок.

### ФИНАЛ СЕССИИ 2026-07-22 08:28 — справочник TheCartridge (set_cost) как источник разбора наборов + универсум продажи∪сток
> Последняя активность прошлой сессии (умерла на вложениях сразу после). Зафиксировано новой сессией gab 07-22.
- **Новый источник разбора НАБОРОВ — справочник `set_cost` (989 строк, TheCartridge).** Раскладывает набор
  на компоненты по external_code; размер каждого компонента берём из `supplier_dims` (cactus/rapid/sakura/изи),
  `rec_vol` = СУММА объёмов компонентов (метод суммы компонентов, утверждён 07-22). Пометка провенанса
  `справочник set_cost (TheCartridge)`; для наборов укладка «2×2» (стороны из компонентов). Заменяет фаззи-детект
  набора там, где справочник знает состав. Пример: nm313184995 (Canon 729 4/4) → 5360/5361/5362/5363 →
  1.8+1.3+1.4+1.7 = ~6.2 л (короб был 34.3 л, cost 804→437 ₽/шт).
- **Универсум расширен: продажи ∪ остаток** (не только продавшиеся). Покрытие с разбивкой ABC (`scratch_brief.json`):
  проданные 3096 карт (A2312/B44/C397/D343), сток 800 карт (A595/B8/C110/D87).
- **ИТОГ (свежий, перекрывает 119.7к/145.5к выше): 1 442 карты, 125 013 ₽/мес.** Профилайн исключён из истины
  (0 реальных ДхШхВ), метод суммы компонентов для наборов, тариф `1.54×(46+14×(⌈Vкарт⌉−⌈Vрек⌉))×qty/мес`.
- **Дефолтные короба (34.3/11.1 л) по-прежнему главный рычаг** — примеры в брифе: 200167236 (11.1→4.5 л, 19 шт/мес),
  327898986 Panasonic (11.1→6.5 л), струйники/чернила раздуты 4–10× (короб 1–2 л против 0.1–0.2 л реальных).
- **Деливераблы (не в git, gitignored/untracked):** `docs/dims_samples.csv` (5 категорий «как сейчас→как станет»
  + источник размера), `docs/dims_fixlist_6mo.csv` (полный список правок, 295 КБ), HTML-выборка в scratchpad
  (`wb_dims_samples.html`). Скрипты: `scratch_dims_samples.py` (+`_html`), `scratch_dims_choice.py` (диагностика
  расхождения коробов поставщиков), `scratch_dims_fixlist_6mo.py`, `scratch_brief.json` (сводка ABC+примеры).

### ⛔ БЛОКЕР ПРИМЕНЕНИЯ (актуально на 07-22): нет токена «Контент» с записью
Правки габаритов НЕЛЬЗЯ применить: WB Content API `cards/update` → **401 «token scope not allowed» на ОБОИХ
аккаунтах** (пилот 15 карт ① отклонён, карточки не тронуты — см. раздел «ПИЛОТ 15 карт»). Разбор JWT-бита
read-only оказался неверным — реальный скоуп записи не даёт ни один текущий токен. **Нужно от клиента:** ЛК WB →
Настройки → Доступ к API → создать токен категории «Контент», снять «Только на чтение» → в `.env`
(`WB_TOKEN_CONTENT_ACC1`/`_ACC2`). До этого gab-работа = только аналитика/деливераблы, применения нет.
