# BRIEF_PRC — прайсы поставщиков → МойСклад

**Поток:** `prc`. **Ветка/worktree:** `prc/colortek-loader` в `.claude/worktrees/prc-colortek`.
**Заведён:** 2026-08-05. **Пилот:** Колортек.

## Зачем

Шаг 1 большой задачи «прайсы поставщиков и выявление новинок»: забрать прайс с почты, положить
остатки и закупочные цены поставщика на склад **«Удаленный»** документом **Оприходование** —
и заодно зафиксировать товары, которых у нас в МС нет. Шаг 2 (новинки) пока НЕ делаем: только
копим данные.

Мы **не заменяем внешний загрузчик целиком**. Часть прайсов приходит по API и продолжит приходить
по API. Убираем только ручной труд по поставщикам без API: как запустимся — человек перестаёт
грузить Колортек, дальше остальные по мере готовности.

## Состояние

Загрузчик написан и **проверен боевой записью**: 06.08.2026 прогон `kaktus_msk --apply` (журнал
`prc_price_load` #9) создал 24 оприходования на 2340 позиций / 164 984 529 ₽, снёс 22 прошлых
(ручных) документа группы и обновил 2250 карточек. Контроль после записи: документов на
«Удаленном» ровно 24, все проведены, сумма сходится с журналом до копейки, расхождений количества
и цены с `prc_price_row` — ноль, хвостов «_old» не осталось, закупочная в карточках равна цене
прайса. Остальные поставщики — каждый со своим «ок» Сергея на первый `--apply`.

Заведено четыре профиля, каждый сверен с ручной загрузкой позиция-в-позицию:

| Поставщик | Файл | Курс | Сухой прогон 05.08 | Сверка с ручной загрузкой |
|---|---|---|---|---|
| **Колортек** | xlsx, шапка стр. 6 | ЦБ ×1.03 | 1139 строк → 1108 позиций / 12 док. / 51 502 554 ₽ | **1108/1108, расхождений ноль** |
| **Одиссей** | xls, шапка стр. 6-8 | **ЦБ ×1.00** | 1353 → 1245 / 13 док. / 74 585 601 ₽ | 1153 общих, **количество и цена сходятся полностью**; +92 у нас, +1 у них |
| **Сакура** | xls, шапка стр. 4 | ЦБ ×1.03 | 2383 → 1343 / 14 док. / 67 710 742 ₽ | **количество 1263/1263**, цена расходится на 667 (см. открытые вопросы) |
| **Кактус** | xlsx, шапка стр. 1 | рубли | 4355 → 2340 / 24 док. / 164 984 529 ₽ | **2107/2107, расхождений ноль**; +233 у нас, +1 у них |

Надбавка у Одиссея **не применяется** — это не догадка: обратный расчёт по 1154 позициям ручной
загрузки дал курс 81.1291 (разброс 81.1250-81.1333 = округление копеек) = ровно курс ЦБ.

**Кактус заведён 06.08** — Сергей настроил пересылку. Письмо шлёт ООО «КОМПАНИЯ ФЕРРЕТ» (бренд
Cactus), в письме ДВА прайса: наш `Прайслист Cactus <дата>.xlsx` и `Прайслист Оригинал.xls`
(оригинальные картриджи, в МС не грузятся) → в профиле фильтр вложения по имени `file_pattern`.
Артикул МС = колонка **«Парт. Но.»**, не «Но.» (внутренний код Феррета). Цены рублёвые, курс не нужен.

**Спринт МСК** — прайса на почте нет, Сергей запросил рассылку у поставщика; **пока грузим файлом
из их ЛК** (`--file`), профиль заведём, когда будет письмо (или сразу с `--file`, если ЛК останется).

## Как устроено

| Файл | Что делает |
|---|---|
| `prices/profiles.py` | Профиль поставщика: папка почты, заголовки колонок, таблица остатков, валюта, надбавка, контрагенты группы |
| `prices/mailbox.py` | IMAP: последнее письмо папки с вложением-прайсом (папки кириллические → modified UTF-7) |
| `prices/parser.py` | xlsx (openpyxl) и xls (xlrd) → строки; колонки ищутся **по заголовку**, не по номеру; шапка может быть многострочной (`header_span`) |
| `prices/cbr.py` | Курс ЦБ на дату + надбавка; пересчёт в копейки |
| `prices/loader.py` | Матчинг, фильтры, сборка документов, правки карточек, запись в МС |
| `prices/anomaly.py` | Проверка аномалий цены: коридор, прыжки к прошлой загрузке, сдвиг медианы; блокировка `--apply` |
| `prices/blacklist.py` | Чёрный список артикулов: импорт файла, чистка списка новинок |
| `prices/mailout.py` | Отправка списка новинок оператору (SMTP с нашего же ящика) |
| `prices/journal.py` | Журнал прогона в Postgres |
| `prices/run.py` | CLI |
| `core/ms_api.py` | Тонкий клиент МС с ретраями (общий; пока пишет только `prc`) |
| `migrations/400_prc_price_load.sql` | `prc_price_load`, `prc_price_row`, вью `prc_unmatched` |
| `migrations/401_prc_blacklist.sql` | `prc_blacklist` + вью новинок с вычетом ЧС |

```bash
./venv/bin/python -m prices.run --supplier colortek           # сухой прогон (по умолчанию)
./venv/bin/python -m prices.run --supplier colortek --apply   # запись в МС
./venv/bin/python -m prices.run --supplier odissey --file /path/p.xls --no-db
# --ignore-anomalies — грузить вопреки блокировке по ценам (осознанно, после разбора отчёта)
```
Ключи профилей: `colortek`, `odissey`, `sakura`, `kaktus` (= группа `kaktus_msk`).

Отчёты каждого прогона — `docs/prc/<группа>_<дата>_skipped.csv` (все пропущенные строки с причиной),
`..._unmatched.txt` (ненайденные, в формате внешнего загрузчика `name;price;quantity;msId;defective;Barcode;sku`)
и `..._anomalies.csv` (аномалии цены с базой сравнения).

## Правила (согласованы 05.08.2026, менять только с Сергеем)

1. **Матчинг строго «артикул поставщика == поле Артикул в МС».** Ничего не угадываем, ни по
   наименованию, ни по штрихкоду.
2. **Не грузим:** нет в МС по артикулу · товар архивный · «брак» в наименовании МС · нет цены ·
   нет остатка (или формулировка остатка не разобрана) · артикул в МС неоднозначен.
   Неоднозначность разрешается только в одну сторону: если из кандидатов ровно один от контрагента
   этой группы — берём его, иначе пропускаем и пишем в отчёт.
3. **Остаток** — из прайса. Текстовые формулировки разворачиваются ТАБЛИЦЕЙ профиля; незнакомая
   формулировка не превращается в число молча, строка уходит в отчёт. Колортек (восстановлено
   из ручной загрузки, исключений на 1108 позициях нет):
   `больше 0, но меньше 10 → 6` · `больше 10, но меньше 50 → 25` · `больше 50, но меньше 100 → 75` ·
   `Больше 100 → 100`.
4. **Цена = цена прайса × курс ЦБ на дату загрузки × надбавка**, округление копеек «половина вверх».
   **Курс НЕ округляем.** Округление курса до 4 знаков даёт 51 расхождение на копейку из 1108 —
   проверено. Колортек: USD, надбавка ×1.03.
5. **Документы:** имя `<группа>_<ГГГГ-ММ-ДД>_p<N>`, описание = группа, организация
   ООО «ЦИФРОВОЙ КВАДРАТ», склад «Удаленный», проведён, **100 позиций** в документе.
6. **Порядок записи: сначала создать новые документы, потом удалить прошлые.** При сбое остаток
   останется задвоенным (видно и чинится), а не обнулится.
7. **Удаляем только документы своей группы И только на «Удаленном складе».** У части групп есть
   документы на других складах (Кантемировская, Звездный) — они не наши и остатка не дублируют.
   Фильтр МС перепроверяется в коде своим regex: удаление документов — не то место, где верят на слово.
8. **Описание и закупочную цену карточки обновляем только там, где совпал И артикул, И поставщик**
   (контрагент карточки входит в группу прайса). Ручные правки описания при этом перезаписываются —
   это решение Сергея.
9. **Проверка аномалий цены** перед записью. **База сравнения — закупочная цена, которую записали
   МЫ** (решение Сергея 05.08): цена этой же позиции в нашей прошлой БОЕВОЙ загрузке. Технически
   берём её из журнала `prc_price_row`, а не читаем `buyPrice` из карточки — в карточке может лежать
   чужая цифра (то, что жило там до нас, или правка руками), и «было 50 ₽, стало 2 ₽» имеет смысл
   только против своей же записи. Проверено на всех трёх поставщиках: цена прайса относится к живущей
   в карточках закупочной как 0.77-0.91 по медиане, то есть чужая закупочная — не эталон цены прайса.
   Пороги (на первое время, пересмотреть, когда наберётся статистика реальных флагов):
   - точечно: **±50%** к нашей прошлой закупочной — флаг в отчёт; цена вне коридора **20-200 000 ₽**
     **снимает строку с загрузки** (цена 1-2 ₽ — сама по себе аномалия, а живой минимум по трём
     прайсам 44.29 ₽, максимум 16 267 ₽, так что коридор реальный товар не задевает);
   - массово: сдвиг медианы всего прайса больше 20% = сломанный файл (смена колонки/валюты),
     блокировка целиком; плюс блокировка, если флагов больше 5% позиций. Обход — `--ignore-anomalies`.
   Сухие прогоны в базу сравнения не идут: иначе первый прогон по битому файлу станет эталоном для
   следующего. Пока боевой загрузки поставщика не было, базы нет — работает только коридор.
10. **Артикул в прайсе повторяется** — полные повторы схлопываем молча, противоречивые
   (разная цена или остаток) **не грузим обе строки** и выносим в отчёт: складывать остатки нельзя
   (задвоится), выбирать «какую-нибудь» — тоже. У Одиссея таких 2 строки.
11. **Остаток НЕ фильтруем.** Грузим любое положительное количество, в том числе 1-2 шт. Внешний
   загрузчик такие позиции пропускал, но отсечка по минимальному количеству у нас стоит дальше по
   цепочке (решение Сергея 06.08) — дублировать её здесь не надо, иначе товар пропадёт дважды.
12. **Порядок замены документов: пометить → создать → обновить карточки → удалить.** Имя
   оприходования в МС **уникально** (первый боевой `--apply` 06.08 упал на `HTTP 412 code 3006`:
   наши имена совпали с ручными документами того же дня, создано 0 документов — сбой безопасный).
   Удалять прошлые ПЕРЕД созданием нельзя: при сбое остаток обнулится. Поэтому прошлые сначала
   переименовываются в `<имя>_old<ЧЧММСС>` (`loader.rename_stale`), потом создаются новые, и только
   когда создались ВСЕ — старые удаляются. `DOC_NAME_RE` знает про хвост `_old…`, иначе документ
   от прерванного прогона следующий прогон не увидит и не уберёт.
13. **Категории прайса фильтруем ДО матчинга** (`profile.keep_categories`, `loader.filter_categories`).
   Прайс поставщика может быть каталогом целиком: у Феррета 46 групп — сейфы, стулья, столы,
   проекторы, 3D-печать, 824 строки запчастей для ремонта. Фильтр стоит до обращения к МС, чтобы
   чужой ассортимент не попадал в список новинок (иначе шаг 2 предложит завести карточку на стул).
   Правило = (префикс группы, только эти подгруппы, кроме этих подгрупп); **префикс, а не точное
   имя** — новый бренд «Совместимые для …» подхватится сам. Решение Сергея 06.08 по Кактусу:
   берём все «Совместимые для …» кроме подгрупп `Валы магнитные / Лезвия / Ролики` (ленты для
   наклеек и термопленки — оставляем), «Материалы для заправки» только `Тонер` и
   `Чернила и заправки` (не чипы и не барабаны), «Для матричных принтеров» только
   `Матричные картриджи`. Итог: 1728 строк отсеяно, новинок 315 → **292** (было 2014).
14. **Чёрный список артикулов** (`prc_blacklist`, миграция 401, модуль `prices/blacklist.py`).
   То, что уже побывало в «необработанных» и человек это забраковал. После каждого прогона
   список новинок чистится по ЧС: строка не выбрасывается, а меняет причину на `blacklisted` —
   остаётся в журнале и в `*_skipped.csv`, но не попадает ни в `prc_unmatched`, ни в файл
   новинок. Ключ сравнения — `upper(btrim(артикул))`: ЧС ведётся руками, регистр гуляет.
   Список приходит файлом в @Pro_Dropbox_bot (одна колонка), импорт:
   `./venv/bin/python -m prices.blacklist --import <файл> --note "<партия>"` — идемпотентно.
   Первая партия 06.08: 2671 артикул (`black_list_4_.txt` от Натальи).

15. **Список новинок уходит оператору на `operator.dsquare@yandex.ru`** — файл
   `*_unmatched.txt` (формат внешнего загрузчика), после фильтра категорий и ЧС.
   `./venv/bin/python -m prices.mailout --to <адрес> --file <файл>` (есть `--dry-run`).
   Шлём с нашего же ящика, SMTP выводится из `MAIL_HOST` (`imap.` → `smtp.`), логин
   и пароль те же — отдельных доступов не заводили. Первая отправка 06.08: Кактус,
   165 строк. Автоматизировать (слать по завершении `--apply`) пока НЕ стали.
16. **Отсев новинок по ассортименту** (`prices/novelty.py`, правила Сергея 06.08). Работает после
   ЧС и только над новинками — на оприходования не влияет. Не берём: тонер больше **150 г** и
   чернила больше **150 мл** (нужна разовая заправка, не флаконы 300/500/1000), промывочную
   жидкость, перезаправляемые картриджи. Цветной товар берём только **полным комплектом**
   (чёрный+голубой+пурпурный+жёлтый): одинокий цвет заводить незачем. Неполный комплект не
   хороним — он уходит в `*_watch.csv` (причина `set_incomplete`) и подхватится сам, когда
   поставщик довезёт недостающий цвет. Моно-серии (голубого/пурпурного/жёлтого в серии нет
   вовсе) правило не трогает. Комплект считаем по ВСЕМУ прайсу, а не по списку новинок:
   чёрный может быть уже заведён у нас. Серия склеивается по пересечению моделей принтеров из
   хвоста «…для Canon iR C3326/C3326i» — точное сравнение хвоста рассыпает комплект, потому что
   у одного цвета написано `C3326`, у остальных `C3326/C3326i`. Промывку и перезаправляемые
   в `prc_blacklist` НЕ заносили: правило по названию ловит их само и на новых артикулах тоже.
   **Бренд в комплекте не учитываем** (решение Сергея 06.08): комплект на принтер мы вправе
   собрать из разных поставщиков, цвет Cet достраивает набор Cactus. Тип товара — учитываем.
   Итог по Кактусу 06.08: новинок 165 → **54**, в наблюдении 5.
   **Следующий этап:** цвета, которые уже заведены у нас в МС, тоже должны закрывать комплект —
   сейчас считаем только по прайсу поставщика, поэтому часть строк лежит в наблюдении зря.

17. **Сверка новинок с каталогом по признакам** (`prices/features.py` + `prices/catalog.py`,
   задача Сергея 06.08). «Не нашлось по артикулу» ≠ «у нас такого нет»: тот же C-EXV65 голубой
   11000 лежит у нас под кодом **6058** с суффиксом поставщика (`6058sk/6058hb/6058gp`…), а
   артикул у каждого поставщика свой. Поэтому названия сравниваем не текстом, а признаками:
   **модель** (общий код: из `C-EXV65C` берём все хвосты по дефисам — `CEXV65C`, `EXV65C`, плюс
   версии без цвета, чтобы `GP-C-EXV65C` и `CS-C-EXV65C` сошлись), **цвет** (строго равен),
   **ресурс** (расхождение до **25%** — норма; у флаконов тонера и чернил ту же роль играет
   объём в г/мл), **чип** (`с чипом` / `с чипом без счётчика` / `без чипа`; у Кактуса все
   картриджи с чипом — задаётся `--supplier-chip`). Молчание каталога про чип или ресурс —
   это «не знаем», а не отказ. Дополнительно требуем совпадения ТИПА товара (`novelty.kind`):
   флакон тонера и тонер-картридж на один принтер — разные товары. Кандидаты ранжируются по
   **редкости** общего кода: код расходника весомее модели принтера (bizhub C250i и Canon iR
   C250i совпали случайно). В МойСклад не ходим — каталог берём из витрины `ms_product`.
   Запуск: `./venv/bin/python -m prices.catalog --file docs/prc/<ключ>_<дата>_unmatched.txt`,
   отчёт — `<...>_match.csv` (до 5 вариантов на строку). Итог по Кактусу 06.08: из 54 новинок
   **44 нашлись** в каталоге (9 подтверждены по всем четырём признакам, у остальных каталог
   молчит про чип/ресурс), **10 — настоящие новинки**. Эталон Сергея сошёлся: C-EXV65 голубой
   11000 → `6058hb` (товар 6058).
18. **Вкладка «Новинки»** (`Склад → ✨ Новинки`, `/warehouse/novelties`, задача Сергея 06.08).
   Решение по каждой новинке принимает человек, а не скрипт: таблица показывает разобранные
   признаки строки поставщика и до пяти вариантов каталога, человек либо подтверждает вариант
   («Это он»), либо вбивает код руками, либо помечает строку **NEW** (полностью новая модель)
   или «не берём». Код можно вводить и голым числом товара (`6058`) — берём карточку с самым
   коротким кодом этого товара; несуществующий код отбивается с ошибкой. Данные — таблицы
   `prc_novelty` / `prc_novelty_candidate` (миграция `402`), решение человека прогоном прайса
   **не затирается**: пересобираются только признаки и подсказки. Вкладку наполняет сам прогон
   (`prices/run.py` → `catalog.sync`, пропускается при `--no-db`), чип по умолчанию берётся из
   профиля (`default_chip`, у Кактуса `chip`).
19. **Артикул поставщика — первый и главный ключ сверки** (задача Сергея 07.08). МойСклад при
   создании оприходования иногда НЕ находит товар, который в базе есть, и строка приезжает в
   новинки зря. Поэтому перед признаками сверяем артикул: колонка «Артикул» прайса **и** артикул,
   вынесенный в наименование (`Profile.article_re`, у Кактуса `CS-/CSP-/GG-/CR-…` — то же правило,
   что на загрузке счетов, `invoice_bot/invoice_to_po.py:64`). Сравнение по ключу `art_key`
   (регистр и разделители не значат ничего: `CS-TN-217` == `CS-TN217`), огрызки короче 5 символов
   не индексируем. Совпал артикул — строка **закрывается сама**, `decision = 'exists'` («уже в МС»),
   в дальнейших процессах не участвует. Решение обратимо: строка видна во вкладке под фильтром
   «уже в МС», кнопка «вернуть» на месте — артикул мог случайно совпасть с карточкой ДРУГОГО
   поставщика (случай `CET141247`, см. открытые вопросы). Пересобрать подсказки висящим строкам
   без нового прайса — `./venv/bin/python -m prices.catalog --rematch [--supplier kaktus_msk]`.
   Прогон 07.08 по Кактусу: 40 из 48 висящих строк получили варианты, **6 закрылись по артикулу**
   (`CS-TN217`→1438cs, `CS-TN221C`→2634cs, `CS-TN-321M`→3438cs, `CS-TN-321Y`→3439cs,
   `CS-I-PG510-CL511M/C`→6753cs, `CET141247R`→2412cs).
20. **Дубли карточек в МС — причина «ambiguous», а не глюк.** Загрузчик отказывается выбирать,
   когда по артикулу нашлось несколько карточек и ни одна не отличается поставщиком
   (`prices/loader.py:pick_card`), — и правильно: приходовать вслепую значит размазать остаток
   по дублям. В каталоге таких **176 артикулов на 362 живых карточках, из них 75 полных
   близнецов** (тот же артикул И тот же код) на 156 карточках — файл
   `docs/prc/ms_duplicate_articles.csv`, отправлен Сергею 07.08. Решение Сергея: **МС чистим
   руками** (не быстрый процесс), а до чистки такие строки просто выбывают из работы по правилу 19.

21. **«Связь» — универсальная модель на несколько наших товаров** (задача Сергея 07.08).
   Универсальный картридж закрывает не одну позицию: `CET141644` продаётся и как TN-328M
   (товар **4310**), и как TN-626M (товар **6827**), и таких соответствий бывает больше двух.
   Поля `ms_code` для этого мало — оно про «какой это наш товар», связь про «каким ещё нашим
   товарам он подходит». Колонка `prc_novelty.link` (миграция `403`), во вкладке — поле «связь»
   в строке разбора и отдельная кнопка «сохранить» у уже разобранных (универсальность вскрывается
   и потом). Каждый код проверяется по каталогу, неизвестный отбивается ошибкой; вводить можно
   как угодно («4310, 6827»), храним канонично.
   **Формат — как в одноимённом доп.поле МойСклада** (проверено по всем 36 заполненным карточкам,
   разнобоя нет): номер товара **четырьмя цифрами с ведущими нулями**, разделитель **`;`**, без
   пробелов — `0027`, `0027;0060`, `0053;0813;0217;1381` (до 7 кодов в строке). Связь взаимная:
   у карточки `0747np` стоит `0027;0060`, у `0027sg` — `0747;0060;0729`. Архивные карточки в
   сверке НЕ участвуют (решение Сергея 07.08: «архивные оставляем в архиве»); их в витрине
   `ms_product` и нет вовсе — коллектор МС их не грузит (`collectors/moysklad.py:125`).

22. **«Неполный набор» — полка ожидания, а не отказ** (задача Сергея 07.08). Правило 4/5 не берёт
   в работу серию, у которой в прайсе есть не все цвета. Такие строки больше не висят в разборе
   и не уходят в «не берём»: решение `decision = 'partial'`, кнопка «Неполный набор» в строке и
   массовая, свой фильтр, метка «неполный набор — ждём цвета», счётчик. Смысл — сохранить строку
   до следующего прайса: доедут цвета — «вернуть» возвращает её в работу.
   **Оговорка Сергея, подтверждена цифрами:** недостающие цвета могут уже лежать в чёрном списке,
   и тогда ждать нечего. На 07.08 из 5 висящих строк это ровно серия `CS-EPT49N`: в разборе
   голубой `CS-EPT49N200` и пурпурный `CS-EPT49N300`, а чёрный `CS-EPT49N100` и жёлтый
   `CS-EPT49N400` — в ЧС (файл `natalya_black_list_4`). У `OSP0201C-100`, `CS-I-CL441C`,
   `CS-PFI121M` соседей по серии в ЧС нет — эти ждут по-настоящему. Автопроверку «остальные цвета
   в ЧС» в интерфейс НЕ встраивал: сначала решение Сергея, что с такими делать (скорее всего
   «не берём», но это его вызов).

23. **Напоминание о полке — каждый прогон** (задача Сергея 07.08: «зашёл PFI121M, а в ЧС или на
   полке есть другие PFI121 — выводим и предлагаем вернуть в работу»). Полка бесполезна, если
   о ней не помнить: `prices/waiting.py` сверяет ждущие строки (`decision = 'partial'`, ПО ВСЕМ
   поставщикам — комплект собираем от разных) с пулом «сегодняшний прайс + чёрный список +
   остальная полка». Один модуль на два выхода, чтобы формулировки не разъезжались: конец
   `prices/run.py` печатает сводку и пишет `<поставщик>_<дата>_waiting.csv`, вкладка «Новинки»
   показывает тот же блок ⏳ через `/api/novelties/waiting` с кнопкой «вернуть в работу».
   - **Сосед по серии** = либо артикулы расходятся ровно в одном знаке при равной длине
     (`CS-PFI121M` ↔ `CS-PFI121C`), либо совпадает основа при разных цветовых хвостах
     (`CS-TN217BK` ↔ `CS-TN217C`). Прежний «префикс 5 + хвост 3» ловил `CS-PFI102M` и `CS-PFI107C`
     — это другие принтеры, не цвета. Второй путь — общая модель принтера в названии при
     одинаковом типе товара (артикулы у поставщиков разные, «для Canon iR C3326» — общее).
   - **ЧС — не приговор** (правка Сергея 08.08). В чёрный список артикул попал одиночкой,
     которой не с чем встать в пару; нашлась вторая половина — причина отсева отпала, и
     в новинки едет ПАРА. Кнопка «вернуть в работу вместе с N из ЧС» (`waiting.revive`,
     `POST /api/novelties/revive`) снимает соседа с ЧС и заводит его в новинки через
     `catalog.sync` по профилю ЕГО поставщика — иначе пара разъедется: строка в разборе,
     вторая половина по-прежнему невидима. Сделано по её примеру `CS-I-CL441C` + `CS-I-PG440`
     (пара уже в новинках, id 5 и 318; ЧС 2671 → 2670).
   - **Не закрывает комплект только то, что мы не берём ПО СУТИ товара** — отсев по правилам
     ассортимента (`bulk_toner`, `bulk_ink`, `cleaning`, `refillable`, `category_off`): флакон
     тонера 500 г остаётся флаконом, сколько его ни жди. Это единственный `dead`.
   - **Сосед из ЧС, которого не было ни в одном прайсе, не возвращается**: артикул из
     присланного списка — ещё не товар, заводить в новинки не из чего. Такому вердикт
     «спросить у поставщика», и в ЧС он остаётся (случай `CS-EPT49N100/400` — в
     `prc_price_row` их нет вовсе, поэтому серия `CS-EPT49N` пока не собралась).
   - Причина в журнале прогона — история, а не приговор: снятый с ЧС артикул навсегда
     остаётся в `prc_price_row.reason = 'blacklisted'`, поэтому «в ЧС ли он» спрашиваем
     у самой таблицы `prc_blacklist`, а не у журнала.
   - В ЧС только артикулы, без названий → цвет не виден. Название добираем из `prc_price_row`
     (забракованный артикул когда-то в прайсе был); не добралось — честный флаг «под вопросом».
   - **Побочно найдена и исправлена настоящая бага:** `\bчерн\w*` ловил слово «Чернила», и ЛЮБЫЕ
     чернила читались как ЧЁРНЫЕ (`prices/novelty.py` и `prices/features.py`). Следствие: у чернил
     все цвета схлопывались в один, комплект не собирался никогда, одиночные цвета текли в разбор,
     а голубой `CS-I-CL441C` лежал в базе с `color = 'BK'`. Лечится оговоркой `(?!ил)`;
     `catalog.rematch()` прогнан — цвета висящих строк перезаписаны верно.

24. **Файл импорта карточек в МС** (`prices/ms_import.py`, задача Сергея 08.08: «добавить
   сопоставленные с нашим каталогом товары Кактуса в МС»). Новая карточка — брат-близнец тех,
   что уже заведены с тем же ВНЕШНИМ КОДОМ: всё про ТОВАР (группа, вес, Code128, «Название WB»)
   берём у родни, всё про ПОСТАВКУ (наименование, артикул, цена, поставщик) — из прайса.
   Не сочиняем ничего: нет у родни — либо реальный вес из `supplier_dims`, либо строка уходит
   в отчёт замечаний. Запуск: `./venv/bin/python -m prices.ms_import kaktus` →
   `docs/prc/<группа>_<дата>_ms_import.xlsx` (шапка 1-в-1 с образцом Натальи от 30.07)
   и `..._ms_import_notes.md`. Импорт руками — МС → Товары → Импорт → из excel,
   «Искать = по Артикулу»; **или напрямую по API**: `--to-ms` показывает, что уйдёт,
   `--apply` создаёт карточки, `--only <коды>` ограничивает список. Существующий артикул
   карточка НЕ перезаписывает — только пропускается.
   - **Решения Сергея 08.08 (важнее того, как заполнено в МС):** «Название WB» — предлог
     «для» перед брендом принтера ВСЕГДА, даже если у родни его нет (в каталоге он лишь
     у 7% значений — это ошибки людей); картриджам **CET аббревиатура `ce`** (`5588ce`),
     не `cs`, как у 44 накопившихся карточек; **вес из прайса поставщика — первоисточник**,
     вес родни в МС только когда в прайсе его нет; **связь, проставленная вручную, пишется
     в «Доп. поле: Связь»**; **расхождение ресурса с роднёй — норма**, строку не трогаем.
   - Аббревиатура кода у остального от Феррета — `cs` (Cactus 3049, G&G 729 из 731,
     Print-Rite 57), обещанных инструкцией «gg» и «pr» в базе 2 и 0; наборы `CSP-` → `csp`.
   - **Code128 = DS + 3 буквы + внешний код с нулями и ОДИН И ТОТ ЖЕ у всех карточек внешнего
     кода** (у 1191 — `DSNXV0001191` на 28 карточках).
   - **ean8 НЕ трогаем — решение Сергея 08.08:** он сгенерируется позже по своим правилам.
     По API МС его не создаёт (проверено на 6969ce: создание без штрихкодов даёт
     `barcodes: null`, у живых карточек ean8 есть у 97.6%) — и это нормально, сами
     номер серии `20xxxxxx` не придумываем и вручную не добиваем.
   - **Группа = папка БРЕНДА ПРИНТЕРА.** Родня иногда лежит в папках поставщиков
     («Картриджи G&G», «Картриджи BULAT») и в «Оригинальных» — такие варианты при выборе
     отбрасываются, из оставшихся берём частотный.
   - Расхождения внутри внешнего кода (вес, группа, «Название WB») не проглатываются молча:
     выбор делается по частоте, но все варианты уходят в отчёт замечаний.
   - **Файл 08.08 после правок Сергея: 41 строка** (CS-CF226X-MPSXL ушла в «новинки»),
     30 с замечаниями — 19 разный вес у родни, 11 разная группа, 8 разное «Название WB»,
     4 ресурс, 4 связи. Суффиксы: 35 `ce`, 6 `cs`.
   - **Тестово создано в МС 08.08 (3 карточки):** `6855cs` CS-CF226X-MPS,
     `6967ce` CET141554, `6969ce` CET141555. Проверены чтением из МС: папка, вес, НДС 22,
     закупка, «шт»/«Китай»/Феррет, Code128, доп. поля — верно.
   - **ЗАЛИТО ЦЕЛИКОМ 08.08 по команде Сергея:** остальные 38 карточек созданы по API
     (`--to-ms --apply`), итого все 41. Сверка файл↔МС по 8 полям (код, внешний код, вес,
     «Название WB», Code128, «Связь», закупка, НДС) на всех 41 строках — **0 расхождений**.
     Первые 3 карточки после смены правила веса не поплыли (сверено, совпадают).
   - **Все 41 в оприходовании** `kaktus_msk_2026-08-06_p24`: было 30 позиций, стало 71,
     сумма 1 741 120 ₽ (доложено 3 + 38 на 415 290 ₽) — количества из последней строки прайса
     `prc_price_row`, цены из `prc_novelty`, остаток на «Удаленном складе». Разово, руками —
     при следующем полном прогоне загрузчика строки попадут в документы сами.
   - **Вес: решение Сергея 08.08 (уточнение).** Прайс поставщика — по-прежнему первоисточник;
     если веса там нет, берём вес **самой свежей карточки того же внешнего кода**, а не
     частотный. Даты заведения в API МС нет — восстанавливаем из id (время-зависимый UUID,
     `created_at()`; сверено на карточке и на документе оприходования). В замечаниях так
     и пишем: «карточка 5807hb, заведена 10.07.2026 — самая свежая по внешнему коду».
   - **«Название WB» выбирается по шаблону, а не по частоте** (`_wb_fit`): из вариантов родни
     побеждает тот, где есть цвет из названия прайса, XL/чип при их наличии и нет аббревиатуры
     поставщика; «для» всё равно доставляется всегда.

## Открытые вопросы

- **ВОПРОС 1 (закрыт 06.08).** Кактус — пересылка настроена, профиль заведён и сверен.
  Спринт МСК — Сергей запросил рассылку на почту, до неё файл из ЛК через `--file`.
- **ВОПРОС 2 (закрыт 06.08). Сакура: берём цену прайса КАК ЕСТЬ** — подтверждено Сергеем.
  Ручная загрузка на 667 позициях из 1343 давала другую цену (у части — цену прайса, округлённую
  до целого доллара: 8.20 → 8.00, 3.70 → 4.00), детерминированного правила в этом нет и повторять
  его мы не будем. Цена расходится с ручной на −174 028 ₽ при обороте 66.8 млн (−0.26%), медиана
  расхождения 3.3%, максимум 40% — это ожидаемо и на боевом прогоне не считается ошибкой.
- **ВОПРОС 3 (закрыт 06.08). «Лишние» позиции против ручной загрузки — ГРУЗИМ, ничего не режем.**
  Разница объяснилась: внешний загрузчик пропускает позиции с маленьким остатком, а отсечка по
  минимальному количеству у нас стоит **дальше по цепочке** (не в этом загрузчике). Проверено
  цифрами: у 57 WB-позиций Одиссея, которых нет в ручной загрузке, остаток 1 шт — 32, 2 шт — 24,
  3-5 шт — 1; у Кактуса из 233 «лишних» 188 с остатком меньше 10. Решение Сергея: грузить как есть,
  фильтр по остатку в профиль НЕ добавляем. Заодно снят и вопрос про WB-ассортимент Одиссея:
  ручная загрузка его берёт (643 из 700), а не игнорирует — прежняя формулировка была неверной.
- **Полный список «ручных» поставщиков** (без API). Заведены `colortek`, `odissey`, `sakura`,
  `kaktus_msk`; в МС на «Удаленном» видны ещё группы `bulat, blossom, profiline, rapid, easy_print,
  vtt, s_print_msk, s_print_spb, ramis` — какие из них ручные, подтвердить у Сергея; профили
  заводим по одному, каждый со сверкой против ручной загрузки.
- **Подтаб «Новинки» на странице распродажи ВБ — чужая территория.** Ссылка стоит в
  `web/static/warehouse.html` (пишется руками) и этого достаточно, чтобы дойти до вкладки. А вот
  `web/static/reports_wb_clearance.html` собирается генератором `reports/wb_clearance_page.py`
  (поток mkt) — вписанная туда руками ссылка исчезает при первой же генерации, поэтому строку
  из мержа убрали (`1806ad5`). Строку `<a class="rtab" href="/warehouse/novelties">✨ Новинки</a>`
  в шапку `rtabs` генератора должен добавить **mkt** — сами не лезем.
- **Связь обратно в МС — не делаем без команды.** Пока `link` живёт только у нас. Когда дойдём
  до заведения карточек, решить: писать ли доп.поле «Связь» в МС и проставлять ли встречную
  связь у второго товара (в МС она заполнена взаимно, но руками). Запись в МС — отдельное «да».
- **Расписание.** Пока запуск руками; после боевого прогона решить — cron/таймер и во сколько.
- **Шаг 2 (новинки) — проверка «а нет ли уже в МС» СДЕЛАНА** (правило 19): без неё заводить
  карточки по списку ненайденных нельзя было — плодились бы дубли с одинаковым артикулом.
- **Чистка дублей МС — за Сергеем.** Файл `docs/prc/ms_duplicate_articles.csv` у него с 07.08;
  после чистки часть строк перестанет быть `ambiguous` сама собой.
- **Снимок каталога `ms_product` устаревает.** На 07.08 самая свежая карточка — от 03.08 19:09,
  и это уже дало призрак: в снимке живёт карточка `2412cs` с артикулом `CET141247R`, в боевом МС
  её нет. Сергей: каталог обновляется целиком по воскресеньям, ждём, не срочно. Заодно снят и
  вопрос про `CET141247` (без `R`) — такая карточка в МС есть, но она **другого поставщика**,
  совпадение артикулов случайное, и строка Кактуса попала в необработанные правильно. Вывод для
  правила 19: артикул сверяем **точно**, никаких «на букву короче».

## Следующий шаг

**Пять шаблонов человека (03.08 … 10.08) проверены и залиты полностью — пункты 31, 32, 35, 36, 37.**
Расхождений 0. Правило про наборы уточнено (правило 25, врезка «Наборы поставщиков»). Ещё: (а) встречная «Связь» `1801` на карточке `1024hb` — в МС не писали, ждёт команды;
(б) набор `PG745XL-CL746XL-SET` не заводили (правило 25), если решение изменится — внешний код
идти вверх от **7177** (занят 10.08). Следующие шаблоны гонять через `tools/prc/tpl_check.py`
→ `tpl_load.py` → `tpl_verify.py` (проверка / заливка / сверка чтением; описаны в пункте 32).

Прайсы остальных поставщиков (Блоссом `BS-`, Солюшнс `SP{code}_MSK`, Позитив, Сакура, Одиссей)
— после шаблона, отдельной сессией.

**Кактус боевой прошёл 06.08** (см. «Состояние»). Дальше — Колортек, Сакура, Одиссей, каждый
со своим «ок» Сергея на первый `--apply`. Открытых вопросов по правилам не осталось: 1, 2 и 3
закрыты 06.08. Порядок для каждого нового поставщика:

1. человек грузит руками КАК ОБЫЧНО — это эталон для сверки;
2. после его загрузки — наш **сухой** прогон и сверка позиция-в-позицию с его документами
   (скрипт сверки: тянем позиции документов группы на «Удаленном» и сравниваем с `prc_price_row`
   последнего прогона по `ms_id` / количеству / цене в копейках);
3. сошлось — `--apply`. Наш прогон пометит и снесёт ручные документы дня и создаст свои,
   остаток не задвоится;
4. после боевого — сверить остатки склада «Удаленный» и договориться, с какого дня человек
   перестаёт грузить этого поставщика руками.

По Колортеку на 06.08 свежего письма ещё не было (последнее — «Отчет ЦК на 05.08.xlsx», ручная
загрузка 05.08 11:41), ждём новый прайс. Свежий прайс важен ещё и как проверка разбора: если
поставщик поменяет формулировку остатка или состав колонок, загрузчик обязан упасть/вынести
строки в отчёт, а не тихо разобрать половину.

До первой боевой загрузки поставщика проверка аномалий работает вполсилы: базы сравнения нет,
блокировать нечем — со второго дня она включается сама. У Кактуса база теперь есть: следующий
его прогон уже сравнивается с записанной нами закупочной.

Дальше по задаче: расписание (cron/timer) — после боевых прогонов остальных; шаг 2 (новинки) —
с дополнительной проверкой на дубли МС в «Необработанных».

**На 08.08 на столе — первый файл импорта карточек** (`docs/prc/kaktus_msk_2026-08-08_ms_import.xlsx`,
правило 24). Порядок: Сергей смотрит отчёт замечаний → решает пять вопросов ниже → импорт руками
в МС → следующий прогон Кактуса кладёт эти позиции на «Удаленный склад» уже сам.
Вопросы к первому файлу:
1. `CS-CF226X-MPS` (12000 стр.) и `CS-CF226X-MPSXL` (20000 стр.) сели на внешний код 1191, где
   у родни 9000 стр. Ресурс лучше — это отдельная модель под НОВЫМ внешним кодом или всё же 1191?
2. «Связь» у 5 строк заполнять не стали (в образце Натальи колонка пустая) — писать или нет?
3. Страну поставили «Китай» всем (как в образце) — проверить.
4. Вес трёх Konica TNP80 взят из прайса Cactus (0.335 кг, арт. `CR-KMTNP80C/M/Y`), у родни в МС
   веса нет вовсе — подтвердить или заменить на замер/nix.ru.
5. Единственная настоящая новинка (`CS-TKY4-100`, тонер-флакон) в файл НЕ вошла: у неё нет
   внешнего кода, его должен присвоить человек.

25. **Новинки: правила Сергея 09.08 (действуют для всех поставщиков).**
    - **Внешний код** — 4 цифры, идём ВВЕРХ от последнего занятого (не в дырки). Последний занятый
      на 09.08 — `7166`; проверять опросом МС, а не снимком `ms_product`: снимок от 03.08 отстаёт.
    - **Полный цветной набор** новинок ставим подряд одним блоком кодов: чёрный, голубой,
      пурпурный, жёлтый, затем остальные цвета. **Карточку на НАБОР в МС не заводим** — набор
      собирается в другом месте.
    - **Code128 у новинок не заполняем** — сгенерируется позже и приедет в МС сам. ean8 МС
      ставит сам НЕ всегда: есть у всех 43 карточек 08.08 и у 7167/7168, у 7169/7170 при том же
      теле запроса не появился. Причина не найдена, на решения не влияет (ean8 не наш) — но
      «ean8 создастся сам» как гарантию не использовать.
    - **Модель новинки — следующая ступень ресурса**, а не копия названия поставщика. Ряд CF226:
      1190 CF226A 3100 → 1191 CF226X 9200 → 6855 CF226XL 12000 → новинка 20000 = **CF226XXL**.
    - **Универсальный картридж (HP + Canon) — отдельная карточка на каждый бренд**, взаимная
      «Связь» внешними кодами (формат поля — коды через `;`, сверено по 36 живым карточкам).
      Артикул поставщика ставится **только на одну** карточку (решение Сергея 09.08): остаток
      и цена считаются один раз, вторая получит свой артикул, когда придёт позиция другого
      поставщика. Задвоенный артикул ломает поиск и в импорте МС, и в нашем загрузчике.
    - **Заведённую новинку сразу добавляем в текущее оприходование** этого поставщика.
    - Сделано 09.08: `7167cs` CF226XXL (арт. `CS-CF226X-MPSXL`, 1225 ₽, вес 1.4 кг из прайса
      Кактуса, папка HP) и `7168cs` 052HXL (без артикула, папка Canon), связь 7167↔7168;
      7167 доложен в `kaktus_msk_2026-08-06_p24` — 500 шт × 1225 ₽, документ 72 позиции,
      2 353 620 ₽.
26. **Тонер/чернила: правило «больше 150 г — не берём» НЕ работает** (решение Сергея 09.08).
    Порог по граммам ошибается в обе стороны: 650 г для TK-3130 — это ровно разовая заправка,
    а 100 г для той же тубы — не заправка вовсе. Разбор идёт от МОДЕЛИ: модель картриджа +
    список принтеров (по популярности в РФ, нероссийские убрать) + доза полной заправки; из одной
    позиции поставщика выходит несколько карточек. Первый разбор линейки Кактуса TKY4 —
    `docs/reports/prc_toner_tky4_2026-08-09.md`: доза полной заправки берётся из УЗКОГО списка
    моделей у флакона (650 г → TK-330/360/3130/3190/3490 — сказано поставщиком), весь модельный
    ряд у флакона = подсыпка, отсутствие моделей = мастер-флакон.
27. **Сыпучий тонер: карточка = МОДЕЛЬ КАРТРИДЖА, а не флакон** (решение Сергея 09.08).
    Все 82 существующие карточки сыпучего тонера заведены по артикулу флакона, модели свалены
    в наименование («Тонер TK-1170/TK-1150/… для Kyocera черный 100 г»). Так делал человек, и
    **это ошибка — не повторять**: покупатель ищет тонер для своей модели, а не артикул флакона.
    Разбор позиции поставщика: список моделей → нероссийские убрать → для каждой модели найти
    дозу полной заправки → карточка заводится **только если у нас есть флакон ровно этой дозы**.
    - **Артикул поставщика — на ОДНОЙ карточке группы.** Один артикул на нескольких карточках =
      `ambiguous` в `loader._pick`, и позиция вылетает из загрузки ЦЕЛИКОМ (не «возьмётся
      случайная»). Остальные карточки группы связываются полем «Связь» — но помнить: это простой
      текстовый атрибут, ни остатка, ни цены он не переносит (в коде его только пишет `ms_import`,
      читателей нет). Остаток и закупка живут на карточке с артикулом.
    - **ean8 не ждать и не проверять** (Сергей 09.08): появляется не сразу и не всегда.
    - **Калибровка дозы:** от поставщика 650 г ↔ 25 000 стр ≈ 38 стр/г; заправочные комплекты
      Кактуса дают 24–31 стр/г. Отсюда 40 000-страничные тубы ≈ 1000 г, 15 500 стр ≈ 400 г.
      Дозы от Сергея: TK-3100/3060/3160 — 300 г, TK-3150 — 350 г, TK-3170 — 380 г (флаконов такой
      дозы у Кактуса нет — задел на других поставщиков).
    - **Сделано 09.08 по линейке Кактуса TKY4** (отчёт `docs/reports/prc_toner_tky4_2026-08-09.md`):
      `CS-TKY4-100` → ЧС (подсыпка). Флакон 650 г разложен на **7169cs TK-3130** (несёт артикул
      `CS-TKY4-650`), 7170cs TK-3190, 7171cs TK-330, 7172cs TK-360, 7173cs TK-3430; флакон 1000 г —
      на **7174cs TK-3200** (артикул `CS-TKY4-1000`) и 7175cs TK-3440. Закупка 1150 / 1526 ₽,
      вес 0.7 / 1.05 (у Кактуса 50 г → 0.1, 100 г → 0.15 по `supplier_dims`, тара ≈ 0.05 кг).
      Документ `kaktus_msk_2026-08-06_p24` — **74 позиции, 2 502 814 ₽** (позиции только на двух
      карточках с артикулами).
    - **Выброшено:** TK-3490 — 0 карточек в МС и 0 независимых строк в четырёх прайсах, единственное
      упоминание во всей базе это компат-лист самого флакона → на нашем рынке не ходит.
      TK-3410 (≈400 г) и ряд TK-3100/3060/3150/3160/3170 (300–380 г) — флакона нужной дозы нет.
    - **Под сомнением, можно снять одной правкой:** 7173cs TK-3430 — поставщик не назвал его в
      компат-листе 650 г, но модель есть в листе того же тонера на 100 г и 1000 г, а ресурс
      25 000 стр. = класс TK-3130/3190. Это наш вывод по ресурсу, не слова Кактуса.
    - **Граница 150 г снята** (Сергей 09.08): в `novelty.py` удалён `REFILL_LIMIT` и блок объёма
      в `check()`; причины `bulk_toner`/`bulk_ink` оставлены в `NOVELTY_REASONS`, `loader.SKIP_REASONS`
      и `waiting.REJECT_REASONS` только как подписи к строкам старых прогонов — новые не ставятся.
      Порог врал в обе стороны: 650 г для TK-3130 — ровно заправка, 100 г для неё же — не заправка.
    - **Найденная ошибка в чужой карточке:** у 7141cs `CS-TKY4-50` «Название WB» перечисляло модели
      линейки TKY3 вместо своей серии 3xxx (копипаста с 7140cs). **Сергей поправил в МС 09.08.**
28. **Разведка модели в сети — платная, только с подтверждением сметы** (задача Сергея 09.08,
    `prices/research.py`, миграция 404, таблица `prc_model_research`). Четыре вопроса, на которые
    нет ответа ни в прайсе, ни у нас в базе, задаются агенту с веб-поиском: **модель российская
    или нет**, **популярна ли** (стоит ли заводить карточку), **доза полной заправки** и **полный
    список принтеров** (у поставщика его в строке нет, а наши названия неполные — у TK-330 в базе
    стоял один FS-4000DN).
    - **Спрашиваем ТОЛЬКО по строкам, которым заводим карточку** (решение Сергея 09.08):
      `decision='new'`, то есть присваиваем новый внешний код. `matched`/`exists` уже в МС,
      `partial` ждёт цвета на полке, `pending` ещё не разобран, ЧС не берём — за них не платим.
      Гейт серверный (`_research_split` в `web/app.py`), в таблице кнопка «🌐 сеть» есть только
      у строк NEW, у остальных выключена с подсказкой. Считать смету «по всей таблице» —
      бессмысленно: 55 строк дают 93 модели, из них 63 в уже разобранных строках, а 24 —
      компат-лист флакона `CS-TKY4-100`, который в ЧС.
    - **ЕДИНИЦА РАБОТЫ — СТРОКА ПРАЙСА, один запрос на строку сразу по всем пунктам** (правило
      Сергея 09.08: «На одну модель — один запрос сразу по всем пунктам»). Так он спрашивает
      руками, и код повторяет это дословно. Два вида запроса:
      **тонер/чернила** — «делаю отдельные карточки по этому товару поставщика для маркетплейсов,
      убрать нероссийские модели, по оставшимся: список принтеров в РФ, объём тонера/чернил для
      полной заправки, ресурс печати, вес, размеры упаковки; точные данные из проверенных
      источников»; **картридж** — «полный список принтеров для России, вес товара в упаковке,
      размеры упаковки». Промпты в `PROMPT_REFILL` / `PROMPT_SINGLE`.
    - **Вес и размеры упаковки** добавлены к разведке (миграция 405): без них карточку не завести —
      логистика маркетплейса считает по коробу. Это **ВЕБ-размер**, он младше короба чистого
      поставщика из `supplier_dims` (CLAUDE.md, габариты, п. 12), поэтому источник помечается
      (`pack_source`) и за замер не выдаётся. Спрашиваем упаковку ОДНОЙ штуки, не мастер-картон.
    - **Ключ кэша — МОДЕЛЬ** (`model_key` без дефисов и регистра), а не строка прайса: одна TK-3190
      приходит в разных прайсах и загрузках, платим за неё один раз. Таблица одновременно кэш и
      журнал расхода (`searches`, `cost_usd` — факт из `usage.server_tool_use.web_search_requests`).
    - **Гейт от случайной траты — в четырёх местах:** `estimate()` — чистая арифметика без сети;
      `ask()` без `confirm=True` поднимает `NeedsConsent`; CLI без `--apply` только печатает смету;
      `POST /api/novelties/research` без `confirm` возвращает смету и `ok:false`. В интерфейсе —
      модалка «Запрос платный… ≈ $X ≈ Y ₽» с кнопками «Отмена» / «Запустить».
    - **Смета — за СТРОКУ, а не за модель:** до 6 поисков × $0.010 + токены ≈ $0.11 →
      **≈ $0.17 ≈ 14 ₽ за строку прайса**, сколько бы моделей в ней ни было (флакон на 7 моделей
      стоит столько же, сколько одиночный картридж). Курс ЦБ через `prices/cbr`, фолбэк 90 ₽.
      Модель и потолки — `PRC_RESEARCH_MODEL`, `PRC_RESEARCH_MAX_USES`, `PRC_RESEARCH_MAX_TOKENS`
      в `.env`. Строка, у которой все модели уже в кэше, не спрашивается вовсе.
    - **Доза, принтеры, вес и короб без ссылки-источника не принимаются** — пишется `null` /
      пустой список.
      Правило то же, что в габаритах: синтетическая цифра дороже отсутствующей.
    - **Спрашивать могут и про принтер:** в хвосте «…для принтера Kyocera P6030CDN» поставщик пишет
      то принтеры, то модели картриджей, различить по формулировке нельзя. Сеть сама называет
      штатный картридж (поле `cartridge`) и отвечает уже по нему.
    - **Разведка ничего не решает за человека** — это подсказка в таблице, а не автоматический
      `skip`. Принтеры, которых нет в нашем названии, подсвечены (`new_printers`): это ровно то,
      что стоит дописать в карточку.
    - **Живой платный прогон — каждый раз отдельным «да» Сергея на конкретную сумму.** Сегодня
      под правило NEW подходит ОДНА строка — `CS-CF226X-MPSXL` (2 модели), **≈ $0.17 ≈ 14 ₽**.
      На 09.08 **ни одного платного запроса не сделано**, кэш `prc_model_research` пуст, всё
      проверено на моках и на арифметике.
29. **Вкладка новинок приведена к сегодняшней работе** (задача Сергея 09.08,
    `web/static/novelties.html`): колонки `☑ | Товар поставщика | Модели и разведка | Наш товар |
    Решение`; тип товара бейджем (тонер/чернила/картридж — `kind` был в БД и не показывался);
    цена и остаток поставщика в строке; липкие шапка и панель действий; поиск по названию/артикулу/
    модели и фильтр по типу — на клиенте; счётчики-плитки кликабельны как фильтр статуса;
    разобранные строки схлопнуты. Появились кнопки **«🌐 Спросить сеть»** (строкой и пачкой, через
    модалку со сметой) и **«В ЧС»** (строкой и пачкой, `POST /api/novelties/blacklist` →
    `prices/blacklist.add(source="ui")` + `decision='skip'`) — раньше пути в ЧС из интерфейса не было
    вовсе, `CS-TKY4-100` уезжал туда скриптом.

30. **Признаков сопоставления ШЕСТЬ, и все они видны человеку** (задача Сергея 09.08:
    «вывести все эти значения по необработанным позициям из прайса поставщика и из нашего
    каталога… а когда накопится опыт — скрипт сам сможет по совпадениям 6/6»). Было пять
    (модель, тип, цвет, ресурс/объём, чип), **бренда принтера в сверке не было вовсе** — хотя
    именно он ловит случайное совпадение кода модели («bizhub C250i» и «Canon iR C250i»).
    - **Бренд** (`features.brand`) — множество, а не строка: картридж законно подходит и к HP,
      и к Canon, сравнение = непустое пересечение. Список `BRANDS`/`BRAND_RE` переехал из
      `ms_import` в `features` (один список на поток, `ms_import` импортирует оттуда).
      Синонимы канонизируются (`BRAND_CANON`): Kyocera Mita → Kyocera; Konica / Minolta /
      Develop → Konica Minolta; Utax → Triumph-Adler; Katusha → Катюша. Без канонизации
      8 строк из 47 давали ложный конфликт. Покрытие: строки прайса 55/55, карточки каталога
      72% — остальное «молчит» (`—`), а не «не совпало».
    - **Три состояния у каждого признака**, а не два: `true` подтвердился, `false`
      противоречит, `NULL` в названии карточки не написано. Молчание в счёт `N/6` не идёт.
      Флаги и счёт лежат в `prc_novelty_candidate` (миграция `406`) — это и есть накопленный
      опыт, по которому потом проверяется правило 6/6 задним числом, без нового прогона.
    - **Бренд НЕ фильтрует кандидатов**, только опускает их в хвост списка (решение Сергея):
      ошибка в названии нашей карточки не должна ПРЯТАТЬ правильный вариант от человека.
    - **Предохранитель автозакрытия:** при совпавшем артикуле поставщика, но конфликте по
      бренду или ресурсу строка НЕ закрывается в `exists`, а остаётся человеку; уже закрытая
      с конфликтом возвращается в `pending`. Обратный ход однозначен: `exists` ставится только
      автоматом, человек ставит `matched/new/partial/skip`.
    - **Пересборка задним числом:** `./venv/bin/python -m prices.catalog --recheck` — по ВСЕМ
      строкам, включая разобранные (`--rematch` по-прежнему только по висящим). Решения
      человека не трогает.
    - В таблице сетка сверки стоит в колонке «Наш товар» под выпадающим списком и
      перерисовывается при смене варианта; у разобранных строк свёрнута в «сверка N/6».
    - **Автосопоставление по 6/6 НЕ включено** — сейчас только показываем и копим счёт.
      Замер на 41 разобранной строке (`docs/reports/prc_match_score_2026-08-09.md`): 6/6 — 12
      строк (29%), все решены как `matched`, ни одного отказа; 5/6 — 26, 4/6 — 2, 3/6 — 1.
    - **Что нашлось сразу:** `CS-TN-321M`/`3438cs` и `CS-TN-321Y`/`3439cs` закрылись сами по
      совпавшему артикулу, а в карточках у нас «Brother 1500 стр.» против «Konica Minolta
      25000 стр.» у поставщика — расхождение в 16 раз. Предохранитель вернул обе в работу.
      **Название карточки в МС не правим сами — решает человек.**

31. **Шаблон карточек от человека — проверка и заливка (10.08.2026).** Файл Натальи из дропбокса
    (`20260810_122346_..._03.08_-_загрузки_новых_товаров_в_МС.xlsx`, 7 строк: ТОНЕРСТОР 4,
    КПД(ВТТ) 3). Проверка — `docs/prc/tpl_2026-08-10_verdict.md` (+ `..._check.md`, `..._rows.md`),
    скрипт заливки `scratchpad/load_final.py` поверх `prices.ms_import.create_in_ms`.
    - **Сверялось живым опросом МС, не снимком `ms_product`:** внешний код занят и держит тот же
      товар, артикул свободен (нет `ambiguous`), Code128 = Code128 родни внешнего кода, вес = вес
      самой свежей карточки того же внешнего кода, группа = папка бренда принтера, шт/НДС 22/
      Китай/365, «Код поставщика» = «Артикул», контрагент есть в МС.
    - **Решения Сергея 10.08:** (1) строка Hi-Black с занятым кодом `1024hb` переехала на
      **внешний код 1801** (та же универсальная модель OKI 44059106/44059118), код `1801hb`,
      «Связь» = `1024`; вес взят у родни 1801 = **0.7** (в файле стоял 0.5 — вес родни 1024),
      Code128 и «Название WB» тоже от родни 1801 (`DSKBA0001801`, «Картридж 44059106 для Oki
      пурпурный»). (2) **Набор в МС не заводим** (правило 25 подтверждено). (3) Grafit получил
      **свою аббревиатуру `gf`** (`4364gf`) — раньше человек клал Grafit на `bt` (=БУЛАТ),
      теперь не путаемся. (4) «Название WB» без предлога «для» — ошибка файла, правим.
    - **Создано 6 карточек:** `47537q`, `47547q`, `47557q` (7Q для HP 659A), `4364gf` (Grafit
      DL-420), `6883hb` (Hi-Black TK240X), `1801hb`. Сверка чтением из МС по 10 полям —
      **0 расхождений**.
    - **Грабли:** числовой артикул (`220095936`) приезжает из xlsx как int, доп.поля МС типа
      «строка» отбивают его `HTTP 400 code 2016` — приводить к строке.
    - **Не сделано намеренно:** встречная «Связь» `1801` на существующей `1024hb` (запись в МС
      по чужой карточке — отдельное «да»); в оприходования эти позиции не клали — у ТОНЕРСТОР
      и КПД(ВТТ) профилей прайса ещё нет.

32. **Второй шаблон человека — проверка и заливка (10.08.2026).** Файл
    `20260810_130040_..._04.08_-_загрузки_новых_товаров_в_МС.xlsx`, 8 строк, один поставщик
    ООО «КОМПАНИЯ РМ» (7 ProfiLine `pl` + 1 GalaPrint `gp`). Отчёт —
    `docs/prc/tpl_2026-08-10_b_check.md`.
    - Все 8 внешних кодов существуют и держат тот же товар: `6983`–`6986` (Canon 067 BK/C/M/Y),
      `5482` (Kyocera TK-1240/1248), `6206` (TK-1270), `0854` (Brother TN-3390), `5488` (Canon 069BK).
      Коды карточек и артикулы свободны, Code128 / вес / группа / шт-НДС 22-Китай-365 /
      «Код поставщика» = «Артикул» — сошлись с роднёй по всем строкам.
    - Единственная правка: `5488gp` «Название WB» без предлога «для» → «Картридж 069 **для** Canon
      5094C002 черный с чипом» (правило 24, то же, что в пункте 31).
    - **Создано 8 карточек**, сверка чтением из МС по 10 полям — **0 расхождений**.
    - Инструмент обобщён под любые следующие шаблоны: `scratchpad/tpl_check.py <xlsx> <out.md>`
      (проверка живым опросом МС + последний занятый внешний код), `tpl_load.py <xlsx>
      [--apply] [--only] [--drop] [--over JSON]` (заливка, по умолчанию проба, числовые артикулы
      приводит к строке), `tpl_verify.py <xlsx> [коды]` (сверка чтением из МС).

33. **Шаблон человека проверяется по ШЕСТИ признакам, как и новинки (задача Сергея 10.08).**
    Первые две проверки шаблонов (пункты 31–32) сверяли внешний код «глазами по названию родни»
    — это не сверка. Теперь проверка шаблона использует ТО ЖЕ каноничное сравнение, что и поток
    новинок: `prices.catalog.compare` → **модель, тип, бренд, цвет, ресурс/объём, чип**, три
    состояния (да / нет / молчит), и сверяется КАЖДАЯ карточка родни, а не «хоть одна».
    - **Чип берётся из «Название WB», а не из наименования МС.** В наименованиях МС чипа нет ни
      у одной из 14 залитых сегодня карточек; с учётом «Название WB» чип написан явно у 5 из 14.
    - **Семантика чипа (решение Сергея 10.08):** если про чип не сказано НИ в прайсе/шаблоне,
      НИ в МС — это **совпадение**, а не «не проверили». Расхождение — только когда обе стороны
      сказали и сказали РАЗНОЕ. Молчание одной стороны — по-прежнему молчание (`None`).
      Правило вшито в каноничный `catalog.chip_ok`, то есть действует и в сверке новинок;
      счёт `N/6` в `prc_novelty_candidate` посчитан по старой семантике — освежается
      `prices.catalog --recheck`, отдельным прогоном (не запускал).
    - **Чип вводится в шаблон:** если родня про чип говорит, а в шаблоне пусто — замечание
      «чип в шаблоне не указан, у родни — с чипом; заполнить». Это не блокирует загрузку.
    - **Порог замечания:** признак противоречит ВСЕЙ родне → это другой товар, строка не грузится;
      противоречит части родни → замечание «глазами», решает человек.
    - **Разбор 14 карточек 10.08** (`docs/prc/six_signs_2026-08-10.md`): тип и бренд — 14/14;
      модель, цвет, ресурс подтверждены у всех, ни одного противоречия ВСЕЙ родне. Все точечные
      расхождения разобраны вручную и оказались либо формой записи (`067` против `067BK`,
      `TN-3390T` против `TN-3390`, `HB-44059118/44059106` против `44059106/44059118`), либо
      **ошибкой в карточке родни**: `4754sg` «1300 стр.» вместо 13000 у W2011A; разбор
      «12k против 30k» у драма `4364` — известный спор родни (решение 08.08: норма).
    - Разбор голыми регулярками пробовал и выбросил: «M7300, 12k» читалось как 7 300 120.
      Каноничный `features`/`compare` — единственный разборщик признаков в потоке.
34. **«Связь» — седьмое поле сверки, тоже берётся у родни (задача Сергея 10.08).** В проверке
    шаблона её не было вовсе: карточка молча выпадала из универсальной модели. Теперь
    `tpl_check.py` сравнивает «Связь» строки с «Связью» родни внешнего кода — пусто у нас при
    заполненной родне и несовпадение множеств дают замечание, а несуществующий внешний код в
    «Связи» — отдельное. Формат прежний: четырёхзначные коды с ведущими нулями через `;`.
    - **`5482` (Kyocera TK-1240/TK-1243/TK-1248 Universal) связан с `7087`** — сказал Сергей 10.08.
      На новой `5482pl` «Связь» = `7087` проставлена и проверена чтением.
    - **Чужие живые карточки не трогаем — решение Сергея 10.08 («ничего не трогать»).** Значит,
      связь остаётся односторонней: `7087at` без встречной `5482`, остальные 16 карточек кода
      5482 без «Связи» вовсе, `1024hb` без встречной `1801` (пункт 31). Мы ставим «Связь» только
      на карточки, которые заводим сами. Правку существующих делает человек в МС.

    - Инструмент переехал из scratchpad в репозиторий: `tools/prc/tpl_check.py` (проверка + сетка
      6 признаков), `tools/prc/tpl_load.py` (заливка, по умолчанию проба), `tools/prc/tpl_verify.py`
      (сверка чтением из МС).

35. **Третий шаблон человека — проверка и заливка (10.08.2026).** Файл
    `20260810_135846_..._05.08_...xlsx`, 3 строки, поставщики ООО «ТОНЕРСТОР» и ООО «КПД»(ВТТ).
    - **Залито и сверено чтением, расхождений 0:** `2949bt` (CET7310U, TN-323 для Konica Minolta
      Bizhub 227/287/367, 23K, 2587.82 ₽ — родня 9, 6/6) и `7176hb` (HB-42STB20 для F+ imaging
      MB401ADFN, 20K, 8418.99 ₽, 1.2 кг).
    - **`7176` — новый внешний код, согласован Сергеем 10.08** (правило 25: вверх от 7175).
      Проверено: `42STB20` в МС нет нигде; соседние `6850/6851/6852` = 40STB06/15/20 (P40/M40/
      MB401), `6853` = 60STB15 — другая серия, привязывать было не к чему. Code128 не ставим
      (новый код); EAN8 МС генерирует сама — это норма, не расхождение (так же у `7173–7175cs`).
      Группа — «Картриджи/Картриджи F+» (в шаблоне был хвостовой пробел): там же лежат `6852hb`
      и `6851wb`. Вторая папка «Картриджи для F+» и `6852ct`/`6820hb` в «Картриджи Lexmark» —
      наследие, не трогаем.
    - **`5730hb` (HB-W1520X) уехал на СВОЙ внешний код `7177` — карточка `7177hb`, залита
      и сверена чтением, расхождений 0.** Родня внешнего кода `5730`:
      5 карточек из 8 пишут «ЧИП БезСчетчика» (`5730at`, `5730wb`, `5730sg`), в названии
      Hi-Black про чип не сказано. **Решение Сергея 10.08: чип НЕ придумываем** — если
      у поставщика в названии не сказано чётко «чип без счётчика», дописывать это в «Название WB»
      нельзя. **И если у поставщика уточнили, что чип обычный (со счётчиком) — это другой товар,
      его надо вязать к ДРУГОЙ карточке.** Отдельного внешнего кода «W1520X с чипом» в МС нет
      (проверено по всем `W1520X`/`152X`: только `5730` и двойная `5407` W1510X/W1520X), значит
      этот вариант = новый внешний код. **Сергей уточнил у поставщика 10.08: чип обычный,
      со счётчиком** → завели `7177hb`, Code128 не ставили (новый код), «Название WB» =
      «Картридж W1520X для HP черный, **с чипом**» — это уже не выдумка, а подтверждённый факт,
      и именно он отличает карточку от `5730`. «Связь» пуста: это вариант по чипу, а не
      универсальная модель.
    - **Разбор ресурса починен** (`prices/features.py`, коммит `39967c1`): пробел внутри числа —
      только разделитель тысяч, поэтому «HP LJ Pro 4004/4104 9,7K» больше не читается как 41049,
      «Bizhub 227/287/367 23K» — как 36723, а «7Q 069K … 2100 стр.» — как 69000 (ведущий ноль =
      код, не ресурс). Обе ложные пометки «ресурс расходится с роднёй» по этому файлу ушли.
    - **Бренд `F+ imaging` добавлен в `BRANDS`** (папки МС «Картриджи F+»); `BRAND_RE` теперь
      собирается через `re.escape` — из-за плюса в «F+» шаблон иначе ломался.
    - `tpl_load.py`: строка без «Кода» (новый внешний код) берётся в `--over` по ключу `""`.

36. **Четвёртый шаблон человека — проверка и заливка (10.08.2026).** Файл
    `20260810_145715_..._07.08_...xlsx`, 2 строки, поставщик ООО «КОМПАНИЯ РМ». Обе привязки
    к существующим внешним кодам верны, залито и сверено чтением, расхождений 0:
    - `5725pl` (PL_THM130, Катюша P130/MFP-M130, 3K, 1730.97 ₽, вес 0.75) — родня 13, 6/6.
    - `6207gp` (GP_006R04360_BK, Xerox C310/C315 Black, 3K, 1020.63 ₽, вес 0.3) — родня 1
      (`6207bs`), 6/6.
    - Чип у `5725pl` не дописывали: ProfiLine про чип молчит, из 13 родни его называет одна
      (`5725t2`, «с чипом»), остальные 12 молчат — обоюдное молчание = совпадение, а выдумывать
      нельзя (решение Сергея 10.08, пункт 35). Замечание проверки «чип не указан, у родни —
      с чипом» здесь информативное, не блокирующее.

37. **Пятый шаблон человека — проверка и заливка (10.08.2026).** Файл
    `20260810_145939_..._10.08_...xlsx`, 3 строки. Привязки к существующим внешним кодам верны.
    - **Залито и сверено чтением, расхождений 0:** `5963sl` (DFKYPA4000010, БУЛАТ s-Line,
      Kyocera TK-1260 PA4000x, 10k, 997.34 ₽, вес 1.0 — родня 11, 6/6) и `4795gp` (GP_CF289Y,
      HP LJ M507/M528, 20k, 2515.63 ₽, вес 1.91 — родня 9, 6/6).
    - Чип у `5963sl` не дописывали: БУЛАТ молчит, 2 карточки родни из 11 говорят «с чипом»,
      9 молчат (решение Сергея 10.08, пункт 35).
    - **`2245hb` (Hi-Black HB-CF350/351/352/353, набор 4 цветов HP 130A, 1621.81 ₽) залит
      и сверен чтением, расхождений 0** — после уточнения правила про наборы (ниже) и
      **исправления веса Сергеем: 4 → 1.8 кг**. Повод для сомнения был такой: одиночные
      CF350A/CF353A у Кактуса 0.42–0.445 кг → набор из четырёх ≈ 1.8 кг; у родни веса нет
      вовсе, Hi-Black в `supplier_dims` отсутствует, сверить было неоткуда.
    - **НАБОРЫ ПОСТАВЩИКОВ — уточнение правила 25 (Сергей, 10.08.2026).** Запрет «карточку
      на НАБОР не заводим» означает ровно одно: **набору не даём НОВЫЙ внешний код** — наборы
      собираются в другом месте. Но если **у поставщика набор идёт отдельной готовой позицией**
      и такой **внешний код в МС уже есть** — привязываем к нему без вопросов, обычной карточкой
      с аббревиатурой поставщика. Так и было сделано с `2245hb`; в МС это давняя норма
      (`0169gp`, `0079ep`, `0160pl`, `0096ct` …).

38. **Кактус — обновление оприходований из почты (10.08.2026, боевой прогон).** Журнал
    `prc_price_load` **#18**, письмо `Прайслист Cactus 10.08.26.xlsx` (4453 строки, шапка стр. 2,
    рубли, курс не нужен).
    - **Записано:** 2471 позиция в **25** документах на **180 645 919 ₽**, обновлено 313 карточек,
      снесено 24 прошлых документа от 06.08. Сверка чтением из МС: 25 документов, все проведены,
      позиций 2471, сумма до копейки равна журналу.
    - Было 06.08 (#15): 2330 позиций / 24 док / 164 069 681 ₽ → рост +141 позиция, +16.6 млн ₽.
    - **Аномалий цены 0**, медиана отношения к нашей прошлой закупочной ровно 1.000 по 2287
      позициям — прайс не «поехал».
    - Отсев: 1705 «категория не нужна», 137 чёрный список, 98 нет товара в МС, 20 неполный
      цветовой комплект, 14 перезаправляемые, 4 неоднозначный артикул, 3 промывка, 1 «брак».
    - **Хвост для работы:** 102 строки новинок, из них 55 нашли пару в каталоге → вкладка
      «Новинки» (`/warehouse/novelties`). Лист ожидания комплектов: **5 строк можно вернуть
      в работу** (цвета появились в сегодняшнем прайсе: `OSP0201C-100`, `CS-I-CL441C`,
      `CS-I-PG440`), **2 спросить у поставщика** (`CS-EPT49N100`, `CS-EPT49N400` — вся серия
      в ЧС, но в прайсах мы её не видели). Файлы: `kaktus_msk_2026-08-10_{skipped,unmatched,
      watch,waiting,anomalies}`.
    - Мелочь: на складах «Звездный» и «Брак» лежат два старых документа `kaktus_msk_2025-09-02_p29`
      и `kaktus_msk_2025-12-22_p29` (по 1 позиции). Загрузчик их не трогает — он работает только
      с группой на «Удаленном». Это правильно, но знать полезно.

39. **Разобранные новинки Кактуса и лист ожидания (10.08.2026).** Проверено, куда легли 47
    строк, разобранных человеком во вкладке «Новинки» 07–09.08 (из них 43 — 07.08): **41 попала
    в оприходования 10.08 и имеет положительный остаток на «Удаленном»**, 6 — нет. Важное
    наблюдение: `prc_novelty.ms_code` хранит карточку-**якорь** (родню, у которой взят внешний
    код), а заведённая карточка — своя, `<вн.код><суффикс поставщика>` (`ce` у CET-артикулов,
    `cs` у `CS-`). Сверять остаток надо по `prc_price_row.ms_id`, не по `ms_code` новинки, иначе
    получается ложный «нет остатка». Отчёт остатков МС отдаёт с задержкой — сразу после загрузки
    часть позиций ещё не видна, через минуту видна вся.
    - Шесть не попавших — две разные причины, обе не в загрузчике:
      **дубли карточек в МС** (`ambiguous`, 4 шт: `CS-TN217`→два `1438cs`; `CS-TN221C`→два `2634cs`
      с артикулами `CS-TN221C` и `CR-KMTN221C`; `CS-TN-321M`→два `2032cs`; `CS-TN-321Y`→два `2033cs`)
      и **артикул привязки ≠ артикул прайса** (`not_found`, 2 шт: `CS-I-PG510-CL511M/C` привязан
      к `6753cs` с артикулом `CS-I-PG510-CL511M/C/Y`; `CET141247R` — к `2412cs`, где артикулы
      `CS-TK8115С`(кир. С)/`GG-TK8115C`). То есть да, 2 из них сидят в тех самых «98 нет товара в МС».
    - **Загрузчик матчит ТОЛЬКО по артикулу** (`loader.lookup_by_article` → `pick_card`) и про
      ручную привязку `prc_novelty.ms_id` не знает. Значит: привязка новинки к существующей
      карточке работает лишь тогда, когда на карточке стоит ИМЕННО артикул поставщика из прайса.
      Лечение — дубли чистит Сергей в МС, а по двум `not_found` нужно решение: дописать артикул
      прайса на карточку или завести отдельную (чужие живые карточки сами не правим).
    - **Лист ожидания: 5 строк возвращены в работу** (`waiting.revive`) — `OSP0201C-100`,
      `CS-I-CL441C`, `CS-I-PG440` (цвета появились в прайсе 10.08), `CS-EPT49N200`, `CS-EPT49N300`
      (соседи `CS-EPT49N100`/`CS-EPT49N400` в ЧС и в прайсах не встречались — остались в ЧС,
      их надо спросить у поставщика). Все пять теперь `decision='pending'` во вкладке «Новинки».
      На полке осталась одна строка — `CS-PFI121M`, соседей по серии нет.

40. **Два артикула дописаны в МС — «нет товара» 98 → 96 (10.08.2026).**
    - `6753cs`: артикул был `CS-I-PG510-CL511M/C/Y`, стал `CS-I-PG510-CL511M/C` — как в прайсе.
      Проверено: старое написание не встречается НИ В ОДНОМ прайсе ни одного поставщика,
      а `.../C` Кактус пишет одинаково во всех 12 прогонах. Терять нечего.
    - `CET141247R`: карточка нашлась архивной, с вычищенными `code`/`externalCode`/`article`
      (уцелел только «Код поставщика»). Три сестры серии живы и грузятся (`2411cs` CET141246R,
      `2413cs` CET141248R, `2414cs` CET141249R), другой живой карточки с этим артикулом нет —
      значит архивация была случайной. Разархивировал и вернул `2412cs` / вн.`2412` /
      `CET141247R`. Действие полностью обратимо (архивировать назад).
    - Результат прогона #20: **2473 позиции** (было 2471), обе строки легли с количеством
      2 и 28 шт. Осталось 96 «нет товара в МС» и 4 «неоднозначен» — последние это дубли
      карточек в МС (`1438cs`, `2634cs`, `2032cs`, `2033cs`), чистка за Сергеем.

41. **Оприходования Кактуса на боевом расписании (10.08.2026).** `ops/prc_price_watch.py`
    + `prices/prc-price-watch.{service,timer}` (установлены в `/etc/systemd/system`, таймер
    включён). Логика: таймер будит сторожа каждые полчаса с 07:09 до 21:39 МСК, сторож
    смотрит последнее письмо в папке «Прайсы Поставщиков|Кактус» и грузит `prices.run
    --supplier kaktus_msk --apply` ТОЛЬКО если письмо свежее уже обработанного (ключ = дата
    письма + имя вложения). Дальше цепочка идёт сама: отчёты в `docs/prc/`, сверка новинок
    с каталогом (вкладка «Новинки»), лист ожидания комплектов. В конце — сводка в телеграм
    Сергею (@Pro_Dropbox_bot, chat_id 1031321444; в `TG_NOTIFY_ID` лежат ДРУГИЕ люди,
    туда прайсы слать нельзя).
    - Состояние сторожа — `logs/prc_price_watch_kaktus_msk.json`, а НЕ журнал `prc_price_load`:
      загрузка, отменённая проверкой аномалий цены, до журнала не доходит, и по журналу
      сторож считал бы такое письмо вечно новым и дёргал бы Сергея каждые полчаса.
    - Отмена по аномалиям приходит в телеграм как «⛔ загрузка ОТМЕНЕНА» с причинами —
      это ручной разбор, повторно само не поедет (нужен новый прайс или `--force`).
    - Проверено вживую: сухой прогон, боевой прогон #20 (телеграм ok), затем запуск юнита —
      «письмо не новое → пропуск». Ручной запуск: `./venv/bin/python -m ops.prc_price_watch
      [--dry] [--force] [--quiet] [--supplier <ключ>]`.
    - Другие поставщики на расписание НЕ ставились: у Кактуса это первый боевой автомат.
      Подключение следующего — одна строка `ExecStart` в отдельном юните после его первого
      боевого `--apply` с «ок» Сергея.

42. **Отчёты сторожа переведены в свой бот `@ds_prc_bot`** (10.08, `ops/prc_price_watch.py`).
    - Токен и адресат — в `.env`: `TG_PRC_BOT_TOKEN`, `TG_PRC_NOTIFY_ID=1031321444` (Сергей).
      `DROPBOX_BOT_TOKEN` из сторожа убран: прайсовый поток теперь не мешается с файлами.
    - **Формат сводки — ОДНА строка** (правка Сергея 10.08, «напиши проще»):
      `Кактус — Оприходование в МС — ОК, новых позиций — 34`. Вердикт: `ОК` / `сухой прогон` /
      `ОТМЕНА` / `СБОЙ`. Ниже добавляются ТОЛЬКО ошибки и предупреждения (`⚠` аномалий,
      `ОШИБКА`, `ОТМЕНА`, `СБОЙ`) — их пропустить нельзя. Файл, суммы, документы и номер
      журнала в бот больше не идут: они есть в `logs/prc_price_watch_<ключ>.log` и на дашборде.
    - «Новых позиций» = очередь `prc_novelty.decision='pending'` по этому поставщику
      (`prices/catalog.py: pending_rows`) — сколько строк ждёт рук человека во вкладке «Новинки».
    - Адресаты — список через запятую в `TG_PRC_NOTIFY_ID`; `tg()` шлёт каждому отдельно и
      пишет исход по каждому, недоступный чат не отменяет доставку остальным.
    - Доставка проверена 10.08: `1231747786` (@Pro_natalya) — ok. **Адресат сейчас ОДИН**:
      Сергей 10.08 велел убрать свой `1031321444` из списка (Start у бота с его аккаунта
      не нажат, телеграм не даёт боту писать первым). Вернуть — дописать id через запятую.
    - Недоступность почты: разовый таймаут — молча в лог, на 3-й подряд (≈1.5 часа) уходит
      алерт «прайс может пройти мимо». Счётчик — `logs/prc_price_watch_<ключ>_mailfail.txt`,
      обнуляется при первом же удачном заходе.
