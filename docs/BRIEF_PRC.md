# BRIEF_PRC — поток `prc` (прайсы поставщиков → МойСклад)

Обновлён 18.08.2026. Лимит брифа — 150 строк, поэтому он держит только состояние.
**Правила потока и знания о системе — `docs/PRC_RULES.md` (читать вместе с этим файлом).**
История этапов до 16.08.2026 — `docs/archive/BRIEF_PRC_история_до_2026-08-16.md`.

## Что делает поток

Забрать прайс поставщика с почты/телеграма → положить остатки и закупочные цены документом
**Оприходование** на склад «Удаленный» → зафиксировать позиции, которых нет в МС («новинки»),
и довести их до заведения карточки. Внешний загрузчик целиком не заменяем: часть прайсов идёт
по API и продолжит; убираем ручной труд по поставщикам без API, по одному, каждый со своим «ок»
Сергея на первый `--apply`.

## Ветка и запуск

- Ветка/worktree: `prc/colortek-loader` в `.claude/worktrees/prc-colortek` (заведена 05.08.2026).
- **Ветка задачи 18.08.2026:** `prc/unlinked-enters` в `.claude/worktrees/prc-unlinked` —
  вкладка «Несопоставлено» + правило по архиву. **Влита в `main` 18.08.2026 по «ок» Сергея**,
  дашборд перезапущен.
- `./venv/bin/python -m prices.run --supplier <ключ> [--apply] [--file …] [--no-db] [--ignore-anomalies]`
- Ключи профилей: `colortek`, `odissey`, `sakura`, `kaktus` (= группа `kaktus_msk`).

## Территория потока

`prices/` (`profiles, mailbox, parser, cbr, loader, anomaly, blacklist, mailout, journal, run,
novelty, features, catalog, ms_import, waiting, research, supplier_group, unlinked, unlink_apply`, юниты
`prc-price-watch@.{service,timer}`) · `core/ms_api.py` (общий клиент МС, пишет пока только prc) ·
`ops/prc_price_watch.py` · `tools/prc/` (`tpl_check, tpl_load, tpl_verify, enter_audit, tc_bench,
tc_ms_diff, tc_links, wb_fill, cat_fix, unlink_auto, unlink_fill, archive_originals`) · `collectors/thecartridge_catalog.py` · миграции **400–409** ·
UI `web/static/novelties.html` + роуты `/warehouse/novelties`, `/api/novelties*`, `/api/blacklist*`
и `/api/unlinked*` в `web/app.py` · отчёты `docs/prc/`, `docs/reports/`.

## Состояние на 19.08.2026

- **Шаблон 18.08 загружен: 6 карточек** — новый поставщик ООО «КОМПАНИЯ РМ» (ProfiLine + GalaPrint),
  занятых кодов нет. Связь `4529pl`→`7091` (решение Сергея 19.08, только новой карточке); у трёх
  Kyocera-строк из «Название WB» убран чужой бренд SP (человек скопировал с родни `4365sp`).
  Отчёт `docs/reports/prc_tpl_load_2026-08-18.md`.
- **Шаблон 17.08 загружен: 5 карточек** (2 дубля Smart Graphics по разделителям — в архив,
  бэкап `reports/data/prc_arch_dupes_2026-08-17.json`). Связь `1425sg`→`1319` проставлена ТОЛЬКО
  новой карточке (решение Сергея 19.08), родня не тронута. Процедура вынесена в инструменты:
  `tools/prc/tpl_fix.py` (сборка `*_fixed.xlsx` по нашим правилам) + `tools/prc/tpl_arch.py`
  (архив дубля с гейтом «остаток 0 на каждом складе»). Отчёт `docs/reports/prc_tpl_load_2026-08-17.md`.
- **Шаблон человека 14.08 загружен: 52 карточки** (`incoming/prc/tpl_2026-08-14_fixed.xlsx`,
  присланный файл не тронут). 17 старых карточек Smart Graphics ушли в архив с очисткой
  Артикул/Код/Внешний код — их артикул отличался только разделителями (или цифрами: `3481sg`,
  `1798sg`), остаток по каждому складу 0; бэкап `reports/data/prc_arch_dupes_2026-08-19.json`.
  Доп. БУКВЫ в артикуле = другой товар (`1325sg`, `5781bt`) — новая карточка, старая живёт.
  Наименование поставщика НЕ правим (решение Сергея 19.08), даже когда ресурс в нём ошибочен
  (`5874sg`: 54 500 против верных 160 000 у `5874pl`). Отчёт — `docs/reports/prc_tpl_load_2026-08-14.md`.

- На боевом расписании (сторож + таймер) четыре поставщика: `kaktus_msk`, `colortek`, `odissey`,
  `sakura`. Прогоны сверены с ручной загрузкой, эталонные цифры — в `PRC_RULES.md`.
- **Дубли артикулов в МС закрыты: их 0** (44 762 живых карточки, 38 924 артикула).
  Путь цифры 169 → 26 → 17 → 8 → 0. Разбор и все решения — `docs/reports/prc_ms_dup_plan_2026-08-12.md`,
  свежий срез `docs/reports/ms_dup_articles_2026-08-16.csv`, копии карточек до записи —
  `backups/ms_dup_2026-08-12/`. Для загрузчика это снимает причину `ambiguous` по всему каталогу.
- Каталог ТК снят ночным слепком, связи универсальных моделей собраны, но в матчинг НЕ заведены
  (нужно отдельное решение).
- **Пустое «Название WB» закрыто 16.08.2026:** `tools/prc/wb_fill.py` собрал поле по формуле из
  каталога ТК и записал **2 350 карточек на 1 483 кодах** (проверено обратным чтением МС: пустых
  с кодом ТК — 0). Остальные 6 792 живых карточки без кода в ТК Сергей решил оставить пустыми.
  Формула и все её решения (сокращения брендов, «Комплект»→«Набор», лимит этикетки 60 знаков,
  чип не придумываем) — в шапке самого файла; правило «брать как у родни» ОТМЕНЕНО.
  Отчёт — `docs/reports/prc_wb_name_fill_2026-08-16.csv`.
- **Категории МС приведены в порядок 17.08.2026** (`tools/prc/cat_fix.py`): разложено 3 236 карточек
  (2 191 были без категории, 955 вынуты из мусорных папок `Товары интернет-магазинов/…exchange1c.php`,
  89 переехали при слиянии), слито и удалено 11 дублирующих папок (Avision/Katusha/Deli/F+/Huawei
  и корневые Oki, Lexmark, Ленточные), `Картриджи Pantum совм.` → `Картриджи Pantum`, заведены
  `Картриджи Hyundai` и `Картриджи Т1000`. Категория берётся по бренду принтера из ТК; струйное
  уходит в подпапки `струйные hp/canon/epson/brother`. Дерево `Оригинальные картриджи/…` не трогаем
  (решение Сергея: другой товар). Проверено обратным чтением МС: переносить нечего.
  Отчёт — `docs/reports/prc_cat_fix_2026-08-17.csv`.
  Грабли МС: у товаров нет фильтра `productFolder` (412), фильтруем по `pathName`; архивные карточки
  в списке не видны, но держат папку (409 «объект уже используется») — их надо переносить отдельно.
- **Аудит уже разложенного, 17.08.2026:** тот же `cat_fix.py` чинит и чужую категорию — перенесено
  ещё **1 317** карточек (472 — расформирована `Картриджи G&G`: производитель совместимок, а не бренд
  принтера; 227 — ошибка бренда; 618 — струйное/лазерное не в той подпапке). Проверено: 0 к переносу,
  верно стоят 37 082. Отчёты `docs/reports/prc_cat_audit_2026-08-17.csv` (аудит) и
  `prc_cat_relocate_2026-08-17.csv` (переезды). Правило теперь одно: карточка лежит в папке бренда
  своего принтера, где бы она ни была. Исключения зашиты в код — оригиналы и папки по ТИПУ товара
  (ленточные, мастер-плёнка, термоплёнка, фотобумага, чипы, упаковка).
  Осталось людям: в `Картриджи G&G` 16 живых + 13 архивных карточек без 4-значного кода или без кода
  в ТК — разложить руками; у кода `6736` в ТК бренд записан перечнем «HP / Lexmark / Canon».
- **Одиссей разведён по двум юрлицам 18.08.2026** (правило Сергея): бренд **White Box** —
  «белая», небрендированная коробка — живёт на контрагенте `ООО "ОДИССЕЙ" WB`. **Туда же вся
  СТРУЙКА любого бренда** (решение Сергея 18.08.2026): признак струйки — КАТЕГОРИЯ МС
  `Картриджи/…/струйные <бренд>` (`ms_import.is_inkjet`), это родной признак справочника,
  надёжнее разбора наименования. На `ООО "ОДИССЕЙ"` остаётся брендованная лазерка
  (Аквамарин `at`, Hi-Black `hb`). Аббревиатуру кода это не трогает: струйный Аквамарин
  остаётся `…at`, меняется только юрлицо. Признак White Box берём
  **из НАИМЕНОВАНИЯ** (`ms_import.is_white_box`), префикс артикула `WB …` — подстраховка:
  в живых карточках нашлись имена без метки (`WB MLT-D203U`) и с опечаткой («Wite Box»).
  `tools/prc/odissey_wb_fix.py` двигает карточки ТОЛЬКО «ОДИССЕЙ» → «ОДИССЕЙ WB»; обратный ход
  из скрипта убран, чтобы он снова не снял струйку.
  Процесс: `suffix()` и `supplier_of()` теперь принимают имя; вызовы поправлены в `ms_import.build`,
  `web/app.py` (кнопка «➕ В МС») и `prices/unlink_apply.py`.
  Разбор старого: `tools/prc/odissey_wb_fix.py` — 61 карточка с меткой White Box переставлена
  с «ОДИССЕЙ» на «ОДИССЕЙ WB» (18 архивных), расхождений с суффиксом кода (`1234wb`) — 0.
  Тем же прогоном 461 карточка была снята с «ОДИССЕЙ WB» — и **возвращена обратно решением
  Сергея 18.08.2026**: на «ОДИССЕЙ WB» живут не только White Box, но и **струйные** картриджи
  (Аквамарин `at`, `aq`, `od`), их отделять не надо. Откат сделан из бэкапа, 461/461.
  Затем по тому же решению на «ОДИССЕЙ WB» переехала струйка — **125 карточек** (13 архивных,
  117 с кодом `…at` + 8 без кода; категории: epson 79, canon 29, hp 14, brother 3),
  отчёт `docs/reports/prc_odissey_inkjet_2026-08-18.csv`, снимок до записи —
  `backups/odissey_wb_2026-08-18/before_inkjet_move.json`.
  Итог в МС: **ОДИССЕЙ 517 / ОДИССЕЙ WB 2 230** (всего 2 747; не на своём месте — 0).
  Отчёт `docs/reports/prc_odissey_wb_2026-08-18.csv`, состояние до записи —
  `backups/odissey_wb_2026-08-18/before_2747_cards.json` (откатывать только из него).

## Открыто

0-бис. **Аудит струйных подкатегорий МС (18.08.2026, сделан; правка папок ЖДЁТ «ок»).**
   Вопрос Сергея: лежит ли в папках `струйные <бренд>` только струйка своего бренда.
   Проверено по каталогу ТК (`prc_tc_model.raw`): 10 струйных папок, 9 743 карточки
   (699 архивных). Отчёт — `docs/reports/prc_inkjet_folders_2026-08-18.csv`.
   · **Чужого бренда — 0.** Ошибка в бренде не встретилась ни разу.
   · **Не струйное — 21 карточка** (18 живых): обычная ветка 12 (тонер Cactus для лазерных
     Brother `7111cs`–`7115cs`, комплекты Epson AcuLaser C3900N `5432dsk/msk/spb`,
     HP LJ E82560 `6946wb/6946sg`), ветка «Оригинальные» 9 (Canon i-SENSYS X T08/T09
     `5550t2`,`5636ep`,`5638t2`–`5641spb`, Lexmark 808H `5890msk/spb`). Целевые папки
     проставлены в CSV колонкой «куда просится».
   · **`assignment` из ТК врать умеет:** 7 карточек помечены там «для лазерного принтера»,
     хотя принтеры в том же ответе струйные (HP 971XL Officejet Pro X451, Canon BCI-6M,
     Brother LC-472XL, Canon iPF TM-200). Поэтому тип печати определяем ПО СЕМЕЙСТВУ
     ПРИНТЕРОВ (`printer_models`) + лазерным словам наименования («тонер», «фотобарабан»),
     а не по одному полю `assignment`. Те самые 4 карточки, что утром переложили в струйные
     папки (`5474sf`, `0976sf`–`0978sf`), — как раз ошибка `assignment`, лежат верно.
   · 269 карточек обычной ветки нет в каталоге ТК (185 архивных) — тип неизвестен, лазерных
     слов в наименованиях нет; ветка «Оригинальные» в ТК почти вся отсутствует (1 043 из 1 076).
   · Папка `Марвел/…/МФУ струйные` (7 карточек) — струйные МФУ, не картриджи: чужое дерево,
     под правило не подпадает.
   · **Закрыто 18.08.2026.** 11 кодов ушли Сергею в бот PRC, ТК их поправил (перепроверено
     прогоном `collectors.thecartridge_catalog`): `0976`–`0978`, `5474` теперь струйные —
     лежат верно, не двигали; `5432`, `6946`, `7111`–`7115` лазерные — **10 живых карточек
     перенесены** из `струйные <бренд>` в корень бренда (`Картриджи/Картриджи Brother|Epson|HP`),
     снимок `backups/odissey_wb_2026-08-18/inkjet_folder_before.json`. Контроль: в струйных
     папках ветки «Картриджи» 8 650 карточек, живых не на месте — 0.
   · Коды `5443` (LC-472XL) и `6558` (PFI-321) Сергей поправил в ТК 18.08.2026 — перепрогон
     каталога подтвердил «для струйного принтера»; карточки под ними только архивные и уже
     лежат в струйных папках, в МС менять нечего. **Вопрос закрыт.**
   · **Ветку «Оригинальные картриджи» не трогаем** (решение Сергея 18.08.2026) — 9 лазерных
     карточек (T08/T09 Canon, Lexmark 808H) в её папках `Картриджи струйные …` остаются как есть,
     вопрос закрыт. Так же вне области: 3 архивные не на месте (архив не трогаем) и папка Марвел.

0. **Новая СТРУЙНАЯ позиция Одиссея должна заводиться сразу с поставщиком `ООО "ОДИССЕЙ" WB`
   (задача Сергея 18.08.2026, делает сессия `new`).** Правило разведения юрлиц — `PRC_RULES.md` п. 34.
   Автоматика уже умеет: `ms_import.supplier_of` ставит «ОДИССЕЙ WB», если карточка White Box ИЛИ
   лежит в категории `Картриджи/…/струйные <бренд>`. Дыра ровно одна: категорию (и всё «про товар»)
   новая карточка берёт У РОДНИ того же внешнего кода — у позиции с НОВЫМ внешним кодом родни нет,
   категории тоже, и струйник по умолчанию уедет на «ОДИССЕЙ».
   Что сделать сессии `new`: при заведении новой карточки Одиссея определять тип товара
   (струйный / лазерный) на своём шаге и (а) класть карточку в струйную папку бренда принтера,
   (б) ставить юрлицо «ОДИССЕЙ WB». Пункт (а) не косметика: категория — это и есть признак,
   по которому потом работают `is_inkjet` и разбор `tools/prc/odissey_wb_fix.py`; карточка мимо
   струйной папки тихо ломает правило. Источник типа — прайс Одиссея (в нём струйка отделена)
   либо каталог ТК; наименование как признак НЕ использовать (по имени струйник виден только
   кодом модели — T0801, CL-41, PGI-425, список бесконечен).

   **То же самое по КВК ТРЕЙД (задача Сергея 18.08.2026, туда же сессии `new`).** Правило —
   `PRC_RULES.md` п. 35: OEM-товар (артикул `OEM …`, наш код `…oem`) и вся струйка → контрагент
   `ООО "КВК ТРЕЙД" OEM`, остальное → `ООО "КВК ТРЕЙД"`. Автоматики у КВК нет вовсе (профиля
   прайса нет), карточку заводит человек — юрлицо и струйную папку ставит он же.

   **И ТОНЕРОПТТОРГ (задача Сергея 18.08.2026, туда же).** Правило — `PRC_RULES.md` п. 36:
   признак СТРОГО один — вхождение «T2» в НАИМЕНОВАНИЕ (`ТОНЕРОПТТОРГ (Изипринт) T2`,
   `daffd8b7-…`), без метки — `ТОНЕРОПТТОРГ (Изипринт)` (`c9c7b549-…`). У поставщика два бренда
   (T2 и EasyPrint), профиля прайса нет — карточку и юрлицо ставит человек. Буква «T» бывает
   латинской и кириллической; `ms_import.abbr_by_name` ловит только латинскую (`\bt2\b`) —
   для кода это терпимо, для юрлица разбор имени брать из п. 36.

   **Разбор старого сделан 18.08.2026.** Было 2 020 карточек (123 архивных): «Изипринт» 899 /
   «T2» 1 121; расхождений с правилом 61. Переставлено **47 живых** (снимок до записи —
   `backups/odissey_wb_2026-08-18/toneroptorg_before_move.json`): 46 на «Изипринт» (в имени
   прямым текстом EasyPrint, наш код `…ep` у 46 из 46) и 1 на «T2» (`4751t2`, комплект Canon
   460XL+461XL). Итог: **Изипринт 866 живых / 78 архивных, T2 1 031 / 45**, всего 2 020,
   живых не на месте — 0. **14 архивных расхождений оставлены как есть** (решение Сергея:
   архив не трогаем); 9 из них несут наш код `…ep` при метке T2 в имени — если когда-нибудь
   разархивируют, смотреть глазами, а не переписывать по правилу молча.

   **Разбор старого сделан 18.08.2026.** Аудит: 6 285 карточек, было «КВК ТРЕЙД» 5 067 /
   «КВК ТРЕЙД OEM» 1 218; расхождений с правилом 1 158, отчёт `docs/reports/prc_kvk_oem_2026-08-18.csv`.
   Переставлено на «КВК ТРЕЙД OEM» **104** карточки (10 архивных): 33 по OEM-признаку
   (наш код `…oem`, у 30 и артикул `OEM …`) + 71 струйная из обычной ветки
   `Картриджи/…/струйные <бренд>`; пересечения между наборами нет. Снимок до записи —
   `backups/odissey_wb_2026-08-18/kvk_before_move.json`. Итог: **КВК ТРЕЙД 4 963 /
   КВК ТРЕЙД OEM 1 322**, всего 6 285, не на месте — 0.
   · **Оригинальные струйные (1 024 карточки, ветка `Оригинальные картриджи/…`) НЕ трогаем** —
     решение Сергея 18.08.2026: пусть лежат на «КВК ТРЕЙД». Правило про струйку касается только
     обычной ветки `Картриджи/…`. Вопрос закрыт, не возвращаться.
   · На «КВК ТРЕЙД OEM» осталось **30** карточек без обоих признаков — разбирать НЕ стали:
     26 из них архивные, у 24 нет ни кода, ни артикула (обрывки заведения: «*Не продаем*
     HP Q6000A», Kyocera TK8365 в четырёх цветах, HP CF259A «БЕЗ СЧЕТЧИКА»). Живых там 4 —
     `5474sf` SF-BCI6M, `0976sf`/`0977sf`/`0978sf` HP 971XL: это НАСТОЯЩАЯ струйка, лежащая
     в простой папке `Картриджи/Картриджи HP` без струйной подпапки. Отсюда предупреждение
     для сессии `new`: **у КВК категория проставлена местами криво**, вслепую опираться на неё
     нельзя (у Одиссея было чисто).
   · Эти 4 живые струйные карточки 18.08.2026 переложены в свои струйные папки
     (`…/струйные canon`, `…/струйные hp`) — теперь `is_inkjet` их видит и юрлицо совпадает
     с правилом; бэкап прежних папок `backups/odissey_wb_2026-08-18/kvk_folder_before.json`.
     **26 архивных обрывков не трогаем** — решение Сергея: пусть лежат как есть, без внешнего
     кода. Вопрос закрыт.


0. **Несопоставленные карточки оприходований — вкладка «🔗 Несопоставлено», разбор 18.08.2026.**
   На «Удаленном складе» было 361 карточка без нашего внешнего кода, 27 111 шт. Сбор —
   `prices/unlinked.py` (из ПОЗИЦИЙ оприходований: прайсы Булата, ВТТ, Рамиса, Блоссома к нам
   не приходят), миграция **409**, роуты `/api/unlinked*`. Позиции из документов НЕ убираем —
   вкладка даёт только видимость.
   - **«Свести» = карточка уходит ПОЛНОЙ по 11 обязательным полям** (правила 41–42 в
     `PRC_RULES.md`): `unlink_apply` пишет внешний код, `code` и «Название WB» по формуле,
     следом `unlink_fill` добирает описание, группу, поставщика, вес, ед. изм. и code128.
     Дозаполняются только пустые поля, отбракованную строку кнопка не трогает.
   - **«Свести» = карточке проставить наш внешний код + `code` + «Название WB»**
     (`prices/unlink_apply.py`, формула — `tools/prc/wb_fill.compose`). Слияния карточек нет:
     один внешний код и так несёт по карточке на поставщика. Отбраковка до записи: не 4 цифры,
     сведена под ДРУГОЙ код, карточка в архиве, под кодом есть карточка того же бренда
     **с тем же артикулом** (дубль — человеку). Тот же бренд с ДРУГИМ артикулом дублем не
     считается: у поставщика бывает два SKU на наш товар со своими остатками (Blossom «1 туба»
     106R02611 и 106R02608 → 3146), такие сводим, `code` совпадает с роднёй — МС это допускает. **Карточка уже несёт наш код (человек проставил руками) — коды не трогаем,
     но пустое «Название WB» дозаполняем по формуле** (правило Сергея 18.08.2026; значение
     собираем формулой, у соседа по внешнему коду НЕ копируем — родню заполняли руками).
     Кейс `BS-LQ-2080`→2205: код `2205/2`→`2205` поправили в вебе МС за 16 с до нажатия,
     кнопка отбраковала строку целиком и поле осталось пустым — теперь заполнено.
   - **Аббревиатура = бренд ИЗ НАЗВАНИЯ товара, не из артикула** (`ms_import.abbr_by_name`):
     s-Line→`sl`, e-Line→`el`, БУЛАТ→`bt`, Blossom→`bs`, ProfiLine→`pl`, GalaPrint→`gp`,
     SuperFine→`sf`, T2→`t2`, EasyPrint→`ep`, Hi-Black→`hb`, NetProduct→`np`, Cactus→`cs`,
     Sakura→`sk`, **7Q→`7q`** (18.08.2026). Порядок важен (s-Line раньше голого «БУЛАТ»,
     «БУЛАТ» раньше 7Q); сверка на живых карточках — 99.1 %. 7Q считаем брендом ТОЛЬКО в начале
     названия (1–3-е слово): глубже это упоминание чипа («с чипом 7Q») у другого бренда.
   - **Автоотбор `tools/prc/unlink_auto.py`** (`docs/reports/prc_unlinked_auto.md/.csv`):
     A 99, B 21, C 64, D 136, E 41. A = один код в лидерах + модель/тип/бренд/цвет + ресурс;
     B = то же без ресурса; D = разные коды, спор за код или код занят. Два фильтра появились
     после ложной пары (DV-160 и DV-130 оба метились в 6859 «Блок фотобарабана DK-1270»):
     строгий тип узла по названию и запрет спора за код — сняли 22 + 19 строк.
   - **Ручной разбор во вкладке (вечер 18.08.2026), 4 нажатия «Свести»:** `BS-X106R02611` → 3146
     (записано, карточка полная; позже Сергей откатил её и убрал в архив), `DFKYEP2040100` → 5600
     (записано, все 11 полей), `BS-X106R02609` → 1109 и `BS-X106R02610` → 3145 — отбраковано,
     карточки в архиве, строки переведены в «Пропустить». Состояние вкладки: pending 234,
     matched 115, skip 12. Поставщик Blossom добавлен в `unlink_fill.SUPPLIER`
     (ООО «КОМПАНИЯ БЛОССОМ» — 10 из 12 живых bs-карточек; у двух «КАРТРИДЖ ТРЕЙД»);
     у `unlink_fill` появился `--id` (без него скрипт брал ВСЕ сведённые строки разом).
   - **Сведено и записано в МС по «ок» Сергея: 113 карточек** — A 94 (bulat 58, vtt 31,
     kaktus_msk 5) и B 19 (bulat 18, vtt 1). Пишем только там, где заполняются все три поля.
     Уровень B оказался целиком широкоформатной струйкой с ТЕМ ЖЕ OEM-кодом (расходился лишь
     заявленный объём, XL-подмен нет). Сквозная проверка всех девяти полей — пропусков 0.
     Логи `reports/data/prc_unlink_*`, `prc_fill_*`, `prc_check_113.txt`.
   - **Дозаполнение (`tools/prc/unlink_fill.py`):** Описание = имя карточки (наименование
     поставщика), Группа = папка родни по внешнему коду, Поставщик по ключу (булат → ИП Капшук,
     ВТТ → ООО «КПД»(ВТТ), кактус → ООО «КОМПАНИЯ ФЕРРЕТ» — сверено по 12 живым карточкам каждой
     аббревиатуры), Ед.изм = шт, **Code128 = тот же, что у родни** (code128 общий на ТОВАР,
     личный только ean8). **Вес** — от родни того же кода, заведённой ПОСЛЕДНЕЙ: даты создания
     в карточке МС нет, но `ms_id` — UUID v1 и несёт время создания (`born()`), сверено с
     `/audit` (событие `create`) до секунды; берём последнюю карточку с НЕнулевым весом.
     Пустые поля только добираем, заполненные не трогаем; идемпотентно.
   - **Оригиналы (8 шт) заархивированы** (`tools/prc/archive_originals.py`): в названии «ориг.»,
     «(о)», «оригинал» — мы ими не торгуем, наш 4-значный код обозначает СОВМЕСТИМЫЙ товар,
     и автоподбор тянул их на аналоги (Q2613X→0022, TK-160→0239). Сняты код и артикул, карточки
     в архиве, во вкладке «пропустить»; остаток (251 шт) не важен — не продаются. Внешний код
     в МС обязателен, очистить нельзя — остаётся служебная строка МС. **OEM ≠ оригинал**
     (поправка Сергея): у КВК ТРЕЙД это обезличенная коробка, картридж внутри совместимый —
     метка из признака убрана; ни одна из 8 по ней не проходила. Других формулировок
     («не совместим», «NEW С ЧИПОМ», «стартовый») в остатке нет.
   - **Уровень C (64) — целиком человеку, автомат там не работает:** 37 «разные узлы»
     (драм/девелопер против тонер-картриджа — пары нет вовсе, кандидат подобран по принтеру),
     17 монохром (цвет не выделен из названия), 6 опорный признак, 2 комплект vs одиночка,
     2 цвет. Ловушки DK-5140→DK-1270 и HB-ML-D3050B→MLD3050A проходят все проверки, а
     литеральная сверка кода цепляется за модель принтера в названии. Разбор —
     `reports/data/prc_tier_c_groups.txt`.
   - **Не трогать:** 2 строки Рамиса (`C9362HE`, `CB336`, «восстановленный» против нового
     совместимого Cactus — открытый вопрос, один ли это товар, и бренда в названии нет);
     SolutionPrint `SP350640/41/42` — это поставщик «Солюшнс принт» против «Солюшнс принт МСК»
     (артикул с `_MSK`), обе карточки живут с кодом `39*sp` с 12.01.2026, проблемы нет.
   - **Человеку осталось 240 строк:** C 64, D 136, E 33, 5 неполных A, 2 Рамиса.
   **Архив закрыт правилом в загрузчике (18.08.2026).** Премиса «архив попадает в оприходования»
   не подтвердилась (там архивных 0; все 136 — ручные документы 2014–2019 на других складах, не
   трогаем). Беда была другая: `/entity/assortment` архивные карточки не отдаёт, поэтому архивная
   позиция выглядела «не найдено» и звала завести ВТОРУЮ карточку. Теперь `loader.lookup_archived()`
   добирает `filter=archived=true`, `live_twins()` берёт живую родню того же кода и группы, иначе
   причина `archived` и в новинки не идёт; `build_docs` падает, если архивная дошла до позиций;
   ⚠-строки `run.py` → `prc_price_watch.troubles()` → `@ds_prc_bot`. Сухой прогон Сакуры: 6
   архивных задержано, новинок 0 (было бы 6). Срезы аудита — `docs/reports/prc_enter_audit_2026-08-18.*`.

1. **Встречные «Связи» записаны в МС 18.08.2026** («да» Сергея получено). Доп. поле «Связь»
   (`e5020d07-…`, string) проставлено двум карточкам, у которых пара уже ссылалась в одну сторону:
   код `1024`, артикул `HB-44059118/44059106` → `1801` (обратная связь стояла у `220095936` Hi-Black);
   код `7087`, единственная живая карточка без артикула → `5482` (обратная стояла у `PL_TK-1240/TK-1248`).
   Запись пачкой `POST /entity/product` с сохранением остальных атрибутов, контроль — перечитыванием
   карточек; снимок «до» — `backups/prc_links_2026-08-18/links_before.json`. В ТК обе пары двусторонние
   (`docs/reports/tc_links_2026-08-14.csv`). ОТКРЫТО: остальным карточкам этих кодов «Связь» НЕ ставили
   (у `1024` пусто у 7 из 8, у `5482` — у 16 из 17); связь — свойство внешнего кода, и `tpl_check`
   ругается на «пусто у карточки, у родни заполнено». Массовую заливку связей ТК → МС не делали:
   ждёт отдельного решения Сергея.
   · **Закрыто 18.08.2026:** 4 строки Одиссея заведены человеком кнопкой «➕ В МС» —
     `7189wb` (WB WT-201 WASTE), `7190wb` (WB Deli T108), `7191wb` (WB W9018MC DRUM),
     `7192wb` (WB B5L37A WASTE). Проверено: юрлицо `ООО "ОДИССЕЙ" WB`, папка бренда, вес,
     ед.изм, штрихкод, описание и «Название WB» заполнены у всех четырёх; во вкладке
     «Несопоставлено» строк по ним не осталось.
2. **Делают люди, не мы** (расшифровка 18.08.2026, вопрос Сергея):
   · **КЛАСС C ЗАКРЫТ 19.08.2026 — его не было.** Все 15 пар оказались артефактом устаревшего
     справочника: `collectors/ms_products.py` обходит `/entity/product` без фильтра, а МС по
     умолчанию отдаёт ТОЛЬКО неархивные → архивированная человеком карточка навсегда остаётся
     в `ms_product` живой, со старым артикулом и кодом. Пара «живая карточка + её архивный
     предшественник» и читалась как «одна модель под двумя кодами». Сергей проверил руками
     (`1163sp` = `SP193194`, `1164sp` = `SP193195`, `7119cs` — голубой тонер на голубом коде) —
     в МС всё правильно. `ms_product` синхронизирован с фактом МС (279 строк: 97 архивных,
     158 удалённых, 24 со сменившимся внешним кодом; журнал `docs/reports/ms_zombie_sync_2026-08-19.csv`),
     общих артикулов внутри пар стало **0 из 22**. Разбор — `docs/reports/prc_ms_stale_2026-08-19.md`,
     старый `prc_classC_2026-08-18.md` помечен ОТМЕНЁННЫМ, задание из бота PRC отозвано.
     ОТКРЫТО (территория `fin`, ждёт решения Сергея): без патча коллектора зомби вернутся при
     следующем прогоне — нужен второй проход `filter=archived=true` + пометка исчезнувших из обеих
     выдач; та же болезнь в `collectors/moysklad.py` и таблице `products` (45 040 строк против
     44 785 живых в МС, колонки `archived` там нет вовсе).
   · **Кириллица в артикулах — 270 живых карточек** (`CS-TK5345С`, `SG-C831/С841Y DRUM`,
     `OEM CE255Х/CRG724H`): русские буквы, неотличимые от латинских. РЕШЕНО 19.08.2026:
     пришёл артикул от поставщика в таком виде и с прайсом сходится — не трогаем (119 карточек);
     у поставщика не найден вовсе — вердикта нет, данных нет (142). **9 карточек, где у поставщика
     латиница, а у нас кириллица**, обработаны по команде Сергея: остатков нет ни на одном складе →
     старая в архив с пустыми артикулом/кодом/внеш.кодом, новая заведена с верным артикулом
     (тот же код, внеш.код, штрихкод, поставщик, группа, вес, цены, «Название WB»; 11 обязательных
     полей проверены после записи, замечаний 0). Разбор — `docs/reports/prc_cyrillic_articles_2026-08-19.csv`
     и `prc_ms_stale_2026-08-19.md`, бэкап — `backups/prc_cyr_fix_2026-08-19/`.
   · **928 расхождений ТК↔МС на 865 карточках** — считает `tools/prc/tc_ms_diff.py`, отчёт
     `docs/reports/tc_ms_diff_2026-08-13.csv`. Сверяются 4 поля живых карточек с непустым внешним
     кодом: ресурс 643, цвет 197, бренд 64, чип 24. Признаки МС парсятся ИЗ НАЗВАНИЯ карточки
     (`prices.features.parse`), расхождение засчитывается только при прямом противоречии (молчание
     МС — не ошибка); ресурс с допуском 25 % и не сверяется у тонера/чернил. 865 карточек на 582
     уникальных внешних кода — одна ошибка размножена по карточкам разных поставщиков. Правит
     человек, потому что часть «расхождений» по ресурсу — заявленный повышенный ресурс совместимки,
     а не опечатка, и править надо свободный текст названия.
   · **`0014` (106R01159)** — в каталоге ТК такого внешнего кода нет, сама модель 106R01159 лежит
     там под `5340`; ссылки `5340→0014` и `5341→0014` ведут в пустоту. В МС под `0014` три живые
     карточки без кода и артикула (две Cactus 106R01159 и одна ЧУЖАЯ — Colortek ML-1610D2).
     Похоже на дубль модели на стороне ТК: вопрос программисту ТК, сами не трогаем.
3. **Пары `msk`/`spb` Спринта — РЕШЕНО Сергеем 18.08.2026:** разводим по контрагенту,
   `s_print_msk` → `ООО "Солюшнс принт" МСК`, `s_print_spb` → `ООО "Солюшнс принт"`
   (`PRC_RULES.md` п. 37). Живые карточки уже так и лежат: СПб 530 (все `…sp`, без `_MSK`),
   МСК 1 736 (все с `_MSK`), нарушений 0; 5 архивных с `_MSK` на питерском юрлице не трогаем.
   Остаток — 3 живые карточки на МСК без артикула и без нашего кода («Картридж TN-241C/M/Y …
   SP», внешний код служебный): это те, у кого артикул снимали руками, пары в МС у них нет,
   для ТК они невидимы. Заводить их — работа вкладки «Несопоставлено», когда подключим профиль
   Спринта.
4. **Мелочи:**
   · `colortek_*_p13` — **разобрано 18.08.2026, не наши и не трогаем.** Это РУЧНЫЕ оприходования
     Шарова А., созданные в реальном времени: `colortek_2024-02-06_p13` (119,51 ₽),
     `colortek_2025-07-21_p13` (1 325,80 ₽), `colortek_2025-12-24_p13` (1 576,32 ₽), склад
     «Кантемировская», 1–2 позиции. Тем же именем названы ещё 6 его документов (`easy_print_*_p13`,
     `odissey_2024-06-11_p13`, один на складе «Брак»). Совпадение с шаблоном имени нашего
     загрузчика (`<профиль>_<дата>_pN`) случайно; наши документы — только 2026 года на
     «Удаленном складе».
   · 3 позиции Кактуса «поставщик не указан» в аудите оприходований: `CS-CLI451BK/M/C/Y`
     (109 шт, 370 ₽), `CS-CLI471XLBK/M/C/Y` (160 шт, 421 ₽), `CS-CZ109-12` (80 шт, 500 ₽) —
     внешнего кода у них нет, показаны Сергею на проверку.
   · Лист ожидания: совет «вернуть в работу» правился — теперь только при полном комплекте
     (`PRC_RULES.md` п. 38, `waiting.ready()`); на `CS-I-CL441C` / `CS-I-PG440` совет больше
     не выдаётся (проверено прогоном: «комплект не полон — нет пурпурный, жёлтый»).
   · Ссылка «✨ Новинки» в генерируемом `reports_wb_clearance.html` — **чужая территория (mkt)**, не лезем.
5. **Снято 16.08.2026:** «товары Капшука без остатка — в архив» в объёме всего поставщика (997 карточек)
   — Сергей вопрос снял, карточки не трогаем. Сделаны только 4 карточки Капшука из самих дублей.

## Следующий шаг

0. **По «Несопоставлено» автомат исчерпан** — свели всё, что сводится без сомнений (113 из 361),
   оригиналы убрали в архив. Остаток разбирает человек во вкладке (pending 234); кнопка теперь
   отдаёт карточку полной по 11 полям, дозаполнение прицеплено к ней же. Новых прогонов
   `unlink_auto --mark` не запускать без отдельного решения Сергея. Открытое: у `blossom` нет
   правила аббревиатуры в `ms_import.CODE_SUFFIX` (код для таких карточек не собирается —
   у 3146bs его пришлось брать из родни), решает человек.
1. Подключить **Блоссом**: профиля ещё нет. Заготовка — лист `Лист_1`, шапка стр. 6, USD ×1.04,
   артикулы `BS-`; в `supplier_ids` нужны ОБА контрагента (ООО «КАРТРИДЖ ТРЕЙД» активный
   и ООО «КОМПАНИЯ БЛОССОМ» историческая); `Profile.qty()` не понимает «1 040,000» с неразрывным
   пробелом; разделы прайса идут строкой без наименования и несут признак чипа — `parser.parse`
   их не отбрасывает; прайс 10.08 пришёл БЕЗ цен (обновили только остатки), ждём прайс с ценами.
2. Дальше по очереди ручных поставщиков: **Спринт МСК** (письма нет, грузим `--file` из ЛК),
   Солюшнс, Позитив. В МС видны ещё группы `bulat, profiline, rapid, easy_print, vtt,
   s_print_msk/spb, ramis` — какие из них ручные, подтвердить у Сергея.
3. Порядок ввода нового поставщика (не сокращать): человек грузит руками (эталон) → наш сухой прогон
   и сверка позиция-в-позицию по `ms_id`/количеству/цене → «ок» Сергея → `--apply` → сверка остатков
   и договорённость, с какого дня человек перестаёт грузить руками → **засев состояния сторожа** →
   `systemctl enable --now` таймера.
4. Хвосты по качеству: подставлять связь ТК при заведении карточки из «➕ В МС»; сверить связи ТК
   с проставленными руками в МС; пересчитать `N/6` в `prc_novelty_candidate` через
   `prices.catalog --recheck` (после смены семантики чипа не запускался).
