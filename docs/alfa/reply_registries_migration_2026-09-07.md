# Ответ банку: переход на новые endpoints реестров (письмо от 07.09.2026)

Статус: черновик, отправляет Сергей.

---

Добрый день!

Спасибо за уведомление о переходе на целевые адреса методов создания реестра рублёвых платежей
и получения списка платежей реестра.

Мы проверили нашу интеграцию с Alfa API. На текущий момент методы работы с реестрами платежей
(`POST /api/jp/v2/payments/registries` и `GET /api/jp/v2/payments/registries/{registryId}`)
в нашем контуре не используются: боевая интеграция ограничена получением выписки
(`GET /api/jp/v1/statement/transactions`). Соответственно, изменения не затрагивают наши рабочие
процессы и не требуют доработок в продуктивной системе.

Внутреннюю техническую документацию мы уже актуализировали под целевые адреса:

- создание реестра рублёвых платежей — `POST /api/jp/v1/registries`;
- получение списка платежей реестра — `GET /api/jp/v1/registries/{registryId}/payments`.

Работы по переходу с нашей стороны завершены на 07.09.2026, то есть до установленного срока
01.10.2026. Если мы начнём использовать реестры платежей, интеграция будет сразу построена
на новых endpoints.

Небольшое уточнение по тексту письма: в блоке «Метод Получения списка рублевых платежей реестра»
для среды prod указан адрес `https://sandbox.alfabank.ru/api/jp/v2/payments/registries/{registryId}`.
Полагаем, это опечатка и для prod имелся в виду хост `https://baas.alfabank.ru`. Просим подтвердить.

Также будем признательны за прямые ссылки на документацию обоих методов — в полученном письме
подписи «Актуальная ссылка на документацию» есть, а сами ссылки не проставились.

С уважением,
ООО «ДИСКВЭР»
