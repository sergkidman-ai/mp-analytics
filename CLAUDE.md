# CLAUDE.md — mp-analytics («Пульт бизнеса»)

BI-система маркетплейс-аналитики для продавца картриджей «Цифровой квадрат»: забрать по API
данные всех площадок, свести в одну БД, считать реальную экономику каждого SKU.

Карта проекта (видение, принципы, дорожная карта) — `ARCHITECTURE.md`: читать **по необходимости**
(новая подсистема, спор о принципах), а не при каждом старте. Этот файл — операционный ориентир.

## Это отдельный проект

Живёт **только** в `/opt/mp-analytics` (свой git, свои сессии Claude Code, своя память).
НЕ трогать соседние проекты: `/opt/sokol-server` (Сокол — анализ ТЗ), `/opt/tz-analyzer-src`,
`/var/www/sokol`. Кросс-правок между проектами нет.

## Инфраструктура (уже развёрнута)

- **БД:** PostgreSQL 16 в Docker (контейнер `mp-postgres`), `127.0.0.1:5433`, база `mp_analytics`,
  том `/opt/mp-analytics/pgdata` (переживает рестарт). Доступ — `DATABASE_URL` из `.env`.
- **Python:** venv в `/opt/mp-analytics/venv` (Python 3.10). Запуск скриптов — `./venv/bin/python ...`.
- **Веб-дашборд:** `uvicorn web.app:app` на `127.0.0.1:8090`, systemd-юнит `web/mp-dashboard.service`.
- **git:** репозиторий здесь, ветка `main`.

Структура репозитория (кто за что отвечает) — `docs/archive/CLAUDE_реализованное.md`.

## Аккаунты

WB: `wb_acc1`, `wb_acc2`. Ozon: `oz_acc1` (Премиум-Про, есть отзывы/рейтинг), `oz_acc2` (Дисквэр).
Различаются полем `account`; площадка — полем `platform`.

## Потоки работы (домены) — читай свой бриф первым

Работа разнесена на параллельные сессии-потоки: **fin** (себест/COGS/маржа/P&L, `run_daily.py`,
миграции 0xx), **mkt** (Джем/реклама/ABC/просадки, `run_marketing.py`, миграции 1xx), **rev**
(отзывы/автоответы), **gab** (габариты), **inv** (банк/платежи). Каждая сессия первым делом читает
свой `docs/BRIEF_<ПОТОК>.md`.

**Граница:** `margin_by_sku` пишет ТОЛЬКО fin; остальные читают (read-only). Параллельные сессии —
каждая в своём git worktree и своей ветке `<поток>/*`. **Флаг чужой территории** активен через git-хук
(`tools/territory_guard.py`, `git config core.hooksPath tools/hooks`): коммит правок чужих файлов
блокируется. Самопроверка: `python3 tools/territory_guard.py --status`. Подробности — в брифах.

## Команды

```bash
./venv/bin/python core/db.py          # self-check подключения к БД
./venv/bin/python run_daily.py        # полный прогон сбора + пересборка витрин
systemctl restart mp-dashboard        # перезапуск веб-дашборда (:8090)
```

## Правила

1. **МойСклад — источник правды** по товарам и себестоимости. Артикул — единый ключ связи МС↔площадки.
2. **Сырьё отдельно от расчётов:** raw-слой (JSONB) хранит ответы API как есть; любой расчёт
   пересобирается из сырья без повторного обращения к API.
3. **Идемпотентность:** повторный сбор за период не плодит дублей — `upsert` по натуральному ключу.
4. **Секреты только в `.env`** (gitignored). Никогда не хардкодить и не выводить ключи в чат.
5. **Себест для анализа** брать из витрины `margin_by_sku` (ключ `article` = nm_id), НЕ наивным
   джойном `external_code → cost_seb` (дырявый путь). Себест конкретной отгрузки — `report/stock`
   на ДАТУ отгрузки, не усреднённый. Подробности — в памяти проекта.
6. Каждый шаг сверять с реальностью (контроль: nmID 216421567 за май 2026 → чистая **3741.77 ₽**).
   История цифры — `docs/archive/CLAUDE_реализованное.md`.
7. **Сторно COGS возвратов WB / Ozon / Яндекс** — политика реализована и влита по всем трём
   площадкам. Полный текст правила (период, оценка, мосты, гейт по складу) —
   `docs/archive/CLAUDE_реализованное.md`. Менять семантику — только по согласованию с потоком fin.
8. **Сплит строки «Продажи» Ozon** — источник сплита ТОЛЬКО отчёт о реализации
   `/v2/finance/realization`, постинги врут. Реализовано и задеплоено; полный текст —
   `docs/archive/CLAUDE_реализованное.md`.
9. **При старте сессии ПЕРВЫМ делом читать `CLAUDE.md` + свой `docs/BRIEF_<ПОТОК>.md`** — и больше
    ничего. Полный `docs/HANDOFF.md` при старте НЕ читать (см. раздел «Экономия токенов», п. 1).
10. **В конце значимого блока работы обновлять свой `docs/BRIEF_<ПОТОК>.md`** — состояние / сделанное /
    ветка-worktree / открытые вопросы / следующий шаг. Это предохранитель от рассинхрона и потери
    контекста при переполнении сессии (см. память про 32MB и устаревший эталон).
11. **Гигиена контекста (следствие смертей на ~32MB):** изображения и бинарные файлы читать через `Read`
    ТОЛЬКО при реальной необходимости. Скриншоты/xlsx/pdf НЕ тянуть в контекст целиком — извлекать нужное
    скриптом в текст (парсер xlsx/pdf, OCR/срез изображения) и читать уже текстовый результат. Помнить: медиа
    и вложения в контексте `/compact` НЕ сжимает (жмётся только текст диалога) → раздутый запрос роняет сессию.
    То же на стороне API-запроса и вывода: запросы к МС/маркетплейс-API делать узкими (`limit=10–20`, только
    нужные поля через `expand`/фильтры, не тянуть коллекции целиком); ответ/файл **>1 МБ** — сохранять на диск,
    не в чат; разбирать `jq`/`grep`, а НЕ `cat` целиком; в чат выводить **≤50 строк** (агрегаты/итоги), никогда
    не сырой JSON-дамп.
12. **Территория потока (см. `docs/SESSIONS_MAP.md`).** Каждая сессия работает в СВОЁМ потоке
    (`fin` / `mkt` / `rev` / `gab` / `inv`). При старте определи свой поток, читай свой
    `docs/BRIEF_<ПОТОК>.md`. **Файлы чужих потоков не менять**; если задача требует чужой территории —
    зафиксируй вопрос в СВОЁМ брифе и не лезь. **Новые модули помечай комментарием-шапкой `# поток: <имя>`.**
    Одно каноничное имя на поток, без версионных суффиксов (`mkt`, а не `mkt3/mkt4`).
13. **ПЛАТНЫЕ РЕСУРСЫ — ТОЛЬКО С СОГЛАСИЯ СЕРГЕЯ. Правило для ВСЕХ потоков, не только `gab`.**
    Под платным ресурсом понимается всё, где расходуются живые деньги или лимиты:
    API-ключи с оплатой по факту (`ANTHROPIC_API_KEY`, `DEEPSEEK_API_KEY`, `RAPID_API_KEY`,
    любой новый), платные сервисы и подписки (Icecat Full, платные каталоги, SMS/прокси),
    любые серверные инструменты, тарифицируемые отдельно (`web_search` у Anthropic).
    - **ДО запуска** — письменная оценка стоимости: сколько запросов × цена за единицу = сумма
      в рублях и долларах, плюс что произойдёт при худшем сценарии (все запросы впустую).
    - Запускать **только после явного «да»** Сергея на конкретную сумму и объём.
      **Молчание, отсутствие возражений и «продолжай» по другой теме согласием НЕ являются.**
    - Разрешение **разовое**: на эту партию/этот прогон. Следующий прогон — новый вопрос,
      даже если он такой же. Расширение объёма (было 20 моделей, стало 216) — новый вопрос.
    - **При любом сомнении — остановиться и спросить**, а не «сделать и показать результат».
    - Ключ из `.env` не мандат на трату: наличие ключа означает лишь техническую возможность.
    - В сводке расхода **разделять** «лимиты подписки» и «живые деньги» и не подавать
      пересчёт токенов по прайс-листу как списание со счёта (это оценка потребления).

## Экономия токенов

Политика Сергея от 2026-07-30. Причина: стоимость запроса = размер контекста × число запросов,
и весь контекст переоплачивается на КАЖДОМ шаге. За 27–30.07 ушло 820 M токенов на 20 сессий,
из них 92 % — чтение кэша, то есть переплата за раздутый контекст, а не за работу.
Действует для ВСЕХ потоков.

1. **БРИФЫ.** На каждый поток — `docs/BRIEF_<ПОТОК>.md` до 150 строк: ветка/worktree, что сделано,
   что открыто, запреты, следующий шаг. Стартовая команда любой сессии: «прочитай CLAUDE.md и
   BRIEF_<поток>.md». Полный `docs/HANDOFF.md` — только по прямой просьбе для координации;
   историю этапов — в `docs/handoff/archive/`.
2. **ДИЕТА CLAUDE.md.** Правила — дословно, описательные и устаревшие блоки — в архив
   (`docs/archive/CLAUDE_реализованное.md`). При добавлении нового правила проверять, не пора ли
   вынести старое.
3. **МОДЕЛЬ** выбирается на старте сессии и не меняется по ходу: **Sonnet** — механические потоки
   (банк, МойСклад, габариты-сбор, прогоны, сводки); **Opus** — только вердикты, архитектура,
   ревью чужого кода, спорные решения. Переключение модели в середине сессии обнуляет кэш —
   весь контекст пишется заново.
4. **МАССОВОЕ ЧТЕНИЕ** файлов и кода — через субагентов: разведка агентом, в основную сессию
   возвращается абзац вывода; правки — в основной сессии. Прочитанное напрямую едет в контексте
   до конца сессии, прочитанное субагентом умирает вместе с ним.
5. **СЕССИИ.** Закончил кусок — обновил бриф, закрыл сессию. Воскрешать вчерашние сессии через
   `--resume` **запрещено** — кэш протух, весь накопленный контекст переписывается заново; новая
   сессия с брифом дешевле.
6. **МЕНЬШЕ ШАГОВ.** Независимые команды — одним пакетом; проверки — одним скриптом с готовой
   сводкой; файлы не читать целиком ради трёх строк.
7. **ФОНОВЫЕ ПРОГОНЫ** будят сессию не чаще раза в 50 позиций.

Эффект зафиксировать через неделю (к 2026-08-06) сравнением `/usage`.

## Делегирование Codex

Codex (OpenAI) подключён как MCP-инструмент (`mcp__codex__codex`, `mcp__codex__codex-reply`,
user scope). Выход к OpenAI идёт через forward-proxy на релее — прокси уже прописан в env
регистрации MCP, трогать/дублировать его не нужно.

**Модель по умолчанию — `gpt-5.6-terra`** (последняя линейка 5.6): передавать параметром `model` при
КАЖДОМ вызове `mcp__codex__codex`. Плоские алиасы `gpt-5.6` и `gpt-5.6-codex` на этом ChatGPT-аккаунте
отклоняются (400 «not supported with ChatGPT account») — доступны только конкретные слаги линейки
(`gpt-5.6-terra` / `gpt-5.6-luna` / `gpt-5.6-sol`). Без `model` Codex падает на GPT-5 (устарело).

Правила делегирования:

1. **Анализ/ревью — только `sandbox: read-only`.** Файл `.env` не читать, секреты не выводить
   (правило 4 распространяется и на Codex).
2. **Правки кода — `sandbox: workspace-write`, всегда в ОТДЕЛЬНОЙ git-ветке** (`fin/*` или `mkt/*`
   по домену). Одна правка = одна ветка = точечный diff.
3. **Git делает Claude, не Codex.** Sandbox Codex блокирует запись в `.git`, поэтому ветку/коммит
   создаёт оркестратор (Claude). Коммитить ТОЛЬКО целевые файлы правки; не подхватывать чужие
   незакоммиченные изменения в рабочем дереве.
4. **В `main` не мержить без явного «ок» пользователя.** Границы доменов fin/mkt и `territory_guard`
   обязательны и для Codex.
5. **Финансовые правки проверять цифрами:** после патча — прогон отчёта на реальном месяце и дельта
   в рублях до/после. Эталон — цифры площадок; подгонять запрещено, остаточные расхождения объяснять.
6. `approval-policy: never` допустим только внутри этих sandbox-рамок (read-only / workspace-write).

## Габариты (поток `gab`) — ЖЕЛЕЗНЫЕ ПРАВИЛА

**При старте новой сессии по габаритам — сначала прочитай `docs/HANDOFF.md`** (секция «🧭 ПАСПОРТ ГАБАРИТОВ»).

Действуют во ВСЕХ будущих сессиях без исключений. Нарушать запрещено. Причина строгости: за
неправильные габариты маркетплейс перемеряет короб и штрафует — синтетический размер = реальный
денежный убыток. История: в июле-2026 в WB ушло ~2.4–3.1 тыс. карточек с синтетикой (медиана по
модели + пропорциональная ужимка), всё откачено на оригинал (см. память `gab-rollback-tail-closed`).

1. **ЗАПРЕЩЕНО придумывать, оценивать или рассчитывать габариты** через коэффициенты, медианы по
   модели, пропорциональную ужимку `k=(rv/cv)^⅓` и любые другие расчётные методы. Синтетические
   размеры недопустимы. **Единственное исключение — короб НАБОРА как сумма реальных коробов
   одиночных картриджей (правило 6).**
2. **У каждого габарита — явный ИСТОЧНИК:** конкретный поставщик из `supplier_dims`, ссылка (URL) на
   сайт производителя, либо правило наследования (правило 4). **Габарит без источника = габарита нет.**
3. **Профилайн — источник ТОЛЬКО веса.** Размеры от него не брать и не достраивать короб под его
   литраж. (У profiline в `supplier_dims` 0 строк с ДхШхВ — только вес.)
4. **Наследование родитель→ребёнок:** материнская карточка = картридж, артикул РОВНО 4 цифры;
   дочерние = карточки принтеров, первые 4 цифры совпадают. Дочерняя ВСЕГДА наследует габариты
   материнской. Для цветных серий принтерные карточки ВСЕГДА получают габариты **ЧЁРНОГО** картриджа.
5. **Не найдено в чистых источниках → «РАЗМЕР НЕ НАЙДЕН»**, позиция в отдельный список, ничего не
   додумывать. Чистые источники: `solutionsprint, изи, sakura, cactus, rapid` (где размеры реальны).
   profiline — НЕ источник размеров.
6. **Наборы (2, 4, 5, 6, 8 и более картриджей):** короб = сумма реальных коробов одиночек с учётом
   укладки. **Перед массовым применением показать Сергею 3–5 примеров расчёта на согласование.**
   Одиночный короб на набор ставить ЗАПРЕЩЕНО (грубое занижение).
7. **ЗАПРЕЩЕНО отправлять изменения на WB / Ozon / Яндекс без отдельной прямой команды Сергея.**
   Наличие данных ≠ разрешение на запись. Dry-run и подготовку — можно; запись — только по команде.
8. **Каждый отчёт → файл `docs/reports/NN_название.md`.** В чат — только короткая сводка цифрами и
   путь к файлу. Большие таблицы в консоль не печатать (см. также правило гигиены контекста #11 выше).
9. **Тай-брейк расхождений поставщиков.** Согласие = коробки различаются не больше чем **на 10% ИЛИ ±1,5 см
   по каждой стороне** (сравнивать отсортированные ДхШхВ — ориентация не важна). Из согласующихся РЕАЛЬНЫХ
   коробов брать **БОЛЬШИЙ** (безопаснее от штрафа за занижение при перемере ВБ). Ничего не вычислять — только
   выбор из реальных значений поставщиков. Если один источник подозрительно похож на **мастер-картон** (объём в
   разы больше остальных) — отбросить его; но если после отброса осталось **меньше двух источников** или конфликт
   не решается — в **ручной разбор** (`docs/gab_discrepancies.csv`). Медианы/усреднение запрещены (правило 1).
10. **Укладка наборов (короб = сумма реальных коробов компонентов).** Диапазон кодов раскрывать ПОЛНОСТЬЮ
    (W2200X-W2203X = 4 кода CMYK, не 2 конца). Геометрия укладки: **2 шт** — стопка по меньшей стороне;
    **3 шт** — в ряд; **4 шт** — квадрат 2×2; **5–6** — 2×3; **8** — 2×4 или 2×2×2 (выбрать компактнее).
    **Смешанный набор (компоненты разного размера, вне допуска 10%/±1.5см) — решение Сергея 2026-07-24:**
    каждую ячейку укладки считать по габаритам САМОГО КРУПНОГО компонента набора (по объёму), затем обычная
    геометрия по числу штук (4 → 2×2). Линейное суммирование разных коробов в ряд больше НЕ применять.
    Расчёты наборов по этим правилам Сергей СОГЛАСОВАЛ 2026-07-24 (метку «не согласовано» снять); на WB всё равно
    ничего не отправлять — итог идёт в таблицу на проверку.
11. **Цветовая серия = набор.** Если в названии набора ОДИН код БЕЗ цветового суффикса (`Картриджи 069`,
    `067`, `82`) — это серия по цветам: раскрывать в набор всех цветовых вариантов кода (069 → 069BK+069C+069M+069Y).
    Фактический перечень цветов определять по `supplier_dims` (коды с суффиксами BK/C/M/Y и вариациями: K, LC, LM,
    PK, MK, GY…) И по нашим материнским карточкам этой серии — брать НАЙДЕННЫЙ перечень, не фиксированную четвёрку
    (у широкоформатных HP цветов больше). Если цветовые варианты не найдены нигде — в `docs/nabor_unclear.csv`.

Рабочие списки габаритов этапа 2026-07-24 (наборы, расхождения, «размер не найден») —
`docs/archive/CLAUDE_реализованное.md`. Живое состояние потока — `docs/BRIEF_GAB.md`.

12. **Приоритет источников размера (итог аудита 78 пересечений, 2026-07-26).** Строгий порядок:
    1) **короб поставщика** из чистых источников (`cactus/rapid/sakura/изи`, тай-брейк по правилу 9) —
       это наш СОВМЕСТИМЫЙ товар, его и продаём;
    2) **веб-размер упаковки ОРИГИНАЛА** — ТОЛЬКО если поставщицкого размера нет ни у одного чистого
       поставщика; помечать источник `веб-оригинал` (это упаковка оригинального картриджа, не нашего
       совместимого — применять осознанно);
    3) **ручной замер Сергея**.
    - **241 CONFIRMED веб-размеров НЕ применять** к карточкам, у которых уже есть поставщицкий размер —
      там web только для СВЕРКИ. Аудит показал: web-конфликт с поставщиком в 71/78 = разница
      «оригинал vs совместимка», а не ошибка мэтчинга (`docs/matching_audit/`).
    - **solutionsprint = внутренние числовые коды** (OEM не проверяется артикулом) → для приоритета
      источника считать НЕ чистым; 25.5% базы «Покрыто» держится только на нём — под отдельный аудит.
    - Физически невозможный короб поставщика (объём картриджа не влезает в габарит, напр. 775 мл в
      175 см³) = брак строки прайса, а НЕ повод менять наш OEM-код. Отбраковывать размер, код не трогать.
