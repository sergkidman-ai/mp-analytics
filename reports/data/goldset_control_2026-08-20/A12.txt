# A12
# prompt: buy_price = null в ответе TheCartridge — это ноль?
# sec: 8.8

Нет. `buy_price = null` означает **no_lu** (нет в наличии), а не ноль. Товар не продаётся TheCartridge, не учитываем в калькуляции маржи. Ноль — это отдельный статус (товар есть, но бесплатный).
